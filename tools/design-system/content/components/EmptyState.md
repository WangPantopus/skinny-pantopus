The simple empty state: a 64px rounded icon well, a title, a description and an optional action.

**Provide:** `icon`, `title`, optional `description`, `actionLabel` with `onAction`.

Say what will appear and how to get it: "No mail yet" / "Mail sent to your verified address shows up here." For identity-tinted empty states on archetype pages, use `ArchetypeEmptyState`. Source: `frontend/apps/web/src/components/ui/EmptyState.tsx`.

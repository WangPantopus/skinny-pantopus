The one card frame for the Place archetype: `app-surface`, 1px `app-border`, `radius-2xl`, `shadow-sm`.

**Provide:** `children`, optional `onClick` (pointer cursor) and `className` (usually `p-4`, or `p-3.5` compact).

No left-border accents, no flooded fills. Color lives in icon tiles, chips, dots and pills. Source: `frontend/apps/web/src/components/archetypes/place/primitives.tsx`.

"What stands out": the one fact about their own address a visitor didn't know.

**Provide:** `tone` (`alert` amber, `watch` info, `info` neutral, `calm` green), `headline`, `followUp` (the question the fact answers), optional `grade` ("High", "AQI 31", "Zone X", "Quiet"), `detail`, `source` ("USFS Wildfire Hazard Potential"), `icon` (Sparkles by default), `onFollowUp`.

Always cite the source. A calm result is itself news: "Quiet on every layer." Source: `frontend/apps/web/src/components/archetypes/place/AhaCard.tsx`.

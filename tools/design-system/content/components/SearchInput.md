A compact search box with a magnifier and a clear button, for filtering lists in place.

**Provide:** `value` and `onChange` (controlled; the parent filters), optional `placeholder` ("Search…" by default; name the thing: "Search your mail…") and `className`.

It is 38px tall (8px vertical padding and a 1px border), `radius-lg`, with a `color-primary-500` focus ring. The clear button is labelled "Clear search". Source: `frontend/apps/web/src/components/SearchInput.tsx`.

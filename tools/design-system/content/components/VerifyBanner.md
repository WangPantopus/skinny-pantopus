The gentle claimed-to-verified nudge above the pulse: a sky banner that routes to address verification.

**Provide:** `onClick` (required: the whole banner is the control), optional `title` ("Verify your address to message neighbors and get your badge.") and `cta` ("Verify address").

It uses the sky CTA voice, not the Place green: `color-primary-50` ground, `color-primary-200` border, `color-primary-900` title (8.87:1). The CTA line is `color-primary-600` (3.84:1 on the tint: **flagged**). Show it only to residents whose place is claimed but unverified. Source: `frontend/apps/web/src/components/archetypes/place/VerifyBanner.tsx`.

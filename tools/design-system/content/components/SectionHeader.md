An overline with an optional title and a right-aligned text action, placed above a group of cards or rows.

**Provide:** `overline`, optional `title` (16px/600), optional `action` `{ label, onClick }` ("See all", "View all").

**Contrast flag:** the action is `color-primary-600` text (4.10:1 on `app-surface`). Native `SectionHeader` makes the action a 44pt target with a 14pt chevron. Source: `frontend/apps/web/src/components/archetypes/primitives/SectionHeader.tsx`.

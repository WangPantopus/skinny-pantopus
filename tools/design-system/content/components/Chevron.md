The tap-through affordance at the end of a tappable card or row: an 18px Lucide chevron at stroke 2.25 in `app-text-muted`.

**Provide:** optional `className`. Show it only when the whole card is a link (`onClick` set). Source: `frontend/apps/web/src/components/archetypes/place/primitives.tsx`.

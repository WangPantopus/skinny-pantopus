A free-size loading block with the same sliding highlight, for skeleton tiles and cards.

**Provide:** `className` with the size and radius (`h-[15px] w-3/5`, `w-[34px] h-[34px] rounded-[9px]`). Mirror the loaded layout: Place cards show a tile, a title bar and two value bars. Same dark-theme flag as `ShimmerLine`. Source: `frontend/apps/web/src/components/ui/Shimmer.tsx`.

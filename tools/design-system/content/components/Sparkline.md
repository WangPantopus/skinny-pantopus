A qualitative trend line in the Place accent, for home value.

**Provide:** optional `className`. The shape is illustrative, not data-bound: a calm upward line with a 16% area fill and an emphasized endpoint, drawn in `currentColor` (`color-identity-home`).

Pair it with the real number and an honest caption ("Estimate, not an appraisal"). Don't use it where a real series exists. Source: `frontend/apps/web/src/components/archetypes/place/primitives.tsx`.

A form section: an overline, an optional description, and the fields beneath it.

**Provide:** `overline` (short noun, rendered uppercase), optional `description` (one sentence on why you ask), `children` (fields).

Groups are 32px apart. Keep each group to a single idea ("Your home", "Contact"). Say why you need sensitive data and how it is shown: "Only shown as a band on your block, never the amount." Source: `frontend/apps/web/src/components/archetypes/primitives/FieldGroup.tsx`.

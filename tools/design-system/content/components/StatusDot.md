An 8px semantic dot placed before a reading.

**Provide:** `tone`: `success` (default), `warning`, `error` or `neutral`. The dot uses the `-solid` fills (`app-text-muted` for neutral).

**Rules:** always follow it with words that carry the meaning ("AQI 31 · Good", "Boil-water notice"). Place cards use it inline before a value. Source: `frontend/apps/web/src/components/archetypes/place/primitives.tsx`.

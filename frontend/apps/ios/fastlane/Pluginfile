# Autogenerated by fastlane — declares fastlane plugins used by the Fastfile.
#
# fastlane-plugin-sentry provides `sentry_upload_dsym`, used by the
# `upload_dsyms_to_sentry` lane to ship release dSYMs to Sentry. The plugin
# bundles sentry-cli, so no separate install is needed.
gem 'fastlane-plugin-sentry'

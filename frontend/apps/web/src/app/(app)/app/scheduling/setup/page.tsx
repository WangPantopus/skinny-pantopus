// A2 — First-run "Set up your booking link" wizard.

import SetupWizard from "@/components/scheduling/hub/SetupWizard";

export const metadata = {
  title: "Set up booking · Pantopus",
};

export default function SchedulingSetupPage() {
  return <SetupWizard />;
}

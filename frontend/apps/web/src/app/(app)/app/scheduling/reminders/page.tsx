// W16 · H1 — Default Reminders Quick-Setup route.

import RemindersQuickSetup from "@/components/scheduling/automations/RemindersQuickSetup";

export const metadata = {
  title: "Default reminders · Pantopus",
};

export default function SchedulingRemindersPage() {
  return <RemindersQuickSetup />;
}

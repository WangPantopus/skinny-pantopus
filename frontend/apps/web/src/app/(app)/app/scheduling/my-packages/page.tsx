// W15 · G11 — My Packages / credits route (customer). Thin client wrapper around
// the stream-owned MyPackages component.

"use client";

import { MyPackages } from "@/components/scheduling/packages";

export default function MyPackagesPage() {
  return <MyPackages />;
}

// Today — the daily briefing as a first-class tab (wedge Phase 1.5, D2).
// Hosts the same screen the Hub's Today card opened at /app/hub/today;
// that route stays for deep links and the morning push.
export { default } from '../hub/today/page';

// W1 Setup & Hub — public surface. The Scheduling Summary Card (A5) is a pure,
// reusable component other streams may import read-only.

export { default as SchedulingSummaryCard } from "./SchedulingSummaryCard";

<?xml version="1.0" encoding="utf-8"?>
<!-- P6.6 — exposes the app cache dir so generated PDFs (emergency card)
     can be shared via FileProvider content:// URIs. -->
<paths>
    <cache-path
        name="shared_cache"
        path="." />
</paths>

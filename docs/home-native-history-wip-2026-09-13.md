# Native reviewer history — active WIP, September 13

This branch preserves 24 native files (10 iOS, 14 Android) above backend/browser history eb22e615. It also corrects the frontend SDK default import, separately verified on the primary branch. Native history is NOT integrated into PR #32 or accepted installed.

iOS asynchronous test setup removes all six new MainActor diagnostics. The signed rebuild, 31 focused checks, full 4,459 checks/168 skips, and both installed reader phases pass. Actual old list/detail 200 bytes cannot restore content after current permission denial; all 679 installed app files match the retained signed product. See [the bounded iOS reader report](home-ios-residency-review-history-2026-09-13.md). Separate fresh native applicant/reviewer cycles remain pending; the new six-phase opt-in cycle driver compiles in a separately signed UI-only runner but has not executed.

Android has 14 candidate files. CI exposed style violations; local gates additionally found complexity and timestamp readability issues. Bounded formatting, extracted UI blocks and named timestamp fields now pass style/static analysis and all 21 focused tests. Debug assembly/lint also pass; full Debug is running, while full Release product gates and installed acceptance remain open. Driver preparation is only syntax checked; dialog accessibility tags require real hierarchy inspection. Cancellation of a held response is not proof of delivering an obsolete 200.

Then prove both native own-history readers and separate fresh applicant/reviewer cycles, including current authority, lost replies, account/foreground changes and post-ack history. Integrate only after required product and installed gates. Neither native candidate completes R03.

Private evidence: `/private/tmp/pantopus-home-residency-cycle-r1/{ios-history-candidate-r1,android-history-candidate-r1,android-history-driver-preparation-r1,history-acceptance-preparation-r4}`. The r4 fixture binding incorporates only the corrected SDK import; accepted prior HTTP/browser evidence is preserved with its original limits. Consult the private operator index for durable copies and exclusive leases. Both owned devices are stopped with data retained. Never install loopback builds on a physical phone.

PR #35 now tracks this branch as an explicitly labelled unfinished draft. PR #36 separately tracks current claims. Both originally appeared ready with empty descriptions; their draft state and concrete scope/limits were corrected on live inspection. PR #36’s initial safeguard failure is the missing migration compatibility declaration; it is not an accepted current-claims repair. Primary #32 and paid #34 remain unfinished drafts.

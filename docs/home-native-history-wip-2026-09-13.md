# Native reviewer history — active WIP, September 13

This branch preserves 24 native files (10 iOS, 14 Android) above backend/browser history eb22e615. It also corrects the frontend SDK default import, separately verified on the primary branch. Native history is NOT integrated into PR #32. Both installed readers and the separate fresh iOS cycle now pass; the fresh Android cycle remains open.

iOS asynchronous test setup removes all six new MainActor diagnostics. The signed rebuild, 31 focused checks, full 4,459 checks/168 skips, and both installed reader phases pass. Actual old list/detail 200 bytes cannot restore content after current permission denial; all 679 installed app files match the retained signed product. See [the bounded iOS reader report](home-ios-residency-review-history-2026-09-13.md). The separate fresh iOS cycle passes submission, lost-reply rejection and normal cold recovery, resubmission with preserved identifiers, independent Member approval, current dashboard entry, protected removal and denied old Home-link access. Each reviewer retains only their own immutable decision. Final counts are two submissions, two decisions and one removal; all three observed protected original stores are empty. SQL canonical replay is verified, but rejection wire-body hashes differ. Driver-only interruptions and accepted subsegments are preserved in [the iOS report](home-ios-residency-review-history-2026-09-13.md).

Android formatting/static checks, Debug and Release lint/build/signing, 21 focused checks and both full variants pass (4,647 checks/80 skips each). Actual installed pagination, exact first/oldest/other-account detail, failure/retry, foreground, account changes and current-authority denial pass. Held requests cancel rather than deliver old bytes. The optimized twelve-record codec probe passes without installing Release, and its installed Debug APK/preferences stay unchanged. See [the bounded Android report](home-android-residency-review-history-2026-09-13.md). Secure dialog row IDs/raster capture limit the exact visual assertions; stopped driver attempts are preserved.

Next complete the separate fresh Android applicant/reviewer cycle, including current authority, lost replies, account/foreground changes and post-ack history. Integrate only after required product and installed gates. Neither native candidate completes R03.

Private evidence: `/private/tmp/pantopus-home-residency-cycle-r1/{ios-history-candidate-r1,android-history-candidate-r1,android-history-driver-preparation-r1,history-acceptance-preparation-r4}`. The r4 fixture binding incorporates only the corrected SDK import; accepted prior HTTP/browser evidence is preserved with its original limits. Consult the private operator index for durable copies and exclusive leases. The shared populated native-reader fixture is exactly cleaned (374 candidate/366 retained tables and logical catalogs). The separate fresh iOS fixture is also exactly cleaned; its durable archive verifies 26,871 files with manifest `11ef590537b867509ffe7ae0d58875773d684b1367b1a9ec13d9bf50b8f9765f`. iOS F9 is stopped with userdata retained; root exclusively owns Android emulator-5556/API18084/REST18086 for the separate zero-command cycle. The Android durable archive verifies 1,186 files, manifest `2d5fe29803753e54222ee630cbf2477f5d93ea83ebf52a649eba11124d75cf89`. Never install loopback builds on a physical phone.

PR #35 now tracks this branch as an explicitly labelled unfinished draft. PR #36 separately tracks current claims. Both originally appeared ready with empty descriptions; their draft state and concrete scope/limits were corrected on live inspection. PR #36’s missing migration compatibility declaration was corrected at `621ddde84`; its fresh CI now identifies an ambiguous alias in the SQL contract. Current-claims repair is still unaccepted. Primary #32 and paid #34 remain unfinished drafts.

The reader/product source `9350896c4f6a822afdc2fc5cdb8e59a4970e063d` passes all 16 checks in [CI 34768705945](https://github.com/WangPantopus/skinny-pantopus/actions/runs/34768705945). Later documentation and UI-only cycle-driver corrections require their own pushed-head CI; the retained application binaries remain unchanged.

# Pantopus peer-stream takeover — September 25, 2026

**Observed 2026-09-25 20:29 UTC / 13:29 PDT.** This updates the [September 24 detailed handoff](HANDOFF-2026-09-24-0957-PDT.md) for live Git, runtimes and ownership. It does not erase that handoff's actual-app evidence or the earlier paused/final checkpoints. The user now wants **three independent, parallel agents**, one per stream, with no parent agent directing subagents. Give each agent its own [copyable prompt](NEXT-AGENTS-PROMPT-2026-09-24.md). Each agent reports directly to the user and works its domain through real web, iOS simulator and Android emulator journeys; together they cover every reachable feature/function/workflow and edge case. Use the existing 80-row backlog, original155 UX inventory and screen catalogs without inventing a duplicate tracker.

## Live state to verify again before work

- Coordination checkout `/Users/yingpengwang/pantopus-coordination`, clean branch `codex/workstream-coordination` at `5bdf0a89b4d8352455d121e85e316f30e1258371` **before this update**. Fetched `origin/master` is `630bc49b5a81b26c3fc231d223aca689ff868906`. Fetch and check actual heads/status again on takeover.
- Batch428 and reviewed source PRs411/417/420–424 are merged. [PR418](https://github.com/WangPantopus/skinny-pantopus/pull/418), founder-owned design docs, is now **merged** at `ae6eaab17de595e85f05770f025261a8784e5f85`; do not reopen it. [PR426](https://github.com/WangPantopus/skinny-pantopus/pull/426), Stream1 native bids/camera, is now **merged** via updated head `d3a0537510a817e05e7d4c4dced73985459ba236` and master merge `630bc49b5`. Those two later merge heads were not in the September24 exact review; verify integration/source ancestry before mapping accepted IDs. [PR425](https://github.com/WangPantopus/skinny-pantopus/pull/425), Shared native Hub, remains **open/CI failing** at newer head `002e7deaab2a4e2db1eb732c9edf203ac3839447` (Android `DiscoverHubSnapshotTest` empty/populated assertions in [run36076436295](https://github.com/WangPantopus/skinny-pantopus/actions/runs/36076436295)); its two post-review commits require source/diff review. [PR427](https://github.com/WangPantopus/skinny-pantopus/pull/427), Stream3 web booking actions, remains **open/CI green** at `5cdcecba697bced63ca956058be1344d75330364`. Other open [PR429](https://github.com/WangPantopus/skinny-pantopus/pull/429) Ballot and [PR430](https://github.com/WangPantopus/skinny-pantopus/pull/430) geo are outside these saved three-stream reviews; inspect ownership before touching/queuing them. No reviewed batch11 is known.
- Last reconciled original155 issue ledger remains **85 merged /70 unfinished** before batch428/426 ID reconciliation; broad80 remains **13 closed /67 partial**. These are different, overlapping measures; neither is an app-completion percentage. The 80-row document has not had a post-September23 closure reconciliation. Map accepted source-bound real-app evidence to each relevant criterion before changing either count. Do not replay unchanged journeys just to update accounting. The fresh complete-app pass remains ahead.
- This Mac has **11 minutes uptime** at observation, pressure1,39GiB disk available. No test simulator/emulator is booted; no owned stream ports18130/18138/18142/18143 or isolated database ports64531/32/64553/54/64561/62 were listening. `/private/tmp/pantopus-tools` and the previous `/private/tmp` stream worktrees/runtimes are **absent**; the old September24 live-process/lease instructions are stale. Do not assume the prior dirty Stream2 Mail draft or25 live fixture rows survived. The private paused-runtime backups and immutable audit bundles in the durable founder-independent store still exist, but a quick search did **not** find the Stream2 Mail draft/03:05 page2 screenshot/fixture receipt there. Preserve any surviving Docker volumes; inspect before starting, rebuilding, creating fixtures or restoring. Never recreate a fixture merely from a historical description without exact current DB check and reviewed scope. Restore only the missing committed helper scripts from `coordinator-state-2026-09-24/tools/`, with an empty queue; never restore an old queued PR.

## Peer ownership and shared work

- **Stream 1:** gigs, marketplace, offers, payments and its remaining original IDs; also Shared UX **Hub/Discover/Pulse/Posts and PR425**. It has the technical **integration/merge steward** role because this repository requires one serial exact-head queue. This is a narrow file/queue ownership, **not** supervisory authority over Streams2/3. It reviews their submitted PR/evidence, coordinates shared-file integration, and merges only reviewed, green, exact heads using original §8.2/§8.3. It maintains the single hub README/project-handoff/Claude-memory resume block after peer milestones. Stream1 may not silently change another stream's source, fixtures or decisions.
- **Stream 2:** Home, residency, Place, intelligence, records, bills, Mail/guests, Support-train UX tied to those journeys, and its remaining original IDs. Its previous S2-11 iOS Mail Dismiss and S2-22 Android page2 failure are **unfinished**; prior 2-file draft existed only in a vanished `/private/tmp` worktree and was never built/verified. Start from current source and saved descriptions/accepted evidence; locate any private backup before reconstructing. The 25-letter fixture may no longer be running; verify exact IDs/counts in an isolated retained DB, do not assume preservation or recreate automatically.
- **Stream 3:** accounts, social, notifications, scheduling/booking and related Shared UX, plus its remaining original IDs. PR427 is its reviewed web slice; S3-51/52 remain separately scoped. The extra Personal BookingPage created implicitly by GET was intentionally retained in the old isolated database; verify whether its volume exists before any access. Never treat GET `/booking-page` as guaranteed read-only. No unverified paid/provider or native18/52 closure.

All three peers independently use real web UI, installed iOS simulator app and installed Android emulator app for **every reachable journey in their domain**, across normal, empty, partial, offline/unavailable, slow/stale, permission/role, repeated tap, retry, back/deep-link, restart/account-switch and persisted-result cases where relevant. Trace screen→caller→API/service→database; distinguish real provider responses from injected faults and synthetic auth. Record exactly what passed on each app and what remains unverified; a build or green CI alone is never acceptance. Preserve existing working designs, navigation and data/privacy behavior. Make the smallest demonstrated repair in existing code, rerun that journey and affected regressions, and run required CI. No new unit tests/local unit campaigns. Do not use one agent's fresh sweep to erase another's accepted evidence; divide cross-domain flows by owning caller and request peer review of shared contracts.

**Peer coordination without a manager:** each agent claims its own stream status file (`01-gigs-payments.md`, `02-home-household.md`, `03-accounts-social.md`) and own application worktree. Before touching a shared file, fixture, runtime, heavy build or iOS driver, send a direct message to the other two agents naming files/ports/device, purpose and expected release; wait for an explicit handoff where ownership overlaps. Stream1 maintains only the common integration queue/hub. Streams2/3 send PR head, changed paths, source/evidence binding, exact cleanup and CI results directly to Stream1 **and the user**; they do not merge. If Stream1 is unavailable, leave reviewed PRs open rather than start a competing queue. Work read-only/web/API while a device or heavy lease is occupied; never leave an automatic waiter. One heavy build/install and one iOS UI driver across peers. A peer's failed or partial journey is not a block on other independent stream work.

Use explicit safe cwd; never modify `/Users/yingpengwang/skinny-pantopus`, contact founder64521/64522/backend8000 or boot simulatorEB5AD759. No search-filter security audit, founder§7/A17/data/marketing scope. Stripe TEST/manual only, no capture. No secrets/raw tokens/DB archives/operator logs in Git/chat. No bare stash, gc, maintenance, repack or worktree removal. Preserve retained databases and audit bundles; do not wipe/reseed. Inherited founder.env may be read-only loaded only after proving isolated overrides and disabled jobs/providers. Stream1's inherited harness uses SIGINT, never SIGTERM. Escalate money/security/legal/retention/new-table choices to the user with exact evidence and a concrete proposal; continue independent authorized work.

Read this file, the full September24 handoff, original paused handoff and checkpoint files, `AGENTS.md`, `docs/PROJECT_HANDOFF.md`, current hub README, original founder direction blocks, the three stream status files and `coordinator-state-2026-09-23/tools/AGENT-RULES-2026-09-23.md` before application work. The user's peer arrangement and real-app verification instructions above supersede older coordinator/subagent topology and stale runtime snapshots. The detailed historical evidence and design-preservation rules remain in force.

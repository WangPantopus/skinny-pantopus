# Message to send to the independent Stream 1 agent

You are **Stream 1**, a peer of Stream 2 and Stream 3, not their parent or manager. Own gigs, marketplace, offers, payments and the remaining Stream1 original inventory. Also own Shared UX **Hub, Discover, Pulse, Posts and PR425**. You alone hold the technical serial integration/merge queue and shared hub status file, because this repository needs one exact-head merge writer; this is a coordination duty, not authority to direct the other two peers. Communicate directly with them and the user, and keep progressing your own stream in parallel.

Before application work, read `/Users/yingpengwang/pantopus-coordination/docs/workstreams/HANDOFF-2026-09-25-PEER-TAKEOVER.md` and the entire September24 handoff it links, then the paused handoff and four checkpoints, `/Users/yingpengwang/pantopus-coordination/AGENTS.md`, `docs/PROJECT_HANDOFF.md`, current `docs/workstreams/README.md`, founder15:00/17:00/19:30 directions, original155 inventory, 80-row backlog, your `docs/workstreams/01-gigs-payments.md`, and `coordinator-state-2026-09-23/tools/AGENT-RULES-2026-09-23.md`. Work with an explicit safe cwd in `/Users/yingpengwang/pantopus-coordination` and isolated application worktrees only. Fetch and verify live master, PR heads/CI and Git ancestry; never use a handoff SHA as a substitute for Git. September25 snapshot: master630bc49b5, PR418 and PR426 merged, PR425 open at002e7dea with Android DiscoverHubSnapshotTest empty/populated failures, PR427 open/green, PR429/430 outside saved review. Reconcile accepted batch428/426 original IDs only against their exact criteria and real evidence, without replaying unchanged journeys. The last reconciled155 count85/70 and broad80 count13/67 are tracking ledgers, not app-completion percentages.

First inspect `uptime`, memory pressure, disk, owned ports/devices, heavy/iOS leases, private backup locations and any running agents. At the last check the Mac had recently restarted: no test devices, isolated app ports or `/private/tmp/pantopus-tools`/stream worktrees were present. Restore missing helpers only from committed `coordinator-state-2026-09-24/tools/` into a fresh private tools directory with an **empty queue**, after confirming no prior watcher/runner. Do not restore an old queued PR. Preserve old immutable audit bundles and any Docker volumes; never wipe/reseed to recreate yesterday's fixture. Coordinate one heavy build/install window and one iOS UI driver through direct peer messages and explicit handoffs; no automatic waiter or blanket process kill. Your inherited Stream1 harness must receive SIGINT, never SIGTERM.

For Shared UX, inspect PR425's **new two commits since the September24 accepted head**, its actual Android snapshot failure and current source/goldens. Repair only a demonstrated discrepancy in the existing design and run required CI; do not redo accepted C03 Android/iOS notification→Bob-profile→Back simply because CI failed. Continue remaining C18 comment-count and C32 Android report-success-toast checks with actual caller/API/persistence and exact fixture/cleanup review. Preserve accepted Posts/Support/Hub afters and correct default-size List/Map evidence. For your Stream1 inventory, start from the newest accepted PR426 native bids/camera integration and remaining S1-09/S1-22/S1-25 boundaries; leave S1-08 money ownership and A17 founder-only package contract gated. Do not claim physical flash, AI provider, listing publication, paid settlement or hosted behavior from synthetic/local checks.

**Launch the real web app, installed iOS simulator app and installed Android emulator app** against isolated backend and retained or explicitly reviewed fixtures. Inventory every reachable feature, control and journey in your ownership on each platform (or document a genuinely absent platform surface). Trace real screen→caller→endpoint/service→database; verify successful persistence and reload, empty/loading/partial states, explicit 403/404/5xx and offline/slow response, retry, duplicate taps, late/stale responses, role/privacy, Back/deep links, app restart/account switch where relevant. Check the frontend message and recovery action as well as backend status and absence of unintended writes. Preserve existing designs/navigation; record a real before or concrete unmet requirement, make the smallest in-place repair, then rerun that journey and affected regressions. Required CI must pass; do not add unit tests or run local unit campaigns. Reuse accepted evidence when source/config/behavior are unchanged. Record exactly what passed in each real app and what provider/physical/hosted boundary remains unverified.

For integration, Streams2/3 independently produce PRs and sealed evidence and report directly to you and the user. Review exact changed heads, real-app evidence and shared-file conflicts, then alone run the original §8.2/§8.3 combined-batch queue for green reviewed PRs. PR427 combined with merged417 has a known booking-detail import conflict; preserve both `useRef` and `useRouter`, compare to the exercised runtime binding in the handoff, and rebuild from fresh master. Never merge a runtime harness wholesale, update every branch merely for “behind,” or touch founder PR46/§7/A17 or unrelated429/430 without ownership. After a meaningful milestone update the single hub README, PROJECT_HANDOFF and Claude memory, commit/push the coordination branch, and send precise status to the user and both peers. Continue until your domain's end-to-end inventory and integration are actually complete, not until a build goes green.

Hard limits: never modify `/Users/yingpengwang/skinny-pantopus` or contact founder64521/64522/backend8000 or simulatorEB5AD759. No search-filter security audit. Stripe TEST/manual only, no capture. No secrets/raw tokens/DB archives/operator logs in Git/chat. No bare stash, gc, maintenance, repack or worktree removal. Escalate money/security/legal/retention/new-table decisions to the user with a concrete reviewed proposal; continue independent work meanwhile.

# Stream 2 checkpoint, 2026-09-23 (paused by the founder)

- **Written:** 2026-09-23T19:52:25Z (`date -u`). Stream 2 agent (Home & household); reports to the coordinator
  ("main").
- **State:** paused at a clean point. No build is running, no device is booted, and no device or heavy slot is held.
  No Next server is running.

## Open PRs (heads from `gh pr view` at 19:51:51Z)
| PR | Branch | Head | State / batch |
|---|---|---|---|
| #257 | claude/stream2-native-notification-links | `fab5fa4fd6100192cd9678509d8559e514bd1513` | Open. Batch 3. Native notification links. |
| #325 | claude/stream2-native-mail-task-linked | `cce809b5714f883404669f0af2a088380d1ac3b7` | Open. Master `9779bf9d3` merged in (no force-push). **iOS re-check pending**, then batch 4. |
| #346 | claude/stream2-native-earn-honest | `d0527d5453c396bd77f5a119d9454a588732a9b3` | Open. Batch 3. |
| #380 | claude/stream2-web-home-dead-ends | `bae7ab6cc74ea4ae9defa7e93b8f6e6a5fe1a1ca` | Open. Approved; batch 3. The database job failed on a ghcr rate limit; the ECR fallback is on master now. |
| #381 | claude/stream2-web-visit-access-row | `5c1ceda88805ebb49dbd9cddac952d7b44a9491a` | Open. Approved; batch 3. |
| #386 | claude/stream2-share-eta-sender | `9d63e55e723dea69cfebca4d75bcad5acd7749c7` | Open. Batch 3. Share-ETA sender + qualified HomeOccupancy→User embeds (option B). |
| #388 | claude/stream2-native-mail-trust-chips | `9acfcebf8cc0324f9f08e2164d68745a1b568e59` | Open. Accepted for batch 4 once CI finishes. |

Merged earlier today, in batch 2 (`9779bf9d3`): #320 #321 #332 #363 #368 #369 #370.

## Unpublished work (pushed to wip refs only; no PR)
| Ref | SHA | Contents | Left to do |
|---|---|---|---|
| `refs/heads/wip/stream2/home-trust-claims` (local branch `claude/stream2-home-trust-claims`) | `759be809c8e3d06661dde978b47f1c537b63a7b0` | The Home group, on master `9779bf9d3`. Web: Travel Mode note + "Ask a Verified Neighbor" removed; no community household counts. Native: Home Security keeps only Address precision, and the sample footer "14 Elm Park Lane · Last audit 2h ago" is gone; no Trusted neighbors entry; no "N reached"; Hub pill reads "N neighbors within 1 mi"; hub.js comment. Android S2-14: the Today tab error state instead of "Claim your address". | Build, iOS/Android after captures, RESULT, seal, push, PR. See the bundle STATUS. |
| `refs/heads/wip/stream2/native-place-home` (local branch `claude/stream2-native-place-home`) | `0623d89d0168993a8e8694493efd402d6aa83d3d` | The Place group, on master `9779bf9d3`. Android S2-21: Place dashboard pull-to-refresh + reload after a verification flow. iOS S2-15: You-stack Property details (+ correction), in drawsOwnHeader. iOS/Android S2-05: the dead "Link an access code" row is removed. | Build, before/after captures (S2-05 needs `calendar.edit`; S2-21 needs a T3 fixture), RESULT, seal, PR. See the bundle STATUS. |
| `refs/heads/wip/stream2/vacation-hold-dates` (local branch `claude/stream2-vacation-hold-dates`) | `65bc96a970f26fa8fc8dcd9d1d01561e190eb170` | An older vacation-dates branch, pushed only because it had no remote copy. Its fixes shipped in #325's grouped commits. | Nothing expected. Confirm it's superseded, then delete the ref. |

Also local but already on origin: `claude/stream2-native-home-links` (= #257's head; it was created for the routing
group and has no commits of its own), `claude/stream2-native-notification-links-build` (on origin via #257 and batch 3).

## Verification in progress
- **Home group** (`.pantopus-recovery/audits/20260923-stream2-home-trust-claims-r1/`, STATUS.md, not sealed).
  - Captured: web before/after; iOS befores (settings, Trusted neighbors, Security, footer); Android befores (Hub
    pill "4 verified neighbors within 1 mi", settings, Security, community "4 reached", S2-14 offline/online).
  - Missing: the build (`build-home-claims.sh`) and all after captures.
- **Place group** (`.pantopus-recovery/audits/20260923-stream2-native-place-home-r1/`, STATUS.md, not sealed).
  - Captured: iOS S2-15 before (the tap changes nothing).
  - Missing: the build, and S2-15 after, S2-05 before/after and S2-21 before/after.
- **#325 re-check** (`.pantopus-recovery/audits/20260923-stream2-native-mail-task-merge-r1/`, STATUS.md).
  - Captured: nothing. The fixture notice `18ab76dd` is created.
  - Missing: the iOS rebuild at `cce809b57`, and the re-check of the 409 reason, owner replay and vacation dates.
- **Sealed today:**
  - `20260923-stream2-web-home-dead-ends-r1` (#380)
  - `20260923-stream2-web-visit-access-row-r1` (#381)
  - `20260923-stream2-share-eta-member-lookups-r1` (#386)
  - `20260923-stream2-native-mail-trust-chips-r1` (#388)
  - plus earlier bundles for #368/#369/#370/#346/#257.

## Runtime
- **Database.** Supabase project `pantopus-stream2-native-r1` (workdir `/private/tmp/pantopus-stream2-native-r1`),
  API 64553, DB 64554, shadow 64555, 64556. Its Docker containers are **left up**; volumes persist across a reboot.
  - After a reboot the workdir under /private/tmp is gone.
  - Restart the containers with `docker start` on the `*_pantopus-stream2-native-r1` containers. Never touch
    `pantopus-home-gig-replay` (64521/64522).
  - `psql.sh` = `docker exec -i supabase_db_pantopus-stream2-native-r1 psql -U postgres -d postgres`.
- **Backend on 127.0.0.1:18143 (left up).**
  - Source: `node /private/tmp/pantopus-stream2-r06-runtime/run-backend.cjs /private/tmp/pantopus-stream2-work-backend`,
    where the backend worktree is detached at master `9779bf9d3`.
  - Started by `start-backend-src.sh <checkout>`, which sources
    `/private/tmp/pantopus-workstream-home/.stream2-verification/native/supabase.env`.
  - It forces the harness overrides: PORT 18143, APP_URLS 18144, PGBOSS/CRON off, and every provider key blank.
    Stripe gets a non-key placeholder.
  - Restart it (or reset its in-memory login limiter) by killing the pid in `backend.pid`, not with pkill.
- **Proxy on 127.0.0.1:18142 → 18143 (left up).** `cd /private/tmp/pantopus-stream2-r06-runtime && node proxy-18142.cjs`
  (logs to proxy.jsonl). The apps point at 18142: iOS `127.0.0.1:18142`, Android `10.0.2.2:18142`.
- **Web.** Next dev on 18144 is **stopped** (it used about 7 GB). When needed, start it from
  `/private/tmp/pantopus-stream2-work/frontend/apps/web` with `next dev -p 18144 -H 127.0.0.1`, and stop it right
  after.
- **Worktrees** (all under /private/tmp, so a reboot wipes them; everything is on origin or in wip refs):
  - `pantopus-stream2-work` (web; detached `9779bf9d3`);
  - `pantopus-stream2-work-native` (on `claude/stream2-native-mail-trust-chips`, clean);
  - `pantopus-stream2-work-backend` (detached `9779bf9d3`).
  - After a reboot, recreate them from origin. Native builds need the gitignored local.properties/.env (see memory
    "worktree native build setup").
- **iOS derived data.** `/private/tmp/pantopus-stream2-r06-runtime/ios-dd` (about 8.6 GiB). Reuse it and don't
  delete it this session. A reboot wipes it.
- **Devices.**
  - iOS simulator `6F914A30-8585-4B05-9E05-94441675F10A` ("Pantopus Stream2 Packages"), shut down. rdios is signed
    in; Face ID enrollment and match go through `simctl spawn … notifyutil`.
  - Android AVD `Pantopus_Home_Recurrence_Acceptance` → emulator-5556, off. The installed APK is the mail-trust build
    `1142062f` (18142).
  - Boot with `emulator -avd Pantopus_Home_Recurrence_Acceptance -port 5556 -no-snapshot-save -no-boot-anim
    -no-audio`, after `device-slot.sh acquire`.
- **Fixture accounts** (names only; credentials are in the env files): owner, viewer (lease resident), editor (lease
  resident), rdios (member, the iOS account), rdmember (moved out), rdnohome (no home). Home
  `f0e51100-0000-4000-8000-000000000200`, with no coordinates; they were set only during the pill capture and
  restored.
- **Fixtures still in the database:**
  - 3 CommunityMailItem rows from the T42 web runs (`733eae1d`, `fe704e73`, `8879316d`);
  - #325 re-check notice `18ab76dd`;
  - T38 businesses (kept for native checks);
  - older rd fixtures (VacationHold `c1c1cf92`, misfiled Mail `11b73710`).
- **Harness backup** (no env files or tokens): `.pantopus-recovery/checkpoints/stream2-harness-2026-09-23/`
  (runtime scripts, plus `scratchpad/` build scripts). The runtime directory itself is under /private/tmp.

## Decisions received from main (today)
- #363 approved; misfiled-row repair is a founder action (no migration).
- #346 Paparazzi re-record: done.
- Translate: backend 501, the web hides it, native Translate is hidden. Cached rows are a founder question.
- Web Mailbox PR = S2-12/13/18/19/08 + translate (#369).
- Security: senderBusinessName spoofing fixed server-side from the business permission helpers (#368).
- Web compose "Send as" picker (#370).
- Unboxing save-to-vault: leave it (unreachable).
- share-eta: relabel as the sharing member with `pantopus_user` (#386).
- Mail-detail scope:
  - task chips, Translate hidden at the call sites, senderTypeLabel from stored trust;
  - party change marked code-only; no more unreachable edits;
  - A17 variant set is a founder note.
- Mail-detail additions:
  - iOS/Android hero pill from stored trust;
  - sender card no longer under the dock;
  - community reach number hidden;
  - avatar check only when verified ("include").
- Home:
  - Members badge unchanged;
  - Hub pill "N neighbors within 1 mi" + hub.js comment + self-count in Limits;
  - Home Security option (c): keep only the enforced toggle (address_precision), hide the rest on all clients,
    stored values untouched;
  - web Travel Mode note removed; Trusted neighbors entry hidden on both apps;
  - web Community drops the number;
  - hide "Ask a Verified Neighbor" on web vacation/travel.
- Home group additions:
  - the Security footer shows a real address only if the screen has it, otherwise nothing; no "Last audit";
  - S2-14 shows an offline/error state (C-28 class).
- #380 approved: S2-20 pickers removed (no upload route).
- #381 approved: removal, not a link to the access-codes page.
- #386 option B: qualify the embeds for share-eta and compose; routeMail unchanged, as a founder question in Limits.
- #325: merge master (no force-push), rebuild iOS, re-check the three sections, report the head; then batch 4.
- #388: batch 4 after CI.
- Disk and memory:
  - no `git gc`/maintenance/repack;
  - delete build outputs after verification, but reuse ios-dd;
  - heavy slot for builds and installs;
  - Next only for web checks.
- Gradle: `./gradlew --stop` only while holding the heavy slot, before release.

## Open questions
- **S2-16 (unboxing and ceremonial Share).** What should Share send? Or hide it? The rest of those controls get
  hidden.
- **S2-06 (landlord `tenant_request` link on natives).** This is a product call. At minimum, open the Home?
- **routeMail's resident step.** Founder question, recorded in #386.
- **Home scheduling hub for plain members.** The whole hub fails on a manage-only booking-page 403. Reported to
  main; probably Stream 3.
- **Old wip ref.** Delete `wip/stream2/vacation-hold-dates` once it's confirmed superseded.

## Next actions, in order
1. **#325 iOS re-check.** Acquire the heavy slot, then run `scratchpad/stream2/build-ios-325-merge.sh` (builds
   `cce809b57`; it waits for `work/ios-free`). Boot the simulator under a device slot and create `work/ios-free`.
   - Re-check as rdios: 409 on `caf3e55b`; replay on `18ab76dd` (create twice); vacation From/To/Back.
   - Delete fixture `18ab76dd` and its task, then write the r2 bundle and report the head (`cce809b57`) to main.
2. **Home group.** Run `build-home-claims.sh` (heavy slot; it ends with `gradlew --stop` before release).
   - iOS after: settings, Security.
   - Android after: install `work/t42-home-claims.apk`; Hub pill with coordinates set temporarily, then restored;
     settings, Security, community; S2-14 offline error state + Try again online.
   - Delete the 3 CommunityMailItems, then RESULT (per-toggle table), seal, push the branch, open the PR.
3. **Place group.** Build.
   - S2-15 after (iOS).
   - S2-05 before/after with a temporary `calendar.edit` override (removed after).
   - S2-21 before/after with the Home label change and the T3 fixture (removed after).
   - Then RESULT, seal, PR.
4. **Remaining inventory.**
   - Routing group: S2-06 (after the decision), S2-10, S2-17, after #257 merges.
   - Mail list group: S2-11, S2-16 (after the decision), S2-22 and the S2-03 remainder, after #346 merges.
   - Earlier backlog items (#278 native check, #263 Android "Invalid Home id", Decision 4 landlord placeholder, rd
     fixture cleanup).

# Stream 3 Codex resume checkpoint

Written 2026-09-23T21:46:10Z. Geo completion in progress.

- Existing branch `claude/stream3-native-business-geo-decode` pushed at `2e7ad13f1edad86a82f1bc336b4ff32a57e7ca29`. Only new application change: combine nullable lat/lng guard to satisfy detekt ReturnCount. No new unit tests.
- Owned worktree `/private/tmp/pantopus-stream3-work`; API source `/private/tmp/pantopus-stream3-api`, current canonical master cef95ab67864ccca3ca243cc0d936a87d342cc8f.
- Docker contexts default and desktop-linux both address the same socket. Both had zero containers/volumes and SQL64532 had no listener. Old retained DB is unavailable; no claim its fixture was deleted.
- Coordinator approved fresh isolated canonical replay. Project `/private/tmp/pantopus-stream3-block-r1`, API64531/SQL64532; 89 ledger entries ending 20260923000300. No old archive restored, no new application tables or migrations.
- API18130 uses `resume-api-launch.py` in `/private/tmp/pantopus-stream3-20260923-r1`; real full backend, jobs off, external providers disabled, local credentials kept private.
- New synthetic actor/business/location identities in `resume-geo-fixture-safe.json`; private credentials separate. Business created through real API. Location seeded directly after disabled address provider refused synthetic address. Cleanup remains required.
- Existing iOS before/after retained and not rerun. `source-reuse-and-schema.json` in geo bundle proves reused iOS after DTO identical and both before decoders still match current master.
- Android fresh login succeeded on owned emulator-5554, AVD Pantopus_Stream3_Accounts_R3. Current cold build geo2 had detekt ReturnCount failure; source fixed during build. Must run `run-geo3-resume.sh` after geo2 exits to bind final build to current head and install under heavy slot.
- Heavy slot held by geo2; device slot held stream3. After geo3 install, capture profile/service area, delete exact new fixture, seal existing geo bundle, open PR, notify coordinator, stop owned emulator/API and return. Do not start scheduling build in this slot.
- Scheduling/You branch remains unchanged e7d72161a; next follow original checkpoint after coordinator resumes agent.
- Coordinator reviewed publicShare WIP: exact same blob already on master, so leave unused.

## Update 2026-09-23T21:56:26Z

Final Android geo3 ktlint/detekt/assemble passed; no unit tests. APK and generated BuildConfig/dex network constants recorded in bundle `android-build-port-receipt.json`. A first install check matched non-network `:8000` source-map bytes; precise URL scan is clean. No install happened from that failed guard. Queued `resume-geo-install.sh` waits for Stream2 active Xcode build, then owns heavy/device slots and boots/installs Android. Emulator currently off, device slot released. Do not interrupt active Stream2 build.

Scheduling preparation only: current native SchedulingHubModel.fetch and SchedulingHubViewModel.fetch both return whole-hub error when booking-page403. `schedulingShared.canManageOwner(home,view)` requires existing calendar.view; do not change this permission policy implicitly. Current web SchedulingHub.tsx first awaits booking-page then allSettled the remaining reads; its behavior still needs real browser verification in that later group. MeViewModel.swift and Android you/me/MeViewModel.kt still bind personal trust to profile.verified and Business to unbound context on master; original pushed scheduling branch remains untouched.

## GEO COMPLETED — 2026-09-23T22:02:32Z

- PR392 https://github.com/WangPantopus/skinny-pantopus/pull/392 published and attached; head `2e7ad13f1edad86a82f1bc336b4ff32a57e7ca29`. Required CI not awaited; coordinator handles review/batching.
- Final Android real app passed business deep link/profile + service area map against real backend18130/SQL64532. Screenshots, accessibility text, native request receipt, API numeric longitude/latitude and installed APK hash match retained. APK SHA2565b924b653e734d751dbe8f3d337b33b42a1b9e4d3c3f877c8a1fee9ce6883fd6.
- Existing iOS accepted before/after reused; byte-identical DTO source receipt in bundle. No fresh iOS run or broad acceptance claim. No unit tests run or added. Android ktlint/detekt/assemble passed.
- Sealed bundle `/Users/yingpengwang/estimate-rescue/skinny-pantopus/pantopus-stream-2-home-3ef380/.pantopus-recovery/audits/20260923-stream3-native-business-geo-decode-r1`:20 files, MANIFEST76a3a83ed7463c7e91a676d37aabe824e4dfb5c92846bfd2b18a00e0a64b30c8. Corrections explicitly retained: missing prior Docker engine, detekt failure, overly broad port-byte gate, emulator snapshot restoring older APK before final reinstall/hash match.
- All NEW synthetic actor/business/profile/location/auth rows cleaned to zero by exact IDs. Old location absent on fresh engine is not represented as deleted from old engine.
- API18130 stopped, emulator5554 shut down; Stream3 device slot released and no heavy slot held. Fresh canonical DB retained (89 versions through20260923000300). External providers disabled. Native worktree clean on existing branch, API worktree detached atcef95ab678.
- Current remote master was61c64f9c1778b9b11fda12dd0cfa4762fff3bef0 at finalcheck (docs391, application unchanged).
- STOP HERE for worker rotation. Next Stream3 task is unchanged scheduling/You branch and original checkpoint, only when coordinator resumes. Never use WIP publicShare (already identical on master). New scheduling fixtures required because old engine and original accounts absent; reuse new canonical runtime, no old archive restore.


---

## Preserved original paused checkpoint

# Stream 3 checkpoint (accounts, social, notifications, businesses, scheduling)

## Timestamp

2026-09-23T19:53:41Z (`date -u`, written when the file was finalised). The founder paused the work. Everything is pushed; no build, device or server of mine is running.

## Open PRs (all in batch 3, #387, head 91e0cc1726007b02c2cfe7df914587d840e4567a)

| PR | Branch | Head SHA | State |
|---|---|---|---|
| #349 | claude/stream3-native-delete-account-path | 0148579190875b6880c312f12e7775713796213d | open, batch 3 |
| #350 | claude/stream3-native-privacy-your-data-rows | ffbfcd368db33813dacb7e85d4dc8b31beb38885 | open, batch 3 |
| #371 | claude/stream3-native-business-profile-verified-claims | 4a7b0367221c20986e605ed5f6f7e96a3af92832 | open, batch 3. Master merged in (merge commit); re-verified; bundle resealed (MANIFEST 493b5c51…) |
| #372 | claude/stream3-native-chat-verified-claims | 2b076dc5f823d36987799422a8acb752d1375a49 | open, batch 3 |
| #373 | claude/stream3-native-profile-identity-claims | f5a38aab4cf4abdb52b844700ef204dcd8e0edac | open, batch 3 |
| #383 | claude/stream3-native-scheduling-trust-claims | 11eca404590ddc55bcadf0da80211e8d8f0ef0a2 | open, batch 3 (MANIFEST 624235e6…) |
| #384 | claude/stream3-native-policy-copy | b33c3294f03c12e80fc0843d142b2682d49200b3 | open, batch 3 (MANIFEST c717a480…) |

On all three of #371, #383 and #384, the batch-3-era database job failed on the ghcr.io rate limit. The ECR fallback (#385) is on master now.

## Unpublished work (pushed branches / wip refs, no PR yet)

1. **Business lat/lng decode fix (BLOCKING, top priority).**
   - Branch `claude/stream3-native-business-geo-decode` @ `a9f8be76913e9c993ca4bb0c59d116f80cab92aa`, on master 9779bf9d3.
   - iOS and Android `BusinessGeoPoint` read `latitude`/`longitude` (what `parsePostGISPoint` sends) and still accept `lat`/`lng`. The Android Pulse business-target picker handles the now-nullable values.
   - Left to do:
     - Android build of a9f8be769: ktlint, detekt, tests, APK. The first build, of e14772edc, failed to compile in the Pulse picker, which the amend fixes.
     - Android after capture.
     - Seal the bundle and open the small separate PR.
     - Delete the fixture BusinessLocation 980f6eb3-278e-456b-b902-54b903480e96 on Stream3 Biz R1.
   - Bundle (partial): `.pantopus-recovery/audits/20260923-stream3-native-business-geo-decode-r1/` (STATUS.md).
2. **Scheduling/You PR.**
   - Branch `claude/stream3-native-scheduling-you-owner` @ `e7d72161ab80ac663a98f4f139c2e472020840fc`, stacked on #383 (11eca4045).
   - Committed:
     - 8f4e4b43f: the hub's Business pill uses the first business from my-businesses and hides without one; the LIVE PREVIEW shows real next open times or "No open times yet". Android hub view-model test and hub snapshot fixture updated.
     - e7d72161a: onboarding "Times show in your time zone (<zone>)", with no CONFIRMED chip.
   - Not built or verified yet. The combo7 build was abandoned for the blocking fix.
   - Left to do:
     - Merge origin/master (9779bf9d3, batch 2) into the branch.
     - Home hub for a plain member (new main item): a 403 on booking-page must not fail the hub. Show what a member can see and offer no manage actions (iOS SchedulingHubModel.fetch and Android SchedulingHubViewModel.fetch). Check web's hub for the same pattern.
     - Me Business context from my-businesses (hidden with no business; Scheduling entry opens that business), both apps. Batch 2 already reworded the placeholder (Shared UX d254f5ad7d).
     - You header: the "Verified" TRUST stat and avatar check come from `residency.verified`, not `User.verified`, both apps.
     - Builds (split heavy-slot holds), E2E, bundle, PR to master (say it is stacked on #383).
3. **Items 6 and 7 (not started).**
   - 6: Help "Why do I need to verify my email?" claims verification unlocks posting and messaging. Check server enforcement; rewrite if it isn't enforced.
   - 7:
     - hide Report for a business's owner;
     - Identity Center "Visible only to verified connections": match it to the API;
     - "No verified businesses nearby yet": plain copy if the list isn't filtered by verification;
     - the AI prompt "Summarize today's mail and packages": keep only if the AI endpoint reads them;
     - the endorse 403 copy;
     - "Recent" / "verified recently".
4. **Old WIP found at the checkpoint.** `wip/stream3/accounts-social-publicshare-nostore` @ `3d39029b52bc6ba4f6f4cd54d46192e2f9561970` (from the accounts-social worktree).
   - Web `publicShare.ts`: post and persona fetches use no-store.
   - tsconfig lists the local `.next-stream3` types.
   - Origin unknown (an older session). Review before any use.
5. The carried rebase item (31668384e, connection/persona links) is on origin (`claude/stream3-native-connection-persona-links`). Nothing is unpushed.

## Verification in progress

- Geo fix:
  - iOS before and after done (after: master-geo build, dylib 6a503349…, opens Biz R1 with the location, service-area map and Directions).
  - Android before done (logcat `Required value 'lat' missing`). Android after not done.
- Scheduling/You: nothing verified yet.

## Runtime (owned; the founder's stack, :8000 and EB5AD759 are never touched)

- Supabase stack `pantopus-stream3-block-r1`: Kong 64531, DB 64532 (container `supabase_db_pantopus-stream3-block-r1`), plus mailpit `pantopus-stream3-mail-r3` (SMTP 64535, UI 64536). Still running; Docker volumes survive a reboot.
- API :18130: **stopped** at 19:51:38Z.
  - Source worktree `/private/tmp/pantopus-stream3-api` (master 39961f3e05).
  - Restart: `/private/tmp/pantopus-stream3-20260923-r1/api-restart.sh <N>` (log `api-runtime-<N>.log`; last N was 32).
  - It runs `api-launch.py`, which reads the env from `/private/tmp/pantopus-stream3-20260920-r1/transient-private-api-launch.json`. That file holds secrets, so it isn't copied here.
- Web login helper :18134: **stopped**. Restart: `nohup python3 /private/tmp/pantopus-stream3-20260923-r1/web-login-helper.py &`.
- Next (:18131): not running. Launcher `next-launch-s3.py`, with NEXT_PUBLIC_API_URL :18130 and a separate NEXT_DIST_DIR (.next-stream3).
- Fixture accounts (names only): Auth Bob, Auth Dana, Auth Evan, Stream3 Biz R1, Stream3 Draft Biz R2.
  - Credentials are in `/private/tmp/pantopus-stream3-20260920-r1/auth-fixtures.json` (not copied).
  - Sign-in helpers: `ios-signin.sh` (harness app, launch env) and `android-login.sh`. Neither ever prints the credential.
- Devices (both shut down; device slots released):
  - iOS simulator 0AE16FA0 (Pantopus Stream3): master-geo build installed, Dana signed in.
  - Android AVD Pantopus_Stream3_Accounts_R3 (emulator-5554): app-debug-biz371 installed, Dana signed in.
  - Boot: `emulator -avd Pantopus_Stream3_Accounts_R3 -ports 5554,5555 -no-window -no-audio -no-boot-anim`.
- Build tree `/private/tmp/pantopus-stream3-work` (branch stream3-batch6-verify, HEAD a9f8be769 = geo). iOS derived data `/private/tmp/pantopus-stream3-20260923-r1/ios-dd`.
  - Kept apps: `ios-apps/harness`, `ios-apps/master-geo`, `android-apks/app-debug-batch4-harness.apk`.
  - `android-build.sh` skips if `HOLD-ANDROID` exists (it doesn't now).
- **Reboot note:** everything under /private/tmp (worktrees, run dirs, fixture credentials, launch env) is lost on reboot.
  - Code is on origin.
  - Helper scripts (no secrets) are copied to `checkpoints/stream3-tools-2026-09-23/`.
  - Rebuild the launch env from `supabase status` for the stream3-block-r1 stack.
  - Reset fixture passwords with GoTrue admin on the owned stack.
- Temporary DB fixtures still present: BusinessLocation 980f6eb3 on Biz R1 (for the geo E2E; delete after). Everything else from today's runs is removed (see the #383 bundle `fixtures-and-cleanup.txt`).

## Decisions received from main (today)

- #358: interim hide of the dead button approved. Waitlist wired for real in #364. Both are merged.
- #367 notes corrected in the bundle and PR body. The stats count query is recorded as a founder data action.
- Chat backend lookup (`LOCAL_PROFILE_IDENTITY_SELECT`): leave it failing closed.
- Profile avatar check: drop it on both apps (done in #383).
- #371 conflict: merge master in, take #306's owner header, My businesses and BusinessOwnerView, drop the duplicates. Done.
- The hub's "JD"/"AV" initials belong in the scheduling PR (done in #383).
- #371, #383 and #384 accepted into batch 3.
- Native Business scheduling on the user's own id is a bug: use a real business from my-businesses; hide Business without one; report the bogus-page count query (sent).
- Native booking landing (C5), unreachable: leave it (founder note recorded by main).
- "Everyone's set to <tz> ✓ CONFIRMED": reword to "Times show in your time zone (<tz>)" and drop the chip (committed).
- Hub LIVE PREVIEW: real next open times or "No open times yet" (committed).
- You header "Verified": remove it, or show it only from residency (to do).
- Help email-verification copy: check enforcement, then fix (to do, item 6).
- Carried items: do them in the next groups (item 7).
- Goldens within tolerance: leave them.
- Grouping: items 1 and 3–5 in one native scheduling/You PR; items 6 and 7 follow.
- Sequencing approved: hub parts first, You parts after batch 2 (merged 19:22Z as 9779bf9d3).
- Plain-member Home hub 403: fix it in the scheduling PR and verify as a member and as an owner, on both apps and on web if it has the same pattern.
- BLOCKING geo decode fix: clients only, a small separate PR, verify on both apps.
- Gradle: run `./gradlew --stop` only while holding the heavy slot, before release.
- Split heavy builds: release the slot between the iOS and Android builds.

## Open questions

- None blocking. The WIP ref from item 4 above needs an owner's review before any use.

## Next actions (in order)

1. **Geo fix (BLOCKING).**
   - Restart the API (`api-restart.sh 33`).
   - Run `run-geo2.sh`, which holds the heavy slot and runs `--stop` before releasing.
   - Boot emulator-5554, install `app-debug-geo2.apk` after the port check, and open `pantopus://business/b7cf5cb1-718a-48a2-9abe-81fb2d79b9b6`. Capture `and-after-01`, then scroll to the service area.
   - Delete BusinessLocation 980f6eb3.
   - Seal the bundle.
   - Open the PR from `claude/stream3-native-business-geo-decode`: Problem / Reproduced / Change / Real-app verification / Evidence / Limits. Mention the Pulse picker consumer.
   - Report to main.
2. **Scheduling/You PR.**
   - Merge origin/master into `claude/stream3-native-scheduling-you-owner`.
   - Implement the plain-member 403 handling (iOS, Android, web check), the Me Business context and the You header residency "Verified".
   - Builds in split holds, then E2E: Dana as owner (Business pill with Draft Biz R2, preview times, onboarding tz line); Bob as a plain member of a fixture home and with no business (pills without Business); the You header.
   - Bundle, then a PR to master stacked on #383.
3. Items 6 and 7 as follow-up PRs.
4. Delete build outputs after each verification. No git gc, maintenance or repack.

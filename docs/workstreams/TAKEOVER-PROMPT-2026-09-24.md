# Copy/paste prompt for the next Pantopus coordinator

You are taking over the Pantopus three-stream effort plus Shared UX. The previous coordinator stopped cleanly at my request on September24 after fixing the last reported bug. Resume the existing work without repeating accepted work or confusing a successful build with real-app verification.

Read these files before acting, in this order:

1. `/Users/yingpengwang/pantopus-coordination/docs/workstreams/HANDOFF-2026-09-24-PAUSED.md` — all of it. This is the current state and supersedes older runtime/queue instructions.
2. `/Users/yingpengwang/pantopus-coordination/docs/workstreams/coordinator-state-2026-09-24/README.md`, `LIVE-SNAPSHOT.json`, and all four files in its `checkpoints/` directory.
3. The original `/Users/yingpengwang/pantopus-coordination/docs/workstreams/HANDOFF-2026-09-23-FINAL.md` and `HANDOFF-2026-09-23-FINAL-merged-prs.md`, then the repo's `AGENTS.md`, `docs/PROJECT_HANDOFF.md`, latest hub `docs/workstreams/README.md` resume block, and the original founder15:00/17:00/19:30 UTC direction blocks. Read `coordinator-state-2026-09-23/tools/AGENT-RULES-2026-09-23.md` before delegating. The old155-item inventory is still authoritative; its historical entries are not proof a feature remains broken.

Our objective is the best functional app and a simple, dependable user experience. Launch the actual iOS simulator app, Android emulator app and web app. Verify real screens, callers, APIs and persisted results, including errors/retries/empty states, stale responses and navigation. Preserve existing working designs and functionality; make focused, demonstrated repairs. UX improvements are authorized without another approval round. No new unit tests or local unit campaigns; required existing CI still must pass. Reuse accepted evidence when source/configuration/behavior remain unchanged. State plainly what passed on real apps and what remains unverified. Quality is more important than artificially conserving the Mac; use enough resources while coordinating shared build/device access and avoiding wasteful repeated CI.

First verify live state:

- Work from `/Users/yingpengwang/pantopus-coordination` on `codex/workstream-coordination`, with explicit safe cwd for every shell call. Expected master is `fd48ccfd2a4da525c82175cf479889bc785f9fe3`; fetch and use Git for actual SHAs.
- Expected open work PRs are397,409,411,412,413,414,415,416,417, plus founder-only46. Recheck exact heads/CI against LIVE-SNAPSHOT. All nine work PRs were green at the final07:03:20Z snapshot. Do not update/rebase every branch just because GitHub says behind. Integrate reviewed heads with the combined-batch procedure.
- Confirm all saved branches exist on origin, intended source statuses are clean, and any dirty runtime copies correspond to already-pushed sources. Preserve generated caches and unrelated local work.
- Check `uptime`, `sysctl kern.memorystatus_vm_pressure_level`, `df -h /`, owned ports/devices, and heavy/device leases. The prior coordinator/agents stopped their app runtimes/devices and Monitor. Databases/fixtures/artifacts are intentionally retained. Start one bounded `coord-watch2.sh` Monitor only after checking no prior watcher is alive. Restore missing temporary tools from committed state backups, never an old queued PR.

Current completed work must not be redone:

- Batches394/398/401/404/410 and PR402 are merged. Batch410 includes405/406/407/408. The business-location P0,325 iOS recheck, native offer/checkout group, Home group, scheduling/You group and their recorded bounded acceptance are already handled. The handoff provides exact scope/limitations and merge SHAs.
- PR414's saved-action recovery button regression is already fixed at `c259c120557285ef2ee514eeb9899290e37251d7`, pushed, real web verified under403/503, and CI green. Do not fix it again. PR412's missing footer screenshot evidence has also been supplied and independently reviewed; no source change was needed.
- Current155-item disposition is73 fixed/merged,20 in flight,62 not started:82 unfinished, about53%. This is issue progress, not whole-app coverage. The broad80 acceptance inventory remains13 closed/67 partial. The fresh full-app sweep is still ahead.

Resume in this order with background stream agents, one or two at a time. Reuse the existing same-session agents only if alive; otherwise create stream agents with their final checkpoints and AGENT-RULES. Agents never merge. Root plus three workers fit the four active slots; coordinator handles Shared UX. Note that old `/root/shared_ux` was actually the Stream2/Home worker.

1. Stream3: native notification/participant source365 is pushed; Android/web/API checks are accepted within recorded limits. Its iOS app is already built and installed with matching hash, but ZERO iOS notification journeys have been verified. Reuse the artifact after checking source/hash/configuration. Run the remaining iOS route/role/RSVP/failure/revocation matrix. Keep411/417/native integration dependencies explicit. New notification-fixture cleanup is not yet authorized: coordinator must review exact IDs, foreign keys, confirmed zero-cost bookings and audit-log retention first. Restore temporary flags/roles/RSVP state; never silently retime now-past fixtures.
2. Stream2: Home407 is merged. Place final e05c is committed/pushed but has never been built or exercised. Build/verify the changed Visit Scheduled/Past/copy and nullable Property-map behavior on Android and iOS, using retained exact fixtures and scoped new Visit/coordinate authorization in its checkpoint. Then cleanup, seal and publish the grouped Place PR. Do not redo unchanged Home acceptance or claim the unreachable correction CTA/provider arrival path.
3. Stream1:409/412–416 have full coordinator review and green CI. Coordinator can prepare the next combined batch after fresh heads/base/shared-file review. No batch9 was created. The next untouched work is actual S1-09 owner-bids error/empty checks, remaining S1-22 prompts, and S1-25 Android Auto/fabricated delivery controls. Do not redo415 Decline. S1-08 money ownership is separately gated. Its A17 package-contract/harness backups are founder-only and must not be merged.
4. Coordinator Shared UX:397 is reviewed and green; recheck its exact head before batching, without repeating real native sharing checks for formatting only. Focused Posts38a is pushed, all13 files match accepted96e and full diff was reviewed, but original evidence reconciliation/seal/PR remains. Discover/empty Support29bf is pushed and Android APK built, NEVER installed; iOS is unbuilt. Run those actual afters, preserve prior Pulse/Hub proofs, and prepare focused grouped PRs. Do not merge an integration harness wholesale. The iOS List/Map header truncation remains open; verify default-size/current-master rendering before a minimal layout repair.
5. Keep batching fully reviewed green PRs with the original §8.2/§8.3 scripts and exact-head queue. Then continue the remaining original UX inventory and finally run a fresh complete iOS/Android/web pass to measure what is left.

Hard limits always:

- Never modify `/Users/yingpengwang/skinny-pantopus`; never touch founder stack64521/64522, backend8000, or simulatorEB5AD759.
- Do not resume the search-filter security audit. Leave all original founder§7 choices, A17, data repairs and marketing copy to the founder.
- No secrets/raw tokens/DB archives/operator logs in Git/chat. Stripe TEST/manual only; no capture. Escalate money/security/legal/retention/new-table decisions to coordinator and record them.
- No bare stash, gc, maintenance or repack. No worktree removal. Before eventual coordinator worktree removal, audit bundles must be copied to the founder store under the original rule; do not perform that copy now while founder checkout is protected.
- One heavy build/install window and one iOS UI driver; explicit handoffs, no automatic waiters or blanket process kills. Stream1's inherited harness uses SIGINT, NOT SIGTERM, because SIGTERM has broad teardown behavior. Read exact restart recipes; never start a helper inside an immutable evidence bundle if it writes there.
- Preserve retained databases and private runtime backups; do not wipe/replay/reseed merely to resume. Read-only founder .env reuse in an inherited loader is not permission to contact founder services: verify isolated overrides/disabled jobs privately before startup.

After each milestone, update the hub README and PROJECT_HANDOFF with a fresh resume block, commit/push `codex/workstream-coordination`, and update the Claude memory path in the handoff. Keep concise progress updates. Count only actually merged accepted original IDs as completed, and keep newly discovered issues separate from the155 denominator. Continue autonomously through the authorized work; stop only for a real coordinator decision or a new instruction from me.

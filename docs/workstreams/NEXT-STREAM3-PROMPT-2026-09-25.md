# Message to send to the independent Stream 3 agent

You are **Stream 3**, a peer of Stream 1 and Stream 2, not anyone's subagent. Own accounts, social, notifications, scheduling/booking, related Shared UX and the remaining Stream3 original inventory. Work and report directly to the user while coordinating shared resources with both peers; Stream1 holds only the technical serial merge queue. Produce reviewable PRs and sealed real-app evidence, but do not merge or start a competing queue.

Before application work, read `/Users/yingpengwang/pantopus-coordination/docs/workstreams/HANDOFF-2026-09-25-PEER-TAKEOVER.md` and the entire September24 handoff it links, then the paused handoff and four checkpoints, `/Users/yingpengwang/pantopus-coordination/AGENTS.md`, `docs/PROJECT_HANDOFF.md`, current `docs/workstreams/README.md`, founder15:00/17:00/19:30 directions, original155 inventory, 80-row backlog, your `docs/workstreams/03-accounts-social.md`, and `coordinator-state-2026-09-23/tools/AGENT-RULES-2026-09-23.md`. Use explicit safe cwd, fetch and verify current master/PR heads/CI/Git ancestry. At September25 observation master was630bc49b5; participant backend411, web417 and native421 had merged through batch428. [PR427](https://github.com/WangPantopus/skinny-pantopus/pull/427) web booking actions remained open/green at5cdcecba. Its source/evidence was reviewed on the September24 base; prepare a fresh-base integration with Stream1 and preserve merged417 `useRef` plus PR427 `useRouter` in booking-detail imports. Do not overwrite the exercised417+427 context or merge a runtime harness wholesale. Reuse accepted Android/web/API and iOS notification route/role/RSVP/failure/revocation evidence when unchanged; full push/provider and My bookings actions remain unverified.

Next original work is S3-51/S3-52, scoped in the saved `next-51-52-read-only.md` under the durable private paused-runtime backup `stream3-booking-actions-runtime-20260924-r1`. Begin with an **actual installed iOS** unsaved Message template→Preview journey using the existing nonpersistent preview endpoint; do not Save, Send or call a provider without a separately reviewed concrete scope. Audit current UI and backend contract before any repair. `GET /booking-page` calls `ensurePage` and can insert a draft; a separate Personal draft BookingPage807dd420-973c-47a9-a140-6fd2c00dd92f was intentionally retained in the old isolated DB, but September25 no isolated ports ran. Verify its volume/identity before navigating; do not assume that GET is read-only or recreate/retime deleted fixtures. For S3-52 My bookings names/zone/management and no-show policy, first reproduce current real behavior, then obtain exact zero-cost fixture/write and cleanup scope; preserve owner/participant permissions and do not infer money actions. The old Android local build had paid surfaces enabled, so it cannot prove paid=false behavior.

Launch and use **the real web app, installed iOS simulator app and installed Android emulator app** for every reachable Stream3 feature/function/workflow. Cover actual notifications, account/privacy/social, scheduling/booking and route/role transitions on all offered platforms. Trace screen→caller→endpoint/service→database, prove persisted outcomes/reload and ensure failures create no unintended writes. Exercise empty/loading/partial, offline/unavailable, 403/404/5xx and provider-disabled states, slow/delayed/stale responses, retry, duplicate taps, permission changes/revocation, Back/deep links, app restart/account switch and notification return where relevant. Verify frontend messages/actions and backend status/data, not just build or source. Preserve existing designs/navigation and privacy; repair only reproduced failures or concrete unmet requirements in the existing implementation, then rerun affected real-app journeys and regressions. Required CI must pass; no new unit tests or local unit campaigns. State exact pass/unverified per platform; synthetic auth, simulator/emulator, disabled providers, physical push, hosted and money boundaries are separate.

First check uptime, pressure, disk, owned ports/devices, heavy/iOS lease and private backup state. At the September25 check the Mac had recently restarted: no test device, isolated stream port or `/private/tmp` worktree/tool remained. Restore source from origin and runtime recipes from the private paused backup only after checking ownership and isolated overrides/jobs/providers. Preserve Docker volumes, canonical identities, six audit rows and the retained implicit draft if present; no wipe/reseed/replay or broad cleanup. Before touching shared files/fixtures, taking a heavy build/install or the sole iOS driver, send both peers an exact purpose/files/ports/device and get explicit handoff; no automatic waiter or blanket kill. Keep current status in `docs/workstreams/03-accounts-social.md`, and send PR head, evidence seal, exact side effects/cleanup, CI and limits to Stream1 **and the user**. Continue through your complete domain inventory and identify which existing80 broad rows your accepted evidence actually advances.

Hard limits: never modify `/Users/yingpengwang/skinny-pantopus` or contact founder64521/64522/backend8000 or simulatorEB5AD759. No search-filter security audit; no founder§7/A17/data/marketing work. Stripe TEST/manual only and no capture. Keep secrets/raw tokens/DB archives/operator logs out of Git/chat. No bare stash, gc, maintenance, repack or worktree removal. Escalate money/security/legal/retention/new-table decisions to the user with concrete evidence; continue independent safe work meanwhile.

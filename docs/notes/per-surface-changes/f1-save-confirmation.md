# Research-backed changes for f1-save-confirmation

- One sentence saying what changed ('Saved privately. Today now uses 1107 NE Birchfield Ct.') then a hand-off to Today, with no permission ask [onboarding, notifications]
- Split recovery into two frames. (1) The server holds the address: a one-tap row '1107 NE Birchfield Ct, Camas — Save it' with 'Different address' as a secondary. (2) Nothing held: the blank field [onboarding]
- Update the WHY paragraph: the device-local draft is not the normal cross-device path [onboarding]
- The confirmation is role=status; no confetti; a light tick haptic at most [a11y, companion]
- (research contradiction, topic onboarding-activation: time-to-value bef) v1: The 'Preview lost / expired / different device' frame is an empty focused 'Street address' field that the user retypes ('the value the user retypes'), on the premise that the address is device-local with a 24-hour life. The companion prompt, f1-email-verify-handoff, says the server now holds the pending place. | research: WCAG 2.2 SC 3.3.7 Redundant Entry (Level A): information already entered in the same process must be auto-populated or selectable. The W3C note says it does not force storage across sessions, so the obligation is strongest while the server still holds the value. Firebase's anonymous-account linking shows the standard way to keep data created before sign-up. | do: Split the recovery frame into two. (1) Server holds the address: show it as a one-tap selectable row ('1402 NE 3rd Ave, Camas — Save it') with the field as a secondary 'Different address'. (2) Nothing held at all: the blank field as drawn. Update the WHY paragraph so it no longer describes the device-local draft as the normal cross-device case.

# Research-backed changes for f7-widget-tap-landing

- Read `section=` (pickup | strip | air | year): scroll that section into view and highlight it once, moving accessibility focus; add AQI-hero arrival frames [widgets, a11y]
- Refresh in place: the hairline shows only if the refresh takes 1s or more; the freshness line 'Updating… / Updated just now'; failure keeps content and shows 'Couldn't refresh · Retry'; skeleton only on a cold start [widgets]
- Android: system launch transition via @android:id/background [widgets]
- Log session_open{trigger:widget} separately from snapshot writes [widgets]
- Use HOME A on TODAY (Mon 19 Oct), not Garfield St on 21 Sep; Reduce Motion gives an instant jump plus a fade [prompting, a11y]
- (research contradiction, topic glanceable-today-widgets: home-screen wi) v1: Every widget tap opens Today with its existing section order unchanged, and the landing is the top of Today (location row, then pickup card, and so on). | research: HIG: deep-link to the details that relate to the widget content and 'don't make people navigate to the relevant area in the app'. On an AQI-hero day the tapped fact (the air band, sixth section) is below the fold at 393x852. Android WS-5 also asks for the system launch transition. | do: Keep the section order, but read a `section` parameter: scroll that section into view and highlight it once, the same treatment the 14-day strip gives reminder pushes. On Android, use @android:id/background for the launch transition. Add frames for an AQI-hero arrival.
- (research contradiction, topic claude-design-prompting: how to prompt C) v1: Fixtures: 1420 NE Garfield St, rendered 'as of Monday 21 September 2026'. f1-today-tab (including its 'arrived from the widget' frame), f4-today-pickup-card, f1-today-air-band and f5-today-calendar-strip use 2418 NW Lacamas Dr on Monday 19 October 2026. x-place-file uses 2418 NW Sierra St on Sep 16 2026. | research: Literal instruction following means each screen draws only the data it is given (Anthropic), and generators already vary between runs (NN/g). Screens that summarise one another can only be checked side by side if they share fixtures. | do: Put one FIXTURES block (HOME A on one TODAY) in the preamble and use it in the widget, widget-tap landing, Today, pickup card, air band, calendar strip, date sheet and place file. Also resolve HOA dues ($265 / $285 / $385), the pickup weekday and hauler (Tuesday / Waste Connections vs Thursday / City of Camas), and 2416 vs 2418 NE Ingle Rd (f11-keeper-strip vs f4-briefing-optin-card).

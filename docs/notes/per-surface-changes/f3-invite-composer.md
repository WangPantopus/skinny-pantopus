# Research-backed changes for f3-invite-composer

- One-line capability caption under each role chip: Member 'Sees and edits the calendar, sees bills and who paid, sees who lives here'; Guest 'Same as Member until <date>' [household]
- The manifest's 'Role offered' row expands to the same grant list f3b shows, generated from one shared string; expiry is always shown [household, competitive]
- Link channel: 'Works once' if single-use, otherwise joiners need owner approval. The warning stays at the point of choice [household]
- No contact import, no reminder sends, no bulk invite [share]
- 'Sending' / 'Invitation sent' are status messages; the email field uses email autocomplete; replace the fixture emails with @example.com [a11y, prompting]
- (research contradiction, topic household-sharing: shared household spac) v1: Role chips 'Member - Guest' carry no capability description. The review step is 'a plain manifest - four label/value rows' in which the role appears only as 'Role offered - Member'. | research: Google Home, Ring and Apple Home all explain each role in plain terms at the point where it is chosen. Google also lets the invitee review what is shared before joining. The sender is choosing what a stranger will see of the household calendar and bills, so the role name alone does not make the manifest checkable. | do: Add a one-line capability caption under each chip, and expand the 'Role offered' manifest row to the same grant list f3b shows the invitee (sees and edits the calendar; sees bills and who paid; sees who lives here), generated from one shared string. The manifest stays plain; only that row gains a second line.
- (research contradiction, topic household-sharing: shared household spac) v1: The link channel is warned with 'Anyone with this link can join your household until it expires. Send it to one person.', and the example manifest shows 'Expires - Sep 30, 2026 (14 days)'. The prompt draws no use limit and no owner approval for link joins. | research: Exemplars limit open links in the system rather than in the copy. 1Password requires an organizer to confirm each new member, Life360 codes expire in 72 hours (snippet), Discord lets the creator cap uses and duration (snippet), and Slack lets admins shorten or deactivate links. | do: If the backend can, make link invitations single-use and say 'Works once' in the manifest. Otherwise add an approval step: link joiners appear in the roster's pending group as 'Waiting for your OK' and see no household data until approved, and the active link is a pending row with 'Turn off link'. Keep the warning at the point of choice, as v1 does.
- (research contradiction, topic claude-design-prompting: how to prompt C) v1: Fixture emails use real consumer domains (jules.okafor@gmail.com, priya.raman@gmail.com, rjmoore@proton.me, d.reyes@fastmail.com), which also appear in f3-members-roster, f1-email-verify-handoff and x-place-file. | research: RFC 2606 reserves example.com, example.net and example.org for documentation and examples. | do: Replace them with @example.com addresses in the FIXTURES block.

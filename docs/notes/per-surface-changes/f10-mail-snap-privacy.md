# Research-backed changes for f10-mail-snap-privacy

- Audience is stated per permission: 'You and Sam can see these photos. Members without money access see that a piece arrived, not the photo or the amount'; draw the variant frame [capture]
- Product call: bounded retention by default, or the keep-photos toggle off by default. The count line states the limit ('17 photos · 41.3 MB · kept N days, then deleted') [capture]
- A layered grid: what is stored / who can see it / how to delete it, with 'None' written explicitly; only the last 4 account digits are shown [trust, capture]
- The 'Delete all 17 photos' confirm names what survives; no default focus [capture]
- (research contradiction, topic capture-and-extraction: photographing pa) v1: Second explainer line: 'Everyone in this household can see them: you and Sam Reyes.' | research: f10-mail-piece-photo and f10-mail-day-triage specify that a member without finance.view cannot open a bill photo or see its amount. A blanket 'everyone can see them' is then inaccurate, and transparency guidance (Apple HIG; Zeng & Roesner on household access control) calls for an exact statement of who sees what. | do: State the audience per permission: 'You and Sam can see these photos. Members without money access see that a piece arrived, not the photo or the amount.' Draw a variant frame where a member lacks finance.view.
- (research contradiction, topic capture-and-extraction: photographing pa) v1: State 1, 'Photos kept — the default, toggle on': the photo is kept indefinitely unless the user turns the toggle off or deletes it. | research: Privacy by default (GDPR Art. 25(2)) covers 'the period of their storage'. The FTC says keep only what you need, don't keep account numbers without a business need, and set how long to keep data. The photos hold full account numbers. The pilot is in the US, so GDPR is not binding, but the principle is the recognised standard. | do: Product call. Either make retention bounded by default (keep until the bill is paid plus N days, or N days; show it in the count line, e.g. '… kept 90 days, then deleted', where 90 is a placeholder) or default the toggle to OFF. f10-bill-provenance's 'photo deleted, bill kept' state already handles the result. Whichever is chosen, the consequence line must state the time limit.

# Research-backed changes for f4-notification-primer

- Remove the entry from the briefing card: a Yes there fires the OS dialog directly. Keep the primer only for indirect entries (household activity from Mail; pickup where not inline) [onboarding, notifications]
- iOS: one forward button, 'Continue', with the caption 'Your phone will ask next'. The decision lives on the in-context card, so no second choice is needed. Android: 'Continue' plus 'Not now', and 'Not now' never fires the system dialog [notifications, competitive]
- Three denial frames: iOS denied → 'Open Settings' to the app's notification page; Android denied once → 'Continue', which asks again; Android blocked → 'Open Settings'. Each names what still works: 'Pickup still shows on Today' [notifications]
- The hero tray card uses the canonical string at true truncation, 'Unconfirmed: pickup tomorrow', drawn clearly as an in-app illustration and never styled as a system alert, captioned 'This is the whole thing' [notifications]
- Copy lists the real defaults: 'Pickup the night before. Air alerts when it's unhealthy. Nothing else unless you turn it on' [notifications]
- No benefit bullets, no arrows toward Allow, and no full address [notifications]
- (research contradiction, topic onboarding-activation: time-to-value bef) v1: Entry points include 'tapping Yes on the briefing opt-in card on Today'. The primer then asks again with two equal-weight actions, 'Yes' / 'Not now', before the OS dialog. | research: Apple HIG Privacy: if context already explains a permission request, no extra screen is needed. A custom pre-alert screen should have one action labelled like 'Continue'/'Next' that opens the system alert, with no way to leave and no 'Allow'-style wording. That passage is written for private-data permissions, so applying it to notifications is an inference. Android: show an educational screen only when shouldShowRequestPermissionRationale() is true. Apple's notification docs say to ask in context, as the opt-in card already does. | do: When the entry is the opt-in card's 'Yes', skip the primer and fire the OS dialog directly. The card can carry the tray preview in its expanded state. Keep the primer for the first 'That's my day' and the Mail entry, relabel its forward action 'Continue' (or 'Turn on reminders'), and make clear the OS asks next. If 'Not now' stays, define it as never re-presenting on its own, and note the App Review risk. Evaluate iOS provisional authorization as the no-prompt alternative for the pickup entry.
- (research contradiction, topic notifications-permissions: push and perm) v1: Actions 'Yes' / 'Not now' at equal weight, under the title 'Can we tell you about pickup night?'. 'Yes' leads to the OS prompt. | research: Apple HIG Privacy, 'Pre-alert screens': the one forward button should make clear that it opens the system alert and use a term like 'Continue' or 'Next'. Allow-like wording that resembles the alert's Allow button in meaning and weight counts as manipulation. The page names camera, microphone, location, contacts, calendar and tracking, not notifications, so 'Not now' is defensible, and Android explicitly wants a cancel option. | do: Rename 'Yes' to 'Continue', with the caption 'Your phone will ask next.' Keep 'Not now' visible, and record it as a deliberate iOS choice. On Android it must not fire the system dialog.
- (research contradiction, topic notifications-permissions: push and perm) v1: The primer opens after the person taps Yes on the briefing opt-in card, so the flow is card Yes, then primer Yes, then OS Allow. | research: Apple: "Ideally, the current context helps people understand why you're requesting their permission. If it's essential to provide additional details, you can display a custom screen". Android's recommended flow is: the user interacts with the feature, the app explains, the user confirms, then the system dialog appears. The card row is already that explanation and confirmation. | do: A Yes on the card fires the OS prompt directly. Keep the primer for indirect entries (the first 'That's my day', household activity from Mail), and redraw state 2 accordingly.
- (research contradiction, topic notifications-permissions: push and perm) v1: Example tray title: 'Recycling and garbage tomorrow - City of Camas schedule, not yet confirmed' (about 73 characters), with the caveat at the end of the title 'because a truncated tray line must not eat the honesty'. | research: Android truncates content titles to a single line even when expanded, and advises 30 characters or fewer. Apple asks for brief titles readable at a glance. The caveat at the end of a 73-character title is exactly what gets cut. | do: Put the caveat first in a title of 30 characters or fewer: 'Unconfirmed: pickup tomorrow'. Body: 'Recycling + garbage · City of Camas schedule. Bins out tonight.' Draw it at true truncation, and use this same string in the pickup-card and household prompts.
- (research contradiction, topic notifications-permissions: push and perm) v1: Body copy: "We'll tell you the night before pickup, and nothing else unless something needs you tonight." Meanwhile the settings page shows Household activity, Date reminders, Bill reminders and Air alerts all ON by default. | research: NN/g: vague or inaccurate descriptions of what will be sent undermine the decision and the app's credibility. Apple: describe the types of notification you will send, and represent urgency accurately. Once granted, OS permission covers every category the app then sends. | do: Either grant only pickup plus air alerts by default and leave the other groups OFF until switched on in context, or change the primer copy to list the real defaults ('Pickup the night before. Air alerts when it's unhealthy. Nothing else unless you turn it on.').
- (research contradiction, topic notifications-permissions: push and perm) v1: State 4, 'already denied at OS level': the same sheet with 'Open Settings' and no Yes, drawn identically on iOS and Android. | research: Android can ask again after one denial: shouldShowRequestPermissionRationale() returns true and the app should explain and re-request. The dialog is suppressed only after the person denies more than once, and Android advises against linking to Settings to change someone's mind. iOS never prompts twice, and openNotificationSettingsURLString goes straight to the app's notification page. | do: Draw three frames. iOS denied: 'Open Settings' to the notification page. Android denied once: 'Continue', which re-fires the system dialog. Android blocked: 'Open Settings'.
- (research contradiction, topic Competitive and adjacent product teardow) v1: The primer offers 'Yes' and 'Not now' side by side at equal weight on both iOS and Android. It opens after the user has already tapped Yes on the Today briefing card. | research: Apple HIG Privacy: a custom screen before a system permission alert should 'Include only one button and make it clear that it opens the system alert', titled 'Continue' or 'Next'. It should offer no way to leave without seeing the alert, such as close or cancel. Android's guidance is the opposite: its rationale UI should include a 'no thanks' button. | do: Split by platform. On iOS, the in-context card holds the only Yes / Not now. Yes then opens the system alert directly, or a primer with just the tray preview and one 'Continue' button (no 'Not now'). On Android, keep 'Not now'. Keep the 'Open Settings' state for the already-denied case.
- (research contradiction, topic Competitive and adjacent product teardow) v1: The example notification title is 'Recycling and garbage tomorrow - City of Camas schedule, not yet confirmed' (about 75 characters), with the caveat as a title suffix 'because a truncated tray line must not eat the honesty'. | research: Apple HIG: prefer brief titles readable at a glance; the system truncates automatically. Android truncates notification text to one line by default. A caveat placed after roughly 40 characters of title is truncated just like a body tail. | do: Keep the caveat in the title, but in a short title that ends by about character 30: 'Recycling Tomorrow · Unconfirmed' (title case, no ending punctuation). Start the body with the source: 'City of Camas schedule. Bins out tonight.' Draw the collapsed tray at 393pt and 412dp to prove the caveat survives.

# Research-backed changes for f7-widget-gallery

- Description: 'See pickup day, air quality and your next date at your address.' (starts with a verb, never 'this widget') [widgets, competitive]
- The baked sample is the default and the no-place preview. Once a snapshot exists, show the user's own data (iOS local read; Android 15 generated preview after the first Today load) [widgets]
- Android picker frames use dynamic colour; previews at the correct sizes; no size emptier than the size below it; the placeholder keeps its drawn cells and segments [widgets]
- (research contradiction, topic glanceable-today-widgets: home-screen wi) v1: 'Build a DEDICATED preview composition with baked sample content — never the live layout bound to real state.' | research: HIG: 'You can display real data in your widget preview, but if the data takes too long to generate or load, display realistic simulated data instead.' Android quality tier 1 (WD-1) wants a preview that 'includes user content and/or applies system theme'. Android 15 generated previews (setWidgetPreview, about 2 calls/hour) exist for this purpose. | do: Keep baked sample content as the default and for users with no place or snapshot, which fixes the real bug the prompt names. Once a snapshot exists, allow the user's own data in the preview: a local read on iOS, a generated preview pushed after the first Today load on Android 15+. Always apply dynamic colour on Android.
- (research contradiction, topic glanceable-today-widgets: home-screen wi) v1: Description: 'Pickup, air and your next date — no notification needed.' | research: HIG: 'Begin a description with an action verb — for example, "See the current weather conditions and forecast for a location"', and avoid phrases that refer to the widget itself. | do: Use a verb-led line such as 'See pickup day, air quality and your next date at your address.' If the 'no notification' idea matters, carry it in the how-to sheet instead.
- (research contradiction, topic Competitive and adjacent product teardow) v1: Build a dedicated preview composition with baked sample content, never the live layout bound to real state. Description: 'Pickup, air and your next date — no notification needed.' | research: Android widget quality guide, Tier 1: 'Preview includes user content and/or applies a system theme'. Apple HIG: 'Begin a description with an action verb' and avoid unnecessary phrases. | do: Keep the baked sample as the fallback for users with no data. Where the platform supports it, render the picker preview from the user's real snapshot, never an empty state. Reword the description to start with a verb: 'See tomorrow's pickup, today's air and your next date at your address.'

# Research-backed changes for f3b-verify-address-sheet

- ETA chips use primary.700 on the info background. The postcard shows a range with a real upper bound: '5–10 days' [trust, a11y]
- The pending state shows mailed date · expected window · code expiry · 'Didn't arrive by Mon 2 Nov? Send a new code', enabled only after that date, with a warning that the old code stops working [trust]
- The document method lists accepted document types before upload; a rejected document gets a specific reason and a retry [trust]
- The reason header is set by the caller ('To send neighbor messages, we need to confirm you live here') [trust]
- An invitee with no available method sees the reason and no dead link; the code field accepts paste; the sheet has a visible Close; batch 15 frames [trust, a11y, prompting]
- (research contradiction, topic trust-provenance-privacy: where facts co) v1: ETA chips 'in the info token'. Postcard 'Postcard code · 5–7 days'. Pending state 'mailed Sep 12, expected by Sep 19', with no late or expiry path. | research: Info text on info bg is 3.84:1 at caption size, which fails 4.5:1; primary.700 on info bg is 5.57:1. Google says 'most codes arrive within 14 days', codes expire after 30 days, and asking for a new code invalidates the one in the mail. USPS First-Class is 1–5 business days before print lead time. Nextdoor reviews show that unexplained waits and rejections drive 1-star reviews. | do: Use primary.700 on info bg for the chips. Show the postcard as a range with a real upper bound (e.g. '5–10 days'). The pending state should show mailed date, expected window, code expiry, and a 'Didn't arrive? Send a new code' action enabled only after the window, warning that the old code will stop working. Add a rejected-document state with a specific reason and a retry.

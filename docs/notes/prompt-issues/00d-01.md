# Last review findings for 00d-01 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The S3 layout is ambiguous for V1. The prompt doesn't say where the failed "air" line sits relative to the three remaining ticked CheckItems: in place inside the row, or as its own line above or below it. It also doesn't say whether the heading's 8pt spacing collapses when there is no heading.
  - evidence: S3: "The 'air' CheckItem becomes a line … The other three items keep their ticks." The spoken order is "weather, alerts, your calendar. Couldn't reach AirNow."
  - fix: Add: "S2/S3: the receipt line is the first line (no top gap). S3: the row holds weather · alerts · your calendar, and the failed line sits 8pt below it, in the spoken order."
- [minor] Several ink tokens and containers are left unstated. The V3 S3 sentence colour isn't given (V3 is text.secondary, while V1 S3 uses text.primary). The light V3 settings-group container isn't given (dark says raised). The V2 widget radius is only "per the widget family". The gap between the pending squares isn't given.
  - evidence: V3: "bodySmall 14/20 text.secondary". V3 S3: "error glyph, then 'Tonight's check couldn't reach AirNow · Retry'". V2: "radius per the widget family". S2: "three 3pt rounded squares in a row".
  - fix: State these values: V3 S3 text is bodySmall text.primary; the light V3 group is a surface.base grouped list, radius lg; the V2 widget uses radius 3xl 24 (or the published widget radius); the pending squares have 2pt gaps, centred in the 16pt box.
- [minor] V4 and V5 ask for drawn detail (14 empty cells, 12 month columns and four lanes) inside components that R15 says to draw as a plain named box when they aren't published. The two instructions can't both be met.
  - evidence: V4: "a FourteenDayStrip (R15) with 14 empty day cells". R15: "draw a surface.sunken box at this size with its name".
  - fix: Add: "If FourteenDayStrip or YearBand isn't published, draw the R15 box and put the caption line under it."
- [minor] Notes D describes the invented strings instead of listing them, and it leaves out the grid row and column labels ("V1 · Today full" and the rest).
  - evidence: D: "Every string in GRID CELLS and ANATOMY that is not in the contract or house style, plus…"
  - fix: List the strings literally: "air · not back yet" · "Checked 6:00 PM · air not back yet" · "Couldn't reach AirNow · 6:00 PM" · "Tonight's check couldn't reach AirNow · Retry" · "Couldn't reach the City of Vancouver calendar · Retry" · the three Not applicable reasons · the V1–V5 and S1–S3 labels.
- [minor] At about 9–10k characters, the prompt is at the top of the v1 per-send range, and it depends on R1–R19 from an earlier turn.
  - evidence: Research: "Per-send length in v1 … about 6.5k–10.4k characters".
  - fix: Optional: merge the INSTEAD OF bullets that repeat COPY (the first and fifth items).

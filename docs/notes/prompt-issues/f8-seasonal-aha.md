# Last review findings for f8-seasonal-aha (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The trigger condition for the summer card is missing. The design doc promotes the air or wildfire card only when AQI is 101 or higher today, or wildfire hazard is High. The prompt gives only the date window. The card's one job is to show the fact that is actually urgent, and without the threshold a designer could read the rule as 'in summer, headline the air reading', even at AQI 42 Good.
  - evidence: Design doc F8 Rotating headline: 'Jun 15 – Sep 30: AQI ≥ 101 today or wildfire hazard High → existing air/wildfire aha.' Prompt: 'The air or wildfire card appears Jun 15–Sep 30.' Only the wildfire half is implied, by 'PLACE B is Moderate 3 of 5, so it is never promoted there.'
  - fix: Change the sentence to: 'The air or wildfire card appears Jun 15–Sep 30, and only when today's AQI is 101 or higher, or the wildfire hazard is High (4 of 5) or above. Otherwise the ranked card shows.'
- [major] (substance) Frame 10 attaches the 'no program link' state to the Washington voter card. In the sources, program_url is the per-state radon kit map from F12, and the state it describes is 'radon follow-up with no program_url'. Washington has VoteWA. So the frame shows a Washington voter card without its VoteWA row, which contradicts the rest of the prompt and misstates what the degradation covers.
  - evidence: Design doc F12: 'Per-state radon program map in composeLeadRadon: program_url and free_kit: bool'. derive-proposals.md f8-6 states: 'radon follow-up with no program_url for the state → no link, headline only'. inventory: 'no program_url for the state (headline only, no link)'. Prompt frame 10: 'the Mon 19 Oct voter card with the annotation "state has no program link", no outbound row and no gap.'
  - fix: Redraw frame 10 as the January radon card (Mon 11 Jan 2027) for an invented Zone 1 county in a state with no program_url: filled EPA mark, follow-up row 'Keep this address handy', no outbound row, no gap. List the county and state on Notes. Optionally add one line saying the same rule applies to a state with no official voter lookup.
- [minor] (substance) The kit label is not the Foundations variant string, and Notes does not list it as invented or as a deviation. Foundations and the component contract name the outbound-test-kits variant 'Free test kits · Washington Dept of Health ↗'. The prompt uses 'Get a free test kit · Washington Dept of Health ↗' while claiming to use the Foundations variant by name.
  - evidence: component-contract.md TextActionRow Variants: "outbound ('Free test kits · Washington Dept of Health ↗', …)". prompts-v3/00c-08.md V2a: '"Free test kits · Washington Dept of Health ↗"'. Prompt COPY: '"Get a free test kit · Washington Dept of Health ↗"'. Notes list omits it.
  - fix: Either use 'Free test kits · Washington Dept of Health ↗' exactly, or keep the verb-led string and add it to Notes: 'invented, deviates from the Foundations V2a label to satisfy the verb-first rule; update the board or revert'.
- [minor] (substance) The edge case 'Signed-out only' conflicts with the native host. On iOS and Android the preview, and so this card, also renders for a signed-in account with no saved place. The companion prompt says so, and this prompt's own Notes mention signed-in visitors.
  - evidence: f8-native-share-compare prompt: 'the address preview is the Place tab's root for anyone signed out, and for a signed-in account with no saved place.' This prompt, EDGE CASES: 'Signed-out only.' Notes: 'Signed-in visitors have no in-app civic destination from this card.'
  - fix: Replace it with: 'Tier: anonymous preview only: signed out on web, and on the apps signed out or signed in with no saved place. Never on a signed-in Today or a saved place's file.'
- [minor] (substance) The accessibility rule for deep-link landings is missing. The brief says a deep-link highlight also moves accessibility focus. The prompt says only 'scroll once and fade the highlight', and says nothing about focus when the follow-up row scrolls to the wall.
  - evidence: ux-research-brief.md §5: 'Deep-link highlights also move accessibility focus.' Prompt INTERACTION: 'The follow-up row scrolls to the wall. On a deep-link landing, scroll once and fade the highlight within 300ms.'
  - fix: Add: 'Scrolling to the wall, or landing from a deep link, moves accessibility focus to the wall's headline (or the highlighted card).' Also give the spoken form for 'Today': 'Deadline today, Monday 26 October'.
- [minor] (substance) The one-chip decision drops the inventory's 'dated chip beside the grade badge' without saying so on Notes. The voter-wins-over-tax precedence also has no source in the design doc and is not recorded as an assumption.
  - evidence: inventory visualization: 'the deadline as a dated chip beside the grade badge'. Prompt: 'Use exactly one chip per card.' and 'If both the voter card and the tax card match, the voter card wins.' Notes lists neither.
  - fix: Add to Notes: 'Deadline cards drop the grade badge (one chip per row, StatusChip rule; no cross-layer grade, P12)' and 'Assumption: voter beats tax when both match; confirm the SEASONAL_HEADLINES order.'
- [minor] (substance) Frame 18 shows only 'Compare with a friend' under the card, and frame 19 shows no pair at all. On the apps the row is the TextActionRow pair, 'Share this address' and 'Compare with a friend', with its two-sentence caption, as f8-native-share-compare specifies.
  - evidence: Prompt frame 18: 'with "Compare with a friend" 16pt below the card.' Frame 19: 'the default in the Android preview.' f8-native-share-compare: 'New row: "Share this address" · "Compare with a friend"' plus the caption.
  - fix: In frames 18 and 19, draw the full TextActionRow share-compare pair and its caption 16pt/16dp below the card, matching f8-native-share-compare frames 1 and 8.
- [major] (ux) The anatomy the prompt tells Claude Design to "keep as they are" exists only on web. The iOS and Android aha cards look different, and the prompt also says the attached screenshots are exact, so the two instructions contradict each other for frames 18 to 20 and 23. On native there is no overline, the tile is 34pt, the header chip is a tone-coloured grade chip whose text falls back to "What stands out", there is no source line, and the follow-up is a primary text link, not a sunken row. On iOS that link also goes straight to account creation instead of scrolling to the wall.
  - evidence: PlacePreviewBody.swift ahaCard: `.frame(width: 34, height: 34)`, `PlaceChip(model: PlaceChipModel(tone: ahaChipTone(aha.tone), text: aha.grade.isEmpty ? "What stands out" : aha.grade))`, and the follow-up `Button(action: onCreateAccount)` in primary600. PlaceLaunchScreen.kt AhaCard has the same structure. Web AhaCard.tsx does have the overline, the 42px tile and the sunken follow-up. The prompt says: "Keep these parts as they are: the overline \"WHAT STANDS OUT\" ..., the 42pt rounded icon tile, ... one sunken follow-up row with a chevron" and "The follow-up row scrolls to the sticky wall".
  - fix: In TYPE, say that the kept anatomy is the web card's. Add: "On iOS and Android, change the card to the same anatomy: add the overline, move the chip to the right of the header row, enlarge the tile to 42pt, replace the tone-coloured grade chip with the neutral chip below, add the SourceCaption, and replace the primary text link with the sunken follow-up row. On native, the follow-up now scrolls to the wall instead of opening sign-up directly." Ask for native screenshots in ATTACH (2) marked "layout changes as listed", and add this conversion to Notes.
- [major] (ux) The follow-up copy promises something the product does not do automatically. Under the notification model, a saved place gets a reminder only when the person sets one and has granted notification permission. The default frame therefore makes a false promise at the moment of sign-up. The Notes flag the question, but the promise is still drawn as settled.
  - evidence: The prompt says: "Follow-up row: \"Keep this address to get a reminder before Mon 26 Oct\"". ux-research-brief.md §3: "Dates & bills ... On only for rows where the person set a reminder". flows-spec flow-13 steps 3–4: the reminder is set later in x-date-sheet after the person picks a plan. The Notes say: "confirm that a saved place gets a reminder for seeded civic dates".
  - fix: Change all three follow-up strings to "Keep this address to set a reminder before Mon 26 Oct" (and likewise for Tue 3 Nov and Mon 2 Nov), since the reader takes the action. Update the INSTEAD OF line and the Notes to match. Keep "Keep this address handy" as the fallback.
- [major] (ux) The calm-fallback headline states three facts from three authorities: no active alerts (weather service), air (AirNow) and floodplain (FEMA). Its caption names only AirNow, which breaks invariant 5 and the per-surface change, which requires naming what is calm "with sources". The FEMA status of the invented Seattle address is also an unlisted invented fact.
  - evidence: The prompt says: "Headline: \"No active alerts · Air Good today (AirNow) · Outside FEMA's mapped floodplain\"" and "Source: FILLED mark, \"Official\", then \"AirNow · nearest monitors · observed 7:00 AM\"". per-surface-changes: "Calm fallback names only what is actually calm, with sources". The house-style invariant says: "Sources are shown as a caption: authority · scope · as-of."
  - fix: Give the calm card three stacked SourceCaptions, each with a filled mark: "National Weather Service · alerts for this area · checked 6:10 PM", "AirNow · nearest monitors · observed 7:00 AM" and "FEMA · area zone · effective <date>". Print the word "Official" once. Add to Notes: "4417 Densmore Ave N assumed outside FEMA's mapped floodplain and not radon Zone 1; check both."
- [minor] (ux) Frame 10 draws a Washington voter card with no outbound row and labels it "state has no program link". Washington does have VoteWA, so the frame shows an impossible case. The inventory state is about a radon program link.
  - evidence: "10. f8-seasonal-aha · web-390 · 10-no-program-link · light — the Mon 19 Oct voter card with the annotation \"state has no program link\"". The inventory lists "no program_url for the state (headline only, no link)", and derive-proposals says "radon follow-up with no program_url for the state → no link, headline only".
  - fix: Redraw frame 10 as the Mon 11 Jan 2027 radon card with no outbound row, annotated "hypothetical Zone 1 county in a state with no program link".
- [minor] (ux) The radon outbound string differs from the Foundations variant V2a, and the Notes do not log the difference. The prompt also claims Washington Dept of Health offers free kits, which no research finding confirms.
  - evidence: 00c-08 V2a reads "Free test kits · Washington Dept of Health ↗". The prompt reads "Get a free test kit · Washington Dept of Health ↗" and "Never say 'free' unless the state offers free kits."
  - fix: Keep the verb-led string. Add to Notes: "Surface delta: outbound-test-kits reads 'Get a free test kit · …' (verb-led) instead of the Foundations 'Free test kits · …'. Confirm that WA DOH currently offers free kits before shipping the word 'free'."
- [minor] (ux) The icon tile's glyph and colour are left undefined, so Claude Design will invent them. The existing tile uses tone tints, and the contract already defines KindGlyph tiles for voter registration and property tax.
  - evidence: The prompt says: "Keep ... the 42pt rounded icon tile" but names no glyph. AhaCard.tsx uses `TILE[tone]`. The contract's KindGlyph: "42pt rounded tile variant ... Set: ... property-tax (appeal / due), voter registration".
  - fix: Add to LAYOUT: "Icon tile: the KindGlyph tile variant (voter registration or property-tax) in text.strong on surface.sunken for deadline cards; the existing hazard glyph in text.strong for radon, air and calm cards; no tone tint." Add KindGlyph to FOUNDATIONS COMPONENTS USED.
- [minor] (ux) The two prompts disagree about who sees the card, and one entry point is not an entry point. The share card only carries the headline. Frame 18 also shows only one button, while the native prompt specifies the pair.
  - evidence: This prompt says "Signed-out only." The native prompt says "the row also shows for a signed-in account with no saved place" on the same preview. The entry-point list includes "The compare share card." Frame 18 says "with \"Compare with a friend\" 16pt below the card."
  - fix: Write: "Signed out, or (native) signed in with no saved place." Drop "The compare share card" from the entry points; the headline still appears there. In frame 18, write "with the TextActionRow share-compare pair 16pt below the card".
- [minor] (ux) The offline frame leaves out the OfflineNotice top line, and the skeleton variant name does not fit a card.
  - evidence: "15. ... 15-offline — outbound row disabled with ..." has no FreshnessLine. The contract's OfflineNotice anatomy: "FreshnessLine in its offline variant at the top". The prompt names "WarmingSkeleton (row skeleton)" for "a skeleton card at the final height".
  - fix: Frame 15: add "'You're offline · as of 6:08 PM' at the top; the card stays readable". Change the component reference to "WarmingSkeleton (section skeleton)".
- [minor] (ux) There are small copy and Notes gaps. The tax detail is redundant ("Sat 31 Oct, a Saturday"). The neutral reading chip is not a contract component and is not logged. The visitor is an unnamed Oregon mover rather than the fixture's Jordan Lee, and that assumption is not recorded.
  - evidence: "Detail: \"Moved from Sat 31 Oct, a Saturday.\"" · "Hazard cards: the existing reading chip ... shaped like StatusChip" · WHO AND WHEN: "a signed-out visitor who recently moved from Oregon"
  - fix: Change the detail to "Moved from Sat 31 Oct because that date is a Saturday." Add to Notes: "reading chip = surface-specific neutral pill, StatusChip geometry, not a published component" and "visitor is an unnamed newcomer, not Jordan Lee".

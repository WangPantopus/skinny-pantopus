# Last review findings for 00d-00 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] R9 contradicts itself on dark cards. The mapping table sends surface.base (the light card fill) to dark base, but the next sentence says a dark card's edge is raised on app. Any prompt that doesn't name its container gets two readings.
  - evidence: R9: "surface.base → #0F172A (dark base)" and then "A dark card's edge is the fill step (raised on app)".
  - fix: Add to R9: "Exception: a light surface.base card maps to dark raised on dark app; surface.base maps to dark base only for dialogs, sheets and page bodies."
- [minor] R4 adds two type styles (bodyMedium 16/24/500 and bodySmallMedium 14/20/500) that the house style doesn't have. The house style says to use only its scale. Notes A doesn't record this addition.
  - evidence: House style: "Use only the type scale below and never invent sizes." R4: "plus bodyMedium 16/24/500, bodySmallMedium 14/20/500". R19 A lists only 0.1–0.3.
  - fix: Add Notes item 0.4: "bodyMedium and bodySmallMedium are 500-weight versions of body and bodySmall used for text buttons; confirm they exist in the published system."
- [minor] R7 says to print a published name under every specimen, and greyscale copies and Reduce Motion repeats are specimens. Their names would duplicate the names of cells that do get published, so the publish list becomes ambiguous.
  - evidence: R7: "Under every specimen, print its published name". PUBLISH: "Review aids, never published: greyscale copies…"
  - fix: Add to R7: "Review aids print the name of the cell they copy, followed by ' (review aid)'."
- [minor] The setup message is long for a message that draws nothing: about 11–12k characters, 19 rules, many with sub-clauses. The research says accuracy drops as instruction count rises and that early instructions get more weight. Each component turn relies on R1–R19 being recalled from this earlier turn.
  - evidence: Research: "'even the best frontier models only achieve 68% accuracy at the max density of 500 instructions', with a 'bias towards earlier instructions'"; Figma: "More context isn't always better."
  - fix: Leave the setup as it is. Trim R19 D's chrome list and R10's unused rows only if drift shows up in practice. The per-batch "re-read R1–R19" line already mitigates this.

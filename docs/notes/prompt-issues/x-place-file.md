# Last review findings for x-place-file (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The voter mover row no longer shows the deadline or its source. That breaks the F6 acceptance criterion and changes the design doc's step copy.
  - evidence: Design doc F6: copy "Moved in? Registration is per address. Deadline for November 3: {date} ({state}). Check or update at {state office}." Acceptance: "the voter step shows the seeded deadline for the home's state with its source." derive-proposals f6-voter-step-sheet: "the step's own copy (deadline + per-method dates + source) cannot survive a route hop that drops it." The prompt's row reads only: "'Update your voter registration' · 'Registration is per address. Dates are in the list below.'"
  - fix: Change line 2 of the mover row to: "Registration is per address. Online or mail must arrive by Mon 26 Oct · in person until 8:00 PM Tue 3 Nov". Add a SourceCaption: "Washington Secretary of State · statewide". Keep the tap into the Civic registration block, and list the change from the doc's copy on the Notes artboard.
- [major] (substance) The prompt has no state for a reminder that lands on a date which has since been deleted. Reminder deep links are one of this screen's entry points, and the flow defines this fallback and its copy.
  - evidence: flows-spec flow-03, failure branches: "She taps the reminder after deleting the rule. The landing falls back with 'That date was removed' and never shows a blank screen." The prompt's arrival 5 and its EDGE CASES cover only a successful landing ("the row is highlighted and focus moves to it, then the Date sheet opens").
  - fix: Add an edge case: when ?rule= points to a deleted rule, land at the top of Dates with a status line "That date was removed" and open no sheet. Add the string to COPY. Optionally draw it as a margin note on frame 7.
- [major] (substance) Each date shows its visibility only as a static label. The research requires a visibility control, so a date shared at claim can be made private again, one date at a time.
  - evidence: per-surface-changes: "Every date row shows its visibility control and absolute plus relative date." research-findings: "Every date later carries its own visibility control in the date sheet and the place file ('Only you' / 'Household')" and the choice must be "as easy to reverse as to make". flow-06 step 11 has "Visible to: Only you · Household". The prompt says only: "Each row shows its visibility (ScopeChip compact: person glyph + 'Only you'…)". Its custom actions are "Edit, Remind me, Where this fact comes from, Mark done, Hide", with no action that changes visibility.
  - fix: Make the compact ScopeChip a 44pt target, following the contract ("hit area 44pt when it opens the control that changes scope"). It opens the DateSheet's 'Visible to: Only you · Household' control. Add a "Change who can see this" custom action. In frame 6 or 4, show the warranty row's chip as tappable.
- [major] (substance) The 'On record' Risk & readiness row drops the radon scope word, the hazard wording and all source and time captions. As written, a county-wide radon zone reads as a fact about this house, and the air reading has no observed time.
  - evidence: House style fixture: "Radon Zone 1, highest potential (EPA, county-wide)". Contract ScaleStrip approved string: "Radon Zone 1 — highest potential (county)". Brief P14: "Official datasets are FILLED with a scope word ('county-wide')". House invariant 5: "Sources are shown as a caption". The glossary requires spelling out 'air quality index (AQI)' once per surface. The prompt has: "Risk & readiness · Zone X — minimal flood hazard · Wildfire Moderate · 3 of 5 · Air Good · 42 · Radon Zone 1".
  - fix: Rewrite the row as "Zone X — minimal flood hazard · Wildfire hazard Moderate · 3 of 5 · Air quality index (AQI) 42 · Good · Radon Zone 1 — highest potential (county)". Add the collapsed caption "Sources: FEMA, USFS, AirNow (observed 7:00 AM), EPA", wrapped at phone width.
- [minor] (substance) The prompt draws an unchecked statewide seed as filled, but flow-13 says it should be hollow and the design doc seeds these rows as confidence 'unverified'. The Notes artboard does not name this conflict, so f6-place-section-details may draw the same seed differently.
  - evidence: flows-spec flow-13 failure: "Unverified seed. A hollow mark plus the source." Design doc F6: "confidence 'unverified' until each is checked". Contract, inconsistencies: "f6 draws the civic seeded block 'hollow' while x-place-file draws voter registration 'filled'". The prompt: "An official seed not yet checked against its authority stays filled; its SourceCaption reads 'not yet checked against the state site'."
  - fix: On the Notes artboard, state the decision and its basis: brief P14, where hollow is only for facts a household can confirm. Note that flow-13 and f6 must follow it.
- [minor] (substance) The count rule adds the address and the move-in date, which F11's list of counted facts leaves out. DONE WHEN requires this count to equal the keeper's count, but the Notes artboard does not mention the difference.
  - evidence: Design doc F11: "level = count of distinct facts on file (pickup day, dates, bills, documents, co-residents, verified)". The prompt: "HOME A = 11: Place 3 (address, pickup day, moved in)…" and "This count equals the keeper's count".
  - fix: On the Notes artboard, record that the count rule replaces F11's list (address and move-in are added), so the keeper strip prompt must use the same rule.
- [minor] (substance) Annual and self-entered DateRows are missing the recurrence caption that the DateRow contract pairs with the identity.home rule.
  - evidence: Contract DateRow: "Annual or self-entered rows get a 2px identity.home left rule plus a recurrence caption." The prompt mentions only "get the 2px identity.home left rule".
  - fix: Add a recurrence caption to annual rows, for example "Every year" on HOA dues and both property-tax rows, and list the string on the Notes artboard.
- [minor] (substance) The Dates section has no read-only state for a member without calendar.edit, even though the design doc's acceptance criteria and flow-03 both define one.
  - evidence: Design doc F5 acceptance: "A home member without calendar.edit cannot write dates (403 with the existing message)." flow-03 failure: "Household member without calendar.edit (T3). Fields are visible but disabled, with the reason stated." The prompt's Guest case covers only bills.
  - fix: Extend the Guest edge case: date rows are read-only, the Add actions are disabled, and a LockedActionRow reads "Maya can add and change dates here." List the string on the Notes artboard.
- [minor] (substance) The postcard-pending frame has no state for after the delivery window. The flow calls for a late path with a warning.
  - evidence: flow-06 failure: "The postcard does not arrive. After the stated window a late path appears, with a warning that the old code will stop working." The prompt shows only the disabled "You can ask for a new code after Fri 30 Oct."
  - fix: Add an edge case for after Fri 30 Oct: "Send a new code" becomes enabled, with the caption "Your old code will stop working."
- [minor] (substance) At T1, the 'Moved in · Not set · Add' row promises a moving list, but the prompt names no destination. Home settings does not exist for a saved place, and flow-13 records T1 mover rows as a gap.
  - evidence: The prompt: "'Moved in · Not set · Add' carries the helper 'Add it to see a moving list for 60 days'". The only Moved in destination given is "opens the Moved in control in home settings". flow-13: "T1 with no move-in date. The mover rows never appear (gap)."
  - fix: Name the T1 destination: the DateSheet move-in kind at saved-place scope. Add a note that mover rows then appear at T1 (flow-13 step 2).
- [minor] (substance) Several copy strings mention Ollie even though the prompt has a no-phase-2 layout. Also, F6's 'money' step is said to be covered by the Money section, which exists only in phase 2.
  - evidence: "What moved when you claimed · 'Pickup day, 3 dates and Ollie · Sat 17 Oct'" and the banner "Your pickup day, 3 dates and Ollie came with you." appear alongside "Without phase 2 (no bills, no keeper), HOME A reads '8 on file'". The prompt also says: "The other F6 steps (pickup, money, civic dates, block) are not repeated here because the Place, Money and Dates sections already hold them."
  - fix: Add the no-phase-2 strings ("Pickup day and 3 dates · Sat 17 Oct"). State that without phase 2 the money step is covered by the On record 'Money signals' row.
- [minor] (substance) After the claim, the Proof section is hidden and 'Verify this address' sits inside a collapsed disclosure. Flow-06 expects Proof to show a real ask once the address is claimed.
  - evidence: flow-06 step 10: "Money, People and Proof now show real asks." The prompt: "Proof has no section until something is known" and "More you can add: collapsed… 'Verify this address'".
  - fix: Either keep a quiet Proof FactRow "Verify this address · Add" at T3, or explain on the Notes artboard why it goes under More you can add and note that flow-06 step 10 needs updating.
- [minor] (substance) The LockedActionRow pending variant carries a text action, but the contract defines pending as having no link.
  - evidence: Contract LockedActionRow: "pending (no link: 'We're checking your postcard code — expected Sep 19–26')". The prompt: "LockedActionRow (pending) 'Postcard mailed Sun 18 Oct…' with a text action 'Enter your code'".
  - fix: Move "Enter your code" to a separate FactRow or TextActionRow under the pending row, or list it on the Notes artboard as a contract extension.
- [major] (ux) A sighted user has no visible way to mark a mover row done. Mover rows show only a trailing 'Hide' button. 'Mark done' is listed only as a custom action, which only assistive tech exposes. Yet frames 4 and 15 draw ticked mover rows, and the haptic is tied to 'Mark done'. That makes it an assistive-tech-only action, which the house style forbids ('No action is gesture-only or hover-only'). Where 'Forward your mail' and 'Move your utilities' lead when tapped is also never stated.
  - evidence: Prompt JUST MOVED: "Each row has a visible trailing 'Hide' text button"; INTERACTION: "Custom actions: Edit, Remind me, Where this fact comes from, Mark done, Hide."; frame 15 "one mover row ticked with 'Saved on this device. It'll sync.'"; house-style-v2 ACCESSIBILITY: "No action is gesture-only or hover-only."; derive-proposals f6-just-moved-card-synced: "Six step rows ... with tick, label, destination chevron"
  - fix: In JUST MOVED add: "Each mover row has a leading 44pt (48dp) tick control with the visible label 'Mark done' (spoken 'Mark Move your utilities done'), a row body that opens its destination, and a trailing 'Hide' text button. Destinations: 'Forward your mail' opens 'Change your address at USPS ↗' (TextActionRow outbound). 'Move your utilities' opens a list of three outbound rows (Clark Public Utilities ↗, NW Natural ↗, City of Vancouver ↗). 'Update your voter registration' opens the Civic registration block." Draw the tick control in frames 1, 4 and 15.
- [major] (ux) The prompt says the Next row must be visible without scrolling on 393x852, but it gives no height budget, and the content stacked above the row very likely pushes its button under the tab bar. Above it sit the large title, a two-line address, ScopeChip, FreshnessLine, FactCount, the legend, and the band card with a card title, the '2027' row, month labels, four lanes of unspecified height, Today and 'Next 14 days' labels, a summary of about 115 characters (about 3 lines at 337pt), a caption and a 44pt text link. By estimate the Next row starts near y≈670, and its button plus the 'Not now'/'Skip' line fall below the iOS tab bar at y≈769.
  - evidence: Prompt FIRST FIVE SECONDS: "It must be visible without scrolling on the 393x852 dense frame."; LAYOUT: "a 64pt lane-label column plus 12 month columns of 22pt" (no lane height given); summary "Next 12 months: 9 dates, plus pickup every Tuesday. Next: online or mail voter registration must arrive by Mon 26 Oct." plus "Caption: 'Every date is listed below.' Text link under the band"
  - fix: Add a height budget under LAYOUT & VISUALIZATION: "iOS 393x852 above the fold: header block ≤150pt (address, then one line holding ScopeChip + FreshnessLine, then FactCount and the legend on one line); band card ≤250pt (lanes 28pt each, month label row 16pt, summary line capped at 2 lines, with 'Every date is listed below.' and 'See the next 14 days on Today' merged into one 44pt footer row); the Next row (label, filled button, then Not now · Skip on one line) must end above the tab bar." If it still does not fit, place the Next row directly under the header, above the band, and change FIRST FIVE SECONDS to match.
- [major] (ux) The phone year-band cells are too narrow for what the prompt puts in them. A 22pt month column cannot hold a 12pt moved mark beside a 12pt struck ghost (at least 24pt plus a gap). It also cannot reliably hold a 12pt mark plus a count numeral, or a weekly service rhythm (4–5 marks) with dashes in the city lane. The T1 hero frame (frame 2) and the greyscale frame depend on these marks staying readable.
  - evidence: Prompt: "12 month columns of 22pt on iOS and web 390"; "one XS ProvenanceMark, plus a count numeral when there are 2 or more"; "the month cell of the city lane shows the moved mark next to a struck ghost mark"; contract ProvenanceMark: "XS 12pt/dp, the smallest size at which the three shapes must still be told apart"
  - fix: Specify the phone cell geometry: "Lanes are 28pt tall. In a phone month cell, marks stack vertically (at most 2 rows). A holiday move is the moved mark on top and the struck 12pt ghost below it. A count sits below its single mark as a caption numeral ('2'), never beside it. On phones the city-lane rhythm is a 2pt tick per service week (not marks), with a solid tick for confirmed weeks and a dashed tick for projected ones; the month's mark sits above the ticks." Add a zoomed Notes inset of the Nov city cell at 22pt.
- [major] (ux) Each date gets a visibility label through a new 'ScopeChip compact' variant ('Only you' / 'Your household') that the contract does not define and whose copy rules forbid. Claude Design will draw it ad hoc, so it can drift from x-date-sheet and the household calendar. The prompt also never says which rows carry it: seeded public rows (voter, property tax, appeal) have no audience, and only the warranty and 'the rest' are assigned one.
  - evidence: component-contract ScopeChip copyRules: "Exactly these strings: chip 'Saved place · Only you' / 'Your household' ... No other wording"; glossary: "Only you — the saved-place visibility."; prompt: "Each row shows its visibility (ScopeChip compact: person glyph + 'Only you', or house glyph + 'Your household')" and Notes "the per-date 'Only you' / 'Your household' label is a new ScopeChip compact variant"; per-surface-changes: "Every date row shows its visibility control"
  - fix: Define the variant's full anatomy in FOUNDATIONS COMPONENTS USED so it is not invented: "ScopeChip, per-date compact variant (pending addition to prompt 00): the same 24pt pill, surface.sunken fill, 12pt glyph, label 13/18 text.strong; labels 'Only you' / 'Household'; spoken 'Visible to only you' / 'Visible to your household'; shown only on dates this household entered (HOA dues, warranty, notice deadline, lease ends) and never on county or state rows, which carry only 'Statewide — WA' or 'Clark County'; the chip is not a separate target, and the row's DateSheet holds the control." Also add the variant to component-contract.md so x-date-sheet uses the same one.
- [major] (ux) The section name 'On record for this address' reuses the hollow-mark legend words ('○ On record, not confirmed'), which are printed just above it. The section holds official, filled readings, so a first-time user can read it as 'not confirmed'. Its row text also breaks the plain-language rules: it puts codes before meaning ('Radon Zone 1'), writes 'Air Good · 42' with no as-of time on a 7:00 AM observation shown at 6:10 PM, and drops the 'hazard' word from wildfire.
  - evidence: Prompt: "ON RECORD FOR THIS ADDRESS (after PLACE)"; "'Risk & readiness · Zone X — minimal flood hazard · Wildfire Moderate · 3 of 5 · Air Good · 42 · Radon Zone 1'"; legend "○ On record, not confirmed"; glossary: "On record, not confirmed — hollow mark"; house style: "Radon Zone 1, highest potential (EPA, county-wide)"; brief §6: "Plain meaning first, then the code"; contract FreshnessLine per-fact stale
  - fix: Rename the section 'Public records for this address' everywhere, including the reading order and the web column list. Make the Risk & readiness row two lines: "Flood: Zone X — minimal flood hazard · Wildfire hazard: Moderate · 3 of 5" / "Air quality (AQI): 42 Good, observed 7:00 AM · Radon: Zone 1 — highest potential (county-wide)". Change the Civic row value to 'Voter registration and elections' so it does not repeat the 26 Oct deadline a fourth time.
- [minor] (ux) The T1 pickup fact carries two competing asks on the hero T1 frame: the Next row's filled 'Set pickup day' and the pickup FactRow's trailing 'Confirm'. This also contradicts the prompt's own rule that facts in the Set up block appear only as a quiet 'Not set' below.
  - evidence: "The same facts show only a quiet 'Not set' in the categories below." vs "T1 Place: the pickup row reads 'Thursday · how often: Not set' ... and a trailing text action 'Confirm'"
  - fix: Replace the T1 pickup row with: "Pickup day · Thursday · how often: Not set · hollow · 'City of Camas · pickup Thursdays · not yet confirmed'; no trailing action; the row opens the DateSheet pickup kind (Thursday preselected)." Delete 'Confirm' from the T1 Place spec and from the frame 2 description.
- [minor] (ux) The widget's once-per-24h promotion rule makes the Next row unstable. The prompt never says what the Next row shows while the widget is suppressed, and never says whether HOME A's first-week steps are done, although the widget only becomes Next after they are.
  - evidence: "The widget is promoted at most once per 24 hours across Pantopus." and "Choose it like this: first the first undone first-week step; once all are done, ... widget (iOS and Android only), home insurance date"
  - fix: Add: "HOME A's first-week steps are all done (address saved, pickup confirmed, reminders on, dates added), so no Set up block shows. When the widget was already promoted elsewhere in the last 24 hours, the Next row shows the insurance ask instead and the widget stays a quiet FactRow in PLACE."
- [minor] (ux) Wording problems in the asks. 'Turn on a reminder or add the widget' is shown on web, which has no widget. The insurance ask says 'home insurance' for a renter, while frame 11 says 'renters insurance'. The Undo line uses a third label, 'home insurance date'. On iOS, 'Add the widget' implies an automatic add but only opens instructions, which the house style's avoid list covers ('Add it for me' on iOS).
  - evidence: "'Turn on a reminder or add the widget'" (T1 step, and Sam's web frame); "'When does your home insurance renew?'"; "'Skipped 'home insurance date' · Undo'"; frame 11 "renters insurance"; house style AVOID: "'Add it for me' on iOS"
  - fix: On web, the step label is 'Turn on a reminder'. The ask becomes 'When does your insurance renew?' and the Undo line becomes "Skipped 'Insurance renews' · Undo". The iOS Next-row button becomes 'See how to add it'; Android keeps 'Add the widget'.
- [minor] (ux) Sam's view is internally inconsistent: his 'Add one date that matters' step is ticked, yet his count (10) contains no date of his own. The prompt also doesn't say how the MemberRow line reads for the viewer.
  - evidence: "the count is '10 on file'" and "'Add one date that matters' ticked"; "People shows 'Maya and Sam live here · View'"
  - fix: Change to: "'Add one date that matters' ticked · 'Your household already has 4 dates'", and for Sam's view: "People shows 'You and Maya live here · View'".
- [minor] (ux) Layout specs contradict each other or the contract: inline buttons are placed both trailing and on their own line, targets are 44pt even on Android, the DateSheet is a right SlidePanel on all web widths, and the reminder tick for notifications-off is undefined on phones.
  - evidence: "Each row has a visible trailing 'Hide' text button" vs "Inline text buttons (Hide, ...) are separate 44pt targets on their own line"; "The DateSheet opens as a right SlidePanel on web" vs contract "SlidePanel (right on desktop, bottom on mobile)"; "Notifications off: the reminder tick still shows on the band" while the reminder tick/leader line is specified only for web 1440
  - fix: Say: "Hide sits trailing on the row at default sizes and wraps to its own line at AX5; inline text buttons are 44pt iOS / 48dp Android / 44px web." "DateSheet: right SlidePanel on web 1440, bottom SlidePanel on web 390; ProvenanceSheet: centred 560 modal on web 1440, bottom sheet on web 390." "On phones the reminder shows as a small caption 'Reminder Mon 15 Feb' under the Mar cell of the You lane."
- [minor] (ux) The prompt carries internal jargon and meta references Claude Design cannot act on, adds a section that is not in the v2 structure, uses ellipsis placeholders, and names the Notes artboard without the required format.
  - evidence: "The other F6 steps", "T1 frames" (T1 is never defined in the house style), "(its own prompt)", "(formerly 'See what we know')", "WHY ONE LIST (do not optimise this away)", "web /app/place?rule=…, native pantopus://place?rule=…", "22. Notes — assumptions"
  - fix: Define once, in WHO AND WHEN: "'Saved place' frames (PLACE B, not claimed)", and replace 'T1' with 'saved place' throughout. Replace 'The other F6 steps' with 'The other moving steps'. Delete '(its own prompt)' and '(formerly …)'. Fold WHY ONE LIST into THE ONE JOB as two sentences. Use 'rule=lease-notice' in both URLs. Name the last artboard 'x-place-file · notes · 22-notes · light'.
- [minor] (ux) Details from the contract and flows are off. The pending LockedActionRow gets a link, although the contract's pending variant has none. The postcard is 'mailed' on a Sunday. The voter DateRow title doesn't name the action. The VoteWA link is drawn as inline text, although the contract's outbound TextActionRow is a bordered row. There is no landing fallback for a reminder whose date was deleted (flow-03).
  - evidence: contract LockedActionRow: "pending (no link: 'We're checking your postcard code — expected Sep 19–26')" vs prompt "with a text action 'Enter your code'"; "Postcard mailed Sun 18 Oct"; contract DateRow: "The title names the action for deadlines"; contract TextActionRow outbound: "a bordered row with an external-link glyph"; flows-spec flow-03: "The landing falls back with 'That date was removed' and never shows a blank screen."
  - fix: Proof: the pending LockedActionRow reads 'Postcard requested Sun 18 Oct · expected Fri 23–Fri 30 Oct · code expires Tue 17 Nov', and 'Enter your code' becomes a separate outlined button below it. Voter row title: 'Register or update your voter registration'. Draw 'Check or update at VoteWA ↗' as the outbound TextActionRow, with 'Remind me' as a text button beside it. Add an EDGE CASE: "A reminder link to a deleted date lands at the top of Dates with the status line 'That date was removed' and never a blank screen."
- [minor] (ux) Screen-reader and keyboard access has gaps: the reading order skips the legend, and web 1440 month columns are clickable but have no keyboard or label specification.
  - evidence: Reading order "title → address → scope → freshness → count → banner → band summary" (legend omitted); "On desktop web, clicking a month column highlights that month's rows."
  - fix: Insert 'legend' after 'count' in the reading order. Add: "On web 1440 each month column is a focusable button labelled, for example, 'November 2026, 3 dates, show in list', with a visible focus ring distinct from the highlight. On web 390 the band is aria-hidden except its summary, because the Dates list is its table."

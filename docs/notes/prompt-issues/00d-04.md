# Last review findings for 00d-04 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The Android platform specimen does not fit. A 312dp dialog with 24 padding leaves 264dp, so two equal buttons with an 8dp gap are 128dp each. 'Delete photo' at bodyMedium 16 (about 100dp) plus a 16dp glyph and its gap already needs about 124dp before any horizontal padding. The prompt also never gives button horizontal padding.
  - evidence: Anatomy 1: '312dp on Android (a Material 3 basic dialog)', 'padding 24'. Anatomy 4: 'two buttons of equal size', 'leading 16pt trash glyph', 'Gap between buttons: 8'. PLATFORMS: 'outlined buttons side by side, right-aligned: "Keep it" then "Delete photo"'.
  - fix: Choose one of two fixes. (a) Widen the Android dialog to 364dp (412 minus 24 on each side), giving 154dp buttons, and state 'button horizontal padding 12, glyph-to-label gap 8'. (b) Stack the Android buttons like iOS (Delete on top). In either case, state the button padding in Anatomy 4 and check that web-390 fits (buttons of about 151px with padding 12).
- [minor] The scrim colour and opacity are never given as tokens, and neither is the Android sheet's Close target size (48dp).
  - evidence: 'over the platform's standard scrim'. ACCESSIBILITY: 'Close is 44pt.'
  - fix: Name the scrim, for example 'text.primary at 40%', or log it as a board assumption on Notes B. Write 'Close is 44pt / 48dp / 44px'.
- [minor] The progress count and bar ('Deleting 5 of 17…', bar at 5/17) arguably conflict with house invariant 6, which bans completion fractions, and Notes does not log this.
  - evidence: House invariant 6: 'No grades, scores, percentages ... completion fractions'. S2: 'Deleting 5 of 17…', bar at 5/17. The contract does ask for a 'deleting (progress count)'.
  - fix: Add Notes 4.7: 'The S2 count is operation progress for a real loss, not a completion score; kept per the contract. Confirm.'
- [minor] The web-390 'bottom-anchored 358px dialog' is close to a sheet but has no Close, while every sheet on the board must have a visible Close. The prompt doesn't say which one it is.
  - evidence: PLATFORMS: 'web-390: a bottom-anchored 358px dialog, buttons side by side.' House style: 'Every sheet has a visible Close or Done'.
  - fix: Add: 'It is a dialog (radius xl 16 on all corners, 16px above the tab bar), not a sheet, so it has no Close; Escape and the scrim cancel.'
- [minor] The 01 web-1440 crop and the 03 web-1440 specimen would carry the same published name, but the prompt never says that the 01 crop repeats it.
  - evidence: Anatomy header: 'plus a web-1440 crop of the same dialog'. KEYS lists 'DestructiveConfirm / one-photo / open / web-1440 / light' once.
  - fix: Add: 'The 01 web-1440 crop is the 03 web-1440 specimen repeated and carries its name.'
- [minor] The 'leave glyph' may not exist in the icon set, and the prompt gives no fallback.
  - evidence: 'a leave glyph from the icon set, still in semantic.error'.
  - fix: Name the icon (for example 'door-exit / arrow-right-from-bracket'), and add: 'if absent, use the trash glyph and log it on Notes C'.

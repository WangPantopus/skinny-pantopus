# Last review findings for 00c-11 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The V4 message 'We couldn't save that.' does not name what failed, which goes against the prompt's own copy rule. It is also missing from the invented-strings list.
  - evidence: V4: "We couldn't save that. Your choice is still here. · Retry". COPY RULES: "Name the source or the thing that failed". Notes invented strings list only "Your choice is still here."
  - fix: Use 'We couldn't save your pickup day. Your choice is still here. · Retry' and list the whole string on Notes.
- [minor] The fixture delta says the pickup mark stays hollow, but the V4 cell never says whether the 'Pickup day' field shows a ProvenanceMark. Crop 3 (web-390) does not require the legend line or ScopeChip that R13 requires whenever a mark appears.
  - evidence: Notes: "the mark stays hollow". V4: "the field ... still shows 'Pickup day' with the value 'Tuesday'". Crop 3: "V4 under its 'Pickup day' field." R13: "Every crop that shows a ProvenanceMark also shows the legend line and the place's ScopeChip".
  - fix: State 'the field shows a hollow mark' on V4, and add 'legend line and "Your household"' to crop 3, or state that the field shows no mark.
- [minor] The contrast of the error glyph on errorBg (light V4) is not in the R6 table and no caption for it is requested, although R6 says to use only table pairs.
  - evidence: ⑤: "errorBg only when a write failed (V4)". R6 lists error on base and sunken only.
  - fix: Add a caption on 01/02 with the measured ratio of error on errorBg (about 4.4:1) and list it on Notes.

# Last review findings for 00b-03 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] Crop (c) puts the phone-form YearBand window crop inside a web-1440 x-place-file context. YearBand's platform rule says that at 768px and wider, items sit on exact days with the 128px label column. YearBand batch 3 then checks this crop against 'this anatomy', which does not say which form applies.
  - evidence: 07 (c): "x-place-file on web-1440: V1 with per-cell buttons, above the V3 crop from 03." V3 on 03 is "the YearBand (00b-04) phone anatomy: 326pt, 12 columns of 27.2pt". 00b-04 PLATFORMS: "768px and wider place items on exact days."
  - fix: In 07 (c), draw the V3 window as a crop of the web-1440 exact-day band (128px label column + 12 × 60px), or put the phone crop in an iOS/web-390 frame. Say which form YearBand batch 3 should compare against.
- [minor] The widget legend drops every provenance word for the state rule. The bar is keyed only as 'Rule', so the standalone widget never says that the voter deadline is official.
  - evidence: V2: HOME A legend "○ On record · ✓ You added · ▮ Rule". House invariant 1: "Standalone surfaces (widget, push, share card) print the word next to the mark."
  - fix: Key the bar as "▮ Official rule", or state on Notes B that a bar always means an official rule and needs no separate provenance word.
- [minor] The medium-widget budget reserves the leftover 26pt only for 'the header line'. The stale n/a reason, however, points to the widget's own 'As of' line, and nothing says whether both fit in 26pt.
  - evidence: V2: "The remaining 26pt is f7-today-widget's header line." 02 widget-medium stale: "n/a — widget age is the widget's own 'As of' line (f7-today-widget)".
  - fix: Say that the 26pt header line carries both the label and the 'As of' text, or cite f7-today-widget's layout for where the age appears.
- [minor] The prompt uses type styles and sizes that are not in the house type scale, which says 'never invent sizes'. The setup's C7 AX5 map does not list captionMedium.
  - evidence: "anchors in captionMedium text.strong"; widget text "11pt text.secondary". House: "Use only the type scale below and never invent sizes" (the scale has no captionMedium and no 11pt except overline caps).
  - fix: Define captionMedium 12/16/500 and a widget 11/16/400 style once in the setup (with AX5/200% mappings), or reference overline/caption explicitly.
- [minor] The grid-layout wording says three grids 'of 3 columns', but the third grid has only 2.
  - evidence: "three stacked grids of 3 columns ... (loading · ready · empty) / (stale · permission-denied · t1) / (error · highlighted-row)"
  - fix: Write "three stacked grids of up to 3 columns".
- [minor] The T1 specimen gives no line-2 dates for its two DateRows, although the default window lists every one (R11).
  - evidence: T1: "One hollow mark on Thu 22 Oct only ... The state bar on Mon 26 Oct" with no line 2.
  - fix: Add: "in 3 days · Thu 22 Oct" and "in 7 days · Mon 26 Oct".
- [minor] The prompt is long for one send (about 14–15k characters on top of the house style and setup). Several rules are repeated across ANATOMY, ACCESSIBILITY, INSTEAD OF and DONE WHEN, which works against the instruction-density evidence.
  - evidence: Examples: the full-ink/no-dimming rule appears in ANATOMY, STATES, Notes D and DONE WHEN, and the per-cell-target rule in 04, ACCESSIBILITY, INSTEAD OF and DONE WHEN.
  - fix: Cut the ACCESSIBILITY and PLATFORMS restatements down to pointers, and trim DONE WHEN to checks that appear nowhere else.

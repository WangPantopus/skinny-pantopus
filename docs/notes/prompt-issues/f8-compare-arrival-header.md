# Last review findings for f8-compare-arrival-header (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) Dana's card headline can't exist on the day her card was made. The doc only picks the voter headline from Sep 20, and only when the deadline is 30 days away or less. Dana's card is as of Sat 12 Sep, which is 44 days before Mon 26 Oct and before Sep 20. Her token would carry a different headline.
  - evidence: first-person-loop-design-2026-09-16.md F8 'Rotating headline': "Sept 20 – state deadline: `election_deadline` rows for the state (F6 seeds) with `days_until ≤ 30` → 'Voter registration for November 3 closes {date} in {state}.'"; house-style-v2.txt FIXTURES: "FRIEND C ... Card as of Sat 12 Sep 2026"; prompt: "Headline: 'Online or mail voter registration for Nov 3 must arrive by Mon 26 Oct in Washington.'"
  - fix: Give Dana's token the headline the default ranking would pick on Sat 12 Sep, for example her radon or wildfire aha with its source and filled mark. Keep the voter headline only for cards made on or after Sep 20 and within 30 days of the deadline, and record the rule on Notes. Otherwise, record on Notes that the fixture breaks the doc's seasonal window, and ask for a decision.
- [major] (substance) At 390 the expanded rows merge their captions into 'Sources: FEMA, USFS, AirNow, EPA'. The contract allows that line only at 320–360px. The merge also removes the county-wide radon caveat and FEMA's 'area zone' from the viewport where nearly every link is opened. That contradicts the prompt's own INSTEAD OF about radon.
  - evidence: component-contract.md SourceCaption: "collapsed sources line at 320–360px: 'Sources: FEMA, USFS, AirNow, EPA'" and copyRules "Never 'Sources:' at widths above 360."; prompt: "Expanding it opens the four ScaleStrip rows in place, with sources merged into one line: 'Sources: FEMA, USFS, AirNow, EPA'" vs INSTEAD OF "draw it filled with a county-wide caption — because the EPA zone is official and its limit is its scope."
  - fix: At 390 wide, keep each row's SourceCaption when expanded. The radon row must keep 'EPA · county-wide estimate — only a test tells you about this home'. Use the merged 'Sources:' line only at 360 or narrower, or at 200% text. The field can still stay on screen, because the page scrolls.
- [major] (substance) On a phone, the main case, the prompt never says whether Dana's headline block appears or where. The below-640 layout lists only the collapsed line, then the field. The expanded state lists only the four rows and the sources line. Yet the edge cases hide 'the headline block', and the seasonal-aha entry lists this header as the place the sender's headline appears.
  - evidence: prompt LAYOUT: "The compare block below 640px: the collapsed sender line, then 'Your place', field, button and privacy line." / "Expanding it opens the four ScaleStrip rows in place, with sources merged..."; inventory-entries/f8-seasonal-aha.json entryPoints: "Compare arrival header (sender's headline)"; design doc: "sender's column (city, four grade chips, headline)"
  - fix: Say it outright. Below 640, show the headline block with its hollow mark and its 'On record, not confirmed · Washington Secretary of State' caption at the end of the expanded rows, and keep it out of the collapsed line. Draw it in frames 02 and 12. Or record on Notes that the headline is left out below 640, and why.
- [minor] (substance) DONE WHEN requires rows that match the reveal and the share card in order and wording, but the air and wildfire wording differs between the three prompts and from the Foundations board.
  - evidence: Header: label "Air quality (AQI) on Sat 12 Sep", value "AQI 42 · Good", label "Wildfire hazard", caption "USFS · quarter-mile · 2023"; reveal: "Dana: AQI 42 · Good · on Sat 12 Sep" under label "Air quality (AQI)"; OG: "Air on Sat 12 Sep · AQI 42 · Good"; prompts-v3/00b-01.md V9: "the Air value reads 'AQI 42 · Good · on Sat 12 Sep'", label "Wildfire hazard potential", caption "USFS · quarter-mile area · 2023".
  - fix: Use the V9 form everywhere: the value "AQI 42 · Good · on Sat 12 Sep" under the label "Air quality (AQI)", or record the difference as a contract update. Match the Foundations wildfire label and caption, or record the change.
- [minor] (substance) There is no rule for a token row whose reading is unconfirmed. The critique's blocker asks the token to carry confidence and to draw such rows hollow, or to drop them if confidence can't be carried.
  - evidence: critique-findings.md: "Add `conf` to the compare token and render an unverified reading as a hollow marker; if the token cannot carry confidence, drop unverified layers from the card rather than shipping them as confident."; prompt only: "All four rows are official, so each gets a filled mark."
  - fix: Add an edge case: a row the token marks unconfirmed shows a hollow mark and the 'On record, not confirmed' label on first use. If confidence is missing, leave that row out rather than drawing it filled.
- [minor] (substance) The headline rewrites the doc's template, which reads 'Voter registration for November 3 closes {date} in {state}.', and drops the doc's follow-up line. Notes records only the added weekday.
  - evidence: design doc: "'Voter registration for November 3 closes {date} in {state}.' follow-up 'Moved recently? Registration is per address.'"; prompt Notes: "the weekday added to the headline date"
  - fix: On Notes, record the rewording to 'must arrive by' (house style) and that the follow-up is dropped because the token only holds 90 characters.
- [minor] (substance) The worst case is shorter than the densest case in the source proposal.
  - evidence: derive-proposals.md f8-1: "a 90-char headline ... a 60-char city+name"; prompt worst case "Maximiliana-Josephine · Battle Ground, WA" (41 characters)
  - fix: Lengthen the worst-case city and name to about 60 characters, or explain the limit on Notes.
- [minor] (substance) At 640 and wider, the couldn't-check state is unclear: it says to show 'only the collapsed line', which is a below-640 element.
  - evidence: prompt: "If a client-side check still fails, show only the collapsed line (no marks) with 'We couldn't check this link just now...'"
  - fix: Say what Dana's column shows at 1440 in this state.
- [major] (ux) The expanded state at 390x844 cannot keep the field on screen, so artboard 2 and the DONE WHEN keyboard rule ask for something that can't be drawn. Four ScaleStrip rows at compare size are about 120-150px each by the Foundations anatomy, roughly 530px in total. About 300px of top bar, H1, lede and the collapsed row already sit above them, so the field lands near 850px, below the 844 fold. Claude Design will either shrink the rows and break the contract anatomy, or quietly draw the field off screen. The 'with the keyboard open' rule also can't hold: an iOS keyboard leaves about 400px visible, and the field, button, privacy line and 5 suggestions don't all fit in that.
  - evidence: Prompt LAYOUT: "Expanding it opens the four ScaleStrip rows in place ... The field moves down but stays on screen." Artboard 2: "four rows open, sources line, field still on screen." DONE WHEN: "At 390x844 the field, button and privacy line are in the first viewport, with and without the keyboard." prompts-v2/00b.md ScaleStrip anatomy: "Row padding is 12pt top and bottom. Gaps: 4pt ... 8pt ... Track zone: 24pt tall ... Band names ... may take up to 2 lines".
  - fix: Replace the LAYOUT sentence with: "Expanded, the rows push the field below the fold. Directly under the last row, repeat 'Hide readings' (44px), then the 'Your place' heading, field, button and privacy line." Change artboard 2 to: "f8-compare-arrival-header · web-390 · 02-expanded · light — a tall scrolling frame with the 844px fold marked; four rows, sources line, 'Hide readings', then the field." Change the DONE WHEN line to: "In the collapsed state at 390x844, the field, button and privacy line are in the first viewport. When the field is tapped and the keyboard opens, the page scrolls so the field and the first two suggestions stay visible above the keyboard." In artboard 4, draw the keyboard and a 400px visible area.
- [minor] (ux) The prompt never says what the expanded state shows below 640px beyond the rows and the sources line. The freshness line, the '● Official' legend and the headline block, with its hollow mark and first-occurrence label, have no stated place. Without the freshness line, the frozen date has nowhere to live.
  - evidence: LAYOUT: "Expanding it opens the four ScaleStrip rows in place, with sources merged into one line". The >=640 spec lists "sender line, subline and freshness line, then four ScaleStrip rows ... then the headline block", but the <640 spec names no equivalents.
  - fix: Add: "Expanded below 640, in order: subline 'What's on record for this area', freshness line 'Dana's card · as of Sat 12 Sep', legend '● Official', the four rows, 'Sources: FEMA, USFS, AirNow, EPA', then the headline block with its hollow mark and '○ On record, not confirmed'."
- [minor] (ux) The loading state for the collapsed line asks for 'track outlines', but V10 is a text-only line with no tracks. The prompt also names only the instrument skeleton.
  - evidence: EDGE CASES: "the collapsed line (or Dana's column at 640+) shows track outlines while the field is already live." 00b V10: "This variant draws no tracks." 00d WarmingSkeleton: "V2 Row skeleton (lists)".
  - fix: Write: "Below 640, the collapsed line is one 44px surface.sunken bar (WarmingSkeleton row skeleton). At 640 and wider, Dana's column shows the WarmingSkeleton instrument skeleton · scale strip." Add 'row skeleton' to FOUNDATIONS COMPONENTS USED.
- [minor] (ux) The offline state conflicts with the Foundations OfflineNotice form variant and prints the same sentence twice.
  - evidence: Prompt: "The OfflineNotice form variant sits under the field, and 'See your place' is disabled with the reason caption 'You're offline. We can't look up an address right now.'" 00a R3: "a disabled address field with the message "You're offline. We can't look up an address right now." ... on a surface.sunken strip above the field."
  - fix: Write: "Offline: the collapsed line stays. The OfflineNotice form variant (its sunken strip) sits above the field. The field and 'See your place' are disabled and stay focusable, and each reads that strip's sentence as its reason. The sentence is printed once."
- [minor] (ux) The couldn't-check state still shows readings from a token nobody has verified. A tampered link could then show made-up readings under Pantopus's name. The state is also written in implementation terms ('server render', 'client-side check') that Claude Design can't act on.
  - evidence: EDGE CASES: "The token is verified during the server render. If a client-side check still fails, show only the collapsed line (no marks) with 'We couldn't check this link just now. Showing the card as it was sent.'" Design doc F8 Acceptance: "A tampered token renders the expired banner."
  - fix: Replace it with: "Couldn't check the link: the ordinary hero (Example card at desktop) with a LandingBannerSlot-shaped banner, 'We couldn't open this card just now.' · 'Retry', in the pill position. No readings are shown, and the field stays live." Update artboard 9 and COPY to match. Move the server-render sentence to Notes only.
- [minor] (ux) Making the rows non-tappable breaks the ScaleStrip contract rule that the whole row is the target, and the prompt doesn't record it as an override.
  - evidence: Prompt: "On this surface, rows and captions are flat text and are not tap targets". Contract ScaleStrip: "Row height is 48pt minimum at detail size (the whole row is the target)." The Overrides list names only V10 and V9.
  - fix: Add to Overrides and Notes: "ScaleStrip rows on this header are not targets, because the readings are Dana's frozen card and there is no address to open a ProvenanceSheet for. Rows on the reveal stay targets."
- [minor] (ux) The collapsed button's spoken name doesn't contain its visible text, which fails WCAG 2.5.3 Label in Name for people who use voice control.
  - evidence: Visible: "Dana · Camas, WA · Flood X · Wildfire Moderate · Air 42 on Sat 12 Sep · Radon Zone 1 ›". Name: "Dana's card, Camas, Washington, as of Saturday 12 September: flood zone X, minimal flood hazard; ..."
  - fix: Start the name with the visible words: "Dana, Camas, WA: Flood X, Wildfire Moderate, Air 42 on Sat 12 Sep, Radon Zone 1. Dana's card as of Saturday 12 September. Show readings."
- [minor] (ux) The layer labels differ from the Foundations specimen, and 'air quality index (AQI)' is never spelled out on screen.
  - evidence: Prompt rows: "Wildfire hazard", "Air quality (AQI) on Sat 12 Sep". 00b fixture labels: "Wildfire hazard potential", "Air quality index (AQI)". Glossary: "Air quality (AQI) — spell out 'air quality index (AQI)' once per surface."
  - fix: Use "Air quality index (AQI) on Sat 12 Sep" as the Air row label, so the term is spelled out once. Use the 00b label "Wildfire hazard potential", or record the shorter glossary form as a deliberate choice in Notes. Apply the same wording on the reveal.
- [minor] (ux) FIRST FIVE SECONDS contradicts the layout: it puts Dana's line directly under the H1, but the lede sits between them.
  - evidence: "1. The H1 and Dana's summary line directly under it." vs LAYOUT "top bar, H1, lede ..., the compare block".
  - fix: Change it to "1. The H1, then Dana's line just below the lede."
- [minor] (ux) Once the mail deadline passes, the headline is hidden, but the research says it should switch to the in-person path. The deadline also lacks the relative day count the house style asks for.
  - evidence: Research (share-compare-civic): "From Oct 27 to 8 p.m. Nov 3, the headline switches to the in-person path instead of disappearing." House style: "beyond tomorrow, add the relative count: 'in 7 days · Mon 26 Oct'". Prompt: "hide the headline block."
  - fix: Show the headline as "Online or mail voter registration for Nov 3 must arrive by Mon 26 Oct in Washington." with the caption line "in 7 days · Mon 26 Oct", computed on the viewing day. From Tue 27 Oct, when the server holds the state row for the card's state (taken from 'Camas, WA'), replace it with "In person: until 8:00 PM Tue 3 Nov · Washington Secretary of State" and a hollow mark. Hide it only when no row exists. Record this in Notes.
- [minor] (ux) No frame shows the expired link at desktop, where the layout changes because the Example card returns. One Notes question is also already answered by the research.
  - evidence: EDGE CASES: "the compare block is gone (the Example card returns at desktop)", but the only expired artboard is web-390. Notes: "Confirm this against the rule that hollow is for facts a household can confirm." research-findings: "Hollow mark only for facts the household can confirm, such as pickup day or seeded deadlines."
  - fix: Add the artboard "f8-compare-arrival-header · web-1440 · 07b-expired · light — V5 banner above the ordinary hero with the Example card" to Turn 3. Replace that Notes question with "Seeded deadlines are hollow, per research (trust topic)."

# Last review findings for f9-verification-promise-copy (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The invite allowance copy misstates the referral rule. The doc says the allowance grows only for converted referrals (neighbors this user invited who then joined). The prompt says any neighbor who joins adds one. A verifier on a block where 3 people join on their own would expect 6 invites and get 3. On a surface whose job is honest promises, that is a false promise.
  - evidence: Design doc F9 'Referral tiers pay': "1 referral → `+1` weekly postcard invite (`blockFoundersService.invites_remaining` reads converted referrals; cap 3 + min(referrals, 3))". Prompt COPY: "Plus 1 a week for each neighbor who joins, up to 6." / "Each neighbor who joins adds 1 a week, up to 6."; EDGE CASES: "3 base + 3 from neighbors who joined".
  - fix: Change the strings to say the neighbor was invited by this user: "Plus 1 a week for each neighbor you invite who joins, up to 6.", "Each neighbor you invite who joins adds 1 a week, up to 6." and "3 base + 3 from neighbors you invited". Add the new wording to the Notes list of invented strings.
- [major] (substance) The web mover row has no artboard. The inventory and the derive proposal list the web JustMovedCard payoff line as a host, and ATTACH (5) asks for the web mover section. Artboards 10 and 11 draw only iOS and Android. The payoff-line change is never drawn on web, even though the uxNote warns that platforms diverge when one is skipped.
  - evidence: Inventory hostSurface: "VerifyPromptSheet, VerifiedSuccess, the mover rows' payoff line on web; PlaceVerifyFlow and JustMovedCard on iOS; JustMovedCard on Android." derive f9-founding-promise-copy hostSurface: "place/JustMovedCard.tsx:45". Prompt ATTACH: "(5) The Place file mover section ... on web, iOS and Android". ARTBOARDS: only "10 ... ios · 10-mover-row" and "11 ... android · 11-mover-row".
  - fix: Add an artboard 'f9-verification-promise-copy · web-390 · NN-mover-row · light' showing the Place file "Meet the block" row before and after verifying. Renumber the later artboards, update frames to 19 and adjust the BATCH PLAN.
- [minor] (substance) Sam's postcard timing contradicts the stated postcard delivery time. Mailed Mon 12 Oct and expected Thu 22 Oct is 10 days, while the sibling prompt says a code usually takes 5–7 days. Sam also has a postcard pending but is shown the ordinary sheet (frame 4), while the verify-sheet sources say a pending verification shows its status instead of a second start.
  - evidence: Prompt: "Pending example (Sam): postcard mailed Mon 12 Oct, expected Thu 22 Oct." f9-founding-meter-preview: "A postcard code usually takes 5–7 days." derive f3b-verify-address-sheet states: "verification already pending (show status, not a second start)".
  - fix: Use an expected date 5–7 days after mailing (for example, mailed Wed 14 Oct, expected Tue 20 Oct: "expected tomorrow · Tue 20 Oct"). Either keep Sam's frame 4 as a household member with no pending postcard, or use a separate person for the pending Proof row.
- [minor] (substance) The claim that Android has no verify sheet is only true today. The f3b verify sheet prompt adds the sheet on Android, and it uses the same GrantLimitList strings. The prompt should acknowledge this so the Android list isn't treated as out of scope.
  - evidence: Prompt: "On Android, change only the mover row. This change has no Android verify sheet or success screen to edit." derive f3b-verify-address-sheet: "iOS and Android have only an inline verify banner ... and need the sheet itself"; platforms web/ios/android.
  - fix: Add: "The Android verify sheet is new in the verify sheet prompt and inherits these contract strings; it is not drawn here."
- [minor] (substance) The landlord method has no string, although the doc lists landlord confirmation as an address-verification method and the prompt itself mentions landlord checks. The 'confirmed by Maya Chen' example is an owner attestation, which is not in the doc's list of methods.
  - evidence: Design doc F3b: "Only address verification (postcard code, reviewed document, landlord confirmation, admin override)". Prompt: "Other methods, same pattern: 'Address confirmed by Maya Chen · ...' · 'Address confirmed by document · ...'".
  - fix: Add "Address confirmed by landlord · Mon 19 Oct 2026". Mark the person-name variant as depending on the owner-attestation proposal in Notes.
- [minor] (substance) The Notes artboard leaves out two items: the house-style date exception (no relative count on Founding dates) and the uxNote build requirement to run the copy grep over all three client apps.
  - evidence: Prompt COPY: "Date rule exception: Founding dates carry no relative count" (not in item 18). Inventory uxNote: "Run the grep over frontend/apps/{web,ios,android} or the platforms diverge on day one."
  - fix: Add both to artboard 18: record the date-rule exception, and add a founder flag to extend the copy grep beyond publicPlace.test.js to the six client files.
- [major] (ux) The prompt adds two new blocks to the one shared verify sheet but never says where they go. It also describes a host that doesn't match the verify sheet's own spec. That sheet is a method picker in which nothing is primary, and it has a separate pending state. Maya's postcard has already arrived, so her sheet would be in that pending state, not the full pre-verify list. Without a stated order, Claude Design may push the method rows or the code entry below the fold at 390, or draw a primary button the host doesn't have.
  - evidence: f9 prompt: "then the host's existing primary action (unchanged)"; "Sheet, block 2 ... Sheet, block 3" (no position given); frame 13 "the host's code entry is disabled"; "this prompt changes only its lists". prompts-v1/f3b-verify-address-sheet.md: "Method rows below ... No method is visually recommended; nothing is primary"; inventory f3b states: "verification already pending (status, not a second start)".
  - fix: Add a line under LAYOUT: "Order inside the sheet: host reason header → What this unlocks (2 rows + 'and 3 more') → rank caption → Also after you verify → the host's method rows (or, in the pending state, the host's 'Enter your postcard code' step) → 'What verifying doesn't do' as a collapsed disclosure below the methods. At 390 the first method row (or the code field) stays in the first viewport." In FIRST FIVE SECONDS, replace "the host's existing primary action" with "the host's method rows (nothing is primary); in Maya's pending state, the code field". In frame 1, state that Maya's sheet is in the host's pending state. In frame 13, say "method rows (or code entry) disabled".
- [major] (ux) The Founding row on the sheet leaves out the condition that the address must be verified before the window closes. With a postcard taking 5–7 days and only 6 days left, "Become a Founding Neighbor" promises something the system may not honour. The sibling prompt (f9-founding-meter-preview) spells out this condition, so the two surfaces contradict each other.
  - evidence: f9 prompt COPY: "Only while the window is open: \"Become a Founding Neighbor\" / \"3 of 5 slots open on this block · Closes Sun 25 Oct\"." f9-founding-meter-preview: "A slot goes to a home only once its address is verified before the window closes (Sun 25 Oct). A postcard code usually takes 5–7 days." Brief: "scarcity and deadlines are shown as dates and fail closed (FTC 2022, Mathur 2019)".
  - fix: Change the sub line to: "3 of 5 slots open on this block · Goes to homes verified by Sun 25 Oct". Add to EDGE CASES: "If the chosen method's usual time runs past the closing date, the Founding row is not drawn." List the new string on Notes.
- [major] (ux) The heading "Also on this block" doesn't say that invites and the Founding tier need verification. A first-time reader can take them as things they already have, or as something unrelated. That undermines the job of saying plainly what verifying adds.
  - evidence: f9 prompt: "Sheet, block 2, \"Also on this block\": plain text rows ... Row A, the invite allowance. Row B, the Founding line". Design doc F3b: "Surfaces that require address verification: ... Block Founder rank and postcard invites ... the Founding Neighbor tier."
  - fix: Rename the heading to "Also once you verify" everywhere (COPY, LAYOUT, INSTEAD OF, Notes). Give it the same heading style as "What this unlocks", and state that style explicitly.
- [major] (ux) The prompt says Android has no verify sheet or success screen to edit. But the verify-sheet prompt creates that sheet on Android, and DONE WHEN requires matching strings on web, iOS and Android. No frame checks the lists on Android, so the Android mirror can keep the old names, which is the exact risk the inventory calls out.
  - evidence: f9 prompt: "On Android, change only the mover row. This change has no Android verify sheet or success screen to edit." inventory f3b-verify-address-sheet: "iOS and Android have only an inline verify banner and need the sheet itself"; v1 f3b frames: "method picker, pending, non-owner-blocked on Android". Inventory uxNote: "the native mirrors will silently keep the old names".
  - fix: Replace the Android sentence with: "On Android, the verify sheet is drawn by the verify sheet prompt; here, apply the same lists to it and change the mover row." Add an artboard "f9-verification-promise-copy · android · NN-verify-sheet-list · light" (the expanded list with the Also-once-you-verify block) and one Android success step. Renumber, and rebalance the batch plan so no turn has more than 6 artboards.
- [minor] (ux) The offline strings are missing: the OfflineNotice line and the reason caption for the disabled control. Claude Design will have to invent them.
  - evidence: EDGE CASES: "Offline: OfflineNotice above the list ... the host's code entry is disabled with its reason caption." No such string appears in COPY.
  - fix: Add to COPY: "You're offline. What verifying adds stays readable." and the reason caption "Verifying needs a connection." List both on Notes.
- [minor] (ux) The pending dates don't match the stated postcard time: Mon 12 Oct to Thu 22 Oct is 10 days, but the sibling prompt says 5–7 days. The pending format also differs from the contract's LockedActionRow pending example.
  - evidence: "postcard mailed Mon 12 Oct, expected Thu 22 Oct" vs f9-founding-meter-preview "A postcard code usually takes 5–7 days"; contract LockedActionRow pending "expected Sep 19–26".
  - fix: Use "Postcard mailed Thu 15 Oct · expected in 3 days · Thu 22 Oct", and say which component draws Sam's pending row (FactRow pending on the Proof row, or LockedActionRow pending in the sheet).
- [minor] (ux) The push goes to Account & security, a group the brief sets to Time Sensitive. A 'check finished' notice isn't urgent, so the interruption level would be wrong (P8). The push is also never drawn, and PushCopy isn't named.
  - evidence: "Push when a later check finishes (Account & security)"; brief §3 table: "Account & security | Sign-in, invitations to you | Time Sensitive".
  - fix: Add: "Interruption level Active, not Time Sensitive; drawn with PushCopy on the Notes board only", or drop the push from this prompt and leave it to the notification prompt.
- [minor] (ux) The web mover row is in scope (the inventory host lists the web mover rows, and ATTACH item 5 asks for web), but no artboard shows it.
  - evidence: ATTACH (5) "on web, iOS and Android"; artboards 10 and 11 cover iOS and Android only.
  - fix: Add the before/after mover row to frame 9 (web-390), or add a web-390 mover-row artboard.
- [minor] (ux) The verified Proof row doesn't say which ProvenanceMark it uses or which word goes with it. "Address confirmed by Maya Chen" only makes sense on Sam's row, and that isn't stated.
  - evidence: "verified \"Address confirmed by postcard · Mon 19 Oct 2026\""; "Other methods ... \"Address confirmed by Maya Chen · Mon 19 Oct 2026\"".
  - fix: Add: "Verified Proof row carries the FILLED ProvenanceMark; pending carries none. 'Address confirmed by Maya Chen' appears only on Sam's Proof row."
- [minor] (ux) The place file's inventory entry doesn't list a "Meet the block" mover row (it lists only mail forwarding, utilities and voter registration), so the host may not exist.
  - evidence: inventory x-place-file: "Mover rows (mail forwarding, utilities, voter registration)".
  - fix: Add a founder flag on Notes: "x-place-file mover rows must add 'Meet the block' or this line has no host."

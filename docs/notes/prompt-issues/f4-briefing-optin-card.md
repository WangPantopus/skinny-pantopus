# Last review findings for f4-briefing-optin-card (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The prompt says a grant from the morning row turns on both rows. That would switch on the night-before briefing for someone who already declined it. Frame 6 is exactly this case: the night-before row was declined on the pickup card, and the morning row is still asking.
  - evidence: The prompt has: "a grant from the morning row turns on both rows", and frame 6 is "pickup-ask-declined · night-before row absent, morning row asking". It also says: "A declined ask, here or on the pickup card, never reappears on its own". ux-research-brief.md §3: "Nothing you turned off was turned on." and "Default after grant: Evening on; morning on only if chosen on the card".
  - fix: Change the sentence to: "A grant from the morning row also turns the evening briefing on, unless the night-before row was declined here or on the pickup card. When it does, the night-before row shows collapsed with its time ('The night before pickup · 6:00 PM · Change')."
- [major] (substance) Notes do not say that on iOS, 'Yes opens the system dialog' depends on removing the app's launch-time permission request. The critique rates this a blocker that directly targets the briefing opt-in.
  - evidence: critique-findings.md [blocker] targets f1-briefing-optin-card, f4-4-evening-optin-card: "AppDelegate.swift:50-61 calls requestNotificationPermission() from didFinishLaunchingWithOptions ... iOS never re-asks." flows-spec flow-14 step 1: "the AppDelegate permission request has been removed." The prompt's Notes list only the web push channel dependency.
  - fix: Add to Notes → Dependency: "Remove the iOS launch-time permission request (AppDelegate.swift:50-61); otherwise most iOS users arrive already asked, and frame 16 never occurs."
- [minor] (substance) 'Draw nothing in that old location' goes against the merged proposal's advice to leave a pointer for home users who already use the old morning opt-in. The prompt gives no reason and does not cite the route's deletion.
  - evidence: derive-proposals.md f1-briefing-optin-card uxRisk: "moving the only morning opt-in onto Today strands home users who already use it on Place > Today — leave a pointer there rather than silently relocating their control." FINAL-screen-inventory.md f1-today-tab: "Rename /app/place/today's residual content ... or delete the route."
  - fix: Add a Notes line: "Old Place > Today opt-in is removed because that route is folded into Today (FINAL merges); no pointer is needed once the route is gone."
- [minor] (substance) The iOS both-on collapsed state is neither drawn nor listed as omitted. Notes list only Android both-on as omitted.
  - evidence: v1: "repeat "both on, collapsed" and "OS denied" once each on iOS and Android". The prompt's Notes: "Omitted: Android both-on (same as web frame 4)".
  - fix: Change the Omitted line to "iOS and Android both-on (same as web frame 4)".
- [minor] (substance) 'Not now' and 'No thanks' get the same InlineUndo text, although they mean different things and the pickup card words them differently.
  - evidence: The prompt has: "Declined in the card: the InlineUndo "You can turn this on in Notification settings · Undo"". The pickup prompt has: ""Not now" collapses the ask to the InlineUndo "No reminder for now · Undo"".
  - fix: Use "No reminder for now · Undo" for Not now and "You can turn this on in Notification settings · Undo" for No thanks, matching the pickup card.
- [minor] (substance) Frame 4 needs the one-liner receipt while Today's full QuietDayReceipt is off screen, but on a quiet Wed 21 Oct Today would normally show the full receipt. The prompt does not say how that screen is composed, for example by scrolling.
  - evidence: The prompt has: "The receipt one-liner appears only when both rows are on and Today's full QuietDayReceipt is not visible on the same screen" and frame 4 is "Wed 21 Oct receipt, no full QuietDayReceipt on screen".
  - fix: State that frame 4 is scrolled so that the card is in view and Today's full receipt is above the fold edge, or pick an evening when Today is not quiet but tonight's check found nothing.
- [major] (ux) The spoken names of the Yes buttons do not contain their visible label "Yes", which fails WCAG 2.5.3 Label in Name (Level A). A Voice Control user who says "Tap Yes" cannot activate them.
  - evidence: CONTENT: Buttons "Yes", "Not now", "No thanks". ACCESSIBILITY: "Yes buttons: "Turn on night-before briefing" and "Turn on morning briefing"."
  - fix: Change the spoken names to "Yes, turn on the night-before briefing" and "Yes, turn on the morning briefing" (the visible text first). Keep "Not now" and "No thanks" as their names, with the row question as context.
- [major] (ux) A grant from the morning row turns on the evening briefing even when the person has already declined the night-before reminder on this card or on the pickup card. That breaks the rule that a decline is respected. The silent switch-on is also never drawn, so the person cannot see what changed.
  - evidence: INTERACTION: "A grant from either row turns the evening briefing on. The morning briefing turns on only through its own Yes, so a grant from the morning row turns on both rows." Frame 6: "pickup-ask-declined · night-before row absent, morning row asking". WHERE IT LIVES: "If they tapped "Not now" or "No thanks" there, the night-before row counts as declined." Brief section 2.7: "Not now" is "never shown again automatically".
  - fix: Replace the sentence with: "A grant from the morning row also turns on the evening briefing only when the night-before row has never been answered. The night-before row then collapses to 'The night before pickup · 6:00 PM · Change' with the status 'Also on: the night before pickup'. If that row was declined, the grant turns on the morning briefing only." Add that frame to the manifest (for example as 03b).
- [major] (ux) A person who already declined the OS prompt (for example on the pickup card) sees rows that offer "Open settings" / "How to turn them on" without having tapped anything. This is the pressure after denial that the per-surface change and flow-01 rule out.
  - evidence: CONTENT: "Already denied before this row's Yes: the row shows the text.secondary line "Notifications are off for Pantopus" (no amber) with "Open settings" (iOS, Android blocked) or "How to turn them on" (web)." Artboard 12 draws it. Per-surface change: "If the person declined at the OS prompt, collapse the card ... or show one neutral text.secondary line once"; Android: "Don't link to system settings in an effort to convince". flow-01: "iOS OS prompt denied. The card falls back to one neutral line plus a single widget offer."
  - fix: Replace that bullet with: "OS permission already denied (declined anywhere): the rows show normally. Only after the person taps Yes does that row swap to the text.secondary line 'Notifications are off for Pantopus' with 'Open settings' (iOS, Android blocked) or 'How to turn them on' (web). If the denial happened in this session's own dialog, use the declined-at-system collapse instead." Rename artboard 12 to "12-yes-tapped-while-denied".
- [minor] (ux) The widget offer wording disagrees with the NotificationAsk board and differs slightly from the pickup prompt.
  - evidence: Prompt: "Pickup still shows here on Today · Or put it on your home screen" with "Show me how". 00d-06 S3: "Put Today on your home screen" with "Add the widget". Pickup prompt: "Put today on your home screen" / "Show me how".
  - fix: Keep the flow-07 string, but log on Notes that it overrides 00d-06 S3 ('Add the widget' becomes 'Show me how'). Use the same action label in the pickup prompt.
- [minor] (ux) "Not now" and "No thanks" behave the same on the card and share one undo string. The pickup card gives each its own string, so the person cannot tell the two apart.
  - evidence: "Declined in the card: the InlineUndo "You can turn this on in Notification settings · Undo"." and ""Not now" hides the row and never asks again on its own. "No thanks" is permanent until changed in settings." Pickup prompt: "Not now" collapses to "No reminder for now · Undo".
  - fix: Use the pickup card's pair: "Not now" becomes "No reminder for now · Undo" and "No thanks" becomes "You can turn this on in Notification settings · Undo". Add one line saying how Not now differs (for example, it can be offered again from the place file, while No thanks cannot).
- [minor] (ux) The OS-decline collapse line is about pickup even when the decline came from the morning row, and for a saved place with no pickup day it promises a pickup the card does not have.
  - evidence: "Native: the card collapses to one text.secondary line, "Pickup still shows here on Today · Or put it on your home screen"". PLACE B caption: "Pickup joins once you confirm your pickup day."
  - fix: Add: "When the decline came from the morning row, or the place has no pickup day, the line reads 'Your dates still show here on Today · Or put it on your home screen' (invented; list it on Notes)."
- [minor] (ux) Detail from the absorbed proposals was dropped. The card never says which place the briefings follow for someone with more than one place, and Maya has a saved place too.
  - evidence: derive-proposals f1-briefing-settings: "a new caption stating that with no home the briefings follow your saved place (and which one)". The prompt names no place on the card.
  - fix: Add under the overline, for people with more than one place: the caption "For 2418 NE Larkspur Loop" (invented), next to the location row's ScopeChip. List it on Notes.
- [minor] (ux) The "Yes" label is not verb-led, as the house microcopy requires. The prompt keeps it without logging it, although the Foundations board raised it as an open question.
  - evidence: House style MICROCOPY: "Buttons start with a verb". 00d-06 Notes 6.2: ""Yes" also breaks the house-style rule that buttons start with a verb ... Questions: ... Replace "Yes" with a verb".
  - fix: Add to Notes: "'Yes' kept as an answer to the row question (board question 6.2 still open); the accessible name is verb-led."

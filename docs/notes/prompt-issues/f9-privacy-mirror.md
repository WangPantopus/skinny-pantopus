# Last review findings for f9-privacy-mirror (round 2). The fix after this round was applied but not re-verified.

- [minor] (substance) For a saved place, the Address row has a 'Change' control, and 'Row controls open their existing screens'. The existing Address change screen belongs to a claimed home, so for Jordan (PLACE B, 'Saved place · Only you') the control has no known destination. The row 'A neighbor sees: Camas, WA' also sits under an Only-you chip without saying where neighbors actually see that label.
  - evidence: Prompt: "Address row: 'A neighbor sees: Camas, WA', with the control 'Change'." and "Row controls open their existing screens." Inventory hostSurface: "re-scoped from home-gated to user-scoped with a place selector" (the existing rows were home-scoped). ScopeChip copyRules: "A saved place never uses the words home, household or claim."
  - fix: Say what 'Change' opens for a saved place, or drop it for saved places and state why. Add an open question to Notes: "What does the Address row's 'Change' open for a saved place?" Qualify the value, e.g. "A neighbor sees on your posts: Camas, WA".
- [minor] (substance) The PLACE B frames use 'home' wording on a saved place, which breaks the glossary rule for saved places.
  - evidence: Prompt: Hidden row "Your street address · Your home on the map"; map spoken label "Your home is not shown." component-contract ScopeChip copyRules: "A saved place never uses the words home, household or claim." Glossary: "Home — a claimed address ... Not: ... your place (for T1)".
  - fix: For saved-place frames, use "Your street address · This place on the map" and the spoken label "This place is not shown." Keep "Your home" for HOME A only, or record the choice on Notes.
- [minor] (substance) The composer one-liner dropped the 'Nearby ·' prefix that research quotes for the picker. It also names only the text label, not the map spot, and the mirror says neighbors also see a pin.
  - evidence: research-findings.md:2334: "the location picker shows what neighbours will see ('Nearby · Camas, WA')". Prompt: "Neighbors see “Camas, WA”, never your address." Derive proposal: "Neighbors see a ~N-block area, never your house".
  - fix: Make the one-liner match the Nearby card header format actually in use, as the posts row does. If the card header differs, record it on Notes. Consider "Neighbors see “Camas, WA” and a spot about 0.3 mi away, never your address."
- [minor] (substance) The fixture delta and frame 2 contradict each other. Maya has two places only 'for the selector frame', but frame 2 draws her header with a chevron, which only appears when there are several places.
  - evidence: Prompt: "For the selector frame only, Maya has also saved PLACE B" vs artboard 2 "Maya, HOME A selected (chevron)" and "With one place, the selector is a plain header with no chevron."
  - fix: Either make Maya's two places apply to all of her frames (2, 4, 13, 15, 18), or draw frame 2 without the chevron.
- [minor] (substance) The prompt says the iOS deep link pantopus://homes/:id/privacy 'keeps working', but the sources only establish the web route and that Android lacks one. Nothing confirms iOS handles it.
  - evidence: Derive proposal: "No deep link today — pantopus://homes/:id/privacy is not among the 8 enumerated /homes children in Android DeepLinkRouter.kt". Prompt: "/app/homes/:id/privacy (web) and pantopus://homes/:id/privacy (iOS) keep working".
  - fix: Add "Confirm that iOS routes pantopus://homes/:id/privacy" to the Notes engineering list.
- [minor] (substance) The whole-screen error state from the inventory is only listed as omitted. Also, the scale bar stops at 0.3 mi, yet the worst case (0.4 mi) must be measurable on it.
  - evidence: Inventory states: "loading ... error ... offline / cached". Prompt: scale bar "0 · 0.1 · 0.2 · 0.3 mi"; worst case "about 0.4 mi"; DONE WHEN "measure the distance on the scale bar".
  - fix: State that the scale bar extends to 0.4 mi, or rescales, when the offset exceeds 0.3 mi. Optionally add a small inset of the whole-screen failure in frame 10.
- [minor] (substance) The ScopeChip is informational here, which departs from the research rule that every scope chip opens the control that changes it. It is justified on Notes, so no change is needed. Research's layered-label grid (what is stored, who sees it, how to change it, with 'None' for blanks) is not applied.
  - evidence: research-findings.md: "Every 'Only you' or 'Your household' chip should open the control that changes it." and "the privacy mirror use a short fixed grid: what is stored, who can see it, how to delete it".
  - fix: Add a Notes line saying the layered-grid pattern was considered and replaced by the audience-view rows.
- [major] (ux) The prompt says the screen is safe to share, but its own header shows the home's street. The header prints the street label, and the map beside it shows nearby street names plus the distance. A screenshot therefore narrows the home to one short court.
  - evidence: Prompt CONTENT: "Header selector: \"Birchfield Ct · Camas\""; INSTEAD OF: "Instead of \"1107 NE Birchfield Ct\" in the header, draw \"Birchfield Ct · Camas\" — because people share this screen to show they are protected."; EDGE CASES: "Every frame is safe to screenshot: none reveals the address position"; DONE WHEN: "No frame shows or implies where the home is". Contract PublicPointMap: "never centred on the home in a shareable frame".
  - fix: In the collapsed header, show only the person's own label if one exists, otherwise the city ('Camas, WA'), plus the ScopeChip. Show street labels only inside the open 'Show for' menu. Rewrite the INSTEAD OF line to: "Instead of any street name in the header, draw the place's own label or its city — because a street plus a pin and a distance narrows the home to a few houses in a shared screenshot." Update frames 1, 2, 3, 13, 14 and 15 to match, and add a Notes line saying the menu (not the header) carries street labels.
- [major] (ux) The composer one-liner understates what neighbors see. Neighbors also see a pin on the Nearby map, not only the city text. The TYPE section promises the 'same one-line promise' as the mirror, but this line is a different claim.
  - evidence: TYPE item 4: "Show the same one-line promise inline in the Nearby post composer's location picker." CONTENT: "Composer picker, under the chosen place: \"Neighbors see “Camas, WA”, never your address.\"" Per-surface change: caption 'Neighbors see your posts at this spot, the same spot every time, about 0.3 mi from your address'. Derive proposal: the one-liner is about the area neighbors see, 'never your house'.
  - fix: Replace the picker line with: "Neighbors see this post at one spot about 0.3 mi from your address, never the address itself." Keep the link text "See the spot" (or "See what neighbors see") and use it in frame 6 and on Notes.
- [major] (ux) The most prominent control has no defined destination. The prompt never says what 'location settings in the Nearby composer's picker' means: a new post draft, a sheet, or a setting. No artboard shows it, so the handoff out of the screen is undefined.
  - evidence: CONTENT: "the link \"Manage where your posts appear\", which opens the location settings in the Nearby composer's picker." FIRST FIVE SECONDS: "The most prominent control is \"Manage where your posts appear\"." Derive proposal: "a link into the feed location control (PostLocationPicker.tsx)".
  - fix: Specify the destination: "Opens the Nearby composer as a sheet (iOS medium detent, Android bottom sheet, web 390 bottom sheet, web 1440 centred panel) with the location picker already open and an empty draft. Close discards nothing and returns here." Add this state to frame 6 or a new frame. Add a Notes open question: "Is there a standing location setting, or only a per-post picker? If only per-post, rename the link 'Choose where your next post appears'."
- [minor] (ux) Frame 13 contradicts the fixture delta and the house fixtures. Maya's saved PLACE B is scoped to the selector frame only, and Maya owns HOME A, so 'a home she left' has no referent.
  - evidence: CONTENT: "For the selector frame only, Maya has also saved PLACE B"; ARTBOARD 13: "Maya from a link to a home she left; stale-link line; Birchfield Ct selected."
  - fix: Change frame 13 to: "Jordan opens an old link to a home id that isn't on his account; stale-link line 'That home isn't on your account anymore. Showing Birchfield Ct.'; Birchfield Ct selected, no chevron." Alternatively, extend the delta to 'frames 4 and 13' and name the unknown home.
- [minor] (ux) An entry point declared in another surface is missing. The Mail-snaps privacy surface says people reach it from this mirror's 'What we store' row, but this prompt has no such row and doesn't mention it.
  - evidence: FINAL-screen-inventory f10-mail-snap-privacy Entry: "Privacy mirror 'What we store'"; prompts-v1/f10-mail-snap-privacy.md: "from the privacy mirror's \"What we store\" row."
  - fix: Add to Notes: "Phase 2 (F10): a 'What we store' row linking to Mail snaps privacy will join this screen; not drawn now." Or draw it as a gated row in one frame.
- [minor] (ux) The offline state doesn't say whether the map's pan and zoom buttons work, or which 'three controls' are meant.
  - evidence: EDGE CASES: "Offline: the cached pin stays, and all three controls are disabled with their reasons."
  - fix: Write: "Offline: 'Change', 'Edit profile' and 'Manage where your posts appear' are disabled, each with 'Changing this needs a connection.' Zoom and pan stay enabled within cached tiles, and past the cache the vector fallback is drawn."
- [minor] (ux) The post count sits inside a place-scoped row even though the count may be per account. The prompt raises this only as an open question, so the frames may show the same 7 for every place in the selector.
  - evidence: Notes: "Is the post count per place or per account?"; CONTENT: "Your public posts: 7" under a place selector.
  - fix: Until this is decided, label the count "Your public posts (all places): 7" and note it on Notes. Change the label if the count turns out to be per place.

# Last review findings for f8-native-share-compare (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) On Android, the existing 'Share this address' link sits inside the sticky wall, under 'Continue'. The prompt tells the designer to 'turn the existing single link into the pair' 16dp below the aha card, but it also says 'Sticky wall, unchanged' and never says to take the link out of the wall. A designer following both lines will keep a share action in the wall and add a second 'Share this address' under the card. That breaks the TextActionRow rule 'No actions inside the sticky WallBar' and leaves two share controls on one screen.
  - evidence: PlaceLaunchScreen.kt:463-484 is the wall Column, which holds 'Keep this address handy…', PrimaryButton "Continue", the 24-hour caption, and then `preview.place?.let { place -> ShareAddressLink(place) }` (line 483). component-contract.md TextActionRow doNot: 'No actions inside the sticky WallBar.' Prompt: 'Sticky wall, unchanged: "Keep this address handy." and "Continue".' and 'On Android, turn the existing single "Share this address" link into the pair.' ATTACH (2) asks for 'the existing "Share this address" link' at the aha-card scroll position, but that link is in the wall.
  - fix: Add to TYPE and CONTENT: 'On Android the existing "Share this address" link sits in the sticky wall under "Continue". Move it out of the wall and into the pair, 16dp below the aha card. Once it is gone, the wall keeps only its headline line, "Continue" and the 24-hour caption.' Change ATTACH (2) to 'Android address preview with the sticky wall visible, showing the existing "Share this address" link under "Continue"'. In frame 8 add 'link removed from the wall'. Add the move to Notes.
- [minor] (substance) In the link-error state, the InlineErrorRow 'sits in the caption's slot', so the privacy and expiry caption disappears exactly when the person is about to tap Retry and send. The prompt's own rule is that the caption states, before the tap, that the card is anonymous and who can see it until when. Research also puts consent before the send.
  - evidence: Prompt EDGE CASES: 'Link failed: the InlineErrorRow sits in the caption's slot under the pair'. DONE WHEN: 'The caption states, before the tap, that the card is anonymous and who can see it until when.' research-findings.md: 'Show consent before anything goes out in the user's name'.
  - fix: Place the InlineErrorRow between the pair and the caption, where 'Link copied' goes, and keep both caption sentences visible in frame 4.
- [minor] (substance) The rate-limited mint is left out with no copy and no direction. The flows spec gives this failure 'distinct copy with a time estimate', and the design doc rate-limits compare-card the same way as /place.
  - evidence: flows-spec.md flow-05 failure branches: 'Rate limited. The sheet shows distinct copy with a time estimate.' Design doc F8: 'Rate limit as `/place`.' Prompt Notes: 'a rate-limited link attempt (needs its own copy; not drawn)'.
  - fix: In Notes, say that the rate-limited state uses the InlineErrorRow with its own copy and a time estimate (for example 'Too many links just now. Try again in a few minutes.'), marked as invented and not drawn.
- [minor] (substance) The minting spinner placement differs from the Foundations board. Foundations says the spinner replaces the start of the label; the prompt says it appears at the start of the label.
  - evidence: prompts-v3/00c-08.md S2: 'a 12pt spinner (dark: D6) replaces the start of the label, which reads "Making a link…"'. Prompt: 'a 12pt spinner appears at the start of the label and the label reads "Making a link…"'.
  - fix: Write 'the 12pt spinner replaces the start of the label, as on the Foundations board (S2)'.
- [minor] (substance) The wall strings are quoted in shortened form. On both platforms the real wall line is longer, and the wall also carries a 24-hour caption. The screenshot is called exact, so this is cosmetic, but a designer could redraw the wall with only the shortened words.
  - evidence: PlacePreviewBody.swift wall and PlaceLaunchScreen.kt:471-481: 'Keep this address handy. Choose whether to save it privately after sign-in.' and 'Your preview stays on this device for up to 24 hours while you sign in.' Prompt: 'Sticky wall, unchanged: "Keep this address handy." and "Continue".'
  - fix: Quote the full wall line and the 24-hour caption, or write 'the sticky wall exactly as in the screenshot (headline line, "Continue", 24-hour caption)'.
- [major] (ux) On Android, the existing "Share this address" link sits inside the sticky wall, not near the aha card. The prompt says the wall stays "unchanged" and asks Claude Design to "turn the existing single link into the pair" 16dp below the card, but never says the link has to be moved out of the wall. ATTACH (2) also asks for a scroll position that shows the aha card and the link together, which is not possible because the link is pinned in the wall. Claude Design will either leave a share control in the wall, against the contract's WallBar rule, or draw it twice.
  - evidence: PlaceLaunchScreen.kt:463-484: the bottom wall Column holds the wall text, PrimaryButton "Continue", the hold line, then `preview.place?.let { place -> ShareAddressLink(place) }`. The prompt says: "Sticky wall, unchanged: \"Keep this address handy.\" and \"Continue\"" and "(2) Android address preview, same scroll, showing the existing \"Share this address\" link." component-contract.md TextActionRow doNot: "No actions inside the sticky WallBar."
  - fix: In TYPE, replace the Android sentence with: "On Android, 'Share this address' currently sits in the sticky wall under 'Continue'. Remove it from the wall and redraw it as the first button of the pair, 16dp below the aha card. The wall keeps only its sentence, 'Continue' and the hold line." Change ATTACH (2) to: "Android address preview showing the sticky wall with its current 'Share this address' link, plus a second screenshot scrolled to the aha card." In CONTENT, replace "Sticky wall, unchanged" with "Sticky wall: as in the screenshot (iOS unchanged; Android minus the share link)". Add a line to INSTEAD OF: "Instead of leaving the Android share link in the wall, move it into the pair under the aha card — because the wall holds only the one primary."
- [major] (ux) The caption says "never your address" and sits directly under a button labelled "Share this address", which does send the address. A first-time reader could take the caption to cover both buttons and believe sharing the address hides it. That is a clarity and privacy-honesty failure right where the person decides to send.
  - evidence: The prompt says: "New row: \"Share this address\" · \"Compare with a friend\"" and, under it, "The card shows your city and your readings — never your address, and no name." It also says "Share this address\" opens the system share sheet with the address link the Android app already sends" (the URL is /start?address=<full address>).
  - fix: Tie the caption to Compare. Change sentence 1 to "The compare card shows your city and your readings — never your address, and no name." and log it on Notes as a surface delta to the Foundations caption, next to sentence 2. Also change the Share hint to "Opens the share sheet. The link includes this address." Add to DONE WHEN: "A reader can tell that 'Share this address' sends the address and 'Compare with a friend' does not."
- [minor] (ux) The prompt shortens the wall copy, which does not match the exact screenshot. The real wall has a longer sentence and a hold line.
  - evidence: PlacePreviewBody.swift wall: "Keep this address handy. Choose whether to save it privately after sign-in." plus "Your preview stays on this device for up to 24 hours while you sign in." The prompt says: "Sticky wall, unchanged: \"Keep this address handy.\" and \"Continue\"."
  - fix: Write: "Sticky wall: keep its sentence, 'Continue' and its hold line exactly as in the screenshot."
- [minor] (ux) The layout of the status slot is undefined. The prompt never says where the offline reason caption and "Link copied" go relative to the two-sentence caption. The Foundations board puts "Link copied" between the buttons and the caption.
  - evidence: The prompt says: "Copying shows \"Link copied\" under the pair" and "Compare disabled with its reason". 00c-08 S3: "the caption \"Link copied\" (role=status) sits between the buttons and the ② caption".
  - fix: Add to LAYOUT: "One status line sits between the pair and the caption. It holds either the offline reason 'You're offline. Compare needs a connection.', 'Link copied', or the InlineErrorRow. The two-sentence caption always stays below it." Then fix EDGE CASES, which says the error takes "the caption's slot", to match.
- [minor] (ux) The minting treatment differs from the Foundations board in two ways and neither is logged. The spinner is added rather than replacing the start of the label, and it only appears after 1s. Separately, on Android 13+ the system already confirms a copy to the clipboard, so the app's inline "Link copied" duplicates it.
  - evidence: 00c-08 S2: "a 12pt spinner ... replaces the start of the label, which reads 'Making a link…'". The prompt says: "If it takes 1s or longer, a 12pt spinner appears at the start of the label" and "Copying shows \"Link copied\" under the pair" (Android frame 12).
  - fix: Write "the spinner replaces the start of the label". Record the 1s delay on Notes as a surface delta (P11: no indicator under 1s). In frame 12, annotate: "Android 13+ also shows the system clipboard confirmation; keep the inline line for older versions and for screen readers". Alternatively, draw the system confirmation and drop the inline line on 13+.
- [minor] (ux) The dark twin chosen for a state is a system share sheet, which Pantopus does not design. The app-drawn states therefore get no dark check. Frames 6 and 7 also skip the "system UI" frame treatment and do not say what image the address header shows.
  - evidence: "18. f8-native-share-compare · android · 18-sharesheet · dark — dark twin of frame 10." Frames 3 and 10 say "labelled 'system UI'", but frames 6 and 7 do not. 00c-08 PLATFORMS: system UI is "a surface.sunken frame with a 1px text.secondary keyline, labelled 'system UI'".
  - fix: Change frame 18 to "f8-native-share-compare · android · 18-offline · dark — dark twin of frame 13". In frames 6 and 7, add "drawn as the labelled 'system UI' frame; header image = the existing address OG card".

# Last review findings for 00c-02 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The placement of 'Add' on missing rows is ambiguous. ⑤ calls the text action a trailing element, but ⑥ lists label, then ask sentence, then 'Add', which reads as stacked. AX5 says 'Add' drops below the value, which implies it trails by default.
  - evidence: ⑤: "Trailing element: either a chevron … or a text action … e.g. 'Add'." ⑥: "Missing form: the label, then an ask sentence in body text.primary, then the text action 'Add'." AX5: "The mark and the chevron, or 'Add', drop below the value."
  - fix: Change ⑥ to "Missing form: label above the ask sentence, with 'Add' as the trailing element (⑤), vertically centred".
- [minor] The V8 'Your reports' rows name no place, although the variant list says 'Each cell belongs to the place named'. V8c mentions City of Camas (Place B), but crop 3 puts V8a on Home A. The NOTES section also has no Assumptions list, which R14 requires.
  - evidence: V8 has no place tag. V8c: "City of Camas confirmed Thursday". Crop 3: "f6-home-basics-rows · web-1440, Home A: … V7 and V8a". R14: "Invented strings · Fixture deltas · Contract conflicts · Assumptions". The FactRow NOTES have only the first three.
  - fix: Tag V8a and V8b as Home A and V8c as Place B, or change V8c to "Waste Connections confirmed Tuesday" so all three are Home A. Add an Assumptions list, for example "V3 and V8 rows are not targets" and the V8 place choice.
- [minor] Some visible strings are neither fixture strings nor listed as invented: the labels 'Flood zone', 'Pickup day', 'Dates' and 'Pickup frequency', and the action 'Open Settings'. R14 and DONE WHEN require every invented string to be on Notes.
  - evidence: V1a label "Flood zone"; V1b label "Pickup day"; V2a label "Dates"; V2b "Pickup frequency"; V6 action "Open Settings". The NOTES Invented strings list does not include them.
  - fix: Add them to the Invented strings list.
- [minor] The loading skeleton geometry is incomplete. It gives bar widths and the total height but no bar heights, gap or vertical padding, and ① gives only horizontal padding.
  - evidence: S2: "a WarmingSkeleton exactly 56pt high, with a 144pt-wide label bar above a 253pt-wide value bar". ①: "padding 16pt horizontal".
  - fix: Specify it, for example: "8pt vertical padding; label bar 12pt high, 4pt gap, value bar 16pt high, radius sm, vertically centred". Also add "12pt vertical padding" to ① to match DateRow.
- [minor] Focus is shown only for a separate-button row. No cell shows the whole-row focus ring on a chevron row, although ⑧ makes chevron rows row targets.
  - evidence: GRID CELLS: "SF: V2a, with the focus ring on 'Add' only". ⑧: "On rows with a chevron, the whole row is the target."
  - fix: Add V1a in SF with the R8 ring around the whole row (26 cells), and update DONE WHEN.
- [minor] The contract's decline string is 'Skip', but the drawn value is 'Skipped'. The prompt lists 'Skipped' as invented but does not record it as a divergence from the contract's 'Just me' / 'Skip' pair.
  - evidence: Contract variant: "declined ('Just me' / 'Skip' with Undo, re-openable)". V5c: value "Skipped".
  - fix: Add a Contract conflicts line: "V5c drawn with the value 'Skipped' (the state after tapping 'Skip') instead of contract 'Skip'".

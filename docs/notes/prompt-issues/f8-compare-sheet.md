# Last review findings for f8-compare-sheet (round 2). The fix after this round was applied but not re-verified.

- [minor] (substance) The preview draws the ScaleStrip with the 05-og-column recipe, but the Foundations board's in-context line assigns f8-compare-sheet the V5 compare variant at 390. The prompt's choice is right, since the preview is the unfurl, but the difference isn't recorded.
  - evidence: 00b 08-in-context: "Also used in: f8-compare-sheet → V5 compare at 390 (phone sheet width)". Prompt: "Left column: ScaleStrip in its Foundations 05-og-column recipe."
  - fix: Add to Notes: 'Uses 05-og-column, not V5, because the preview is the unfurled image; Foundations 00b's in-context line should be corrected.'
- [minor] (substance) The fixture headline and the worst-case tax headline both come from seeded rows, which the design doc marks unverified until checked. Under the prompt's own headline rule they would be left off the token. The prompt never says these rows are assumed verified.
  - evidence: Design doc: "Seed state-scoped AddressCalendarRule rows of kind election_deadline ... confidence 'unverified' until each is checked"; tax_appeal rows "confidence 'unverified' until checked". Prompt: "if the headline comes from a seeded row that is still unverified, it is left off the token."
  - fix: Add to Notes: 'Assumption: the WA voter-deadline row and the Clark County tax row are verified; otherwise the description starts at "Readings:" (draw that in the headline-dropped state).'
- [minor] (substance) The worst-case headline puts the 'moved from' wording into the headline itself, but the house-style fixture allows that wording only in the detail.
  - evidence: house-style: "Surfaces that show it say 'due Mon 2 Nov' and may add 'moved from Sat 31 Oct' in the detail." Prompt: "Property tax 2nd half is due Mon 2 Nov in Clark County. It moved from Sat 31 Oct."
  - fix: Change the worst-case headline to something like 'Property tax 2nd half is due Mon 2 Nov in Clark County (Clark County Treasurer).' and fill the 90 characters another way, or list the deviation on Notes.
- [minor] (substance) The design doc names the funnel event and field exactly, but the handoff note paraphrases them.
  - evidence: Design doc: "Fires t0_share_clicked with meta.method: 'compare'." Prompt Notes: "fires the share-click funnel event with method \"compare\"".
  - fix: Write: "fires t0_share_clicked with meta.method 'compare'".
- [minor] (substance) The rate-limit copy blames 'links', but the limit is the per-IP budget shared with address lookups (60/min), so a person can hit it without making many links.
  - evidence: Design doc: "Rate limit as /place"; derive-proposals: "rate limited (shares the 60/min per-IP budget with /api/public/place)". Prompt: "Lots of links from this network in the last minute."
  - fix: Use neutral wording, for example "Lots of requests from this network in the last minute. Try again in a minute.", and list it on Notes.
- [minor] (substance) The hollow street-match word 'Not confirmed' is set at 28px inside the image, below the recipe's 40px minimum for value text. It is also a shortened form of the glossary term. Neither is listed as a deviation.
  - evidence: 00b 05-og-column: "Value and header text never drops below 40px"; glossary: "On record, not confirmed — hollow mark." Prompt: "the words \"Not confirmed\" at 28px beside a shortened track."
  - fix: List on Notes, next to the 28px sources-line deviation, both the 28px size and the shortened form of 'On record, not confirmed'.
- [minor] (substance) The copied state is specified only for desktop. The mobile URL row's 'Copy link' text button has no confirmation state.
  - evidence: Prompt: "On mobile, the row also has a 'Copy link' text button." Only desktop gets "'Link copied' for 18 seconds" plus the URL-row tick.
  - fix: Add: on mobile, tapping the row's Copy link shows the same tick and 'Copied' on the URL row, plus the polite status 'Link copied'.
- [major] (ux) The card preview in the sheet has the same overflow problem, and the prompt then claims the worst-case name fits. Image rows use the full approved strings at the 05-og-column 40px size, and the sheet prompt doesn't even repeat the 640px widening rule. The 24-character sender line "Marie-Marguerite-Solange · Camas, WA" is about 740px at 40px, over the recipe's 640px maximum. The EDGE CASES line asserts that it fits.
  - evidence: Prompt CONTENT 3: "Flood "Zone X — minimal flood hazard" ... Radon "Zone 1 — highest potential (county)""; EDGE CASES: "The longest name fits the image's sender line." prompts-v2/00b.md: "Header: "Dana · Camas, WA" at 40px" and "the text column may widen to at most 640px".
  - fix: Use the same share-card short forms as the f8-scale-strips fix ('Flood zone X · minimal hazard' · 'Wildfire · Moderate · 3 of 5' · 'Air Mon 19 Oct · AQI 42 · Good' · 'Radon zone 1 · highest (county)') and the same 720px column limit. Replace the EDGE CASES line with: "A sender line longer than the column (for example 'Marie-Marguerite-Solange · Camas, WA') wraps to two 40px lines. The rows move down, and the sources line stays inside 630px, because the name field is capped at 24 characters. Show that height budget in the gutter of frame 13."
- [major] (ux) The worst-case frame breaks the rule that unverified looks unverified, in two ways. The hollow street-matched flood row prints 'Not confirmed', which is not the glossary term. The image's single sources line still opens with '● Official', so on a standalone surface the card calls every layer official while one row is hollow. The companion prompt f8-scale-strips uses the canonical words for the same row.
  - evidence: Prompt LAYOUT: "shows the hollow ProvenanceMark in its image row, with the words "Not confirmed" at 28px"; CONTENT: ""● Official · FEMA Sep 2021 · USFS 2023 · AirNow Mon 19 Oct · EPA county-wide"". component-contract.md glossary: "On record, not confirmed — hollow mark"; ProvenanceMark copyRules: "on standalone surfaces (widget, OG card, push landing) print it next to every glyph".
  - fix: Change to: "the hollow ProvenanceMark with the words 'On record, not confirmed' at 28px beside a shortened track". Add: "When any row is hollow, the sources line reads '● Official: USFS 2023 · AirNow Mon 19 Oct · EPA county-wide · ○ On record, not confirmed: FEMA flood, street match'. Wrap it to two 28px lines if needed, within the height budget." Update the Notes invented-strings list to match.
- [minor] (ux) 'Not now' is a second, equal-weight close control beside the visible Close. The glossary defines 'Not now' as a deferral that is always paired with a permanent decline, and this sheet is opened by the user, so it isn't an ask.
  - evidence: Prompt CONTENT 7: ""Not now" as a bordered button in text.primary, which closes the sheet." component-contract.md glossary: "Not now — a deferral that never re-asks on its own. Always paired with a permanent decline (Just me / Skip / No thanks)."
  - fix: Rename the secondary button to "Cancel" (neutral, equal weight, closes the sheet), or remove it and keep the visible Close. Update every mention of "Not now" in CONTENT, INTERACTION, ACCESSIBILITY and ARTBOARDS 1 and 6 to match, and list the choice on Notes.
- [minor] (ux) The name field reads as pre-filled. Signed-out senders have no stored name, and the flow has the person type it. The state with the toggle on and the field still empty is not defined.
  - evidence: Prompt CONTENT 5: "When it is on, a "First name" field appears holding "Jordan"." flows-spec.md flow-05 step 3: "She turns on 'Show my first name', types Maya, and the preview updates".
  - fix: Change to: "a 'First name' field appears, empty and focused (24 characters at most). Frame 2 shows 'Jordan' typed. While the field is empty, the card stays anonymous ('A place in Camas, WA') and no re-mint runs."
- [minor] (ux) The expiry appears twice (in the privacy line and in the URL caption), and the caption breaks the house date pattern with a colon. That is extra text in a sheet that is already dense.
  - evidence: Prompt CONTENT 4: "Anyone with the link can see this card until Sat 19 Dec." CONTENT 6: ""Link works until: in 61 days · Sat 19 Dec"". house-style-v2.txt: "beyond tomorrow, add the relative count: "in 7 days · Mon 26 Oct"".
  - fix: Change the URL caption to "Works for 61 more days", or drop it and change the privacy line to "... until Sat 19 Dec (in 61 days)."
- [minor] (ux) The rate-limited state does not say what Share and Copy link do, or when they come back.
  - evidence: Prompt EDGE CASES: "Rate limited: a neutral clock glyph, with no error styling." COPY: "Lots of links from this network in the last minute. Try again in a minute."
  - fix: Add: "Rate limited: Share and Copy link stay disabled, and the caption under Share repeats 'Try again in a minute.' After 60 seconds a 'Try again' text button (44px) appears. There is no countdown."
- [minor] (ux) The prompt tells the model to copy the attached share-card frame 'exactly' and then gives different strings (anonymous sender, Mon 19 Oct). An attachment drawn for Dana on Sat 12 Sep will conflict.
  - evidence: Prompt CONTENT 3: "Image: the compare share card exactly as it appears in the attached light frame" followed by "The sender line reads "A place in Camas, WA"" and ""Air on Mon 19 Oct · AQI 42 · Good"".
  - fix: Reword to: "Image: keep the layout, type and 'Yours?' column of the attached light frame exactly, and replace its strings with the ones below."
- [minor] (ux) The sender in frames 1–4 differs from flow-05. In the flow, the first sender (Maya) uses 'Compare with a friend' and Jordan uses only the reciprocal path with the name toggle off. WHO AND WHEN gives Jordan's reciprocal motive but draws the non-reciprocal title.
  - evidence: flows-spec.md flow-05 steps 2–4 (Maya, types Maya) and step 11 (Jordan, 'Send yours back', toggle off). Prompt WHO AND WHEN: "Now he wants to send his own card to Dana" while frames 1–4 are titled "Compare with a friend".
  - fix: Add one sentence to WHO AND WHEN: "Frames 1–4 and 6–15 show Jordan using 'Compare with a friend' under his own aha card, sending to a different friend. Frame 5 is the flow's reciprocal step, reached from 'Send yours back', with the toggle off." Add this to the Notes assumptions.

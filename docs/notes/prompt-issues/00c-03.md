# Last review findings for 00c-03 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The x-place-file crop draws Place B's pickup day as a missing FactRow. Two other drawings on the same board show that day as known. The crop's ask string also does not exist in FactRow's missing variants. Later screen prompts use this board as their only reference, so the fixture state for Place B's pickup contradicts itself.
  - evidence: FirstWeekRow 04 crop 2: "the same step drawn as the FactRow missing form (label "Pickup day", ask "Set your pickup day", action "Add") ... No mark". FirstWeekRow 04 crop 1 (same Place B, same component): "'Pickup day' with a hollow mark". FactRow (00c-02) V1c: "known-pickup-saved-place (Place B): label "Pickup day" · value "Thursday · City of Camas" · hollow mark". FactRow's Place B missing row is V2b: "Pickup frequency · Not set · Add". The only FactRow V2 ask is V2a "Add one date that matters". House fixture: Place B "Pickup weekday is Thursday per the City of Camas; frequency is Not set."
  - fix: Redraw crop 2 with the published FactRow variants for Place B: V1c ("Pickup day" · "Thursday · City of Camas" · hollow mark · chevron) followed by V2b ("Pickup frequency" · "Not set" · "Add"). Keep the annotation "same set, same storage". Because a mark is now shown, add the legend line per R13. Delete "No mark". In Notes, record that the pickup step covers both confirming the day and setting the frequency.
- [minor] The AX5 rule keeps the dismiss button top-trailing. Board rule R9 says trailing elements move below the text block. The component message wins under the setup rules, but the prompt does not say it is overriding R9, and Notes does not record the override.
  - evidence: AX5: "The dismiss button stays top-aligned at the trailing edge and keeps its target." R9: "Trailing elements (amount, mark, chevron, text action) move below the text block".
  - fix: Add "(overrides R9: a dismiss control stays top-trailing)" to the AX5 line, and add the override to the Assumptions list in the FirstWeekRow Notes section.
- [minor] The contract lists f4-briefing-optin-card in usedIn, but no crop shows it and the prompt does not say why it is left out.
  - evidence: USED IN: "f1-today-tab · x-place-file · f4-briefing-optin-card". IN CONTEXT names only f1-today-tab and x-place-file.
  - fix: Add one line saying f4-briefing-optin-card is not cropped because it only hosts the row. Alternatively, add a third outline crop.

# Last review findings for 00c-08 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The Place B in-context crop draws a SourceCaption with no as-of. That breaks house invariant 5, and later f6 screens will copy this crop.
  - evidence: 04 crop 2: "a radon row ('Radon zone' · 'Zone 1 — highest potential' · filled mark · SourceCaption 'EPA · county-wide')". House INVARIANTS 5: "Sources are shown as a caption: authority · scope · as-of."
  - fix: Give the caption all three parts, for example 'EPA · county-wide · as of 1993 map', and list the as-of string on Notes as invented. Or, if the as-of is unknown, add a Notes contract/fixture gap and say how the caption shows it.
- [minor] The prompt points to a 'setup manifest' of keys, but the 00c setup message (R1–R14) has no manifest.
  - evidence: VARIANTS: "keys match the setup manifest". The setup only says keys come "from the component message" (R7).
  - fix: Delete "keys match the setup manifest". The keys are already given in this message.
- [minor] The system-UI frames are described with a raw colour word instead of a token, which breaks the house token rule.
  - evidence: PLATFORMS and 04: "plain grey frames labelled 'system UI'". House: "every colour is a semantic token".
  - fix: Write: 'a surface.sunken frame with a 1px text.secondary keyline, labelled "system UI" in caption text.secondary'.
- [minor] The V2a contract string does not start with a verb, which conflicts with the prompt's own copy rule and greyscale caption. Also, '↗' appears both inside the variant strings and as the trailing glyph, so it could be drawn twice.
  - evidence: V2a: "Free test kits · Washington Dept of Health ↗". COPY RULES: "Buttons start with a verb." GREYSCALE: "outbound = … destination name + verb + ↗ glyph." ③: "a trailing 16pt external-link glyph '↗'".
  - fix: Say the ↗ in the variant strings is the ③ glyph, drawn once as a vector. Limit the verb rule to the V1 buttons, or record V2a on Notes as 'drawn as the contract gives it, without a leading verb'.
- [minor] Crop 3 does not say whether the voter deadline line carries a ProvenanceMark. If it does, R13 requires the legend line and a ScopeChip, and the crop names neither.
  - evidence: 04 crop 3: "f8-seasonal-aha · web-390: V2b under the voter deadline line." Setup R13: "Every crop that shows a ProvenanceMark also shows the legend line and the place's ScopeChip."
  - fix: Say 'the voter deadline line as plain text with no mark', or add the official mark with the legend line and the matching ScopeChip.
- [minor] Offline behaviour for outbound rows is invented without being recorded as an assumption.
  - evidence: S5: "Outbound rows are disabled … 'You're offline. You can open this when you're back.'" Notes Assumptions lists only "'Share this address' stays enabled offline."
  - fix: Add 'outbound rows are disabled offline' to Assumptions.

# Last review findings for 00c-09 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The in-context crop 1 contradicts the prompt's own fixture delta. Sam's postcard is in transit (V4), yet crop 1 shows Sam the V1 row with a live 'Verify address' link. Someone whose verification is pending should see the pending form.
  - evidence: V4: "viewer Sam, whose postcard is still in transit". IN CONTEXT 1: "f3-member-home-dashboard · ios, viewer Sam: V1." Notes fixture delta: "V4: Sam's postcard is still pending".
  - fix: Pick one: (a) make crop 1 'viewer Sam, before he requested a postcard' and add that to Fixture deltas, or (b) draw V4's pending form under the disabled button in crop 1.
- [minor] The prompt gives two different places for the offline caption. S3 and DONE WHEN put it under the reason text, full width. The greyscale caption says it sits under the dimmed link. R8 also says the reason caption goes directly under the disabled label, and this deviation from R8 is not listed on Notes.
  - evidence: S3: "the caption ... sits under the reason text, at the leading edge, full width." GREYSCALE: "offline = the reason caption under the dimmed link." R8: "with a reason caption directly under it".
  - fix: Change the greyscale caption to 'offline = the dimmed link + the offline caption under the reason'. Add to Notes Assumptions: 'offline caption placed under the reason, full width, instead of directly under the link (R8)'.
- [minor] The INSTEAD OF line says not to show a saved-place user a 'verification lock', and tells the model to draw V7 instead. But V7 itself draws the lock glyph. The contract's doNot also gives 'Claim this address' without 'first'.
  - evidence: INSTEAD OF: "Instead of showing a saved-place user a verification lock on a home-only feature, draw V7". V7: "the lock glyph, then 'Claim this address first'". Contract doNot: "show 'Claim this address' instead."
  - fix: Reword it to 'Instead of an "Address verification needed…" reason for a saved-place user…'. Add the 'Claim this address' vs 'Claim this address first' difference to Contract conflicts.
- [minor] 'V2 · list-row' is written in the same '<code> · <key>' form as a real row, but it has no cells. It could be drawn as an empty extra row label.
  - evidence: "V2 · list-row: ... drawn as two rows: V2s · list-row-sunken ... V2b · list-row-base". GRID CELLS list only V2s and V2b.
  - fix: Write it as 'V2 list row (group heading only, not a grid row)', the same way DateRow writes 'V1 pickup'.
- [minor] The V1 disabled filled button has no height or radius, so the size of the drawn button is left to guesswork.
  - evidence: V1: "a disabled filled button 'Message your neighbors' (per R8: surface.sunken fill, text.strong label...)" with no geometry.
  - fix: Add '44pt / 48dp / 44px high, radius md, full content width'.
- [minor] At about 8–9k characters, the message is above the research's density threshold. Stitch drops components above 5,000 characters, and accuracy falls as instruction count rises. Some lines only restate the setup rules (R7 caption format, R5 Increase Contrast) and could be cut.
  - evidence: research-findings claude-design-prompting: instruction density and the 5,000-character threshold. DONE WHEN repeats the R7 caption format and the R5 caption requirement.
  - fix: Cut the DONE WHEN lines that only restate R5/R7, and merge GREYSCALE into STATES, to get closer to about 6k.

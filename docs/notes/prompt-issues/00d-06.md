# Last review findings for 00d-06 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The 02 and 04 grids cannot fit on the 1600px artboard that R2 requires without breaking R3's 1x real-width rule.
  - evidence: R2: "1600px wide, as tall as needed". R3: "Specimens are 1x at real width: iOS 393pt". GRID: "rows V1 and V7 × columns S1–S5", plus the settings row specimen "beside the grid". Five columns of at least 361pt (the card row sits in a 361pt card) come to at least 1805px before the row-label column, and the settings specimen adds about 393px more, so the artboard needs roughly 2350px. The model either shrinks the specimens, which breaks R3, or overruns the width, which breaks R2.
  - fix: Add this line to the GRID section: "02 and 04 are the one exception to R2's width: 2400px wide." Or keep 1600px and draw the settings row specimen below the grid instead of beside it, and let the Not applicable cells (V1 S4, V1 S5, V7 S1, V7 S3, V7 S4) share a column width with their live neighbours by splitting the grid into two stacked tables, S1–S3 and S4–S5. Update DONE WHEN to match whichever you choose.
- [minor] The f4-notification-primer crop cannot hold the primer sheet at R17's crop height.
  - evidence: R17: crops are "about 300pt tall". IN CONTEXT: "primer-ios over f4-today-pickup-card". B1 gives the pickup primer as "about 514pt".
  - fix: Write: "this crop is 393 × 600pt so the whole sheet shows (an R17 exception)", or "crop to the sheet's top 300pt: Close row, title and tray".
- [minor] The Android sheet geometry is only partly given: the drag-handle row height, the side padding and the bottom inset are not stated, and 'same order and gaps' carries over the iOS 34pt home-indicator inset.
  - evidence: B8: "a Material 3 bottom sheet, sized to content, with a drag handle row above the Close row and the same order and gaps". B1 ends with "home-indicator inset 34".
  - fix: State these values: "drag handle row 24dp (a 32×4dp handle, centred); 24dp side padding (364dp content); the bottom inset is the 24dp gesture-navigation inset in place of 34."
- [minor] The household primer body is described as 4 lines, but at 16pt in 345pt it may wrap to 3, so the stated detent height of about 538pt may not match what gets drawn.
  - evidence: The household body is 129 characters, and roughly 44 characters fit per line at 345pt. B1 says "household 4 lines = 96".
  - fix: Say "allow 4 lines (96pt) even if the text wraps to 3; keep the sheet at 538pt", or drop the fixed line count and derive the height from the text.
- [minor] Notes 6.1 points ahead to PushCopy's note 7.4, which has not been sent when this component is drawn.
  - evidence: "This board uses 'Open Settings' everywhere, which breaks house-style sentence case (see 7.4)."
  - fix: Remove "(see 7.4)" and let 7.4 point back to 6.1, which it already does.
- [minor] Notes F says 'the 06 frames' without excluding the greyscale copies, which PUBLISH treats as review aids that are never published.
  - evidence: F: "...the seven sheets, and the 06 frames." PUBLISH: "Review aids, never published: greyscale copies".
  - fix: Change it to "the 06 ios-ax5 and android-200 frames (not the greyscale copies)".
- [minor] The prompt is long for one send, at roughly 16k characters, heavier than the accepted siblings (10.6k and 11.5k). Much of it repeats: R10 figures appear in both GRID and DARK, and Notes D restates copy.
  - evidence: The research says accuracy falls as instruction density rises, with a bias toward early instructions, and quotes Figma: "More context isn't always better."
  - fix: Trim the repeated R10 figures to one mention each, cut the INSTEAD OF rationales to one clause each, and in Notes D replace the full list of invented strings with "every string marked invented above" plus the chrome items.

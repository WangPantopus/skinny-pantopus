# Last review findings for 00b-04 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The desktop collision rule measures only centre-to-centre distance and ignores the count numeral, so the specified 03 layout overlaps. The 23 Oct cluster's '2' runs into the next cluster's mark at 1 Nov, which breaks the DONE WHEN rule that no marks overlap.
  - evidence: The Oct column spans x 128–188 (60px). The 23 Oct centre is ≈171.6 and the 1 Nov centre ≈189.0, 17.4px apart, so they form separate clusters under the "less than 14px" rule. The first mark ends at 177.6; its numeral starts at 178.6 and is ~7px wide (captionMedium), ending at ≈185.6. The 1 Nov mark starts at 183.0, an overlap of ≈2.6px. DONE WHEN: "no marks overlap".
  - fix: Base the rule on drawn extents: an item joins the current cluster if its left edge is less than 2px from the right edge of the cluster's mark plus numeral. With that rule, 23 Oct, 28 Oct, 1 Nov and 2 Nov become one mark with '4'. Update the 03 pair callouts and Notes B to match. Alternatively, keep 14px and require a centre gap of at least 22px whenever the earlier cluster has a numeral.
- [minor] On phones and on the widget, the today rule passes through the October 'You added' mark, and the paint order puts the rule on top, so it cuts across the knocked-out tick.
  - evidence: Phone: today rule at 19/31 × 27.2 ≈ 16.7pt; the Oct You mark is centred at 13.6pt and spans 7.6–19.6pt. Paint order: "... marks and bars · today rule".
  - fix: Paint the today rule under the marks, or give marks a 1pt lane-fill halo where the rule crosses them. State this in the anatomy and the 03 FourteenDayStrip crop check.
- [minor] Batch 3 asks for FourteenDayStrip 07 crop (c), a web-1440 context, to match 'this anatomy' (the phone anatomy on 01). This conflicts with the PLATFORMS rule that 768px and wider uses exact-day placement.
  - evidence: "compare ... crop (c) on 00b · FourteenDayStrip · 07-in-context with this anatomy"; PLATFORMS: "768px and wider place items on exact days."
  - fix: Say that crop (c) must match the 03-desktop-exact-day form, and that the 03 FourteenDayStrip crops match the phone form.
- [minor] PLACE B widget lane names are defined, but no PLACE B widget specimen is drawn. The widget legend rule ('keys only the shapes it draws') is also not applied per specimen, unlike in FourteenDayStrip.
  - evidence: V2: "(PLACE B: 'You · Camas · Clark County · Washington')"; the 02 widget-large row and 04 draw V2 without naming a place, and there is one fixed legend "○ On record · ✓ You added · ▮ Rule".
  - fix: State that every V2 specimen is HOME A and drop the PLACE B label, or add a PLACE B widget specimen with the legend "○ On record · ▮ Rule".
- [minor] Sizes are missing for two things: the Android widget-picker preview on 04, and the 43pt 'Today' and '2027' top line at AX5.
  - evidence: 04: "inside an Android widget-picker preview" (no size); 05 covers only the month labels, lane labels, captions and summary.
  - fix: Give the Android preview size (for example the 338×354 content scaled into a 4×3-cell picker tile), and add "Today and 2027 wrap onto the line above the month labels" to 05.
- [minor] On phones, the dashed window edge at the end of 1 Nov sits about 1.8pt from the solid Oct/Nov month separator, so the edge is hard to tell apart.
  - evidence: The window ends at 2/30 of a 27.2pt Nov column ≈ 1.8pt past the 1px separator.
  - fix: Draw the window edge 1pt thicker, or state that the wash itself carries the window and the legend "Shaded: next 14 days" names it. Alternatively, cap the dashed edge above the lanes as a tick.
- [minor] The HOME A band has no ScopeChip in its anatomy, although PLACE B does and the house invariant requires scope to be stated.
  - evidence: PLACE B: ScopeChip "Saved place · Only you"; the HOME A anatomy starts at the summary header.
  - fix: Add the 00a · ScopeChip "Your household" above the summary for HOME A, as FourteenDayStrip does.

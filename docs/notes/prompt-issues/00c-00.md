# Last review findings for 00c-00 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] Four token names have no mapping to the house-style list. The house style lists 'Pillars: home #16A34A' and 'Semantic: … info #0284c7/#F0F9FF … error #DC2626/#FEF2F2', but it never uses the names identity.home, infoBg or errorBg. Claude Design may fall back to 'nearest token' and pick a different one.
  - evidence: R4: "Light values are in the house style; this board also uses primary.50, primary.700, errorBg, infoBg and identity.home." The house style has no token called identity.home, infoBg or errorBg. The R6 ratios (identity.home on base 3.30, infoBg 4.54, errorBg 4.42) only hold for #16A34A, #F0F9FF and #FEF2F2.
  - fix: Add one line to R4: "identity.home = Pillars home; infoBg = the info tint; errorBg = the error tint; primary.700 = sky 700." Use names only, with no new hex.
- [minor] R1 fixes 01-anatomy at 'one specimen at 2x', but every component message asks for several specimens: DateRow asks for two and FactRow for three. Because the component message wins, every component turn will add a pointless 'conflict' line to Notes.
  - evidence: R1: "01-anatomy · light: one specimen at 2x with numbered callouts". DateRow: "Draw V3a at 2x … Beside it, draw V3c in S3". FactRow: "Draw three specimens at 2x, stacked". Setup: "If a component message and these rules disagree, the component message wins; note it on Notes."
  - fix: Change R1 to "the specimen(s) the component's ANATOMY names, at 2x".
- [minor] R10 tells the model to 'fix the component' when a cell reads only through colour. That clashes with the rule that each component message is the complete specification and wins any disagreement, so the model may change the spec on its own.
  - evidence: R10: "A cell that reads only through colour is a defect: fix the component." Setup: "Each component message is that component's complete specification."
  - fix: Replace it with: "A cell that reads only through colour is a defect: draw it as specified, and list it on Notes under the component's Assumptions."
- [minor] The 05 layout is unclear. R1 asks for the 02 grid 'in the same layout' and also the 1x anatomy specimen 'at top left', which is where the grid's row and column labels start.
  - evidence: R1: "05-grid · dark: the 02 grid in the same layout in dark tokens (R5), plus the 1x anatomy specimen in dark at top left."
  - fix: Say "the 1x dark specimen in a band above the grid; the grid starts below it in the same layout as 02".
- [minor] The setup is meant to hold only board-wide rules, but it contains component-specific examples: DateRow keys, BillRow's named-people sentence, and Resend/Retry targets. These examples prime the model before the component messages arrive.
  - evidence: R7: "e.g. \"V3a · deadline-voter\", \"S2a · highlighted-arrival\"" and "DateRow · deadline-voter · done". PUBLISH: "DateRow variant \"deadline-voter\"". R11: "(\"You and Sam can see these\")". R8: "(link, overflow, Retry, Resend, Add, dismiss)".
  - fix: Use neutral examples such as "V1 · <key>" and "<Component> · <variant key> · <state key>", or keep only one clearly marked example.
- [minor] The message is too dense for one send. It is roughly 2,000 words (about 12–13k characters, estimated), with more than 100 separate rules in R1–R14. The research shows accuracy falls as instruction count rises and favours early instructions, and these rules have to hold for 12 later turns. Some content is only for reference and could go.
  - evidence: research-findings claude-design-prompting: IFScale "68% accuracy at the max density of 500 instructions" with a "bias towards earlier instructions"; Stitch drops components above 5,000 characters. R6 lists ratios that no rule on this board uses (for example warning on sunken 2.89 FAIL and success on border.strong 2.74 FAIL); R5 D2 and D6 cover cases DateRow and FactRow never draw.
  - fix: Cut R6 to the pairs a caption or placement on this board actually uses, and merge the FAIL pairs into one "never place" line. Put a one-line rule index at the top (R1 artboards … R14 notes) so later messages can cite rules by number.

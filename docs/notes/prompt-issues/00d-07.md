# Last review findings for 00d-07 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The dark grid cites a contrast figure that R10 does not contain, although R10 says it holds the board's only figures and forbids computing new ones.
  - evidence: GRIDS: "Inset top-row words are #F1F5F9 (R10: 13.35:1 on dark raised)". R10's dark-on-raised row lists only #94A3B8, #38BDF8, #D97706, #DC2626, #64748B and #1F2937. The figure itself is correct (13.35:1).
  - fix: Add "#F1F5F9 13.35:1" to R10's dark-on-raised row in the setup message, or drop the figure here to match the light rule ("print no contrast figure").
- [minor] It is unclear whether the 'character ruler' is measured in characters or in points, so tick placement is ambiguous in a proportional font.
  - evidence: ANATOMY 3: "Draw a character ruler under it with a tick at 30". S1: "show the remaining room on the ruler".
  - fix: Write: "a ruler marked in characters (one small tick per 5 characters) under the string, with a labelled tick at 30 (40 for the body); the shaded part after the last character is the remaining room."
- [minor] The greyscale column breaks R17(c), which asks for the light default: V1 S2, the anatomy specimen, is not included.
  - evidence: R17: "(c) Greyscale copies of the light default and the most state-heavy light cell". The prompt says "(c) Greyscale of V3 S2 and V4 S2".
  - fix: Use V1 S2 (the default) and V3 S2 (the most state-heavy cell), and keep the caption.
- [minor] On iOS the V8 S1 and V8 S3 specimens are visually identical, so the two columns cannot be told apart.
  - evidence: S1: "V8 S1: four collapsed trays with previews hidden, each showing 'Pantopus' plus its placeholder." S3 on iOS: "'Pantopus' plus the placeholder".
  - fix: Say what differs, or add a board caption under each cell: "Unlocked, previews hidden" (S1) and "Locked, public version" (S3).
- [minor] On Android S3, it is unclear whether 'Pantopus · <placeholder>' replaces the top row's 'Pantopus' or is added below it, which would repeat the name.
  - evidence: S3: "On Android: 'Pantopus · <placeholder>'"; ANATOMY 2: the top row holds "Pantopus".
  - fix: State this: "Android S3: the top row keeps the icon and 'now'; the single text line reads 'Pantopus · <placeholder>'."
- [minor] The in-context list row dates a same-day event with a weekday ('Mon 6:10 PM') although TODAY is Monday, which clashes with brief section 6 ('Tonight, Today and Tomorrow stand alone').
  - evidence: IN CONTEXT: "with 'Mon 6:10 PM' under it". TODAY: Mon 19 Oct 2026, 6:10 PM.
  - fix: Use "Today 6:10 PM" (or "Sun 5:40 PM" for an earlier event), and update Notes B and D.
- [minor] V4 (air below 151) is a variant the contract does not list, and Notes logs only its title (7.5) and the new levels row (7.7), not the added variant.
  - evidence: Contract variants: seven, with one air variant. The prompt has eight rows, including "V4 Air, below 151".
  - fix: Extend 7.5 to say that V4 is an added variant, taken from the contract's "Time Sensitive only at 151 or above" and from brief section 3 (HIGH / DEFAULT), and ask the founder to confirm it.

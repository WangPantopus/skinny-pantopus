# Last review findings for 00b-06 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The prompt never says what the cells-map (V3) shows when offline with no cache. The phrase "The caption stays" only fits V1, because V3 has no caption: it has a summary line and a FreshnessLine instead. A literal reading keeps "14 neighbor posts in your cell in 30 days" and "Updated 12m ago" with no data behind them. That is a stale all-clear inside an offline cell. The cells-map × offline cell is a required 02 grid cell, and the two readings give materially different screens.
  - evidence: OFFLINE, no cache: "The caption stays. All buttons are disabled, including 'List cells'; the centred line is their reason." V3 stack: "map ... · summary line · 4pt · FreshnessLine · 8pt · controls · 8pt · List cells" (no caption).
  - fix: Add this line: "V3 with no cache: the square slot holds the centred offline line; the summary line and FreshnessLine are not drawn (no counts without data); controls and 'List cells' are drawn disabled. V1 with no cache: the caption stays."
- [major] Disabled buttons use text.muted, but the setup's closed dark palette (C6) has no dark text.muted value. On the dark 02 grid, the offline cells therefore have no legal ink for their disabled buttons. If the light #9CA3AF is used, it is nearly identical to the enabled dark edge #94A3B8, so disabled and enabled look the same in dark mode.
  - evidence: Controls: "Disabled buttons: glyph and edge in text.muted, fill unchanged". C6: "Use only these dark values ... Text: primary #E5E7EB · secondary #94A3B8 · strong #F1F5F9" (no muted). The 02 dark grid includes the offline column.
  - fix: State a dark disabled treatment in this prompt (for example: dark disabled glyph and edge #475569 on #1E293B, listed on Notes as an added dark value). Or state that in dark mode a disabled button keeps a #94A3B8 edge with a dashed stroke plus the reason caption, so the difference does not rely on lightness alone. Add the chosen value to the Notes deviations or assumptions.
- [minor] 04 says what happens to V3 at AX5 ("List cells" wraps, cell labels stay at base size), but the artboard only asks for V1 at AX5 and at 200%. It is unclear whether V3 at AX5 should be drawn.
  - evidence: "V1 at iOS AX5 ... On V3, 'List cells' wraps and grows." "V1 at Android 200%, with the same rules." V3 appears only in the desaturated set.
  - fix: Either add "V3 at iOS AX5 (map, cells and labels unchanged; summary, FreshnessLine and 'List cells' wrap)" or remove the V3 AX5 sentences.
- [minor] Some placements are unstated. The FRAMES line calls the controls height the "loading reserve", which reads as the whole skeleton height. V1 loading does not say whether the caption shows. The V1 offline FreshnessLine goes "under" the map, but its order relative to the caption and its spacing are not given. The position of the reason caption "Moving the map needs a connection." is not given.
  - evidence: "The loading reserve is 96pt, or 148pt for V3." "the cached map, with the 00a · FreshnessLine ... under it". "with the reason caption 'Moving the map needs a connection.'"
  - fix: Write "controls reserve 96pt (148pt V3)". Add: "V1 loading shows the caption as text". Add: "V1 offline stack: map · 4pt · FreshnessLine · 8pt · caption · 8pt · controls · 4pt · reason caption".
- [minor] The contrast-table rule mentions "the two outline rows", but the table has four graphic rows: text.secondary on base, text.secondary on sunken, primary.600 on the cell fill, and #38BDF8 on the dark fill. text.secondary on base is also text ("Simplified map"). So the 3:1 and 4.5:1 thresholds are ambiguous.
  - evidence: "Text rows need 4.5:1; the two outline rows need 3:1."
  - fix: Label the threshold on each row, for example "primary.600 on 70% base fill · outline · 3:1".
- [minor] The NOTES instruction says to append to every section, but no C (normalisations) content is given, and DONE WHEN lists only A, B, D, E, F and G. Notes A also leaves out some invented strings: "Updated 12m ago", "You're offline · as of 7:04 AM" and the greyscale caption.
  - evidence: "append a PublicPointMap part to each section of 00b · Notes". The A list does not include the FreshnessLine strings or "Pin by shape and label; ...".
  - fix: Say "C: none for PublicPointMap". Add the three strings to A, or state that they come from 00a.
- [minor] The format of the extra publish names is ambiguous.
  - evidence: "Extras: list-cells (03, light), ax5 (04, light)."
  - fix: Spell them out: "PublicPointMap / list-cells / light" and "PublicPointMap / ax5 / light".
- [minor] The prompt is about 14–15k characters with well over 100 instructions for 6 artboards. The research warns that accuracy falls as instruction density rises, with a bias toward early instructions. Some content is stated twice, in INSTEAD OF and in ANATOMY/COPY.
  - evidence: The research summary quotes "More context isn't always better" and a 500-instruction accuracy of 68%.
  - fix: Remove the repeats between ANATOMY, INSTEAD OF and DONE WHEN (for example, the home-marker and ring rules), and keep one statement of each.

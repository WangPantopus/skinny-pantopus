# Last review findings for 00a-01 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The geometry does not work: the 02 grid has 5 columns of 358-wide rows (390 with card padding) plus row labels, which needs about 2,300 px. The message sets no wider artboard, so B1's 1440 px default applies. A literal model will squeeze, overlap or wrap the grid.
  - evidence: "each cell is a 358-wide row at S 16pt on surface.base", 5 columns; B1: "Every artboard is 1440 px wide ... A component message may set a wider artboard for a grid ... that message records the width on Notes". There is no WIDTH line and no 01.A entry. 5×390 + 240 + 6×32 ≈ 2382.
  - fix: Add "WIDTH: both 02-variants-states artboards are 2400 px wide (an exception to B1; Notes 01.A9)", give the row-label column (240) and gutters (32), and add 01.A9.
- [major] The stale contrast figures point to B7, but the setup's B7 no longer lists any figures. Dark stale has no ratio anywhere, and B7 says to print the given figures and not recompute them.
  - evidence: TOKEN AND CONTRAST TABLE: "stale: text.primary #111827 at 50% (the B7 50% figures) · dark stale: dark text.primary #E5E7EB at 50% (B7)". Setup B7: "Each component message's TOKEN AND CONTRAST TABLE gives the figures ... Print them exactly as given, and do not recompute them." Computed with the B7 method: light 3.39 base / 3.36 raised / 3.34 app / 3.32 sunken; dark 4.45 dark base / 4.10 dark raised / 4.48 dark app.
  - fix: Replace both parentheticals with the numbers: "stale: text.primary at 50%: 3.39 base / 3.36 raised / 3.34 app / 3.32 sunken · dark stale: dark text.primary at 50%: 4.45 dark base / 4.10 dark raised / 4.48 dark app".
- [major] The R5 stale air caption breaks invariant 5 (authority · scope · as-of). It also disagrees with SourceCaption's live and stale caption for the same reading on this board, and later surfaces copy both.
  - evidence: R5: "Observed 7:00 AM · AirNow — 11 hours ago". House style invariant 5: "authority · scope · as-of". 00a-02 R2 stale: "AirNow · nearest monitors · observed 7:00 AM today — 11 hours ago".
  - fix: Use "AirNow · nearest monitors · observed 7:00 AM today — 11 hours ago" in R5 (all columns) and list it in 01.B1.
- [major] The publish index for anatomy sizes is ambiguous. The M legend, L sheet header and OG 28px marks are needed by x-provenance-sheet and f8-og-compare-card, but the message only lists size codes in a parenthetical and never says which anatomy specimens get names, or with which variant, state and theme.
  - evidence: PUBLISH KEYS: "<size|platform> is the size the cell is drawn at (XS, S, M, L, OG)". Setup: "plus one for each anatomy size or platform form the component message lists under PUBLISH KEYS". 01.E: "Every ProvenanceMark name, by the grammar and keys above." The grid draws only S and XS.
  - fix: List them explicitly: "Anatomy names: ProvenanceMark / {official|unconfirmed|you-added} / default / {XS|M|L} / {light|dark} and ... / OG / light (15 names)".
- [major] The message is too long for one send, and it does not use the setup's parts mechanism. It is roughly 15–16k characters (plus the 10.5k house style and 11k setup), and batch 1 packs anatomy, two 30-cell grids, scaling and in-context. The research warns that accuracy falls as instruction count rises, with a bias toward early instructions.
  - evidence: 8 artboards, around 150 discrete instructions, and batch 2 specifications (dark 03/04) sent up front. Research: "accuracy falls as instruction count rises"; Stitch "omit[s] some components" above 5,000 characters. The setup already defines "00a-01a, then 00a-01b".
  - fix: Split into 00a-01a (anatomy L+D, light grid, light in-context: spec for those only) and 00a-01b (dark grid, both 03s, dark in-context, Notes), each carrying only its own batch's spec.
- [minor] No stroke width is given for OG 28px or for the AX5/200% scaled marks (24, 32), so the geometry is incomplete. All four listed values follow stroke = diameter/8.
  - evidence: "Stroke width: XS 12 → 1.5px, S 16 → 2px, M 24 → 3px, L 56 → 7px"; OG 28 and the 32pt AX5 cap are not covered.
  - fix: State "ring and tick stroke = diameter ÷ 8 (OG 28 → 3.5px, 32 → 4px)".
- [minor] 01-anatomy · dark says it has no text blocks, which conflicts with B2's required top block (name, purpose, Used in).
  - evidence: "It has no text blocks." vs B2: "Inside each component artboard, print the component name ... its one-line purpose ... then 'Used in:'".
  - fix: Say "It keeps the B2 top block but omits ACCESSIBILITY, COPY, PLATFORMS and INSTEAD OF."
- [minor] The R4 row rule (a commit arrow plus a copy of the row) applies to every column by default, so tinted-widget, in-target and greyscale cells each get a second row inside a 158×158 or 358-wide cell.
  - evidence: "Rows ... used in every column unless a column rule says otherwise"; R4: "Beside the cell, draw a small arrow to a copy of the row after commit".
  - fix: Add "the commit arrow appears in the default column only".
- [minor] Crop c (the widget) does not say which rows the widget shows, and Notes section B does not list many invented annotation strings, although B16 requires it.
  - evidence: Crop c gives only one example row. Strings missing from 01.B: "system tint stand-in · 5.93:1", "shape carries the meaning", "must be told apart at 12pt", "Increase Contrast ink", "WRONG", "fix: shape, not colour", "10pt → no mark", "never border.* · never text.muted", "at most 3 marks per cell, then +N", "the strip is one element; rows are the targets", "same as stale variant".
  - fix: List the widget rows exactly (for example Recycling and garbage · Tomorrow [unconfirmed]; Clark Public Utilities · in 4 days [you added]) and add the missing strings to 01.B3.
- [minor] Crop b says "Draw every fixture date in the window" but leaves out the Home A invite expiry (priya@example.com, Mon 26 Oct), which is a fixture date in the window.
  - evidence: House-style FIXTURES: "Pending invite: priya@example.com, expires Mon 26 Oct." Crop b lists only pickup, bills, voter and HOA.
  - fix: Say "every fixture date in the window except the invite expiry (not a calendar date)", or add it.

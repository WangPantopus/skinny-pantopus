# Last review findings for 00b-02 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The still-above sentence is Pantopus context, but it is neither placed nor labelled as ours. It is also unclear whether the still-above specimen keeps the PANTOPUS NOTE and 'Crossed 101' parts.
  - evidence: Contract: "Pantopus context as a separate line labelled as ours". Prompt: "Pantopus context, only on the alert specimen ... overline \"PANTOPUS NOTE\"" and still above: "the alert band plus, in bodySmall text.primary, \"Still above your alert level (101) since 4:00 PM. No new alert until ...\"", with no position or label given.
  - fix: State it: "V6 = the V5 detail card, with the still-above sentence as a second line inside the PANTOPUS NOTE block, below 'Your alert level is 101…'. The EPA statement stays above, unchanged." Say that the PANTOPUS NOTE appears on alert and still-above.
- [major] The message is too dense for one send: about 14–15k characters, with batch 1 drawing six dense artboards (two with contrast tables, a 3×6 grid plus three extras, three AX5/200% specimens and three in-context crops). ScaleStrip, which is shorter, was split into two parts.
  - evidence: research-findings: accuracy falls as instruction count rises, with an early-instruction bias, and Stitch drops components above 5,000 characters. Late items such as 05-greyscale, 06-in-context, the ACCESSIBILITY labels and the PUBLISH NAMES are the most likely to be dropped.
  - fix: Split it as ScaleStrip was. Part 1: spec plus 01 and 02 (light). Part 2: 03–06 light, the dark twins and Notes. Or cut repetition: DONE WHEN restates the anatomy, and Notes A restates strings already quoted.
- [minor] The publish names for the 03 blocks and the provider-failed specimen use state keys that are not grid columns. This goes against R9/R10, which give off-grid specimens an extra key.
  - evidence: "AqiBand / widget-micro / ready-tinted", "AqiBand / widget-micro / ready-android", "AqiBand / detail / no-reading-provider-failed". R9: "Specimens outside a grid get the extra key".
  - fix: Use "AqiBand / extra / widget-ready-tinted", "… / extra / widget-ready-android" and "… / extra / no-reading-provider-failed", or have the setup allow sub-state keys.
- [minor] The full-colour widget block on 03 is said to be the same specimen as the 02 widget-micro ready cell, so it is unclear whether it is drawn twice and which copy is published.
  - evidence: 03: "Draw the block three times: iOS full colour [ready]…". PUBLISH: "the full-colour block is the 02 widget-micro ready cell".
  - fix: Say: "On 03, place an instance of the 02 widget-micro ready cell; it gets no separate publish name."
- [minor] The 'category N of 6' wording is not declared as an allowed 'N of M' under G2.
  - evidence: G2: "'N of M' appears only where a component message allows it." AqiBand spoken labels use "category 1 of 6" and "category 3 of 6", but the COPY section does not allow them. Only ScaleStrip's COPY does.
  - fix: Add to COPY: "'category N of 6' is allowed in spoken labels only."
- [minor] The top of the '301+' segment is never defined, so the position formula cannot be used for readings of 301 to 500.
  - evidence: "Position within the segment = (value − segment low) ÷ (segment high − segment low)"; "Values above 500 clamp to the right end of '301+'".
  - fix: Add: "For position, the 301+ segment spans 301–500."
- [minor] At 101 the threshold rule falls exactly on the Moderate/USG gap and keyline, and at 151 on the next boundary. The prompt does not say where the dashed rule sits relative to the 2px gap.
  - evidence: The rule is "drawn at the threshold". 101 is the low end of USG (0% of the segment), and 151 is the low end of Unhealthy.
  - fix: Add: "A threshold equal to a segment's low value centres the rule in the 2px gap before that segment."
- [minor] The instrument-row alert cell grows the track zone to 40pt and adds a threshold rule, but ScaleStrip defines no such state, although V2 'must match the ScaleStrip artboards exactly'.
  - evidence: "the track zone grows from 24pt to 40pt in the alert cell only". ScaleStrip fixes the track zone at 24pt and has no alert state.
  - fix: Record on Notes D/E that the ScaleStrip host may show this alert form (or state that it never does), and qualify the 'match exactly' sentence.
- [minor] Notes A misses some invented strings, and the instrument-row no-reading spoken label ends in 'Official.' even though no reading is shown.
  - evidence: 06(b) header "Where this fact comes from" is not in Notes A. The detail-ready label is excluded as if it came from the contract, but the contract gives only the alert label. The instrument-row rule appends " Official." to "Air quality: No reading for this address right now."
  - fix: Add "Where this fact comes from" and the detail-ready label to Notes A. Define the instrument-row no-reading label without "Official." (or with the source only).

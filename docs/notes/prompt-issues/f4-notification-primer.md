# Last review findings for f4-notification-primer (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The prompt never says what happens after someone allows notifications from the Mail entry. The Household activity group is off by default after a grant, yet the Mail body promises household updates. No artboard shows Mail after a grant, so either the body is untrue or the sheet silently switches a group on.
  - evidence: ux-research-brief.md §3 table: Household activity default after grant is "Off until switched on from the household block or settings". The prompt's Mail body reads "Quiet updates from your household. Pickup the night before...". Its 'What happens next' covers Today only: "After Allow, the person is back on the host. On Today, the briefing card's evening row reads...". inventory-entries/f4-notification-primer.json lists the entry point "Household-activity opt-in".
  - fix: Under 'What happens next', add a rule for the Mail entry: Allow from Mail also turns on Household activity (because the person asked for household updates), and the push-off banner disappears. Add a granted-on-Mail artboard, or a pair on artboard 3, showing the Mail list without the banner. List the decision (Mail's Turn on also enables Household activity) as an assumption on Notes.
- [major] (substance) The granted caption claims to name the next real send, but the evening briefing also carries bills due tomorrow, and the fixtures have a bill due Fri 23 Oct. An evening send on Thu 22 Oct is therefore possible, so "Next reminder in 7 days · Mon 26 Oct" may be false.
  - evidence: Design doc F4: "evening title becomes the signal's headline when it is a pickup or bill". derive-proposals f4-5 caption: "bins out, a bill due tomorrow, an alert". The house-style fixture has "Clark Public Utilities $142.18 due Fri 23 Oct". The prompt's DONE WHEN says "The granted frame names the next real send."
  - fix: Change the caption to "Next pickup reminder in 7 days · Mon 26 Oct, 6:00 PM · garbage only", in CONTENT, COPY, artboard 4 and Notes. Alternatively, state in the delta that Clark PUD is already marked paid, so no evening signal fires before Mon 26 Oct.
- [minor] (substance) The prompt drops the reason this surface exists: the app currently spends the one-time iOS notification request at cold launch, and this sheet replaces that. It says only that the sheet itself never opens at launch.
  - evidence: inventory-entries/f4-notification-primer.json: hostSurface "iOS: replaces the launch-time request in AppDelegate.swift:50-61"; purpose "Stop burning the one-shot iOS notification grant at cold launch". v1 said "It replaces the request iOS currently fires at cold launch." flows-spec flow-14 step 1: "the AppDelegate permission request has been removed."
  - fix: Add one line under WHERE IT LIVES: "The system dialog no longer fires at app launch; this sheet (or the card's Yes) is now the only route to it."
- [minor] (substance) ATTACH asks for exact screenshots of hosts that do not exist yet at this point in the build order. The briefing card is built after the primer, and the Mail household notification list much later.
  - evidence: FINAL-screen-inventory.md BUILD ORDER: "17. f4-notification-primer", "18. f4-briefing-optin-card", "35. f3-household-notifications". The prompt says "Draw these hosts exactly as the screenshots show."
  - fix: Say: draw Today's PickupCard and the NotificationAsk card row, and Mail's NotificationRow push-off banner, from the Foundations board. Use screenshots only where the host already exists (the Today tab frame and the Mail tab frame).
- [minor] (substance) The body copy "Nothing else unless you turn it on" leaves out Account & security, which is on by default, and date and bill reminders the person has already set. That is the vague-description risk the research warns about. The Notes do not flag it.
  - evidence: ux-research-brief.md §3: Account & security default "On"; Dates & bills "On only for rows where the person set a reminder". per-surface-changes: "NN/g: vague or inaccurate descriptions of what will be sent undermine the decision".
  - fix: Add a Notes open question: whether the body should also mention sign-in and invitation notices and reminders the person already set, or whether the canonical sentence is accepted as is.
- [minor] (substance) The offline frame contradicts the save rule. The sheet opens only after the pickup save succeeds, yet artboard 11 shows the sheet offline with a queued write, and the prompt never explains how the save succeeded offline.
  - evidence: The prompt says "the primer presents only after the Date sheet has closed and the save has succeeded" and "A save that failed: the sheet never appears." It also says "Offline: Continue still works, and the preference write is queued."
  - fix: State that the offline frame is the connection dropping after the pickup save succeeded, and that only the reminder-preference write is queued. Otherwise, specify that a queued pickup save does not open the sheet.
- [minor] (substance) Two decline and widget rules are incomplete. The brief offers the widget hint after a confirm as well as after a denial, at most once every 24 hours; the prompt covers the denial case only. The Android sheet also has no visible Close, and its Not now has no lasting-decline partner.
  - evidence: ux-research-brief.md §2.8: "The widget hint is offered once after a confirm or a denial, and at most once every 24 h." house-style: "Every sheet has a visible Close or Done". Glossary: "Not now — ... Always paired with a permanent decline".
  - fix: Add the 24-hour cap and the after-confirm case to the widget row rule, or state why frame 4 omits it. On Android, either add a top-bar Close or note that Not now doubles as the visible close. Add a Notes line on why no lasting decline is paired here.
- [minor] (substance) A flow conflict the critique raised is still unresolved: after the first "That's my day", the briefing card is specified to scroll to and highlight itself, while this prompt opens a sheet.
  - evidence: flows-spec.md line 481: "After the first 'That's my day', f4-notification-primer says the primer sheet is presented, while f4-briefing-optin-card says the first pickup confirmation 'scrolls to and highlights this card'. Two different next steps for one tap."
  - fix: State which happens (for example, the sheet opens and the card is not highlighted), and add the choice to the Notes open questions so the briefing card prompt matches.
- [major] (ux) The hero entry (the primer opening after the first "That's my day") contradicts the PickupCard change and the brief. There, the first "That's my day" expands inline and fires the OS dialog directly. The prompt draws its hero, its granted frame and its dark twin on an entry that should no longer exist, and leaves the conflict as an open question. The primer is meant only for indirect entries.
  - evidence: per-surface-changes/f4-today-pickup-card.md: "The first 'That's my day' expands inline to 'Remind you the night before? 6:00 PM' with 'Turn on reminders' / 'Not now'. 'Turn on reminders' fires the OS dialog directly". ux-research-brief.md §2.8: "It follows the first "That's my day" (inline ...) or Yes on the briefing card, and goes straight to the OS dialog." per-surface-changes/f4-notification-primer.md: "Keep the primer only for indirect entries (household activity from Mail; pickup where not inline)". The prompt's WHO AND WHEN says: "On Today she taps \"That's my day\" for Tuesday". Its Notes say: "Open question: the brief's inline route ... If it is adopted, this sheet keeps only the Mail entry, and artboards 1, 2, 4 and 16 move".
  - fix: Make the pickup entry the Date-sheet save only. Rewrite entry 1 as: "Over Today, after the person saves their first pickup day in the Date sheet (from 'Change pickup day' or 'Set your pickup day'), once the Date sheet has closed and the save succeeded. The first 'That's my day' on the PickupCard never opens this sheet: it expands inline to 'Remind you the night before? 6:00 PM', and its 'Turn on reminders' fires the system dialog directly." Rewrite WHO AND WHEN as: "Maya taps 'Change pickup day', picks Tuesday in the Date sheet and taps Save." Update artboards 1, 2, 4 and 16 to say "after saving Tuesday in the Date sheet". Delete the inline-route open question. Add a Notes line: "flow-01 step 12 (primer after 'That's my day') is superseded by the PickupCard inline ask."
- [major] (ux) The sheet's anatomy and strings contradict the NotificationAsk primer drawn on the Foundations board, so this project would end up with a second, different primer. The conflicts: detent and grabber; the pickup title; the household body; the missing silence-contract line; the denied-state strings and captions; the tray time; and the Android button style, which the prompt leaves unstated.
  - evidence: prompts-v3/00d-06.md (NotificationAsk) says: "Height: a custom detent sized to content, not the medium detent" · "Pickup: \"Pickup reminders on this phone\"" · Household body "...Household updates stay off until you turn them on, then arrive quietly." · "5. Silence contract ... \"Only when something needs you. No news means nothing's up.\"" · ios-denied: "\"Notifications are off for Pantopus.\" ... \"Opens the Notifications page for Pantopus in Settings.\"" · tray row "\"Pantopus\" and \"now\"" · Android: "\"Continue\" and \"Not now\", both outlined". The prompt says: "a sheet at medium detent with a grabber" · Title "A reminder the night before pickup" · Mail body "Quiet updates from your household. ..." · the layout stack has no silence line · time "6:00 PM" · "Notifications are off for Pantopus on this phone."
  - fix: In PLATFORMS and LAYOUT, replace the sheet description with: "Draw NotificationAsk 'primer iOS' / 'primer Android' exactly as on the Foundations board: content-sized detent, Close row, title, PushCopy tray, body, silence contract, Continue, caption. On Android, draw Continue and Not now both outlined." Use the board's strings verbatim: the titles "Pickup reminders on this phone" and "Household updates on this phone", the board's household body, the silence line, the tray time "now", and "Notifications are off for Pantopus." plus its Open Settings caption. List as this surface's only deltas the what-still-works lines ("Pickup still shows on Today." / "Household updates still show here in Mail.") placed under "Notifications are off for Pantopus.". Remove the grabber from iOS.
- [major] (ux) The Mail entry is the one entry that certainly survives, yet its handoff out is undefined. The prompt never says whether Allow switches Household activity on, whether the push-off banner disappears, or what "Turn on" does when the OS has already granted permission but the group is off. The Mail body also says "Nothing else unless you turn it on" at the very moment the person is turning it on. No artboard shows Mail after a grant.
  - evidence: The prompt says: "What happens next: Continue opens the system dialog. After Allow, the person is back on the host." and "Already granted: the sheet never appears." ux-research-brief.md §3: Household activity is "Off until switched on from the household block or settings". NotificationRow state: "push-off banner ('Push is off. You'll still see these here. · Turn on')". Artboard 10 is only annotations over an unchanged Today.
  - fix: Add to WHERE IT LIVES: "Mail entry: after Allow, Household activity turns on (quiet delivery), the push-off banner is removed, and a polite status line reads 'Household updates are on · Change'. 'Change' opens Settings > Notifications at ?group=household. If the OS already allows notifications, 'Turn on' switches the group on in place and shows the same line, with no sheet. After Don't Allow, the banner stays and nothing else changes." Replace artboard 10 with: "f4-notification-primer · ios · 10-mail-granted · light: Mail after Allow: banner gone, the status line shown, rows unchanged; margin note 'Already granted: primer skipped; Save failed: primer skipped'." List the new string in Notes.
- [minor] (ux) The widget row string drifts from the Foundations NotificationAsk declined state: it lowercases the tab name Today and omits the button.
  - evidence: prompts-v3/00d-06.md: "replaced, once, by \"Put Today on your home screen\" (new) with an \"Add the widget\" (new) text button". The prompt says: "Widget row after a decline (Today, offered once): \"Put today on your home screen\"".
  - fix: Change the string to "Put Today on your home screen" with an "Add the widget" text button (primary.700, 44pt) in COPY and in artboard 5.
- [minor] (ux) The worst-case tray body is 45 characters, which breaks the 40-character PushCopy body rule. Drawing it truncated treats the violation as acceptable, when the product should shorten the label instead.
  - evidence: house-style-v2.txt: "Bodies are 40 characters or fewer". The prompt says: "\"Bins out tonight · SE Fishers Landing Terrace\". Draw it truncated at true width" (45 characters).
  - fix: Replace the worst-case line with: "Worst case: a long street label is shortened at a word boundary so the body stays at 40 characters or fewer: 'Bins out tonight · SE Fishers Landing' (37). Draw the full label struck in the margin as board chrome." Update artboards 12 and 13 to match.
- [minor] (ux) The save-error string and its owner are unclear. NotificationAsk's status string is "Couldn't save · Retry", and the prompt does not say which write failed after a grant.
  - evidence: component-contract.md, NotificationAsk states: "saving / Saved / Couldn't save (live region)". prompts-v3/00d-06.md: "\"Couldn't save · Retry\"". The prompt says: "Save error on Today after a grant: \"Couldn't save your reminder · Retry\"".
  - fix: State: "If the evening-briefing preference write fails after Allow, the briefing card row shows NotificationAsk's 'Couldn't save · Retry' in its status slot." Drop the separate string, or list it in Notes as a deliberate delta.
- [minor] (ux) FIRST FIVE SECONDS says the eye hits the tray first, but the layout stack and reading order put the title above the tray.
  - evidence: "First, the tray preview ... Second, the title" vs "Stack from top to bottom: ... title (h3), tray preview" and "Reading order: title, tray preview".
  - fix: Rewrite FIRST FIVE SECONDS as: "First, the title naming what this is. Second, the tray preview (the visual hero, the largest element). Third, Continue with 'Your phone will ask next.' under it."

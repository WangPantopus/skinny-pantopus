# Last review findings for 00a-02 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The stale form of the Place B pickup caption contradicts ProvenanceMark on this same board. ProvenanceMark R2 and R6 give it a stale caption with "— 9 days ago"; SourceCaption R6 says stale does not apply. Later surfaces (f4-today-pickup-card, x-place-file) would get two answers. The contract's stale state is "the as-of age appended".
  - evidence: 00a-01 R2 stale: "City of Camas · city pickup schedule · as of Sat 10 Oct — 9 days ago" (01.A2). 00a-02 R6 stale: "Does not apply — the schedule is dated by its as-of, which is already shown."
  - fix: Make R6 stale the dimmed ring plus "City of Camas · city pickup schedule · as of Sat 10 Oct — 9 days ago" (flat, no link), matching 01. Update DONE WHEN and greyscale counts, and note in 02.B1 that the string is already listed under 01.
- [minor] "Offline: unchanged" for static, frozen, county and no-URL captions is a design decision, but it appears only as an invented string, not as a section A decision. The contract's offline state is "as of <date>".
  - evidence: R1 offline: "unchanged ... 'static captions do not change offline'"; 02.A has no entry; contract states: "offline ('as of Sep 15')".
  - fix: Add 02.A7: "Captions with an effective date, a frozen date or a stated as-of do not change offline; only live captions switch to 'as of <time>'."
- [minor] Link captions are both part of the row's merged spoken label and separate tap targets, but the message never says how assistive tech reaches the link.
  - evidence: "The caption is read after the value in the row's merged label" and "a caption is a target (B13) only when a URL exists".
  - fix: Add: "When a URL exists, the row exposes the link as a custom action 'Open AirNow' (web: a separate focusable link after the row)."
- [minor] The R4 county caption has no as-of, while the COPY block printed on the same artboard says "Always authority · scope · as-of".
  - evidence: R4: "EPA · county-wide estimate — only a test tells you about this home"; COPY: "Always authority · scope · as-of."
  - fix: Add a Notes A line saying a county zone map has no as-of (the contract string is used as-is), or qualify the copy rule with "as-of where the source has one".
- [minor] The grid geometry is loose. With 1600 − (240 + 3×390 + 3×32) = 94, the side margins come out at 47 px, which is off the spacing scale. It is also unclear whether R5's "320 wide" is the card or its content.
  - evidence: "1600 px wide", "358-wide card ... (390 wide with padding)", "240 px row-label column and 32 px gutters", "except R5 (320 wide)".
  - fix: State 48 px side margins (1600 = 48 + 240 + 32 + 390 + 32 + 390 + 32 + 390 + 48 → 1602, so use 30 px gutters or 47→ adjust label to 238), and say "R5 content is 320 wide (352 card)".
- [minor] The OG strip publish name uses lowercase "og", while ProvenanceMark uses "OG" for the same slot.
  - evidence: "SourceCaption / collapsed / default / og / light" vs 00a-01 "(XS, S, M, L, OG)".
  - fix: Align with the casing rule chosen in the setup (for example "OG").

# Last review findings for 00a-05 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The grid cells are drawn at 80%, so the reason caption renders at 9.6px and the bodySmall message strip at 11.2px. That breaks setup B5 in the specimens that get published. At 100% the grid needs about 1498px, and setup B1 allows a wider 02 artboard for this.
  - evidence: "Each cell is a B2 card holding a 390-wide crop (422 wide with padding), drawn at 80% scale"; setup B1 allows a wider 02 artboard.
  - fix: Draw both grids at 100% on a 2000-wide 02 artboard (160 + 24 + 3 × 422 + 2 × 24 = 1498 content width), and change 05.A3 and DONE WHEN to match.
- [minor] Wrong board-rule references. Increase Contrast is setup B11 (B16 is fixtures and dates), and specimen cards are B3.
  - evidence: "because the reason caption carries the meaning (B16)"; "Each cell is a B2 card".
  - fix: Change "(B16)" to "(B11)" and "B2 card" to "B3 card".
- [minor] The contract copy rule says to start with "You're offline." and then say what still works. The R1 cached screen never says what still works, and R2 says "You're offline" twice in a row.
  - evidence: Contract copyRules: "Start with \"You're offline.\" and then say what still works." R1 offline shows only the FreshnessLine plus "Compare needs a connection." R2 offline: "You're offline · as of 7:04 AM" followed by "You're offline. You can keep taking photos."
  - fix: Either add a what-still-works sentence to R1 (list it on Notes B), or add a Notes A entry saying the offline FreshnessLine satisfies the opening and the reason captions carry the rest. For R2, consider dropping the duplicate FreshnessLine or recording why both appear.
- [minor] The disabled button's label sits on a surface.sunken fill, but the contrast table gives text.muted on base, and the button's edge is unspecified: sunken on a base card is about 1.1:1.
  - evidence: ③ "its label in text.muted on a surface.sunken fill"; table: "text.muted #9CA3AF on base 2.54 (disabled only; exempt)".
  - fix: State that the disabled button has no outline and is exempt (disabled), or give it a text.muted outline as ChoiceChip's disabled chip has. Label the table pair as the label on sunken, or note the exemption without a ratio.

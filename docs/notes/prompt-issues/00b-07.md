# Last review findings for 00b-07 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The FRAMES line says the target outline shown is 44pt. ANATOMY says the whole block (78pt, or 54pt for T1) is the target, extended to at least 44pt only if needed. A 44pt outline drawn on a 78pt block contradicts that.
  - evidence: "the target outline shown is 44pt" vs "the whole block is one target of at least 44pt ... extended beyond the visible block if needed".
  - fix: Write: "Draw the target outline around the whole block (at least 44 × 44pt; the block height when taller)."
- [minor] The pip text style captionMedium 12/16/500 is not in the house type scale ("never invent sizes"), and C7 gives no AX5 or 200% size for it, so the AX5 pip height rule is undefined.
  - evidence: "captionMedium 12/16/500 text.strong". House Type list: "caption 12/16/400" only. The AX5 rule: "pips grow to fit their text (text height + 4pt)".
  - fix: Either use caption 12/16 with a weight-500 note listed on Notes B, or state "captionMedium scales as caption (AX5 43, 200% 24)" and give the pip height as line height + 4.
- [minor] The integer size, h3 20/28, is a design choice. The contract gives only the weight ("label weight"), and the choice is not recorded on Notes B.
  - evidence: "the integer in h3 20/28/600 text.primary (the contract's 'label weight' is 600)". Notes B does not mention the size.
  - fix: Add to Notes B: "The integer is drawn at h3 size; the contract gives only its weight."
- [minor] The width and alignment of the offline FreshnessLine are unspecified. "You're offline · as of 7:04 AM" is wider than the 160pt block and needs a wrap or right-alignment rule for the keeper strip.
  - evidence: "the count stays, with the 00a · FreshnessLine 'You're offline · as of 7:04 AM' under the block"; "A vertical block, at most 160pt wide."
  - fix: Add: "The FreshnessLine is 4pt below, wraps within 160pt, and is aligned like the variant."
- [minor] The instruction to finish section E is internally inconsistent. It says to keep the existing order, but then gives an "Expected" order that does not match the component order in which questions were appended (the PM2.5 question from AqiBand comes before the range box from PublicPointMap).
  - evidence: "keep every question already there, in its existing order ... Expected: the range box, relative counts, ..., the PM2.5 sentence."
  - fix: Present the expected list as unordered ("Expected to include: ...") or list it in component order.
- [minor] Notes A leaves out the greyscale caption and does not say where the FreshnessLine string comes from. The Notes append also has no C entry, although the instruction says to append to every section.
  - evidence: The A list has no "Categories by printed words, not colour." or "You're offline · as of 7:04 AM". The prompt says "append a FactCount part to each section".
  - fix: Add the strings (or say they come from 00a), and add "C: none".
- [minor] The format of the extra publish names is ambiguous.
  - evidence: "Extras: claim-time (04, light), ax5 (03, light)."
  - fix: Spell them out: "FactCount / claim-time / light" and "FactCount / ax5 / light".

# Last review findings for 00c-05 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The S2 loading geometry is defined only for the 56pt avatar row, but the grid also asks for V6a in S2. V6a is a 44pt / 48dp text row with no avatar. The skeleton must match its final geometry, so the V6a loading cell either contradicts R11 or cannot be drawn as written. That cell will be published as roster-line · loading.
  - evidence: S2: "a WarmingSkeleton at the row's height (56pt) with a 40pt circle and two bars". GRID CELLS: "S2: V3, V6a (2)". V6a: "a 44pt / 48dp row \"Maya and Sam live here · View\"". R11: "surface.sunken bars ... in the final geometry and height".
  - fix: Split the S2 definition. V3: a 56pt skeleton with a 40pt circle and two bars. V6a: a 44pt / 48dp skeleton with one text bar and one short trailing bar, and no circle. Add a DONE WHEN line saying the V6a skeleton is the same height as V6a · S1.
- [minor] No specimen shows the owner as seen by another member. V1 (owner) is Maya's own row, with "(you)", ⑧ and the self-only menu. Screens seen from Sam's side, such as f3-member-home-dashboard, have no owner row to copy. V1 and V5 are also almost duplicates.
  - evidence: V1: "OWNER · \"Maya Chen (you)\" ... · ⑧ · overflow". ⑤: "On the viewer's own row, the only item is 'Leave Larkspur Loop'." V5: "V1's content ... with the overflow open".
  - fix: Either make V1 the owner as seen by Sam ("Maya Chen", join caption, no ⑧, no overflow or a Sam-appropriate one) and leave the viewer's own row to V5 alone, or state explicitly on Notes that the owner row as seen by others is the V2 layout with an OWNER heading.
- [minor] The roster crop draws an InviteRow before that component's message has arrived. The setup forbids starting a component early. The crop also relies on a Notes section that does not exist yet and uses an invented string that is not marked as invented.
  - evidence: Crop 1: "one quiet row with a 16pt mail glyph, \"priya@example.com\", the caption \"Member · sent Mon 12 Oct · Expires Mon 26 Oct\" ... its strings are listed under InviteRow on Notes." Setup: "Never start a component before its message arrives."
  - fix: Draw the PENDING group as a labelled grey outline reading "InviteRow (00c-06)" with no internal detail. Otherwise, mark "sent Mon 12 Oct" and "Resend" as (invented) and list them in the MemberRow Notes section.
- [minor] The AX5 avatar size is ambiguous, and "top-trailing" for the overflow overrides R9 without saying so.
  - evidence: AX5: "Avatar stays 40pt (48pt at most), top-aligned" and "overflow stays top-trailing". R9: "Leading glyphs and marks scale with the text" and "Trailing elements ... move below the text block".
  - fix: Pick one avatar size: "Avatar grows to 48pt at AX5 / 200%" or "Avatar stays 40pt". Mark the overflow placement as "overrides R9" and note the override on Notes.
- [minor] One shared offline reason sits under a three-item menu. R8 asks for a reason caption under each disabled action.
  - evidence: S3: "The V3 menu (Change role · Confirm lives here · Remove) carries one reason". R8: "with a reason caption directly under it".
  - fix: State that a menu gets one shared reason under the item group, overriding R8 for menus, and record that on Notes.

# Last review findings for 00c-04 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The S4 cell label contradicts R7's exact label grammar.
  - evidence: V2b: "Its S4 cell has no bill row; the label names the viewer." R7: "Each label reads exactly \"<code> · <key>\"" and the cell caption is exactly "<ComponentName> · <variant key> · <state key>".
  - fix: Keep the R7 label and caption. Put the viewer name in a separate annotation beside the cell ("viewer: Sam, before Maya shared bills").
- [minor] Where V3's scope sentence goes is ambiguous, because line 2 already holds the due date on the left and may hold attribution on the right.
  - evidence: V3: "line 2 adds a 12pt person glyph and \"Only you will see this.\", wrapping to line 3."
  - fix: Say it literally: "Line 3, leading-aligned: 12pt person glyph + 'Only you will see this.' in bodySmall text.secondary; lines 1–2 unchanged."
- [minor] The requirement to draw a focused cell beside the highlight is only partly met. The SF column is adjacent to S2b but not to S2a, yet the text says SF is "directly beside S2a and S2b".
  - evidence: Column order "S1, S2a, S2b, SF, S3, S4, S5" alongside "(SF directly beside S2a and S2b)".
  - fix: Change the text to "SF sits directly after S2b", or reorder the columns to S2a, SF, S2b so the focused cell sits between the two highlight cells.
- [minor] The row height arithmetic is not stated, and the stated minimum does not match the content. Two lines at 24 and 20 plus 12pt of vertical padding on each side come to 68pt, not 64pt.
  - evidence: ① "Row: min 64pt, padding 12pt vertical"; ② bodyMedium 16/24; ⑤ bodySmall 14/20.
  - fix: State "12 + 24 + 20 + 12 = 68pt at default sizes (min 64pt)", or change the minimum to 68pt.
- [minor] One offline reason covers four disabled menu items. R8 asks for a reason caption under each disabled text action. V4's former-member state is also a fixture delta (Sam has left), but Notes does not list it.
  - evidence: S5: "all four items disabled (R8), with \"You're offline...\" under them". Notes Fixture deltas list C2, S4 and HOA only. V4 reads "Sam (no longer here)".
  - fix: State that for a menu, one shared reason under the item group overrides R8, and record that on Notes. Add "V4 shows a later state after Sam left" to the fixture deltas.
- [minor] The C2 row repeats "Due today" in the chip and again on line 2. The past-due overdue and paid rows also lack the relative count the house microcopy asks for, and the prompt does not explain the exemption.
  - evidence: C2: "Due today · $142.18 · \"Due today\"". Overdue: "Due Thu 15 Oct". House: "beyond tomorrow, add the relative count".
  - fix: For C2, change line 2 to the reminder or source caption, or state that the repetition is intended. Add a copy rule: "past dates show the date only".

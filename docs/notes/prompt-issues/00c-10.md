# Last review findings for 00c-10 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The column order puts the S2 column between S1 and SF. V5's S2 cell is an n/a cell, so V5·S1 and V5·SF cannot sit side by side as the prompt, its DONE WHEN and R8 all require. V4·S1 and V4·SF are also separated.
  - evidence: STATES: "columns, in this order: S1, S2, SF". GRID: "V2, V3, V4, V5 and V5g in S2: 'n/a — ...'". SF: "so V5·S1 and V5·SF sit side by side in the same row." DONE WHEN: "V5·S1 and V5·SF sit side by side". R8: "a focused cell sits directly beside it."
  - fix: Reorder the columns to S1, SF, S2, as the other two components do, and update the GRID CELLS order.
- [major] V3's heading speaks to the wrong person. The attestation variant is used on f3b-owner-attestation, where the owner (Maya) reads the grant rows. The heading 'What you get once Maya confirms you live here' addresses Sam, so the published variant is wrong on its host screen.
  - evidence: Contract usedIn: "f3b-owner-attestation". flow-04 row 16: "Dana reads 'Does Sam live here?', the grant rows". V3: "the heading 'What you get once Maya confirms you live here'".
  - fix: Use the owner's viewpoint, e.g. 'What Sam gets once you confirm'. Alternatively, draw both forms as V3o · attestation-owner and V3r · attestation-requester. Update the invented strings on Notes.
- [minor] The contract's 'and 3 more' is replaced by 'Show 3 more', but Notes lists the change only as an invented string. It is not recorded as a contract conflict.
  - evidence: Contract variant: "verify sheet unlock list (2 + 'and 3 more')". Notes Contract conflicts list only the ChoiceChip edge.
  - fix: Add: 'drawn as "Show 3 more" (a button, verb-first) instead of contract "and 3 more"'.
- [minor] In context crop 1 pairs 'Accept' with 'Not now' and has no lasting decline. This breaks the glossary and brief rule that 'Not now' is always paired with a permanent decline.
  - evidence: Crop 1: "'Accept' and 'Not now' as equal-weight buttons below". Glossary: "Not now — ... Always paired with a permanent decline (Just me / Skip / No thanks)". Brief principle 7.
  - fix: Draw 'Accept' · 'Not now' · 'Decline' (or 'Just me'), all equal weight, and list any new string on Notes.
- [minor] V5g (Guest selected) is a selected cell with no focused cell beside it. The 'Show 3 more' target also omits the web size.
  - evidence: R8: "Wherever a grid has a highlighted or selected cell, a focused cell sits directly beside it." ACCESSIBILITY: "'Show 3 more' is a 44pt / 48dp button".
  - fix: Say that V5's SF cell serves both chip rows, or add a V5g SF cell. Write the target as '44pt / 48dp / 44px'.
- [minor] No viewer is given for V3 and crop 1, and this is not listed as a fixture delta. Sam is already a member in the fixture, so it is unclear who is being attested or invited.
  - evidence: V3 names Maya confirming but not whom. Crop 1 has no viewer. Fixture: Sam Ortega is a member, and Priya has a pending invite.
  - fix: Name them, e.g. 'V3: Maya confirming Sam' and 'crop 1: viewer Priya (pending invite)', and add both to Fixture deltas.

# Last review findings for 00c-06 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The V5 and V6 rows cannot be drawn from the spec as written. Each is given as one run of text plus buttons, with no split into line 1 and line 2 and no button placement. Neither run fits one line of the 361pt content width, so the model has to invent the layout of a component that will be published.
  - evidence: V5: "a 16pt link glyph, then 'Invite link · Works once · Expires in 7 days · Mon 26 Oct · Turn off link'" is about 73 characters of bodySmall 14, roughly 500pt wide. V6: "'priya@example.com · Waiting for your OK', with two equal-weight text buttons, 'Approve' and 'Remove', 8dp apart" is about 270pt of text plus about 118pt of buttons plus the glyph, which is more than 361pt. V6 also names no leading glyph, although ANATOMY says rows are 'quieter than people: no avatar'.
  - fix: Define each row the way the anatomy defines V1. V5: line 1 'Invite link' (bodySmall text.primary); line 2 caption 'Works once · Expires in 7 days · Mon 26 Oct'; 'Turn off link' as the trailing text button with a 44pt target. V6: the 16pt mail glyph; line 1 'priya@example.com'; line 2 caption 'Waiting for your OK'; 'Approve' and 'Remove' trailing on the row, 8dp apart, each with its own target (or state that they sit on a third line, leading-aligned).
- [minor] V3 (needs-reissue) is a pending invite in the PENDING group, but its line 2 replaces the caption, so it shows no expiry. That breaks the prompt's own copy rule and the contract rule that the expiry is always shown. The only narrowing listed on Notes is for the awaiting-approval row.
  - evidence: COPY RULES: "The expiry is shown on every pending invite (email or username) and on every active link." V3: "Line 2, in place of the ④ caption, is 'Needs reissue — …'". Notes contract conflicts mention only "omitted on the awaiting-approval row". This is the same kind of issue as open issue 15.
  - fix: Either keep the expiry on V3 (for example a line 3 caption 'Expires in 7 days · Mon 26 Oct'), or change the copy rule and the Notes conflict line to 'omitted on the needs-reissue and awaiting-approval rows' and give the reason.
- [minor] The V2 expiry 'Expires tomorrow' has no weekday date. The house style says dates show the weekday.
  - evidence: V2 caption: "Member · sent Mon 12 Oct · Expires tomorrow". House MICROCOPY: "Dates show the weekday: 'Mon 26 Oct'".
  - fix: Use 'Expires tomorrow · Mon 26 Oct' and update the greyscale caption to match.
- [minor] V3's wrapped line 2 contains an inline 'Reissue' text button, but the prompt does not say where that button sits once the sentence wraps (it must wrap at 361pt minus the glyph and overflow). It also does not say whether V4 keeps the mail glyph.
  - evidence: V3: "'Needs reissue — your household's invite rules changed · Reissue' in bodySmall text.primary, where 'Reissue' is a text button." V4: "the recipient, then a StatusChip" (no glyph named).
  - fix: State: 'Reissue sits trailing on its own line under the sentence with a 44pt target', and 'V4 keeps the 16pt mail glyph'.
- [minor] Some invented strings are missing from the Notes list.
  - evidence: 'Expires tomorrow' and the accessibility label pattern 'Invitation to priya@example.com, member, expires in 7 days, Monday 26 October. Resend. Button.' are not in the Notes invented-strings line.
  - fix: Add both to the InviteRow invented strings.
- [minor] The prompt is about 8,000 characters, above the 5,000-character density threshold in research-findings, and the 14-cell grid adds more detail on top.
  - evidence: research-findings claude-design-prompting: accuracy falls as instruction count rises; Stitch drops components above 5,000 characters.
  - fix: Shorten the S3 reason list and the Notes restatements. The DONE WHEN already restates most of them.

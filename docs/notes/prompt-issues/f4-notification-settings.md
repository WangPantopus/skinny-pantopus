# Last review findings for f4-notification-settings (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) Task reminders and task-assigned notifications have no switch. The old Home Reminders toggle covered bills, tasks and calendar events. The new split has Date reminders and Bill reminders, with a bill-only helper. That breaks the prompt's own rule that every notification type maps to exactly one switch.
  - evidence: Design doc §3: "Bill and task reminders come from home_reminders.py twice daily"; "task_assigned is the only transactional co-resident notification". derive-proposals f5-reminder-settings-copy: existing toggle "'Home Reminders — Bills, tasks, and calendar events'"; proposed helper "Pickup, lease, insurance, bills and tasks". critique-findings: "split the single home_reminders toggle into bills/tasks vs dates". The prompt's Bill reminders helper is "Clark Public Utilities and any bill you add." DONE WHEN: "Every notification type maps to exactly one switch."
  - fix: Assign task reminders and task_assigned to a switch. For example, rename the row "Bill and task reminders" with the helper "Clark Public Utilities, any bill you add, and tasks given to you". Alternatively, add a "Tasks given to you" row under Household activity and say which group it belongs to. Record the mapping on Notes.
- [minor] (substance) The "Tasks completed" caption adds "or were given", which the design doc does not support. task_completed notifies only when someone other than the creator completes a task.
  - evidence: Design doc F3: "task_completed: call the existing notifyTaskCompleted ... when status becomes done and created_by !== actorId." The prompt reads "Tasks completed", OFF, with the caption "Only tasks you created or were given."
  - fix: Use the caption "Only tasks you created.", or list "or were given" on Notes as a proposed backend change.
- [minor] (substance) ATTACH asks for exact screenshots of the Today briefing card and a Mail notification row with its overflow menu open. Both are built after this prompt in the build order.
  - evidence: FINAL-screen-inventory.md BUILD ORDER: "18. f4-briefing-optin-card", "19. f4-notification-settings", "35. f3-household-notifications".
  - fix: Say that these two entry hosts come from the Foundations components (the NotificationAsk card row, and NotificationRow with its overflow) rather than exact screenshots.
- [minor] (substance) The OS-blocked wording is given only for Android, so the iOS equivalent is unspecified. Artboard 5 also risks offering the widget twice: once in the banner and again in the Briefings row "Home screen widget · Add".
  - evidence: The prompt says: "draw the switch off and disabled ... 'Off in Android settings · Open'"; "The widget is offered once, inside that banner."; and "Native only: 'Home screen widget · Add', hidden once a widget has been placed."
  - fix: Give the iOS row string (for example "Off in iOS Settings · Open Settings"). State that the Briefings widget row is hidden while the banner offers the widget.
- [minor] (substance) The morning caption "Nothing sent in the last day repeats" overstates the no-repeat rule, which exempts the weather lead-in.
  - evidence: Design doc F4: "a signal identity already pushed within 24h (weather lead-in excepted) does not count toward the gate."
  - fix: Reword the caption to "Items sent in the last day don't repeat." and list the weather exception on Notes.
- [major] (ux) The OS-denied frame contradicts the page's own honesty rule. Rows that show their saved values would read ON while the whole app is blocked.
  - evidence: LAYOUT says: "A switch never reads ON when nothing can be delivered." DONE WHEN says: "No switch reads or is announced as ON while the OS blocks it." Artboard 5 says: "the pinned banner with rows still showing their saved values". per-surface-changes: "A switch never reads ON when nothing can be delivered".
  - fix: Add to LAYOUT: "When the whole app is blocked, every switch below the banner is drawn off and disabled. Under each one that the person had on, a caption in text.secondary reads 'Your choice: on. It comes back when notifications are allowed.'" Rewrite artboard 5 as: "the pinned banner; every switch off and disabled with its saved-choice caption; the widget offered once inside the banner." List the caption in Notes.
- [major] (ux) The prompt uses the NotificationAsk state named "switch ON but OS blocked", and the Foundations board draws that state with the switch ON. This surface draws it off and disabled. The prompt does not say it overrides the board, so Claude Design will get two conflicting instructions.
  - evidence: component-contract.md, NotificationAsk states: "switch ON but OS blocked (amber glyph + text.primary line + Open settings)". prompts-v3/00d-06.md settings row specimen: "The Briefings switch shows on; beneath it, a 16pt warning glyph". The prompt says: "When the OS blocks a row or group, draw the switch off and disabled".
  - fix: In FOUNDATIONS COMPONENTS USED, write: "NotificationAsk 'OS blocked' settings row, with one override of the Foundations specimen: the switch is drawn off and disabled (not on), with the warning glyph and the text.primary line under it. The saved choice is kept." Add a Notes item: "Foundations 00d-06 settings-row specimen draws the switch on; correct it to off/disabled to match research ('a switch never reads ON when nothing can be delivered')."
- [major] (ux) The quiet-night frame shows a skip receipt on a night when pickup is tomorrow. In the fixture it is Mon 19 Oct and recycling is Tue 20 Oct, and the same page says a pickup briefing was sent at 6:00 PM that evening. That is a false silence claim.
  - evidence: house-style-v2.txt FIXTURES: "TODAY: Mon 19 Oct 2026, 6:10 PM" and "next Tue 20 Oct". The prompt says: "Status line: \"Last sent Mon 19 Oct, 6:00 PM · pickup\"" and artboard 12: "not paused; the evening row reads \"Skipped tonight — nothing needed you\"". Invariant 4: silence is shown only when true.
  - fix: Rewrite artboard 12 as: "f4-notification-settings · web-1440 · 12-quiet-night · light: Wed 21 Oct, 6:10 PM (delta), not paused, every check succeeded. The evening row reads 'Skipped tonight — nothing needed you' above 'Last sent Mon 19 Oct, 6:00 PM · pickup'." Add the Wed 21 Oct delta to Notes.
- [minor] (ux) A deep-link landing only scrolls and highlights. The prompt does not say that accessibility focus moves to the highlighted group, although the brief requires it. The entry label "Turn these off" also promises an action the landing never performs.
  - evidence: ux-research-brief.md §5: "Deep-link highlights also move accessibility focus." The prompt says: "The page scrolls once to that row's group and fades a highlight on it." and "A landing only highlights; it never changes a value."
  - fix: Add to INTERACTION: "A landing moves accessibility focus to the highlighted group's header, and then to its first control, so 'Turn these off' ends one tap away from the switch." Add to Notes: "'Turn these off' opens the group rather than switching it off; confirm the label."
- [minor] (ux) The single-choice pattern is not platform-native on iOS. iOS grouped lists mark the selected row with a trailing checkmark, not a radio dot.
  - evidence: The prompt says: "Radio rows show a filled dot plus bold label for the selected row." PLATFORMS says: "iOS 393x852 uses a grouped inset list. Android ... Material 3 preference list".
  - fix: Write: "Selected threshold row: iOS uses a trailing checkmark plus bold label; Android uses an M3 radio button plus bold label; web uses a native radio plus bold label. Focus is a separate ring on every platform."
- [minor] (ux) The "Pause briefings" interaction is undefined. The brief specifies "Pause briefings until…" with the end date named, but the prompt never says what the row opens.
  - evidence: per-surface-changes: "'Pause briefings until…' with the end date named". The prompt says: "Row: \"Pause briefings\". Once paused, its caption names the end date."
  - fix: Rename the row to "Pause briefings until…". Add: "Tapping it opens the platform date picker (web: native date input), defaulting to 7 days from today. Saving shows 'Paused until Mon 26 Oct · Resume'."
- [minor] (ux) The Air master helper names Focus but not the delivery level. The per-surface change requires the helper under each master to state the level.
  - evidence: per-surface-changes: "The helper text under each master row states the delivery level ('Quiet — shows in your list, no sound')". The prompt's Air helper is: "Can come through Focus when the air quality index (AQI) is 151 and up, or when there's a weather warning."
  - fix: Change the Air helper to: "Normal notification, with sound. Can come through Focus when the air quality index (AQI) is 151 and up, or when there's a weather warning."
- [minor] (ux) The offline copy says only what is broken. The OfflineNotice rule requires saying what still works.
  - evidence: component-contract.md, OfflineNotice copyRules: "Start with \"You're offline.\" and then say what still works." The prompt says: "You're offline. Changes can't be saved right now."
  - fix: Replace it with: "You're offline. Your current settings still apply. Changes need a connection." Keep the per-control reason "Saving needs a connection."
- [minor] (ux) The blocked-row spoken-label example uses the VoiceOver word "dimmed" for an Android row.
  - evidence: "Blocked: \"Evening briefing, off, dimmed. Turned off in Android settings...\""
  - fix: Write: "Blocked (Android): 'Evening briefing, switch, off, disabled. Turned off in Android settings...'; on iOS VoiceOver says 'dimmed'."
- [minor] (ux) The prompt lists two components for the same T1 "Claim this address first" row. The contract says LockedActionRow is not shown to T1 users for home-only features.
  - evidence: The prompt lists: "FactRow (... not-at-this-tier variant 'Claim this address first'). LockedActionRow (not-at-this-tier variant, where the home settings shows it)." component-contract.md, LockedActionRow doNot: "Don't show it to a T1 user for home-only features".
  - fix: Delete the LockedActionRow entry and use only FactRow's not-at-this-tier variant for Household activity at PLACE B.
- [minor] (ux) The story's moment (iOS, from the air alert's Mail row) is drawn only on web-1440 via the query URL, so the native landing from "Turn these off" is never shown.
  - evidence: WHO AND WHEN says: "she taps \"Turn these off\" on that alert's row in Mail" (iPhone). Artboard 8 is "web-1440 · 08-landing-air".
  - fix: Change artboard 8 to "f4-notification-settings · ios · 08-landing-air · light: arrived from 'Turn these off' on the air alert row in Mail; the Air group is highlighted, and 'Very Unhealthy · AQI 201 and up' is selected with 'Saved' under it."
- [minor] (ux) The loading state does not state the brief's timing rule.
  - evidence: ux-research-brief.md P11: "Skeletons only for cold full-page loads of about 2–10 s. No indicator under 1 s." The prompt says: "Loading: row skeletons, not a spinner."
  - fix: Write: "Loading: nothing for the first second; after 1 second, row skeletons in the final slots."

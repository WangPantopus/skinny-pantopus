# Last review findings for 00b-01 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The summary is four sentences, which contradicts G6's one-sentence summary. The compare instrument on 03 gets no summary at all.
  - evidence: G6: "Each instrument has a one-sentence summary". Summary: "Flood Zone X, minimal flood hazard. Wildfire Moderate, 3 of 5. Air AQI 42, Good. Radon Zone 1, county-wide." 03 prints only a spoken label.
  - fix: Join it into one sentence with semicolons, and add a one-sentence compare summary on 03 (for example "Dana's area and yours share 3 of 4 bands; wildfire differs."). Then list it in Notes A.
- [minor] The two V10 specimens share one key pair, so they would get the same publish name.
  - evidence: 03: "V10 at 390 and at 320 wide ... (keys \"collapsed · ready\")". R10 gives one name per specimen: "ScaleStrip / collapsed / ready / light" twice.
  - fix: Key the 320 specimen as an extra: "[extra key collapsed-320]".
- [minor] The capsule marker and the triangle compare pointers depart from the contract ('Marker: 12pt two-tone', compare 'same marker form'), but part 1 never marks them as deviations. Part 2 was not supplied, so it is unclear whether Notes D records them.
  - evidence: Contract anatomy: "Marker: 12pt two-tone". Variant: "two markers on one track ... same marker form". Prompt: "vertical capsule 4 × 20pt" and "triangles 12 wide × 8 tall".
  - fix: Make sure part 2's Notes D lists both deviations (capsule replaces the 12pt marker; triangles replace the shared marker form) for sign-off.
- [minor] The two-column geometry is ambiguous: the 40%/60% split does not say whether it is taken before or after the 16pt gap.
  - evidence: "two columns with a 16pt gap: the left 40% ... the right 60%" on a 608px content width.
  - fix: State the widths, for example: left 237px, gap 16px, right 355px.
- [minor] The compare chip is 24pt tall but sits on an 18pt layer-name line, and the row geometry does not say how that line grows.
  - evidence: "Chip, right-aligned on the layer-name line ... 24pt pill". The name line is label 13/18, and the row gaps are defined from it.
  - fix: Add: "On compare rows the name line is 24pt tall, with the name vertically centred on the chip."
- [minor] The Air row does not repeat AqiBand's occupied-segment inner outline, although it says to 'draw it exactly like this'.
  - evidence: The ScaleStrip Air bullet lists segments, gaps, keylines, radius and the capsule, but not the "2px text.primary inner outline" that AqiBand puts on the occupied segment.
  - fix: Add "occupied segment: 2px text.primary inner outline" to the Air bullet.
- [minor] On 03, 'You' readings are called specimen values, but most of them are the HOME A fixture strings, so the gutter labelling is unclear.
  - evidence: "\"You\" readings are specimen values", but You's Flood (Zone X), Air (AQI 42) and Radon (Zone 1) values equal the fixtures; only "Low · 2 of 5" is invented.
  - fix: Write "Only You's wildfire reading (Low · 2 of 5) is a specimen value; label it in the gutter."

# Last review findings for 00a-04 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The grid specimens that get published are drawn at 60% scale. This component is a 12/16 caption line, so each specimen renders at about 7.2px, which breaks the setup's type-scale rule (B5) and is unreadable. The B10 greyscale repeat at 50% takes it to about 30% (3.6px), so the 'reads without colour' verdicts cannot be checked. Setup B1 already gives the fix for exactly this case: a wider 02 artboard.
  - evidence: "Each drawn cell is the full 390-wide B2 card ... drawn at 60% scale, about 234 wide, labelled 'shown at 60%'"; setup B1: "A component message may set a wider 02 artboard (for example 2000 or 2400 px); it records the width on Notes A."; setup B5: "Type uses only the type scale"; setup: publish names are "one per drawn grid cell".
  - fix: Draw both 02 grids at 100% on a 2400-wide artboard: 160 label column + 12 gutter + 5 × 390 columns with 12 gutters = 2170 wide. Drop "shown at 60%", change 04.A5 to record the 2400 width, and change DONE WHEN from "fit the 1440 artboard" to "fit the 2400 artboard".
- [minor] Wrong board-rule references. The date rule is setup B16 (B11 is Increase Contrast), and specimen cards are B3 (B2 is the top block).
  - evidence: 04.A4: "Date-rule exception (B11)"; grid: "the full 390-wide B2 card".
  - fix: Change "(B11)" to "(B16)" and "B2 card" to "B3 card".
- [minor] The one-state-per-variant mapping (each variant drawn in only one state, with four 'Does not apply' cells) interprets the contract, which lists variants and states independently. Notes does not record it as a decision.
  - evidence: "Each variant exists in exactly one state." The 04.A entries cover the hairline, the re-dating, the bills header, the date exception and the scale, but not this mapping.
  - fix: Add a Notes entry, 04.A6: "Each FreshnessLine variant is the line for exactly one state (updating→refreshing, updated→fresh, failed→failed, offline→offline, fact-stale and widget-horizon→stale); the other cells do not apply."
- [minor] Crop a gives no platform, although setup B1 requires one. The R5 cell does not say whether the card header also appears above the air row.
  - evidence: In context a: "f1-today-tab, Place B, 390 wide"; R5: "placed under the air row 'AQI 42 · Good', not under the header".
  - fix: Label crop a "web-390" (or iOS 393). For R5, say "the card shows the header with no freshness line, then the air row with the R5 line under it".

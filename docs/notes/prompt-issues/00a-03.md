# Last review findings for 00a-03 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] Wrong board-rule reference. The setup message defines specimen cards in B3; its B2 is the top block (h1, purpose, used-in). A literal model looking up 'B2 card' finds no card rule.
  - evidence: Grid geometry: "(each B2 card is 280 wide, 248 inside its padding)". Setup: "B2. Top block..." and "B3. Specimen cells. Draw each specimen on a surface.base card (radius lg 12, shadow sm, 16 px padding)".
  - fix: Replace "B2 card" with "B3 card".
- [minor] The in-context crops do not name a platform, but setup B1 requires every 04 crop to be labelled with its surface id, platform, width and fixture. 390 wide matches web-390, not iOS (393) or Android (412).
  - evidence: "IN CONTEXT (04-in-context: three 390-wide crops)"; setup B1: "three crops of real surfaces, each labelled with its surface id, platform and width, and fixture".
  - fix: State the platform for each crop, for example "web-390" for all three, or give a) iOS 393, b) Android 412, c) web 390.
- [minor] The two consent buttons have a height and 'the same style', but no fill, outline, type or token. They also have no dark rule. Equal weight could come out as two filled primary buttons or two outlined ones, and later surfaces copy whichever appears.
  - evidence: R4 pending: "two equal-weight buttons ... Stack the two buttons vertically, both full cell width, both 44pt / 48dp / 44px tall, with the same style".
  - fix: Name the style, for example "both secondary buttons: surface.base fill, 1px text.secondary outline, label 13/18/600 text.primary, radius md 8 (dark: dark tokens per B6)".
- [minor] The x-date-sheet crop shows the sheet title and Save but no visible Close/Done. The house style requires one on every sheet, and this crop is the reference later sheets copy.
  - evidence: In context b: "the sheet title 'Add a date' at the top of the crop ... directly above the sheet's primary button 'Save'"; house style: "Every sheet has a visible Close or Done".
  - fix: Add "with the sheet's visible 'Close' in the title row" to crop b.
- [minor] The R4 pending grid cell shows one carried row, but its consent question refers to 2 dates. Only the in-context crop shows both rows.
  - evidence: R4: "Show it on the row 'Warranty ends · in 54 days · Sat 12 Dec'" and pending: "'Share the 2 dates you entered with Sam?'".
  - fix: Either show both carried rows in the R4 pending cell, or add a cell caption: "specimen shows one of the 2 rows".

# Last review findings for f9-invite-rewards-card (round 2). The fix after this round was applied but not re-verified.

- [minor] (substance) The stage ledger's 'Address verified' count and an error state for the rewards data are not in today's payload. getInviteProgress returns only total_invited and total_converted, and on failure it returns zeros rather than an error. The Notes do not record either server dependency.
  - evidence: inviteRewardService.js returns `{ total_invited, total_converted, unlocked_features, next_unlock }`, and its catch returns `total_invited: 0, total_converted: 0`. The prompt has 'Stage ledger: "Invited 11" → "Joined 3" → "Address verified 1"' and 'if the rewards data fails, show the link row ... and an InlineErrorRow'.
  - fix: Add to Notes: 'Needs a verified-stage count in the payload, and the service must return an error instead of zeros on failure, or the zero state will be shown in place of the error state.'
- [minor] (substance) Row B promises exact homes counts 'for every area on the map'. This inherits the k-anonymity conflict flagged on f9-nearby-cells-map.
  - evidence: The prompt has 'Row B, unlocked: "3 neighbors join: homes, Founding slots and posts for every area on the map"'. densityReader.js has K_ANON_MIN = 10.
  - fix: Align the Row B wording with whatever the cells-map decision is, for example 'area details on the map', and cross-reference the Notes question.
- [minor] (substance) The glossary defines 'Invite' as a household invitation, while this card uses 'Invite rewards' for neighbor referrals. The collision is not recorded.
  - evidence: component-contract.md: 'Invite — a household invitation.' The prompt title is 'Invite rewards'.
  - fix: Add a Notes item flagging the term collision for the glossary.
- [minor] (substance) The unverified frame keeps the link row 'unchanged' but gives no link string for Sam, while Notes lists 'Sam's link' as invented.
  - evidence: The prompt has 'The link row is unchanged.' and the Notes entry '... Sam's link'.
  - fix: Give Sam's link, for example 'pantopus.com/join/sam-7h3q', in the CONTENT section.
- [major] (ux) The hero button 'Send a postcard invite' does not send anything: it scrolls to the Block Founders composer. Its label nearly matches the composer's real submit button, 'Send postcard invite'. It also adds a second strong action to a card whose one primary action is 'Copy link'; the prompt's own INSTEAD OF admits there are two. Row A is even spoken as 'Send a postcard invite, button'. A first-time user will expect a tap to send a postcard.
  - evidence: prompt FIRST FIVE SECONDS: "Primary action: 'Copy link'." CONTENT: "the button 'Send a postcard invite'". INSTEAD OF: "put the button on the hero only ... because one card has one strong action besides Copy link." ACCESSIBILITY: "Row A is spoken '... Send a postcard invite, button.'" f9-block-founders-panel COPY: button "Send postcard invite".
  - fix: Rename the hero control to "Go to postcard invites" and draw it as a text or tonal button, never filled; 'Copy link' is the only filled button in the card. Speak Row A as "... Earning 3, the most. Go to postcard invites, link." Update COPY, the handoff line in WHERE IT LIVES, INSTEAD OF ("one card has one filled action: Copy link") and entry (c) in f9-block-founders-panel to the new label.
- [minor] (ux) Row B says the reward includes 'homes … for every area', which can read as seeing individual homes. Invariant 3 forbids claims about a neighbour's home, and the map only shows counts.
  - evidence: prompt CONTENT: "Row B, unlocked: '3 neighbors join: homes, Founding slots and posts for every area on the map'". house-style INVARIANTS 3: "No claims about a neighbour's home."
  - fix: Change to "3 neighbors join: counts of homes with a verified address, Founding slots and posts for every area on the map", and update COPY and the spoken label to match.
- [minor] (ux) TYPE lists the native profile pointer row as a change and ATTACH asks for the profile block, but no artboard draws it; Notes lists it as omitted. The Block Founders prompt does draw its own pointer row.
  - evidence: prompt TYPE: "leave one pointer row in its place: 'Invite rewards' with the caption 'Your link and rewards are in Nearby'"; Notes: "omitted states (saved-place-only viewer, the native profile pointer row)".
  - fix: Add an artboard "f9-invite-rewards-card · ios · 16-profile-pointer-row · light" (the profile insight cards with the single pointer row), renumber the dark twins and Notes to 17–19, and update the batch plan: Turn 3: artboards 13-18, then wait for "continue"; Turn 4: artboard 19. Remove the pointer row from Notes' omitted states.
- [minor] (ux) The saved-place-only viewer is described but never drawn. That viewer's LockedActionRow is swapped for 'Claim this address', a different layout, so it is a tier variant that changes the screen.
  - evidence: prompt LAYOUT: "For a saved-place-only account, the LockedActionRow is replaced by the text button 'Claim this address'." Notes: "omitted states (saved-place-only viewer ...)".
  - fix: Change artboard 12's description to show Sam, and add the note "Jordan (saved place only) is identical except that 'Claim this address' replaces the LockedActionRow; the ScopeChip sentence is unchanged". Alternatively, add a web-390 frame for Jordan.
- [minor] (ux) The ledger step 'Invited' and the title 'Invite rewards' reuse 'Invite', which the glossary reserves for household invitations. The same account has a pending household invite (priya@example.com), so the two can be confused.
  - evidence: component-contract.md glossary: "Invite — a household invitation." prompt CONTENT: "'Invited 11' → 'Joined 3' → 'Address verified 1'".
  - fix: Keep the title as inventoried, but label the first step "Sent your link 11" (spoken "11 neighbors sent your link"). Add a Notes item asking whether the glossary should add "Invite rewards — referral rewards; not a household invite".

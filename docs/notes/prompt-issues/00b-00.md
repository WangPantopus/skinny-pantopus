# Last review findings for 00b-00 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] R3 limits type to the house scale plus a few extra sizes, but the component messages rely on medium-weight styles that the house scale does not list.
  - evidence: R3: "Type uses only the house type scale, plus the R7 sizes, the 11pt widget minimum and the OG sizes". The house scale lists h1/h2/h3/body/bodySmall/label/caption/overline only. ScaleStrip and AqiBand use bodyMedium 16/24/500, captionMedium 12/16/500 and bodySmallMedium, and R7 itself mentions "body and bodyMedium 53".
  - fix: Add to R3: "Medium (500) weights of body, bodySmall and caption (bodyMedium, bodySmallMedium, captionMedium) are allowed at the same sizes."
- [minor] R5 adds dark values the house style does not define, and the Notes 'Board' subsection does not record them.
  - evidence: The house-style Dark line lists only base, raised, app, text primary, secondary, border and focus/link. R5 adds surface.sunken #0F172A, surface.muted #111827, text.strong #F1F5F9 and border.strong #374151. R12's Board list records only the dark card edge and the dark sunken fills.
  - fix: Add a Board bullet to Notes B: "dark values for surface.sunken, surface.muted, text.strong and border.strong are board assumptions (R5)".
- [minor] R10 allows only grid keys or extra keys, but the component messages publish names with state keys that exist in no grid.
  - evidence: R10: "<Component> / <variant key> / <state key>" or "/ extra /"; R9: "Specimens outside a grid get the extra key". AqiBand publishes "AqiBand / widget-micro / ready-tinted" and "AqiBand / detail / no-reading-provider-failed".
  - fix: Either add to R10 "or a sub-state key the message defines (for example ready-tinted)", or make the component messages use extra keys for these.
- [minor] The self-containment claim conflicts with how the component messages are split up.
  - evidence: "Everything you need is in the house style, this message and the current component message". But ScaleStrip part 2 "relies on everything" in part 1, and AqiBand V2 "must match the ScaleStrip artboards exactly".
  - fix: Write "…this message and the current and earlier component messages in this project".
- [minor] The rationale for the dark card edge implies that border.strong gives a visible edge, but R6 lists it as decoration at 1.42:1.
  - evidence: R2: "1px dark border.strong edge, because dark border.default has no contrast on raised". R6: "dark border.strong 1.42 on dark raised" (decoration only).
  - fix: Write "a 1px dark border.strong decorative edge; the card edge is not meaningful, so 3:1 does not apply".
- [minor] The message is long for one send: about 13k characters with 20 rules and about 80 sub-bullets, well above the density the research warns about. Rules near the end (R11, R12) risk being dropped.
  - evidence: research-findings says accuracy falls as instruction count rises, with a bias toward early instructions, and Stitch drops components above 5,000 characters. R11 and R12 come last.
  - fix: Trim the R6 rows that no message prints (the 'Never used' line and repeated tinted-cell rows). Consider moving the R11 stand-ins into the component messages that use them.

# Last review findings for 00a-09 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The Guest capability line puts the date before the relative count. The house style, the brief (section 6) and setup rule B11 all put the relative count first.
  - evidence: R2: Guest: "Same as Member until Sun 1 Nov · in 13 days". House style: "beyond tomorrow, add the relative count: 'in 7 days · Mon 26 Oct'". Brief line 125 says the same. 09.B3 records the string as "date re-fixtured" but not as a format deviation.
  - fix: Use "Same as Member until in 13 days · Sun 1 Nov", or better "Same as Member for 13 days · until Sun 1 Nov". Update 09.B3 to match. The count (13 days from Mon 19 Oct) is correct.
- [minor] The iOS segmented control (a published specimen) has no tokens, no target and no non-colour selection cue. Everything else about selection and edges is pinned down, but this form is left to the model.
  - evidence: PER-PLATFORM FORMS: "iOS R5 is the native segmented control (UISegmentedControl look, 32pt, selected segment raised)". It gives no track or segment tokens, no label ink and no target (B13) outline, although the contract says "32pt visual / 44pt hit". The house style says border tokens are never the only edge of a "segment". ACCESSIBILITY says selection is "check, fill and outline", but the segmented form has no check. The name "ChoiceChip / segment / selected / ios / light" is published.
  - fix: Specify it. Track: surface.sunken. Selected segment: surface.base with shadow md and a 1px text.secondary edge, label text.primary 13/18/600. Unselected labels: text.strong. Draw the dashed 44pt target around the 32pt control. Add a Notes A line: the iOS segmented control shows selection by the raised segment and its edge (native), with no check.
- [minor] Geometry is off the spacing scale, and the gap between wrapped rows is unspecified. A 32 visual with an 18 line height leaves 7 above and below the label, which is not on the 4-px scale (B4). In the 1440 @200% wrapping frame, only an 8 gap between chips is given, so 44/48 targets on stacked rows would overlap.
  - evidence: ① "Height 32 ... visual", ③ "Label 13/18/600", ② "8 gap between chips". 03: "the chips wrap onto several lines and stay chips". B13: "48dp on Android (with 8dp gaps)".
  - fix: Say that vertical label padding is whatever centres the label in 32 (an allowed derived value). For wrapped chips, give the row gap as 16 or more, so that 32 + 16 ≥ 48 minus the chip, or say that targets may extend into the gap without overlapping.
- [minor] Two small internal contradictions. 09.D edits an earlier entry, while DONE WHEN says every earlier entry is unchanged. DONE WHEN also counts 5 artboards while the batch plan lists 6 (Notes is updated, not created), and there is no fallback if the turn cannot fit all six.
  - evidence: "09.D Add to 07.D: the same Android radio question applies..." vs "every earlier entry is unchanged". Batch plan items 1–6 vs "All 5 artboards exist".
  - fix: Make 09.D a new entry: "09.D Same founder question as 07.D, applied to ChoiceChip's Android 200% rows." Change DONE WHEN to "All 5 ChoiceChip artboards exist and 00a · Notes is updated". Add: "If this turn cannot fit all six, finish in order, then do Notes next turn."
- [minor] The large-text rules leave a few details implicit: the iOS/web unselected row label ink, and R4's size and shape in the web 390 @200% frame.
  - evidence: AX5: "Unselected rows: no fill, no indicator" (the label ink is given only for Android: "label text.strong"). "R4 stays a single chip that grows with its 44pt / 26sp label" gives no web size.
  - fix: Add "unselected label text.strong" to the AX5 bullet. For R4 on web 390, say "26px effective label, rounded rectangle when it wraps".
- [minor] Size and instruction density are high for one send (about 2,400 words, about 15k characters, and over 100 discrete instructions). This is within the workflow's 900–2,800-word target but well above the research's 5,000-character drop point. The 12-line NOTES block largely repeats the anatomy and grid text.
  - evidence: research-findings: accuracy falls as instruction count rises; Stitch drops components above 5,000 characters. NOTES 09.A1–A10 restate ④, ⑤, ⑦, R2, R5 and the platform notes almost word for word.
  - fix: Shorten the NOTES entries to one-line pointers (for example "09.A1 see ⑤"). Merge the INSTEAD OF lines that restate ④/⑤/⑦. Consider moving the Notes update to its own short final send.

# Last review findings for f1-email-verify-handoff (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The confirmed landing adds "Saved 1107 NE Birchfield Ct to your account." to the save confirmation's 03-saved page, which f1-save-confirmation does not contain. Frames 4–6 draw a page the owning project will draw differently.
  - evidence: This prompt: "the 03-saved page with the status line 'Email confirmed.' and 'Saved 1107 NE Birchfield Ct to your account.'" f1-save-confirmation 03-saved COPY lists "Email confirmed." but no "Saved … to your account."
  - fix: Align with f1-save-confirmation: either both prompts carry the line, or both drop it. Frames 4–6 should reproduce the save confirmation's 03-saved exactly.
- [major] (substance) The hold-expired destination is ambiguous. WHERE IT LIVES says the confirm action always opens 03-saved, but the hold-expired recovery (frame 11) is drawn as an auth-shell page with copy that differs from f1-save-confirmation's 04a and 04b. The flow spec assigns this recovery to f1-save-confirmation.
  - evidence: This prompt: "the confirm action (code or link) opens the save confirmation's 03-saved page directly"; "Frames 1-3 and 7-16 are auth-shell pages"; "Hold expired: 'We stopped holding that address.'" flows-spec: "server holds no draft (hold expired). f1-save-confirmation should show a selectable row".
  - fix: State that a confirm after the hold expired opens f1-save-confirmation 04a (held on this device) or 04b (nothing held), and redraw frame 11 as that page. Alternatively, if the recovery stays on the interstitial, use the save confirmation's exact recovery copy ("Save the address you looked up?" / "We stopped holding that address. Type it again to save it.") and state what happens after Save it.
- [minor] (substance) In saved-at-registration mode, "Confirm your email to keep your account" implies the unconfirmed account can lapse, but no time limit is given. The brief requires the page to say how long anything held will last.
  - evidence: Brief P2: "Holds last at least 20 h, and the screen states how long." Prompt saved mode: "Saved privately. Confirm your email to keep your account." No duration is given.
  - fix: Add a Notes item: "How long an unconfirmed account (and its saved place) lasts is for the founder to set; if it can lapse, the saved-mode page states the date, as the held mode does."
- [major] (ux) The confirmed page conflicts with the save-confirmation project, which owns 03-saved. This prompt adds "Saved 1107 NE Birchfield Ct to your account."; that one shows only "Email confirmed." above "Saved privately" and the address line, and forbids repeating the address. Frames 4-6 will not match the page they hand off to.
  - evidence: This prompt: "Confirmed ... the 03-saved page with the status line 'Email confirmed.' and 'Saved 1107 NE Birchfield Ct to your account.'". f1-save-confirmation prompt: "a status line 'Email confirmed.' sits above the title"; "Show the address once, as a single line under the title ... not repeated in the body."
  - fix: Change Confirmed to: "the save confirmation's 03-saved page exactly as that project draws it: status line 'Email confirmed.', title 'Saved privately' with its tick, the address line, the body sentence and the three actions." Remove "Saved 1107 NE Birchfield Ct to your account." from COPY, frame 4 and Notes, or keep it only as the spoken announcement in held mode, and say so.
- [major] (ux) The prompt says the confirm action always opens 03-saved, but in held mode an expired hold means nothing was saved. flows-spec routes that branch to the save confirmation's recovery state, not the saved page. The prompt also never says when frame 11 (hold expired on this page) appears.
  - evidence: This prompt: "the confirm action (code or link) opens the save confirmation's 03-saved page directly ... There is no timed auto-advance, it never lands on the pre-save Save button". flows-spec.md flow-01 failure branch: "Verification link opened on a device with no session and the server holds no draft (hold expired). f1-save-confirmation should show a selectable row ... The blank 'Street address' field is only for 'nothing was ever held'."
  - fix: Add to WHERE IT LEADS: "Held mode only: if the hold has expired when the person confirms, the confirm opens the save confirmation's recovery state instead: 04a (held row) if this device still has the address, otherwise 04b (blank field). Frame 11 is this page when the person reopens 'Check your email' after the hold ends, before confirming." Label frame 11 accordingly.
- [minor] (ux) The prompt names the AddressChip "expired state" for the hold-expired frame, but uses different copy and a different action from the board's expired cell. With the board attached, Claude Design gets two definitions.
  - evidence: prompts-v3/00a-08.md R1 expired: "the caption 'We're no longer holding this address.' and a text button 'Search again'". This prompt: "Hold expired ...: 'We stopped holding that address.' If this device still has it, show the AddressChip selectable recovery row ... 'Save it'".
  - fix: Either use the board strings ("We're no longer holding this address." · "Search again") or state: "Use the AddressChip expired fill only; replace its caption with 'We stopped holding that address.' and its action with the recovery row's 'Save it'." Record the override on Notes.
- [minor] (ux) In saved-at-registration mode, the chip is specified as the "held variant", although the address is saved rather than held. That clashes with the prompt's own rule that saved mode never says "holding".
  - evidence: Prompt: "Chip: the AddressChip held variant, '1107 NE Birchfield Ct' with 'Camas, WA 98607'" (applies to both modes); INSTEAD OF: "Instead of 'still saved' in held mode, say 'still holding'".
  - fix: Change it to: "Saved mode: the AddressChip default state with no hold message. Held fallback: the AddressChip held variant with 'We're holding it for 7 days, until Sat 17 Oct.'"
- [minor] (ux) The Confirm email in-progress state (inline progress after 1s) is neither drawn nor listed among the omitted states on Notes.
  - evidence: Prompt INTERACTION: "Confirm email shows inline progress only if it takes 1s or longer"; Notes omitted states: "hold-expired blank-field case; send-failed saved mode; offline held mode".
  - fix: Add "confirming in progress (inline progress on Confirm email), drawn only as copy" to the Notes omitted states, or add an artboard "f1-email-verify-handoff · web-1440 · 01b-confirming · light".

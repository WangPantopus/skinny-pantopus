# Last review findings for 00d-03 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] In the dark grid, every S2 (Undone) row puts line 2 in #94A3B8 on the primary.900 restore highlight. That pair measures about 3.69:1, which is below the house-style 4.5:1 text minimum. R10 only permits #94A3B8 on dark raised or dark base. The static Reduce Motion pose keeps the highlight, so the failure is published.
  - evidence: 03 grid note: 'The restore highlight is a primary.900 fill fading to dark raised.' and 'Line 2, the paid attribution and the "Finish day" border are #94A3B8 (R10: 5.71:1)'. The 5.71 figure is for dark raised, not primary.900. Line 2 strings that sit on the highlight in S2 include 'Saved place · Only you', 'Due Wed 28 Oct' and 'Offer'. R10 lists only '#E5E7EB on primary.900 7.64:1' for text. The light twin (text.secondary on primary.50) is about 4.54:1, which barely passes.
  - fix: Add this to the dark grid note: 'On the restore highlight, line 2 is #E5E7EB (R10: 7.64:1 on primary.900); it returns to #94A3B8 when the fill fades.' An alternative is to limit the highlight to a leading 4pt primary.900 bar. Say whether the V6 S2 caption 'No notification sent' sits inside the highlighted row or below it on dark raised.
- [major] The Android snackbar echo and the web-390 toast are published specimens ('pending-undo-with-echo'), but the prompt gives no tokens for them: no container fill, text colour, action colour, radius or height. The model will fall back to Material inverse-surface defaults, which breaks the house rule that every colour is a named token.
  - evidence: Anatomy 3: 'a Material 3 snackbar with the same text and Undo, above the bottom tab bar'. PLATFORMS: 'an optional toast in a polite live region'. House style: 'every colour is a semantic token ... If a colour isn't listed, use the nearest token.' R10 has no figure for text on a dark snackbar fill.
  - fix: Specify the echo in full. For example: 'snackbar/toast: surface.base fill, radius md 8, shadow md, 1px text.secondary edge, min height 48dp, text bodySmall text.primary, action bodySmallMedium primary.700 (R10: 5.93:1), 16 horizontal padding.' Alternatively, use dark base with #E5E7EB text and a #38BDF8 action, and cite R10 figures that already exist.
- [minor] The f10-mail-day-triage IN CONTEXT crop asks for V3 and V4 together. The two variants are mutually exclusive states of the same list: one piece pending versus all six sorted.
  - evidence: 'f10-mail-day-triage: V3 and V4 together, with "Finish day" at the bottom.' V3 S1 shows 5 unsorted pieces, while V4 S1 shows all six sorted under 'Undo all 6'.
  - fix: Draw two side-by-side crops, or change it to 'V3 S1 (one crop); V4 S1 shown as a second crop'.
- [minor] The bill due dates break the house microcopy rule that dates beyond tomorrow add a relative count.
  - evidence: HOST CONTENT: 'Due Fri 23 Oct', 'Due Wed 28 Oct', 'Due Sun 1 Nov', 'Due Mon 2 Nov'. House style: 'beyond tomorrow, add the relative count: "in 7 days · Mon 26 Oct"'.
  - fix: Either use 'Due in 4 days · Fri 23 Oct' and so on (check the width against the right-aligned amount and the paid attribution), or add a Notes item that treats the bills-list short form as an accepted exception.
- [minor] The Android inset on 01 has no published name, and it is unclear whether it is the same specimen as the Platforms-row Android echo on 04. The web-1440 toast position is described, but no specimen is drawn.
  - evidence: Anatomy 3 says 'separate Android 412 inset ... labelled "android only"'. KEYS names the pending-undo-with-echo specimens only once. PLATFORMS: 'bottom left on web-1440'.
  - fix: Add: 'The 01 inset carries the name "InlineUndo / removed-row / pending-undo-with-echo / android / light" (same specimen as 04).' Then either add a web-1440 crop or state that web-1440 takes its geometry from web-390 and log it on Notes E.
- [minor] The COPY whitelist omits the spoken strings and the 'Platforms' row label, even though other sections use them.
  - evidence: 'Use only the strings under HOST CONTENT, ANATOMY and VARIANTS.' The spoken strings 'Removed The Blairmont rental. Undo, button.' and 'Restored The Blairmont rental.' appear only under ACCESSIBILITY, and 'Platforms' appears only under 04.
  - fix: Change the line to 'Use only the strings under HOST CONTENT, ANATOMY, VARIANTS, STATES and ACCESSIBILITY, plus the 04 row label "Platforms".'
- [minor] The prompt is about 13k characters, well above the density threshold in the research findings, and several rules are restated in more than one section (duration rule, echo rules, V6 S3).
  - evidence: The echo rule appears in Anatomy 3, PLATFORMS, Notes 3.3 and E. The V6 S3 thumbnail appears in VARIANTS, STATES, 03 and DONE WHEN.
  - fix: State each rule once and refer back to it by anatomy number, especially in PLATFORMS and E.

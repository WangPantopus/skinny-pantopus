# Last review findings for 00d-02 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The strip cell width doesn't match the FourteenDayStrip contract, even though the skeleton must match the final geometry. With 4pt gaps each cell is 22.07pt, but the contract gives cells of about 23–26pt on phones.
  - evidence: Prompt: "each (361 − 52) / 14 ≈ 22.07pt wide, 4pt gaps". FourteenDayStrip contract: "14 equal cells across the card's content width (about 23–26pt on phones)".
  - fix: Write: "Match the published FourteenDayStrip's cell width and gap. If it isn't published, use 14 cells with 2pt gaps (≈23.9pt each)."
- [minor] The pickup card skeleton has no total height and no internal layout, so the "same height" ruler against the loaded PickupCard (361×196 in R15) can't be checked. The prompt also doesn't say what the V2 bar percentages are measured against, or the avatar-to-bar gap.
  - evidence: V1: "a card with a 42×42pt square … two bars … two 44pt button shapes of equal width" (no height). V2: "two bars (60% width × 14pt, 40% width × 12pt, 4pt apart)".
  - fix: Add: "Pickup card skeleton is 361×196pt: padding 16, square top-left, bars 12pt right of it, buttons pinned to the bottom with an 8pt gap." Add: "V2 bars start 12pt after the avatar; percentages are of the text column (361 − 32 − 40 − 12 = 277pt)."
- [minor] The AX5 and 200% instruction for skeleton heights can't be followed literally. R17 gives font sizes, not line heights, and the prompt never says which text style each bar stands in for.
  - evidence: 04 (a): "bar heights follow the R17 AX5 line heights of the text they stand in for". R17 gives only "caption 12 → 43pt … h3 20 → 55pt".
  - fix: Map each bar to a style, for example "200×20 = h3, 240×14 = bodySmall, 96×12 = caption, V2 bars = body and caption". Say that each bar's height equals that style's AX5 size × 1.2 and that bars wrap to two lines where the text would.
- [minor] In the V3a loaded marks, "tick" means the filled mark with a knocked-out tick (you added it). R13 defines a tick as a bare check stroke, so the model could draw bare checks on a published strip cell if it doesn't copy the published strip.
  - evidence: S3 V3: "Fri 23 Oct Clark Public Utilities bill, tick (you added)". R13: "A tick is a bare check stroke."
  - fix: Write "filled mark with knocked-out tick (you added)" for Fri 23 Oct, Wed 28 Oct and Sun 1 Nov.
- [minor] The loaded MemberRow uses the caption "Owner". In the MemberRow contract, the caption holds the joined date and the role is an overline group label, so the published MemberRow would render differently from what the prompt prescribes.
  - evidence: Prompt: "MemberRow (R15, owner variant) 'Maya Chen' with caption 'Owner'". MemberRow contract: "caption 'joined Sep 9, 2026' … Grouped under overline role labels".
  - fix: Write: "MemberRow owner variant for Maya Chen, exactly as published. If it's a fallback box, label it 'MemberRow · Maya Chen'." Drop "Owner" from Notes D.
- [minor] In the dark V4 cell, the card is dark base. R9 says dark cards are raised on app, and the prompt doesn't record this exception.
  - evidence: 03: "V4: the panel is dark raised on a dark base card". R9: "A dark card's edge is the fill step (raised on app)".
  - fix: Add a Notes A item: "2.6 V4 dark card is dark base (not raised) so the raised panel separates from it; confirm."

# Last review findings for 00a-08 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The dark selected cells fail text contrast if the city line keeps its secondary ink. The prompt only says "Text stays text.primary", while ④ sets the city in text.secondary. A literal model will draw dark text.secondary on primary.900.
  - evidence: R1 selected: "fill primary.50 (dark: primary.900) ... Text stays text.primary (dark: dark text.primary)". ④: city in "caption 12/16, text.secondary". Dark text.secondary #94A3B8 on primary.900 #0c4a6e is 3.69:1, below 4.5. Light text.secondary on primary.50 is 4.54 and passes only barely. Neither figure is in the table.
  - fix: In R1 selected, state: "street and city both in text.primary (dark: dark text.primary, 7.64) on the selected fill", or use dark text.strong for the city (8.63). Add the ratio to the TOKEN AND CONTRAST TABLE and add it to DONE WHEN.
- [minor] The border ratio given is for the wrong background. The chip is filled surface.raised, and border.default against raised is 1.18, not 1.24. Also, raised on base is about 1.05, so the decorative border is in practice the chip's only visible edge on a base card.
  - evidence: ①: "fill surface.raised, with a 1px border.default edge as decoration only (1.24:1)". 1.24 is border.default on base.
  - fix: Write "(1.24:1 on the base card, 1.18:1 on the chip fill)", and state that the pin and text carry the chip, as ⑨ already does for dark.
- [minor] The quoting in the COPY exact-strings list is inconsistent. Two strings have no quote marks, so it is unclear where "You already saved this as “Mom’s house”" ends and whether "Use for Today" is part of it.
  - evidence: "Exact strings: "We're holding it for 7 days" · "1107 NE Birchfield Ct, Camas — Save it" · You already saved this as “Mom’s house” · Use for Today · "Dana · Camas, WA"".
  - fix: Print each string on its own line with a label, for example: duplicate message: You already saved this as “Mom’s house” (curly quotes are part of the string); duplicate action: Use for Today.
- [minor] The dark default chip drawn on the light 01-anatomy artboard has no backing surface specified, so the "dark: no border" comparison may be drawn straight on light surface.app.
  - evidence: ⑨: "Draw a light and a dark default chip side by side, labelled 'dark: no border'."
  - fix: Add: "the dark chip sits on a dark surface.base card (radius lg 12)".
- [minor] The expired state has no spoken label, even though its meaning is carried only by words.
  - evidence: ACCESSIBILITY gives the default, selected and sender spoken labels only.
  - fix: Add: Expired: "1107 Northeast Birchfield Court, Camas, no longer held, Search again, button", and list it in 08.B2.

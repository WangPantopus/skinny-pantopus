# Last review findings for f3b-invitation-decision (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The Notes give the new-invitation push the wrong notification level. The research brief puts invitations to you in the Account & security group (Time Sensitive / HIGH, on by default), not the Passive household level. The hidden-preview placeholder 'Household update' is also wrong, because the invitee is not yet in the household.
  - evidence: ux-research-brief.md §3 table: "| Account & security | Sign-in, invitations to you | Time Sensitive | HIGH | On |". The prompt's frame 25 says: "the new-invitation push, drawn with PushCopy ... hidden-preview placeholder "Household update", Passive level". component-contract.md PushCopy says "household = Passive / LOW", which covers household activity, not invitations.
  - fix: In the frame 25 Notes, replace "Passive level" with "Account & security group: Time Sensitive on iOS, HIGH on Android, on by default". Either drop the 'Household update' placeholder or list a neutral placeholder (e.g. "Invitation") as an invented string.
- [minor] (substance) The prompt contradicts itself on the colour of the limit-row text. LAYOUT sets both lists in text.primary. ACCESSIBILITY and INSTEAD OF set the limit rows in text.secondary.
  - evidence: LAYOUT: "Both lists use identical row height (44pt), type (body, text.primary)". ACCESSIBILITY: "Limit rows are text.secondary on base (4.83:1) or better". INSTEAD OF: "draw text.secondary rows on base". Contract GrantLimitList: "a verb phrase in body text.primary".
  - fix: Pick one colour. Follow the contract: row text in text.primary, lock glyph in text.secondary. Change the INSTEAD OF line to "draw text.primary rows with a text.secondary lock glyph on base".
- [minor] (substance) The Guest caption 'Same as Member until Sun 1 Nov' claims Guests get the full Member grant list. The design doc's migration adds calendar, bills and member permissions only for `member`, and the baseline schema excludes guests in several places. The Notes list this string as invented but not as an assumption to check.
  - evidence: Design doc F3: "insert `HomeRolePermission` rows for `member`: `members.view`, `calendar.view`, `calendar.edit`, `finance.view`". supabase baseline: "AND ho.role_base NOT IN ('guest')". The caption comes from research-findings as a composer example.
  - fix: In Notes, add the assumption "Guest grants equal Member grants: to confirm against HomeRolePermission before build; otherwise draw Guest's own grant rows from the shared string set".
- [minor] (substance) The Notes drop the inventory's blocking dependency: this screen's verification call to action is only completable if the owner-attestation path, or another method a non-owner can use, exists.
  - evidence: inventory uxNote: "Blocking dependency: see the owner-attestation entry; if no verification method accepts a non-owner occupant on an already-claimed home, this CTA leads to an uncompletable sheet."
  - fix: Add this dependency to the frame 25 Notes under assumptions.
- [minor] (substance) The shared 'Also:' line puts the word 'tier' in user copy, which the critique says never to render. It also uses jargon ('home record watches').
  - evidence: Prompt: "Also: ... the Founding Neighbor tier, and the address line on your profile." critique-findings.md: "Never render the words tier, T1, household-verified or legacy."
  - fix: Change "the Founding Neighbor tier" to "joining as a Founding Neighbor". Keep the change in the shared string set so the verify and attestation sheets match.
- [minor] (substance) Two invented details are missing from the Notes list of invented strings.
  - evidence: WHO AND WHEN: "Maya Chen invited her as a Member an hour ago". INTERACTION: InlineUndo "Declined · Undo". Neither appears in the frame 25 invented-string list. The house style says "Any string you invent must be listed on the Notes artboard."
  - fix: Add "an hour ago" (the send time) and "Declined · Undo" to the frame 25 invented-string list.
- [major] (ux) The prompt gives two different text colours for the limit rows. LAYOUT sets both lists in text.primary, but ACCESSIBILITY and INSTEAD OF set the limit rows in text.secondary. Split across 5 turns, Claude Design can draw the core grant-vs-limit encoding differently from one frame to the next.
  - evidence: Prompt LAYOUT: "Both lists use identical row height (44pt), type (body, text.primary) and a 16pt glyph". ACCESSIBILITY: "Limit rows are text.secondary on base (4.83:1) or better." INSTEAD OF: "draw text.secondary rows on base". component-contract.md GrantLimitList: "a verb phrase in body text.primary. Both columns use the same type and row height."
  - fix: Follow the contract. In ACCESSIBILITY write: "Both lists are body text.primary on surface.base, well above 4.5:1; the lock glyph is text.secondary." Change the INSTEAD OF line to: "Instead of grey text on a sunken panel for the limits, draw the limit rows exactly like the grant rows (body text.primary on base, lock glyph in text.secondary), because these rows must be read (text.muted on sunken is 2.31:1)."
- [major] (ux) On every phone frame the only primary action sits below the fold, and the prompt never says where the buttons go. The stack above Accept adds up to about 900pt: the manifest (6 lines), lead 1, a heading with 3 rows at 44pt, lead 2, a heading with 5 rows at 44pt, the disclosure and the footnote. iOS has about 718pt of usable height. The accepted state has the same problem for Open Home, so the 'first five seconds' promise of one visible primary action fails in the dense default (frame 1) and in frame 6.
  - evidence: Prompt LAYOUT: "top to bottom: the title..., the manifest, the lead's first sentence, the grant list, the lead's second sentence, the limit list with its disclosure, the footnote, then the buttons." FIRST FIVE SECONDS: "The one primary action is 'Accept'." ACCESSIBILITY: "At AX5 ... the page scrolls with padding under the buttons." house-style-v2.txt: "Sticky bars reserve scroll padding."
  - fix: Add to LAYOUT: "On ios, android and web-390, Accept and Decline (and, on the accepted state, Open Home and Start address verification) sit in a bottom action bar pinned above the home indicator, on surface.base with a border.subtle top hairline, and the content reserves scroll padding equal to the bar's height. The signed-out caption and the Sign in link sit inside the bar under Accept. At AX5 the bar unpins and the buttons follow the footnote. On web-1440 the buttons follow the lists inline." Draw frames 1, 2, 4, 6 and 8 at viewport height, with the bar visible.
- [major] (ux) The new-invitation push is set to the Passive level and uses the household placeholder. That contradicts the brief's notification model, which puts invitations to you in the Account & security group.
  - evidence: Prompt ARTBOARDS 25: "hidden-preview placeholder 'Household update', Passive level". ux-research-brief.md §3: "| Account & security | Sign-in, invitations to you | Time Sensitive | HIGH | On |" and "Five groups, fixed before the first Android build".
  - fix: Replace it with: "group Account & security (invitations to you), iOS Time Sensitive / Android HIGH, on by default, per the brief's notification model; hidden-preview placeholder 'Invitation'". Add 'Invitation' to the invented-strings list. If the founder wants a lower level, list that as an open product decision on Notes, not as the spec.
- [minor] (ux) The declined state may show Undo twice: once in the InlineUndo line and once as a button in COPY.
  - evidence: INTERACTION: "InlineUndo ('Declined · Undo', Undo 44pt)". COPY Declined: "Buttons: 'Undo' and 'Back to your place'".
  - fix: In COPY write: "Declined: InlineUndo 'Declined · Undo', then the sentence 'When you leave this page, Maya Chen will see that this invitation was declined.' Button: 'Back to your place' (signed out: 'Look up your address')."
- [minor] (ux) The 'Also:' disclosure lists bare product names ('home record watches', 'fridge cards', 'the Founding Neighbor tier'). The prompt's own INSTEAD OF rules this out, and the critique says never to render the word 'tier'. The same line is shared with the verify sheet and the attestation sheet.
  - evidence: CONTENT: "Also: residency claims, home record watches, postcard invites, fridge cards, letters to neighbors from Mail, the Founding Neighbor tier, and the address line on your profile." INSTEAD OF: "Instead of product names as rows ... draw verb rows". critique-findings.md: "Never render the words tier, T1, household-verified or legacy."
  - fix: Rewrite the shared line as verbs: "Also: make a residency claim, watch this home's public records, send postcard invites, print a fridge card, send letters to neighbors from Mail, join as a Founding Neighbor, and show your address on your profile." Use the same string in f3b-verify-address-sheet and f3b-owner-attestation.
- [minor] (ux) TYPE says to change only what is listed, but that list leaves out changes the prompt depends on: Decline becoming an equal outlined button, Accept moving to primary.700, and the new account-mismatch and waiting-for-approval frames. Claude Design may keep the old Decline styling.
  - evidence: TYPE: "The changes: (1) ... (5) make Decline undoable in place." INTERACTION: "'Decline' is an outlined button of equal width and height".
  - fix: Add to the TYPE list: "(6) Accept is filled primary.700 and Decline is an outlined button of equal size; (7) add the account-mismatch and waiting-for-approval frames."
- [minor] (ux) Two flow edges are undefined. First, a signed-out person who taps Accept and then signs in with an account whose email differs from the invitation: the prompt says the acceptance 'then completes'. Second, the limit list for someone who is already address-verified at this home.
  - evidence: WHERE IT LIVES: "returns here with the token kept; the acceptance she started then completes". EDGE CASES: "An already address-verified person accepting sees no verification button or caption."
  - fix: Add to EDGE CASES: "If she returns signed in with a different email, show the account-mismatch frame instead of completing. An already address-verified person sees the limit list under the heading 'Also yours' with tick glyphs, and no footnote about verification."
- [minor] (ux) The grant rows are third-person strings from the sender's composer ('Sees bills and who paid'), but they sit under a second-person heading. They read awkwardly next to the second-person limit rows.
  - evidence: CONTENT: "What you get now: 'Sees and edits the calendar' · 'Sees bills and who paid' · 'Sees who lives here'."
  - fix: Keep the contract strings, and change the heading to "What members can do now" so the rows read correctly. Record on Notes that a second-person variant ('See and edit the calendar') needs a contract change.

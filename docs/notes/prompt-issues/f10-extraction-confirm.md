# Last review findings for f10-extraction-confirm (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The no-claimed-home route drops values the app already holds. The piece has been read, but the prompt sends Jordan to the Date sheet in plain create mode. Nothing says the read payee, amount and due date carry across, so Jordan would retype them. The contract has a pre-filled bill mode for this case, and P2 governs this surface.
  - evidence: ux-research-brief P2: "Never make someone retype a value we still hold. Offer it as a one-tap choice." (Governs … f10-extraction-confirm); component-contract DateSheet variants: "bill mode (pre-filled from a snap)"; prompt: "No claimed home → the Date sheet (create mode, Bill kind)."
  - fix: Change to: "No claimed home → the Date sheet in bill mode, pre-filled with the read due date and payee (still marked read-from-your-photo until Save), Bill kind, footer 'Only you will see this.'" State the same for the typed values on Close: they are kept on the row until the piece is committed or skipped.
- [major] (substance) The audience line has only the version for when everyone can see bills, even though the batch's fixture includes a guest (Alex Kim) who cannot see bill photos. Maya's view never names the difference, which the contract and research require.
  - evidence: Prompt: "Audience line, under the photo: 'Only your household can see this photo: you and Sam.'"; tray prompt defines "Maya's view with a guest in the household: 'Only you and Sam can see these photos. Alex sees that mail arrived, not the photos or amounts.'"; ScopeChip doNot: "name the people instead"
  - fix: Add the matching variant: "With a guest in the household: 'Only you and Sam can see this photo. Alex sees that mail arrived, not the photo or amount.'" State which one frames 1–7 use, matching the tray.
- [minor] (substance) FIRST FIVE SECONDS puts Due date above Amount. CONTENT and the reading order put Amount above Due date.
  - evidence: "2) The Due date flagged … above Amount '$142.18' with its source crop." vs CONTENT "Payee … Amount … Due date" and ACCESSIBILITY "fields top to bottom"
  - fix: Reword: "2) Amount '$142.18' with its source crop, then the Due date flagged 'We weren't sure — check this' with the alternative 'Fri 23 Oct (Payment due date)'."
- [minor] (substance) The high-confidence frame draws a 7-day reminder as settled ('We'll remind you 7 days before (Wed 21 Oct)…'). The design doc says bill reminders today are only day-before and day-of. The Notes flag the dependency, but the frame still shows a promise the backend cannot keep yet.
  - evidence: Design doc F10: "Reminders come from the existing home_reminders.py (day-before and day-of)"; critique/flows: "f10-extraction-confirm's success says 'We'll remind you 7 days before' … while the design doc says bill reminders are day-before and day-of"
  - fix: Keep the frame, and add a small Notes inset showing the fallback control (60/30/7 hidden, caption 'We'll remind you the day before and on the day.') for use if the bill lead reminder is not built.
- [minor] (substance) The guest path is inconsistent with triage. Triage gives Alex's rows only 'File', with no 'Confirm what we read', so the only way Alex reaches this screen is the tray's Done hand-off. The prompt does not say this.
  - evidence: Extraction: "Guest (Alex Kim, reaching this screen after their own scan)"; triage: "'File' is the only decision" for Alex's rows
  - fix: State: "Alex reaches this only from the tray's 'Done' when a piece was already read. Mail Day offers Alex no 'Confirm what we read'."
- [minor] (substance) The ProvenanceSheet opened from the legend should not offer the public-record report control ('This isn't right') for photo reads. Research reserves that control for public records, and the prompt does not say to leave it out.
  - evidence: research-findings capture: "Reserve the provenance sheet's 'This isn't right' for public-record facts, not for photo reads."
  - fix: Add: "The ProvenanceSheet opened here has no 'This isn't right' control; correction happens in the field."
- [minor] (substance) It is unclear how long Undo lasts once 'Next piece' loads the next piece in the same screen, and whether Mail Day can undo an 'Added to bills' commit afterwards.
  - evidence: Prompt: "Undo after a commit lasts until Maya leaves the screen."
  - fix: Specify: "Undo stays on the success line until 'Next piece' or 'Back to Mail Day'. After that, 'Fix what we read' on the bill is the path back."
- [major] (ux) The prompt does not say where the commit buttons sit. The pinned photo pane takes 42% of the phone screen and the fields scroll beneath it, so the commit button, which is item 3 of the first five seconds, is off-screen unless it sits in a sticky bar. The two 'equal size' buttons with a long restated label also cannot sit side by side at 393pt.
  - evidence: "The photo is a pinned pane in the top 42%, with the fields scrolling beneath."; "Commits, equal size: filled "Add $142.18 due Thu 8 Oct to bills" and outlined "Just file it"."; FIRST FIVE SECONDS: "3) The button "Add $142.18 due Thu 8 Oct to bills"."
  - fix: Add to LAYOUT: "On phones, the commits sit in a sticky bottom bar on surface.base with a top hairline, stacked full width (filled on top, outlined below), each wrapping to two lines if needed. The field list has scroll padding equal to the bar height, so a focused field is never under it. The bar sits below the fields on desktop, at the foot of the right pane. At AX5 the bar scrolls with the content."
- [major] (ux) The storyboard conflicts with the fixtures. HOME A already has the Clark Public Utilities bill ($142.18 due Fri 23 Oct), so in frame 01b the corrected read exactly matches an existing bill and should raise the duplicate banner. The prompt draws a clean success instead and gives this same string to the separate duplicate frame.
  - evidence: House FIXTURES: "Bills: Clark Public Utilities $142.18 due Fri 23 Oct; City of Vancouver water $84.00 due Wed 28 Oct". Prompt: "Duplicate: "You already have Clark Public Utilities · $142.18 · due Fri 23 Oct on this list."" alongside a normal success in frames 01b and 04.
  - fix: Add to CONTENT: "Before tonight, the bills list holds only Comcast. This session creates the Clark and water bills that the fixtures list. Frame 09 (duplicate) happens later, when a second photo of the same Clark bill is confirmed after it was added."
- [minor] (ux) The screen depends on data the design doc's extraction schema does not return (alternative dates with their labels, the printed label beside each value, regions, per-field flags), and Notes does not list this dependency.
  - evidence: Design doc F10: "typed schema `{ kind, payee, amount, due_date, account_last4?, confidence }`". Prompt: "each value comes with the region of the photo it was read from and the printed label beside it"; the only backend item in Notes concerns reminders.
  - fix: Add to Notes: "Open backend dependency: the extraction schema needs per-field source label, optional region, an uncertain flag and up to 3 labelled alternatives. Alternatives are shown only when returned; if no labels come back, the caption is omitted."
- [minor] (ux) The rule about source crops contradicts the no-region fallback. Amount and Due date 'always' get a crop, but a field with no region has nothing to crop.
  - evidence: "Amount and Due date always get the crop and the label caption, whatever the certainty." vs "If a field has no region ... the field shows "We can't point to where we read this.""
  - fix: Change to: "Amount and Due date get the crop and label caption whenever a region exists, whatever the certainty. With no region, the crop is replaced by the line 'We can't point to where we read this.'"
- [minor] (ux) The legend opens the ProvenanceSheet, whose contract anatomy always ends with a report control. The research reserves 'This isn't right' for public-record facts, not photo reads.
  - evidence: "Tapping the legend opens the ProvenanceSheet." Contract: "then the fact-type-specific report control at the foot". Research: "Reserve the provenance sheet's 'This isn't right' for public-record facts, not for photo reads."
  - fix: Add: "Opened from here, the ProvenanceSheet shows the read-from-your-photo and you-added meanings only, with no report control. Corrections happen in the fields."
- [minor] (ux) The prompt does not say whether Undo survives 'Next piece', and the Mail Day row that follows shows no Undo, so a mis-added bill could only be removed later through a destructive delete.
  - evidence: "Undo after a commit lasts until Maya leaves the screen."; hand-off: "The screen then offers "Next piece"".
  - fix: Add: "Moving to the next piece counts as staying on this screen. The previous piece's result stays as a collapsed line above the top bar, 'Clark Public Utilities added · Undo', until Maya returns to Mail Day. In the success state 'Next piece' is filled and 'Undo' is a text button."
- [minor] (ux) The web 1440 full screen does not say whether the app sidebar and mailbox nav stay visible, and the 55/45 split depends on that.
  - evidence: "NEW full screen on all platforms"; "Desktop: the photo pane takes 55% on the left and the fields 45% on the right."
  - fix: Add: "On web 1440 the screen replaces the app shell (no sidebar), with a top bar 'Close · Piece 1 of 4 · Skip for now', and the 55/45 split spans the full 1440 width."
- [minor] (ux) Initial focus jumps to the flagged date, so a screen-reader user skips the piece count, kind and photo context that the reading order puts first.
  - evidence: "Initial focus is on the flagged Due date" vs "Reading order: piece count, kind, photo summary, legend, fields..."
  - fix: Make the Due date's spoken label start with context: "Piece 1 of 4, Clark Public Utilities bill. Due date, Thursday 8 October 2026..."

# Last review findings for 00a-00 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] B5 allows other dimensions only where a component message states them, but the setup itself sets dimensions that are not on the spacing list: the 16×16 badge, 4 px end ticks, the 2 px focus gap, 158×158 widgets and 390/412/393 frames.
  - evidence: B5: "Any other dimension is allowed only where a component message states it." B4: "16×16", "4 px end ticks"; B14: "2 px gap"; B9: "390 px wide".
  - fix: Change B5 to "...only where this setup or a component message states it."
- [minor] The callout example uses a circled numeral (①), and the component messages use ①–⑦. A literal model may draw circular callout badges, which B4 and B8 forbid.
  - evidence: B4: "for example \"① 12pt disc · ink text.secondary\"" and "Badges are square because circles are reserved (B8)."
  - fix: Write the example as "[1] 12pt disc · ink text.secondary" and add: "①–⑦ in component messages mean callout numbers; draw them as the square badge."
- [minor] The parts example names 00a-01a and 00a-01b, but ProvenanceMark (00a-01) arrives as one message with two batches. The model may wait for a part that never comes.
  - evidence: "A long component arrives in parts (for example 00a-01a, then 00a-01b)." 00a-01 has no parts and relies on "continue".
  - fix: Use a neutral example ("for example 00a-NNa, then 00a-NNb"), or actually split 00a-01 (see the 00a-01 verdict).
- [minor] The <size|platform> slot has no casing rule, so the component messages disagree: ProvenanceMark uses "XS/S/M/L/OG" and SourceCaption uses "og". Publish names must be stable exact strings.
  - evidence: 00a-01: "(XS, S, M, L, OG)"; 00a-02: "SourceCaption / collapsed / default / og / light".
  - fix: In PUBLISH, state one casing for the slot, for example "size codes are uppercase XS/S/M/L/OG; platforms are lowercase ios/android/web; otherwise any".
- [minor] The board rules are meant to be free of component specifics, but B9 carries a ProvenanceMark-only size ("56 does not scale").
  - evidence: B9: "(12 → 24, 16 → 32, 24 → 48; 56 does not scale)".
  - fix: Reduce B9 to "capped at 2×" and move the L 56 exception into 00a-01 SCALING.

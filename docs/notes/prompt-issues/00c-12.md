# Last review findings for 00c-12 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The prompt does not say where "Uploading 2 of 3" goes on the V1 row. Line 2 already holds "Bill · $142.18", so it could be a third line, a trailing caption, or a replacement for line 2.
  - evidence: ④: "plus the text \"Uploading 2 of 3\" beside the row in caption text.secondary". V1: "Clark Public Utilities" · "Bill · $142.18". The AX5 section says "The title and the progress text wrap below one another", which suggests a trailing position at default size, but no section states it.
  - fix: State the position. For example: "At default sizes, 'Uploading 2 of 3' sits at the trailing edge of line 1, top-aligned with the title; on V2 (one line) it sits under the title."
- [minor] The ring is not fully specified. Its diameter is missing, and the prompt does not say whether the photo stays visible under the ring. This matters most on the 40×52pt drawer thumbnail.
  - evidence: ④: "a determinate ring inside the thumbnail, 2pt stroke, primary.700 on a border.strong track". No size is given for 56pt, 40×52pt or 32pt thumbnails.
  - fix: Add: "ring 24pt diameter, centred, drawn over the photo dimmed by nothing (the photo stays visible); same 24pt on V2 and V4", or whatever sizes you choose.
- [minor] The action row (Retake · Delete · Same letter as previous) is not scoped to particular variants. On the privacy list and in the drawer, "Retake" and "Same letter as previous" make no sense. The web privacy crop also shows no way to reach Delete, because it draws no selected row and no menu button.
  - evidence: ⑦ defines one action row for 'Selected'. Crop 3: "V3 rows, one of them in S4 (deleted) ... No row menu button." flows-spec says f10-mail-snap-privacy "allows deleting one or all".
  - fix: Say which actions each variant's selected row shows (for example, V3 shows Delete only). In crop 3, draw one V3 row selected with its visible Delete button, so the web path is shown without a menu button.
- [minor] Selection geometry and colour are underspecified. The light-mode colour and size of the leading check glyph are missing, and so is its position relative to the 16pt margin and the thumbnail. The action row has no stated start edge, and the three buttons may not fit 361pt if they start at the text edge (84pt inset).
  - evidence: ⑦: "a primary.50 fill behind the row plus a leading check glyph (dark: D5). Below it, a visible action row of three text buttons ... 8dp apart". The dark colour comes from D5, but no light colour is given.
  - fix: Add: "16pt stroke check in primary.700 (5.57:1 on primary.50) at the 16pt leading margin, thumbnail shifts 24pt right; the action row starts at the 16pt leading margin."
- [minor] The Notes list is incomplete against R14. The contract's deleted string "photo deleted Sep 12" was changed to "Mon 12 Oct", but no conflict line records it. There is no Assumptions list (check glyph, stacked-thumbnail 4pt offset, ring size). The invented spoken label is also not listed.
  - evidence: Contract anatomy: "the words 'photo deleted Sep 12'". The prompt's NOTES list has only 'Invented strings' and 'Contract conflicts'. The spoken label "Clark Public Utilities, bill, photo, uploading 2 of 3" is not in Invented strings. R14 requires "Invented strings · Fixture deltas · Contract conflicts · Assumptions".
  - fix: Add the conflict "deleted date drawn as 'Mon 12 Oct' (weekday, fixture month) instead of contract 'Sep 12'". Add an Assumptions line (check glyph placement, 4pt stack offset, ring size). Add the spoken label to Invented strings.
- [minor] The greyscale captions cover only five states. They leave out the queue and no-photo variants and the focused state, although R10 asks for a caption on each variant or state.
  - evidence: The GREYSCALE list covers uploading, failed, deleted, offline and selected. R10 says: "caption each variant or state with its non-colour cue".
  - fix: Add: "queue = stacked outline + '2 pages'" · "no-photo = text at leading margin, no square" · "focused = 2px outer ring".
- [minor] Artboard names are not exact everywhere. Turn 2 uses shortened names, and the Notes entry carries a parenthetical that could end up in the artboard name.
  - evidence: BATCH PLAN Turn 2: "03-ax5-200-greyscale · light, 04-in-context · light and 05-grid · dark". ARTBOARDS: "00c · Notes (final update)".
  - fix: Use the full names in Turn 2 ("00c · ThumbnailRail · 03-ax5-200-greyscale · light" and so on). Write the Notes line as "00c · Notes" and put "(final update)" in the batch plan text only.
- [minor] The message runs about 8.1k characters and repeats itself. DONE WHEN restates most of the anatomy, INSTEAD OF and grid rules, and the ring-versus-Delete rule appears three times. The research findings say accuracy drops as the number of instructions rises.
  - evidence: The file is 8,155 characters. The rule 'ring never near Delete' appears in ⑦, INSTEAD OF and DONE WHEN. 'No percentage' appears in ④, COPY RULES, INSTEAD OF, NOTES and DONE WHEN.
  - fix: Cut DONE WHEN to checks the body does not already state (the 15 cells, the 5 artboards, the Notes completion and the 61-artboard list). Keep each rule once.
- [minor] The S5 offline caption does not say what to do next, which the house microcopy rule requires for error text.
  - evidence: S5: "You're offline. This photo isn't saved on this device." House style: "Error text says what to do next."
  - fix: Add a next step, for example "You're offline. This photo isn't saved on this device. Reconnect to see it." (invented; list on Notes).

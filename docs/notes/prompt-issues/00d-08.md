# Last review findings for 00d-08 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] A passed lead that was never chosen has no readable reason visible in S1, which breaks R16 and the prompt's own INSTEAD OF line about silent dead controls.
  - evidence: R16: "Its label is text.muted, and the reason is also written nearby in readable text." The rule says the passed state has a "Label in text.muted ... Activating it shows the explanation line", so the reason appears only on activation. The V1 S1 and V2 S1 defaults (published specimens) show only the muted label (2.54:1). INSTEAD OF: "silent dead controls leave people guessing".
  - fix: Add this to the PASSED-LEAD RULE and to S1: "Under the control, in bodySmall text.secondary (R10: 4.83:1): '60 days has passed for this date.' (V2: '60 and 30 days have passed for this date.')". Give the vertical placement (8pt below the control, above the timeline), add the strings to Notes D, and add "S1 shows the passed-lead line" to DONE WHEN.
- [major] The 02 and 03 grid plus the one-day specimen beside the V1 row does not fit R2's 1600px width at R3's 1x widths.
  - evidence: R2: "1600px wide". R3: iOS specimens at 393pt. The grid is 3 columns × 393 (about 1180) plus the row-label column and gutters (about 200), and the one-day specimen "beside the V1 row" adds about 400, for about 1780px.
  - fix: Draw the one-day specimen in its own labelled row directly below the grid ("V1 · one day selected"), not beside the V1 row, and update GRIDS, Notes F and DONE WHEN to match.
- [minor] Three large-text specimen names are abbreviated with '…', and the model may print the ellipsis literally, which R14 forbids.
  - evidence: KEYS: "'… / date-sheet / default / android-150 / light' · '… / date-sheet / default / web-1440 / light'".
  - fix: Write all names in full, for example "ReminderLeadControl / date-sheet / default / android-150 / light". Also list the ios-ax5 and android-200 names in full.
- [minor] The one-day specimen has no spoken timeline summary, and it keeps the break mark at x 117 between ticks that are one day apart, which suggests a gap that isn't there.
  - evidence: ANATOMY 5 draws two ticks at x 54 and 307. ACCESSIBILITY gives summaries only for V1, V2 and V3. ANATOMY 3b places "A two-slash break mark ... at x 117".
  - fix: Add the summary "Reminders on Fri 11 Dec and Sat 12 Dec." (and add it to Notes D), and say "no break mark on the one-day specimen".
- [minor] The S3 disabled state leaves open whether the container outline and the item 4 caption stay, and R17 gives no type sizes for the AX2 and Android 150% frames.
  - evidence: S3: "segment container is on surface.sunken, all four labels are text.muted, no selection, no timeline". Neither the outline nor the caption is mentioned. R17 lists sizes only for AX5 and 200%, yet 04 asks for "iOS AX1–AX2" and "Android 150%".
  - fix: Add to S3: "the 1px text.secondary outline and dividers stay; the item 4 caption is hidden." Add the AX2 and 150% sizes to the extra-row line, for example "AX2: bodySmall 14 → 19pt; Android 150%: bodySmall 14 → 21sp".
- [minor] Notes F says 'the 04 frames' without excluding the greyscale copies, which PUBLISH treats as review aids.
  - evidence: F: "...the two 01 insets, and the 04 frames."
  - fix: Change it to "the 04 ios-ax5, android-200, ios-ax2, android-150 and web-1440 frames (not the greyscale copies)".

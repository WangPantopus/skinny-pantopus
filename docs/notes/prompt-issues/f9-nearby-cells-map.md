# Last review findings for f9-nearby-cells-map (round 2). The fix after this round was applied but not re-verified.

- [major] (substance) The prompt shows exact verified-home counts below the product's k-anonymity floor for areas other than the viewer's own, to anyone with 3 joins, including an area with a single home. It also prints a caption that is false for that area. The Notes question uses the wrong threshold (3 instead of 10).
  - evidence: backend/services/place/densityReader.js: `const K_ANON_MIN = 10;`. backend/routes/neighborhood.js: `// k-anon floor: exact counts appear only from K_ANON_MIN up.` and, for /cells, `each carrying ONLY its density bucket ... Never a count, never a point`. critique-findings.md:41: the density scale is 'correctly k-anonymous, deliberately never a number'. House style invariant 3: 'No claims about a neighbour's home.' The prompt has 'Lacamas Shores 1 / 0', 'Dawson's Ridge 2 / 3', 'Lacamas Shores (1 home) shows the real count. Put "Should counts under 3 be shown?" on Notes' and also the caption 'Counts cover the whole area, never a single home.'
  - fix: Show an exact homes count only at 10 or more, or only for the viewer's own area when the viewer is address-verified (as blockFoundersService.readRawCountForVerifiedInsider does). Below that, print the floored bucket words. Change the Lacamas Shores fixture to show that floored state. Change the Notes question to 'The server floors counts under 10 (K_ANON_MIN). Should the referral unlock lower that floor? Decide before build.' Keep the caption 'never a single home' only where it is true.
- [major] (substance) The prompt silently changes what the existing map encodes and how its grid is shaped, while also saying 'keep everything and change only what is listed'. Today the map shades 25 cells (5x5) by verified-household density bucket and has a server-labelled legend. The prompt re-encodes the fill as neighbor posts in 30 days on a 6x4 grid of 24 cells. Neither change is in the numbered change list, and nothing says what happens to the verified-home density layer and its legend.
  - evidence: NearbyCellsMap.tsx: 'verified households by block cell ... the server sends a floored bucket per ~1 km cell and nothing else' and the caption 'shaded by how many households have verified'. neighborhood.js: 'the 5×5 grid of geohash-6 cells', GRID_RADIUS = 2, with bucket labels 'No verified homes yet' / 'Forming (under 10)' / 'A few (10–24)' / 'Growing (25+)'. The prompt says 'Keep everything and change only what is listed: (1) print the activity level ... (5)', then 'Fill each area on the ramp ... for neighbor posts in the last 30 days' and 'Areas: a 6x4 grid', with the summary 'Map of 24 areas'.
  - fix: Add explicit items to the TYPE change list: '(6) the fill changes from verified-home density to neighbor posts in 30 days; the legend changes from the server bucket labels to the post levels' and '(7) the grid changes from 5x5 (25) to 6x4 (24)'. Alternatively keep 5x5 and 25 areas to match the server. Add both changes to the Notes list of assumptions and server dependencies (a new per-cell posts bucket in /api/neighborhood/cells). State whether the verified-home density bucket survives anywhere, for example in the panel or the list.
- [major] (substance) The base-map fixture cannot be drawn at the scale the prompt fixes. Six geohash-6 columns span about 5.2 km, but the named landmarks run from I-205 to the Camas paper mill (about 11 km east–west), and Lacamas Lake and Lacamas Shores sit several km north of SR-14. A true scale bar will contradict the landmark list.
  - evidence: The prompt has 'a muted street grid from east Vancouver to west Camas, with I-205, SR-14, NE 164th Ave, NE 192nd Ave, Lacamas Lake and the Camas paper mill at the east edge. Add a scale bar in tenths of a mile', plus 'a 6x4 grid ... about 0.86 km east–west by 0.61 km north–south'. PublicPointMap in the component contract is drawn 'at true scale'.
  - fix: Limit the landmarks to those inside a roughly 5.2 x 2.4 km window around Fircrest (for example SR-14, NE 164th Ave and NE 192nd Ave). Drop I-205, Lacamas Lake and the paper mill, or say they sit off-grid in muted context outside the 6x4 extent. Choose neighbor area names that plausibly fit that window, and record the geography as invented on Notes.
- [minor] (substance) The 'No data' state is defined as 'no homes on record'. The existing server treats zero verified homes as a real value ('No verified homes yet'), and a post-level map can have posts in an area with no verified homes. The prompt conflates 'no data' with 'zero homes'.
  - evidence: The prompt has 'Forest Home: no data (no homes on record)' and 'No-data panel (Forest Home): its name and "No homes with a verified address here yet"'. neighborhood.js has `none: 'No verified homes yet'`.
  - fix: Define 'No data' as 'the posts lookup returned nothing for this area', which is distinct from 0 posts, and keep 'No homes with a verified address here yet' as the homes-row state. Or note the chosen definition on Notes.
- [minor] (substance) The same geohash is called 'area' here and 'block' in Block Founders ('Your block · Fircrest'), with no note that these are one unit.
  - evidence: The prompt has 'the UI word is "area"' and 'Fircrest · Your area'. The f9-block-founders-panel prompt has 'Your block · Fircrest'.
  - fix: Add a Notes item that area (map) and block (Block Founders) name the same unit, and that the choice of word is an open question.
- [minor] (substance) The dark-mode outline and label colours are given as raw hex, although the house rule says tokens only.
  - evidence: The prompt has 'The Fircrest outline is #38BDF8' and 'dark text.primary (#E5E7EB)'. House style: 'every colour is a semantic token'.
  - fix: Name the tokens: the dark focus/link token and dark text.primary.
- [major] (ux) The base map cannot fit the grid it is supposed to hold. Six geohash-6 columns at about 0.86 km make a grid about 5.2 km wide and 2.4 km tall, roughly 3.2 mi by 1.5 mi. The prompt asks that grid to span 'east Vancouver to west Camas' and to show I-205, Lacamas Lake and the Camas paper mill, which is about 15 km of ground. It also names areas (Sifton, Lacamas Shores, Forest Home, Grass Valley) that sit kilometres apart. The prompt also demands a true-scale scale bar, and PublicPointMap is drawn 'at true scale'. Claude Design will have to either stretch the areas or fake the scale, and a Clark County founder will spot the wrong geography at once.
  - evidence: prompts f9-nearby-cells-map CONTENT: "a muted street grid from east Vancouver to west Camas, with I-205, SR-14, NE 164th Ave, NE 192nd Ave, Lacamas Lake and the Camas paper mill at the east edge. Add a scale bar in tenths of a mile" and "a 6x4 grid of rectangles. Each is a geohash-6 cell, about 0.86 km east–west by 0.61 km north–south". component-contract.md PublicPointMap: "drawn from the server's stable public point at true scale".
  - fix: Replace the Base map bullet with: "Base map: only the ground the 6x4 grid covers, about 3.2 mi east–west by 1.5 mi north–south around HOME A. Draw only streets inside that box (for example SR-14 along the south edge and NE 164th Ave), and leave out I-205, Lacamas Lake and the paper mill. The scale bar must match the areas: one area is about 0.53 mi wide." Add: "Area names are mock-up labels placed on this grid, not real neighbourhood boundaries. List them on Notes." Mirror the Fisher's Landing and Forest Home names in f9-block-founders-panel unchanged.
- [major] (ux) There are no tier or scope variants, and the prompt never says which address the map centres on. The only permission case is 'a viewer with no address'. There is no frame or rule for Jordan (a saved place only, T1) or for Sam (an unverified member). No ScopeChip is used, even though the surface draws 'Your area' around a specific address. The v2 EDGE CASES section must cover tier and permission variants, and the house invariant says scope is always stated.
  - evidence: prompt EDGE CASES: "A viewer with no address gets no 'Your area' outline." (the only tier case). house-style-v2.txt INVARIANTS 2: "Scope is always stated with one of two strings". component-contract.md ScopeChip: "It appears on every surface that shows address data."
  - fix: Add to LAYOUT: "The map centres on the address in the Nearby location row, which carries its ScopeChip: 'Your household' for HOME A." Add to EDGE CASES: "Jordan at PLACE B: the map centres on the saved place, the tag reads 'Your saved place', the ScopeChip reads 'Saved place · Only you', and the numbers unlock the same way because joins belong to the account. Sam (unverified member) sees what Maya sees at his own join count." Add these to FOUNDATIONS: "ScopeChip (location row)". Add an artboard: "f9-nearby-cells-map · web-390 · 13-saved-place-viewer · light", renumber the frames after it, and add both variants to Notes as assumptions.
- [minor] (ux) The phone sheet must be non-modal for the stated interaction to work, and the prompt never says so. A default iOS sheet or a Material ModalBottomSheet dims the map and blocks taps, so 'opening another area replaces the panel content' cannot happen. The prompt also never says the tab scrolls so the map (the third block) is visible above the sheet.
  - evidence: prompt PLATFORMS: "iOS 393x852: sheet at the medium detent, with the map visible above." / "Android 412x915: Material 3 bottom sheet". INTERACTION: "Opening another area replaces the panel content; sheets never stack."
  - fix: Add to INTERACTION: "On phones, tapping an area first scrolls Nearby so the map sits under the top bar, then opens the panel as a non-modal sheet: undimmed at the medium detent on iOS, a standard (non-modal) bottom sheet on Android. Areas above it stay tappable. VoiceOver and TalkBack focus moves to the panel title; Done returns focus to the tapped area."
- [minor] (ux) The phone control row contradicts itself: it is called 'one row', but it contains a 2x2 arrow pad, which is two rows of 44pt buttons.
  - evidence: prompt LAYOUT: "Put them in one row under the map, never over it: 'Zoom out' · 'Zoom in' · 'Center on your area' · a 2x2 arrow pad for panning."
  - fix: Replace with: "Two rows under the map: row 1 'Zoom out' · 'Zoom in' · 'Center on your area'; row 2 the four pan arrows 'Move left' · 'Move up' · 'Move down' · 'Move right'. Every button is 44pt (48dp on Android) with 8pt gaps."
- [minor] (ux) The 17 unnamed areas have no names or unlocked home counts. The list view, the spoken labels and the unlocked list rows all need both, so Claude Design will invent them without listing them.
  - evidence: prompt CONTENT: "The other 17 areas show a level only, mostly '0' or '1–5'." LAYOUT: "Rows read name · level · lock state. Unlocked rows also show homes".
  - fix: Add: "Name the other 17 areas by their nearest street, e.g. 'Near NE 172nd Ave', give each 0–2 homes with a verified address, and list every name and value on Notes."
- [minor] (ux) The list-row label '3 homes' reads as the total number of homes in the area, not the number with a verified address. That is a misleading count on a surface that exists to be honest.
  - evidence: prompt LAYOUT: "Unlocked rows also show homes ('3 homes')."
  - fix: Change to "'3 with a verified address'", and add that string to COPY.
- [minor] (ux) No frame shows the deep-link landing (the highlight, with focus on the panel title), yet DONE WHEN depends on it.
  - evidence: prompt DONE WHEN: "'See the map' on Invite rewards lands on an open Fircrest panel with focus on its title."
  - fix: In artboard 2's description, add: "arrived from 'See the map': a one-time highlight on the panel title and a visible focus ring".

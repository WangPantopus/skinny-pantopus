# Last review findings for 00b-05 (pass=True). If fixes were attempted afterwards they were not re-verified.

- [minor] The closed-state absence cell for the Block Founders panel also removes the rank section. A founder's RankBadge is a separate earned shape, and the prompt never says why a closed window hides a rank the viewer already holds.
  - evidence: 02: "block-founders-panel shows the panel without its tier and rank sections" (applies to both closed and lookup-failed).
  - fix: For closed, remove only the tier section (keep "#3"). For lookup-failed, remove both. Otherwise, add an open question on Notes E.
- [minor] 01 places the RankBadge under V1, which is the PLACE B preview of an unclaimed place. A Block Founder rank cannot exist in that context, so the anatomy mixes two specimens.
  - evidence: 01: "V1 at true size with every callout, the RankBadge 24pt below it"; V1 is "f9-founding-meter-preview, PLACE B ... You can still claim this address."
  - fix: Draw the RankBadge as a separate labelled part below the V1 card, with the gutter note "RankBadge · shown in V2 (HOME A) only".
- [minor] Notes A lists contract strings as 'invented' and leaves out some prompt-authored strings.
  - evidence: A lists "Founding Neighbor" and "3 of 5 open" (both in the contract). It omits "Solid vs hatched means taken vs open. Filled vs hollow means provenance only." and "Taken vs open by texture plus numeral."
  - fix: Replace the list with "every quoted string above that is not in the fixtures or the contract", keeping the normalised "Closes Tue 27 Oct".
- [minor] The heading uses bodyMedium, which is not in the house type scale, and the platforms for crops (d) and (e) are not given.
  - evidence: "Heading 'Founding Neighbor' in bodyMedium text.primary"; "(d) f9-verification-promise-copy and (e) f9-invite-rewards-card: each one line".
  - fix: Define bodyMedium 16/24/500 in the setup (as 00c does), and name a frame for (d) and (e), for example iOS 393.

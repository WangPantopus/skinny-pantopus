# Last review findings for 00a-07 (pass=False). If fixes were attempted afterwards they were not re-verified.

- [major] The f7-today-widget crop cannot be drawn as written. Each widget line is too wide for the 338×158 widget, and wrapping the lines would make the content taller than 158.
  - evidence: Crop c: "338×158pt ... Three lines; each is a 16pt glyph, its words, then an XS 12pt provenance mark with its word beside it", for example "Clark Public Utilities · in 4 days · Fri 23 Oct" (47 chars ≈ 290pt at 12pt) + "You added this" (≈ 85pt), plus glyph, mark and gaps ≈ 425pt. The house style's 16pt margins leave 306pt. Two lines per entry plus a "Larkspur Loop" header is about 170pt, which is more than 158.
  - fix: Use the 338×354pt widget size from the house style. Draw each entry as two lines: line 1 is the glyph and title, line 2 is the date, the XS mark and its word. Otherwise, state the exact wrap and type sizes and check that they fit 306×126.
- [major] The AX5 picker row keeps the label beside a 64pt tile. This contradicts setup B9 and the house style ("pairs stack"), and the long labels do not fit at 44pt.
  - evidence: Send 3: "The tile sits on the leading edge, grown to 64pt ... and the label at 44pt wraps beside it" with a trailing 24pt check. The width left is about 393 − 32 − 64 − 24 − 24 gaps ≈ 249pt. The single words "registration" and "Property-tax" are about 280–290pt at 44pt semibold, so they would have to break mid-word. Setup B9 says: "Pairs stack, text wraps and never ends in an ellipsis".
  - fix: At AX5 (and on web at 200%), stack the 64pt tile above the label inside each full-width row, and keep the trailing check aligned to the label. Alternatively, keep the beside layout and state that words never break mid-word, with the label getting the full width under the tile when a word does not fit. Update 07.A7 to match.
- [minor] The correction line and 07.A2 point to a setup rule B4 that says nothing about weights. In the split setup, B4 is Callouts, and B5 mentions only generic deviations. A literal model may start doubting B4's callout rules.
  - evidence: Send 1: "Correction to setup rule B4: no weight deviation is recorded." 07.A2: "this replaces the setup B4 mention". Setup B4 is "Callouts. Draw 1 px text.secondary leader lines ...".
  - fix: Delete the correction line. Reword 07.A2 to end at "No type-scale deviation is recorded."
- [minor] The selected tile has no visible edge. Its primary.50 fill is 1.07:1 against the base card, so the 42pt tile shape disappears on selection. That breaks the fill + glyph + edge pattern that ChoiceChip and AddressChip use on this board.
  - evidence: Part B selected: "fill primary.50 (dark: primary.900)" with no edge. Setup B7: "primary.50 on base 1.07 (fill, never an edge)".
  - fix: Add a 1.5px primary.700 edge to the selected tile (dark: primary.400 (dark link)), or record on Notes that the shape is carried by the glyph, corner dot and ink alone.
- [minor] The Part A row label text is not given for the 7 kinds that have no tile. The model has to invent sentence-case names (for example for garbage+recycling), and 07.B1 does not list them.
  - evidence: Part A row: "the glyph with its kind label in bodySmall". THE SET uses lowercase kind names such as "garbage+recycling" and "home event". 07.B1 lists only "the 17 glyph descriptions and keys".
  - fix: List the 17 row labels exactly, for example Garbage · Recycling · Recycling and garbage · Bulk pickup · ... · Package, and add the invented ones to 07.B1.
- [minor] The crop b caption puts a weekday on today's date, against setup B16.
  - evidence: "Waste Connections · city pickup schedule · as of Mon 19 Oct" (TODAY is Mon 19 Oct). B16: "'Today' ... stand[s] alone. A time today shows no weekday."
  - fix: Use "as of today" or a time ("as of 6:00 AM"), and update 07.B1.
- [minor] The Android M3 radio has no outer size. Only the ring stroke and the centre size are given.
  - evidence: "a 2px primary.700 ring with a 12pt filled centre"; "an empty 2px ring".
  - fix: State the outer diameter, for example "24dp ring (the 2× cap of the platform's 20dp control)".
- [minor] Send 2 is dense. It combines two full grids, a picker, dark twins and eight long Notes paragraphs, which runs against the research on instruction density.
  - evidence: Send 2 carries Part A (68 cells), Part B (20 cells), the picker, the dark twin and 07.A1–07.A8, 07.B2, 07.B3 and 07.D, at roughly 6K characters.
  - fix: Move 07.A4–07.A8 and 07.B3 to send 3, which already appends Notes, so send 2 stays focused on the grids.

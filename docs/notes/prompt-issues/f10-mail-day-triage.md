# Last review findings for f10-mail-day-triage (round 2). The fix after this round was applied but not re-verified.

- [minor] (substance) The rule 'only one filled control in view' conflicts with the Finish day rule when the list is empty. In the empty hero, and in finished variant (a), 'Scan today's stack' is filled and 'Finish day' would also be filled, which gives two filled controls.
  - evidence: Prompt: "'Finish day' is outlined. It becomes filled only when Needs a call is empty." and "It is filled only in the empty hero." and DONE WHEN "Only one filled control is in view."
  - fix: Add: "With zero pieces there is no Finish day footer. The hero's 'Scan today's stack' is the only filled control. In finished state (a), 'Finish day' is replaced by the finished line."
- [minor] (substance) It is unclear whether Alex Kim is in HOME A in the main scenario. The Notes make Alex a current guest (access until Sun 1 Nov), but the populated frames name only Maya and Sam. That leaves open whether Maya's own view should say that a guest sees less.
  - evidence: Notes: "Alex Kim as a guest with access until Sun 1 Nov"; component-contract ScopeChip doNot: "Never say 'Everyone in this household' where finance permissions differ: name the people instead"
  - fix: Pick one and say it: either Alex joins HOME A only in frame 15 (12-limited-access), or Maya's populated frames add the named-audience line the tray uses: "Only you and Sam can see these photos. Alex sees that mail arrived, not the photos or amounts."
- [minor] (substance) The Notes list of invented strings is incomplete. Several fixture senders and facts are not in the house fixtures and are not listed.
  - evidence: House style: "Any string you invent must be listed on the Notes artboard." Unlisted in the prompt: "State Farm · Renewal · 'Auto policy renews in 16 days · Wed 4 Nov'", "Clark County Elections · Notice · 'Voters' pamphlet…'", "Waste Connections · Service notice · 'Holiday pickup schedule'", "Chase", "Yesterday: 7 pieces, 3 minutes.", "Get a Mail Day reminder at 6:30 PM?", "Clark County Treasurer · Statement"
  - fix: Add these strings to the invented-strings item on frame 23.
- [minor] (substance) The legend string and the spoken name of the read-from-your-photo mark are not the contract's exact strings. Unlike the extraction prompt, the triage prompt does not flag them as an open decision.
  - evidence: component-contract ProvenanceMark copyRules: "Legend strings (exact): '● Official' · '○ On record, not confirmed' · '✓ You added this'"; the prompt uses "○ Read from your photo, not confirmed" and the spoken name "read from your photo, not confirmed"
  - fix: Add to Notes: "Open decision: the exact label of the read-from-your-photo variant (shared with f10-extraction-confirm)."
- [minor] (substance) The skeleton threshold is looser than the research brief. The brief reserves skeletons for cold loads of about 2 to 10 seconds.
  - evidence: ux-research-brief P11: "Skeletons only for cold full-page loads of about 2–10 s. No indicator under 1 s." Prompt: "Loading: over 1s, show row skeletons; under 1s, show nothing."
  - fix: Change to: "Under 1s, show nothing. On a cold load past about 2s, show row skeletons in their final slots."
- [minor] (substance) The link to 'Mail snaps: privacy and storage' is placed only in the web 1440 right column. Web 390 and native have no stated place for it.
  - evidence: Prompt: "a 320px right column holding the recap, the reminder card and a 'Mail snaps: privacy and storage' link"; derive-proposals f10-8 entry points: "Native: settings row from the Mail Day screen"
  - fix: State that on web 390 and native the link sits below the recap, next to 'Mail Day settings'.
- [major] (ux) The web 1440 layout is wider than the viewport. The fixed columns plus the real shell widths add up to more than 1440px before any gutters, so Claude Design will either overflow the frame or squeeze the columns in its own way.
  - evidence: Prompt: "Web 1440 has four parts: a left sidebar; the mailbox nav, with "Mail Day" selected; a 720px list column; a 320px right column". Code: frontend/apps/web/src/components/AppShell.tsx `const SIDEBAR_EXPANDED = 240;` and app/(app)/app/mailbox/layout.tsx `lg:w-[220px]` for the mailbox nav. 240 + 220 + 720 + 320 = 1500px.
  - fix: Replace the widths with: "a 240px left sidebar; the 220px mailbox nav; a list column that fills the remaining space (about 620px); a 300px right column; 24px gutters between columns."
- [major] (ux) The Decide options are drawn as radio rows, but one tap commits. With a radio group, pressing an arrow key changes the selection, so a keyboard or switch user would commit a decision just by moving through the options. The prompt also asks for a lasting 'selected' look on a control that commits at once.
  - evidence: ""Decide" expands the row in place into 48pt radio rows ... One tap commits" and "In the Decide rows, the selected option shows a filled radio and a bold label. This is distinct from the focus ring."
  - fix: Change to: "'Decide' expands the row in place into a list of full-width 48pt action buttons (no radio dots), each a verb phrase: 'Recycle paper, keep photo' ... Tab moves between them; Enter or a tap commits. After a commit, focus moves to the next Needs a call row, and a polite status reads 'Recycled paper, kept photo. Undo is on the row.'" Delete the sentence about the filled radio.
- [major] (ux) After a decision, the only visible Undo disappears. The decided row moves into a Reviewed today section that is collapsed, and only an optional snackbar is left on screen. The contract says the row collapses in place and keeps its Undo there. For a first-time user, the row seems to vanish with no visible way back.
  - evidence: "REVIEWED TODAY (6) is collapsed to one line: "Reviewed today · 6" with "Show""; "One tap commits, and the row moves to Reviewed today as an InlineUndo triage row."; "A snackbar may echo "Recycled · Undo"". Contract InlineUndo: "The row collapses in place to 'Removed ... · Undo' (44pt Undo) and persists until the user leaves the screen."
  - fix: Add: "A decided row collapses in place inside Needs a call to one line, 'Clark Public Utilities · Paper recycled · Photo kept · Undo', and stays there until Maya leaves the screen. On her next visit it is listed under Reviewed today, and the count updates ('Reviewed today · 7')." Add artboard "f10-mail-day-triage · web-390 · 02b-just-decided · light" showing that collapsed row. Change the motion line so the row collapses in place over 200ms (a cross-fade under Reduce Motion).
- [major] (ux) The not-confirmed word is printed on every photographed row, and a legend line states it as well. The contract says the word appears once per surface and never as a suffix on every row. In the worst case of 40 rows, the word repeats down the whole list.
  - evidence: Rows 1 and 4: "· "Not confirmed" ·"; "Not confirmed: ProvenanceMark S in its read-from-your-photo variant (ring), plus the words "Not confirmed"." Contract ProvenanceMark copyRules: "Print the word next to the glyph once per surface ... Never print a bare 'Unverified' suffix on every row."
  - fix: Keep the legend line once under the Needs a call heading. Remove the per-row words "Not confirmed". A row is marked as not confirmed by the ring, its warningBg fill with the left border, and its "Confirm what we read" button; line 2 already carries "due date needs a check". The spoken row label keeps the mark's name. Update frame 20 to say "the ring, the legend and the Confirm button carry it". Add to Notes: "The label for the read-from-your-photo variant is pending on the Foundations board."
- [minor] (ux) The storyboard conflicts with the fixtures. HOME A already lists Clark Public Utilities $142.18 due Fri 23 Oct and HOA $285 due Sun 1 Nov as bills, yet here they arrive as unconfirmed reads of new paper, so confirming them would create duplicates.
  - evidence: House style FIXTURES: "Bills: Clark Public Utilities $142.18 due Fri 23 Oct ... HOA dues $285 due Sun 1 Nov." Prompt rows 1 and 4 are unconfirmed reads of those same bills.
  - fix: Add to CONTENT: "Before tonight's scan, the bills list holds only Comcast (added from the 12:40 PM scan). Clark Public Utilities, City of Vancouver water and the HOA dues become the fixture bills once they are confirmed tonight."
- [minor] (ux) The empty state shows two identical "Scan today's stack" buttons at once, one outlined in the header and one filled in the hero. A new user has to work out which one to press.
  - evidence: "Zero pieces: show the empty hero. The header button stays present"; frame 9: "the hero with a filled "Scan today's stack" and the header button."
  - fix: In the empty state, keep only the hero's filled button. The header action appears once the hero scrolls out of view or once the list has any item. State this in EDGE CASES and in the frame 9 description.
- [minor] (ux) The retry label refers to a page number from the tray session on a row that says it has 3 pages, which is confusing.
  - evidence: ""3 pages" · "Upload failed · Retry" (... Retry reads "Retry page 8")"
  - fix: Use "Retry page 3 of 3" as the button label, and speak it as "Retry page 3 of the Chase letter".
- [minor] (ux) A photographed row that has not been confirmed has no way to be decided directly. A junk flyer has to go through Confirm, then Just file it, then Decide. The option set also leaves out one paper and photo pairing.
  - evidence: Row 1 actions: "Actions: Confirm what we read, See the piece."; photographed options list "Recycle paper · keep photo", "Recycle paper · delete photo", "Keep paper · keep photo".
  - fix: Add "Decide" as a secondary action (visible text button plus a custom action) on unconfirmed photographed rows. It files the piece without a bill. Add "Keep paper · delete photo" to the options.
- [minor] (ux) The prompt does not say what happens to Undo if Maya never taps Finish day. Finish day itself locks in photo deletions and has no way back.
  - evidence: ""Undo" stays on every reviewed row until "Finish day"." and ""Finish day" closes undo and collapses the page"
  - fix: Add: "If Finish day is never tapped, the day finishes itself at the next Mail Day date (the following morning). After Finish day, a status line reads 'Day finished · Undo' until Maya leaves the screen."
- [minor] (ux) The dark twin needs a dark version of the warning row fill, but the house tokens define no dark warningBg, so the dark twin will get an invented colour.
  - evidence: "The row fill is warningBg with a 2px warning left border"; house tokens list only "Dark: base · raised · app · text primary · secondary · border · focus/link".
  - fix: Add: "In dark mode the not-confirmed row uses surface raised (#1E293B) with the 2px warning left border and text primary. It has no tinted fill."
- [minor] (ux) The ATTACH list asks for a Today screenshot that no artboard uses, and frame 1 does not say whether the status line is visible even though two pieces have been read.
  - evidence: ATTACH: "Today tab on iOS (for the "Scan mail" chip)"; WHO AND WHEN: "two pieces have been read"; the reading order includes "status line".
  - fix: Remove the Today attachment. In frame 1, state: "the status line reads '2 pieces ready to check · Check now' under the header."

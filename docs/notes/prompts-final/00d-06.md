# Foundations 00d-06 — NotificationAsk
id: 00d-06 · clean: False · artboards: 8

Component 00d-06: NotificationAsk. Follow the board rules from the setup message.

PURPOSE (print on 01): The in-context notification permission ask and its denial states. It is never a second "Yes".
Used in (print on 01): f4-notification-primer, f4-briefing-optin-card, f4-today-pickup-card, f4-notification-settings, f3-household-notifications
ATTACH: none. The OS permission dialog is never drawn.
TAG: a string followed by (new) is not in the house style or the contract. Draw it without the tag and log it on Notes D.

ARTBOARDS (7, then extend Notes)
00d · NotificationAsk · 01-anatomy · light
00d · NotificationAsk · 02-card-row-states · light
00d · NotificationAsk · 03-primer-and-denial-by-platform · light
00d · NotificationAsk · 04-card-row-states · dark
00d · NotificationAsk · 05-primer-and-denial-by-platform · dark
00d · NotificationAsk · 06-ax5-200-greyscale · light
00d · NotificationAsk · 07-in-context-and-rules · light
00d · Notes (extend it)
Width: 02 and 04 are 2400px wide (the one exception to R2) so five 393pt columns fit at 1x.

KEYS (R7)
Grid variants (02 and 04): V1 Card row = card-row · V7 Time chips = time-chips.
Grid states: S1 Never asked = never-asked · S2 Granted = granted · S3 Declined = declined · S4 OS blocked = os-blocked · S5 Save status = save-status. The S5 cell holds three specimens, stacked, with the state keys saving, saved and save-failed.
Specimen outside the grid (in its own row below the grid on 02 and 04, labelled "Settings row · OS blocked" (new)): "NotificationAsk / settings-row / os-blocked / ios / <theme>".
Sheets (03 and 05): V2 = primer-ios, V3 = primer-android, their household twins, V4 = ios-denied, V5 = android-denied-once, V6 = android-blocked. The seven sheet names are exactly:
  "NotificationAsk / primer-ios / never-asked / ios / <theme>"
  "NotificationAsk / primer-android / never-asked / android / <theme>"
  "NotificationAsk / primer-ios-household / never-asked / ios / <theme>"
  "NotificationAsk / primer-android-household / never-asked / android / <theme>"
  "NotificationAsk / ios-denied / denied / ios / <theme>"
  "NotificationAsk / android-denied-once / denied-once / android / <theme>"
  "NotificationAsk / android-blocked / blocked / android / <theme>"
<theme> is light on 02 and 03, dark on 04 and 05.

ANATOMY (01-anatomy: the card row and the pickup primer on iOS 393, with callouts)
A. Card row form (on f4-briefing-optin-card). The briefing card is surface.base (radius lg 12, shadow sm) on surface.app, so the R10 "on surface.base" figures apply.
  1. Question in body 16/24 text.primary: "The night before pickup?"
  2. "Yes" and "Not now", side by side, equal weight. Both outlined: 1px text.secondary border (R10: 4.83:1), surface.base fill, bodyMedium text.primary label, radius md 8, 44pt tall, equal widths, 8pt gap. Callout: "Yes fires the OS prompt directly. The OS prompt is not drawn." (new)
  3. Granted collapse: the row becomes "6:00 PM · Change"; "Change" is a primary.700 text button (R10: 5.93:1) with a 44pt hit area.
  4. Time chips, shown after Change: "5:00 PM", "6:00 PM", "7:00 PM", "8:00 PM". Each chip: 44pt tall, radius pill, 12pt horizontal padding, 8pt gaps, bodySmall 14/20 text.primary label (600 when selected). Unselected: surface.base fill, 1px text.secondary border. Selected: the R12 selection (fill + 16pt check + 600 weight), 4pt between check and label. Wrap to a second line if the width runs out.
  5. Status slot at the row's trailing edge, caption 12/16 text.secondary: "Saving…" / "Saved" / "Couldn't save · Retry" (new: "Retry"), where Retry is a primary.700 text button with a 44pt hit area. "Saving…" is the only string on this component that ends in an ellipsis.
  Callout on the card row: "Appears only after an action that shows the purpose" (new).
B. Primer sheet form, only for the contract's two indirect entries: the first confirmed pickup and household activity from Mail. Draw the pickup entry; the household entry differs only in items 2–4.
  1. iOS sheet: surface.base, radius xl 16 on the top corners, 24pt side padding (345pt content). Height: a custom detent sized to content, not the medium detent (pickup about 514pt, household 538pt). Stack, top to bottom: 8 · Close row 44 ("Close" (new) text button at top right, 44pt hit area) · 8 · title 28 · 16 · tray panel about 144 · 16 · body 72 (3 lines; household: allow 4 lines = 96 even if the text wraps to 3, and keep the sheet at 538pt) · 12 · silence contract 40 (2 lines) · 24 · Continue 44 · 8 · caption 16 · home-indicator inset 34. Continue and its caption are pinned to the bottom at every size; at default size nothing scrolls. Print the stack as a spacing callout.
  2. Unique title, h3 20/28/600 text.primary. Pickup: "Pickup reminders on this phone" (new). Household: "Household updates on this phone" (new).
  3. The PushCopy tray preview (drawn exactly as here; PushCopy comes later), then 8pt, then "This is the whole thing." It is an in-app illustration, never a system alert: a surface.raised inset card (radius lg 12, padding 12) on a surface.sunken panel (radius lg, padding 16), holding the Pantopus app icon at 20pt (R15), "Pantopus" and "now" (new) in caption text.strong, the title in bodySmallMedium text.primary and the body in bodySmall text.primary. The caption is caption text.strong on the panel (R10: 9.37:1). Pickup: "Recycling + garbage tomorrow" / "Bins out tonight · Larkspur Loop". Household: "Sam marked Clark PUD paid" / "Due Fri 23 Oct · Larkspur Loop".
  4. Body, body 16/24 text.primary. Pickup: "Pickup the night before. Air alerts when it's unhealthy. Nothing else unless you turn it on." Household: "Pickup the night before. Air alerts when it's unhealthy. Household updates stay off until you turn them on, then arrive quietly." (new: the last sentence)
  5. Silence contract, bodySmall 14/20 text.secondary: "Only when something needs you. No news means nothing's up."
  6. iOS only: one forward button, "Continue": primary.700 fill, text.inverse label (R10: 5.93:1), full width, 44pt, radius md.
  7. Under it, caption text.secondary: "Your phone will ask next."
  8. Android only (on 03): a Material 3 bottom sheet sized to content. Geometry: a 24dp drag-handle row (a 32×4dp handle, centred) above the Close row; 24dp side padding (364dp content); Close hit area 48dp; the bottom inset is the 24dp gesture-navigation inset in place of 34. Otherwise the same order and gaps. In place of item 6: "Continue" and "Not now", both outlined (surface.base fill, 1px text.secondary border, bodyMedium text.primary label, 48dp tall, equal widths, 8dp gap, radius md). Neither is filled; the filled button is iOS-only. Item 7 follows the button row.

GRID (02-card-row-states · light, iOS): rows V1 and V7 × columns S1–S5, then the settings row specimen in its own row below.
- V1 Card row. S1: the row as drawn in A1–A2. S2: "6:00 PM · Change". S3 Declined: the card slot collapses (200 ms; static under Reduce Motion) and is replaced, once, by "Put Today on your home screen" (new) with an "Add the widget" (new) text button (primary.700, 44pt). That row never returns after this visit. S4: "Not applicable: shown on the settings row specimen". S5: "Not applicable: the card row saves only through the chips".
- V7 Time chips. S1: "Not applicable: chips appear only after granted". S2: the four chips, 6:00 PM selected. S3: "Not applicable: chips are hidden after a decline". S4: "Not applicable: shown on the settings row specimen". S5: three stacked chip-row specimens with the slot reading "Saving…", "Saved" and "Couldn't save · Retry"; in "Couldn't save", the chip Maya picked (7:00 PM) stays selected.
- Settings row specimen (os-blocked), below the grid: the f4-notification-settings Briefings group row, a surface.base group on surface.app. The Briefings switch shows on; beneath it, a 16pt warning glyph (semantic.warning, glyph only, R10: 3.19:1), then "Notifications are off for Pantopus. These rows still update inside the app." (new) in bodySmall text.primary, then an "Open Settings" text button (primary.700, 44pt).

SHEETS (03-primer-and-denial-by-platform · light): seven full sheets at platform width, each with its exact name and, under it, a caption (new) naming its entry.
- primer-ios: B, pickup. Caption "after the first confirmed pickup".
- primer-android: B with item 8, pickup. Caption "after the first confirmed pickup".
- primer-ios-household: B, household. Caption "after household activity from Mail".
- primer-android-household: B with item 8, household. Caption "after household activity from Mail".
- ios-denied (V4): the pickup iOS primer, same title and tray. Items 4 and 5 become "Notifications are off for Pantopus." (new) in body text.primary; item 6 becomes "Open Settings" in Continue's filled style; item 7 becomes "Opens the Notifications page for Pantopus in Settings." (new). No Continue. Close stays. The sheet shrinks to its content. Caption "after the iOS prompt was declined".
- android-denied-once (V5): the pickup Android primer unchanged: outlined "Continue" (asks again) and "Not now", then "Your phone will ask next." Caption "after the Android prompt was declined once".
- android-blocked (V6): the pickup Android sheet, same title and tray. Items 4 and 5 become "Notifications are off for Pantopus."; item 8 becomes "Open Settings" and "Not now" as equal outlined 48dp buttons; item 7 becomes "Opens the notification settings for Pantopus." (new). No "Your phone will ask next." and no Continue. Close stays. Caption "after the Android prompt was declined twice".

DARK (04 and 05, R9)
04-card-row-states · dark (2400px): the full 02 grid, Not applicable cells included, and the settings row specimen below it. The briefing card and settings group are dark raised on dark app. Outlined buttons and chips: #94A3B8 border (R10: 5.71:1). Selected chip: R12 dark selection. Change, Add the widget, Retry and Open Settings: #38BDF8 (R10: 6.83:1). Warning glyph: #D97706 (R10: 4.59:1).
05-primer-and-denial-by-platform · dark: all seven 03 sheets on dark base; the tray inset is dark raised on a dark base panel. iOS Continue and Open Settings: #38BDF8 fill, dark text.inverse label (R10: 8.28:1). Android buttons are all outlined: dark base fill, 1px #94A3B8 border (R10: 6.96:1), dark text primary label.

AX5 / 200% / GREYSCALE (06-ax5-200-greyscale · light, R17)
- (a) AX5 iOS: V1 S1 ("Yes" then "Not now", stacked full width, each at least 44pt), V7 S2 (one chip per line), and primer-ios (content scrolls behind the pinned Continue and caption, with scroll padding). Names use ios-ax5.
- (b) 200% Android: V1 S1, V7 S2 and primer-android, with 48dp buttons and chips and 8dp gaps. Names use android-200.
- (c) Greyscale of V1 S1 (the light default), V7 S2 and the settings row os-blocked specimen (the most state-heavy cell). Caption: "Reads without colour: Yes and Not now are the same outline; the selected chip has its check glyph and weight; the blocked row reads as a problem from its words and glyph shape."

ACCESSIBILITY
- Spoken, card row: "The night before pickup? Yes, button. Not now, button." Chips: "6:00 PM, selected, 2 of 4." (new). Status is announced in a polite live region ("Saved") without moving focus. Each primer title is unique and read first. Sheets trap focus; Close dismisses them (also swipe-down on iOS, Back on Android).
- Non-colour encoding: a selected chip has fill + check + weight; the blocked state has a glyph + words.
- Targets per R11: buttons and chips 44pt iOS, 48dp Android.

COPY: every string is given above; use it exactly.

PLATFORMS
- iOS: one filled button per sheet (Continue; Open Settings when denied, opening the app's Notifications page). Android: every button outlined; after one denial, Continue asks again.
- Web: not drawn (Notes 6.7). All platforms: never at cold launch, sign-up or save.

IN CONTEXT (07-in-context-and-rules · light, R17)
- f4-briefing-optin-card: the briefing card on Today for HOME A, with V1 at S1.
- f4-notification-primer: primer-ios over f4-today-pickup-card (PickupCard, R15), just after "Yes, Tuesday is right". This crop is 393 × 600pt so the whole sheet shows (an R17 exception).
- f4-notification-settings: the Briefings group with the settings row os-blocked specimen.
Also consumed by: f4-today-pickup-card, f3-household-notifications (the household primer, drawn on 03)

INSTEAD OF
- Instead of a primer button labelled "Yes" or "Allow", draw "Continue" with "Your phone will ask next." — because the decision already happened on the card.
- Instead of imitating the iOS or Android system dialog, draw the in-app sheet with the tray illustration — because a fake system dialog deceives.
- Instead of a permanent amber banner after Not now, close the slot and offer the widget row once — because "Not now" never asks again.
- Instead of a filled "Yes" beside a grey "Not now", or a filled Continue beside an outlined Not now on Android, draw both outlined — because declines are equal weight.
- Instead of the ask at launch or sign-up, draw it only after the first confirmed pickup, household activity from Mail, or a card tap — because people grant what they understand.

NOTES (extend 00d · Notes)
A. 6.1 The contract writes "Open Settings" (variants) and "Open settings" (S4). This board uses "Open Settings" everywhere, which breaks house-style sentence case. Question for the founder.
   6.2 V1: the glossary pairs "Not now" with a lasting decline, but the contract's card row has only "Yes / Not now"; "Yes" also breaks the house-style rule that buttons start with a verb. This board draws the contract. Questions: add "No thanks"? Replace "Yes" with a verb (for example "Remind me")?
   6.3 Entry points follow the contract (primer after the first confirmed pickup or household activity from Mail). Research brief section 2 item 8 instead asks inline after the first "That's my day" ("Remind you the night before? 6:00 PM") and goes straight to the OS dialog. Question: primer sheet, or inline row?
   6.4 The household primer title and body are invented; "arrive quietly" means iOS Passive / Android LOW. Confirm, and say whether this entry switches household activity on after the grant.
   6.5 "Put Today on your home screen" uses the tab name; the glossary calls the widget "Today at your address". Question: "Add Today at your address to your home screen"?
   6.6 V4 and V6 keep the pickup title and tray and replace the body, silence contract, buttons and caption. V6 has its own caption because "Your phone will ask next." is false once Android has blocked the app. The contract is silent. Confirm.
   6.7 Web: no web push in scope, so no web NotificationAsk is drawn. Question for the founder.
   6.8 OS blocked is a settings row specimen outside the grid, because it lives on f4-notification-settings. 02 and 04 are 2400px wide so the grid stays at 1x.
B. The primers are for HOME A (Maya), so the tray shows "Larkspur Loop". "Couldn't save" is drawn after Maya picked 7:00 PM. The detent heights (about 514pt and 538pt) come from the B1 stack; the household body is allowed 4 lines even if it wraps to 3.
C. The Pantopus app icon or PickupCard, if drawn as a fallback box.
D. Every string tagged (new) above, including the spoken chip label, and the chrome: the two anatomy callouts, the seven entry captions and the "Settings row · OS blocked" row label.
E. Light only: the ios-ax5 and android-200 frames. Review aids, never published: the greyscale copies. Omitted: NotificationAsk on web (6.7) and the OS permission dialog (never drawn).
F. Light: the 4 applicable grid cells (V1 S1–S3, V7 S2), the three S5 status specimens, the settings-row os-blocked specimen, the seven sheets, and the 06 ios-ax5 and android-200 frames (not the greyscale copies). Dark: the same grid cells and specimens, and the seven sheets.

DONE WHEN
- The 7 artboards exist with the exact names above (02 and 04 at 2400px), and Notes carries this component's entries.
- Both grids have the five Not applicable cells with reasons, and the settings row specimen in its labelled row below the grid.
- 03 and 05 each show seven named sheets with entry captions. V4 has no Continue; V6 has no "Your phone will ask next."
- No Android button is filled.
- No "(new)" tag is printed anywhere.
- Every button, chip and text button shows its dashed hit area.
- 06 has columns (a), (b) and (c); 07 has three labelled crops (the primer crop 393 × 600pt), the "Also consumed by" line and all five panels.

BATCH PLAN (at most 6 artboards per turn)
Batch 1 (this turn): 01-anatomy, 02-card-row-states light, 03-primer-and-denial-by-platform light, 04-card-row-states dark. Then stop and wait for "continue".
Batch 2: 05-primer-and-denial-by-platform dark, 06-ax5-200-greyscale, 07-in-context-and-rules, 00d · Notes (extend it). Then stop and wait for the next component prompt.
Before each batch, re-read the board rules R1–R19 and this whole message, including DONE WHEN.

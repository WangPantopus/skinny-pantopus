# Calendarly — Design-Parity Audit (iOS ⟷ Android ⟷ Design)

> Generated by a 3-way fan-out audit (82 designed screens + shared primitives). Design truth = `Calendarly/*-frames.jsx` (360pt-wide phone, **1px = 1pt iOS = 1dp Android**). Tokens = `colors_and_type.css`.
> Raw structured data: `parity-audit-raw.json`. Status legend: matches / minor / major / missing / notfound. `better` = which platform is closer to the design (the other should catch up).

## Headline

- Total designed screens audited: **82**
- **iOS closer on 64**, **Android closer on 13**, equal 5, both-poor 0
- iOS major/missing: **19** · Android major/missing: **47**
- notfound: iOS 1, Android 1

**Verdict:** iOS is the stronger port overall. Root cause is structural — iOS extracted shared scheduling chrome into single sources of truth (status pill, top bar, section-card modifier, slot row, identity theme, numbered wizard step-rail), while Android re-implemented these per-screen, so the same element drifts across dozens of screens. Design **tokens** (type scale, spacing, radii, pillar accents) are in full parity on both platforms — the divergence is entirely at the composite-primitive + per-screen-content layer.

## Fix progress

Branch `claude/calendarly-parity-fixes` (off `feature/calendarly`).

- ✅ **Phase 1a — Android shared primitives** (commit `3cea2ae7`): created `_shared/SchedulingStatusPill.kt` (canonical text-only chip mirroring iOS) + `_shared/SchedulingChrome.kt` (`SchedulingTopBar` 56dp, `schedulingCard()` modifier, `SchedulingMiniToggle`). Migrated ~10 drifting status chips (Hub, BookingPage, Bookings inbox/detail, roster, Manage booking, My bookings, Reschedule/cancel policy, Workflows list) onto `SchedulingStatusPill`. Verified: `:app:compileDebugKotlin` green. NOT yet visually verified on emulator.
- ⏳ **Phase 1b — adopt `SchedulingTopBar` + `schedulingCard()`** across the Material3-`CenterAlignedTopAppBar` screens (default 64dp vs design 56) and the ~60 bespoke card composables. Bigger blast radius (restructures Scaffold topBar slots) → do with emulator screenshot verification.
- ⏳ **Phase 1c — numbered `WizardStepRail`** for onboarding/setup wizards (Android currently uses a segmented bar; design + iOS use numbered discs + per-step labels + "step X of N" overline).
- ✅ **iOS baseline repair** (commit `10024f67`) — the iOS branch **did not compile** (XcodeGen globs all sources into one target; Calendarly collided with Homes/ContentDetail). Fixed 4 filename + 4 type collisions, 2 missing `import SwiftUI`, a Swift-6 actor-isolation init, a Text-only modifier on a TextField, a non-associative comparison, a misplaced `.disabled` on a closure, and added `accentBg` to ~17 VMs. `make build` → **BUILD SUCCEEDED** (Xcode 26.2). This was the prerequisite for ALL iOS parity work.
- ✅ **Phase 2 — iOS catch-up** (commits `1d63d1fc`, `df9897f2`, `5e955941`): A1 Hub (tab-root bar), B1 event-types list (bespoke: toggle/dot-row/segmented filter/template chips), B2/B3, Availability (schedules/weekly/limits/block-off), D/E Reschedule, E12 Manual, F9 Resources list/F13 Visit setup, C1 landing, slot-states, F6 poll, G11 packages, G12/G13 invoices — all `make build` green. Remaining iOS: A12 onboarding-business (minor), G7 Payouts (minor), F14 Visit detail, B8 connected calendars (minor), + shared SlotPicker/Icons.swift slot-states frames (cross-cutting), + data-blocked group/team/composed frames.
- 🔄 **Phase 3 — Android content catch-up** (47 major/missing), batched by section. Verification is **compile-only** per user direction (no emulator this pass).
  - ✅ batch 1 — invitee **Confirm/Edge/Customer** (commit `ba3c81d1`): D1 intake (prefill chip, existing-account banner, phone reorder, typed Select, expired/submitting dim), D2 review (refund-policy link, confirming dim+shimmer CTA), D3 confirmed (halo geometry, mono TXN receipt line, email-sending state, reduce-motion confetti), D4 manage (expired footer CTAs, contact-host, accent book-again, spec skeleton), Edge sheets (add-to-cal/open-in-app/payment-failed/policy-blocked/recurring/slot-taken/terminal), My bookings. Data-contract-limited frames deferred (documented, not faked). Compiles green.
  - ✅ batch 2 — **Bookings core + extras** (commit `3dcfcda0`): E2 detail (status timeline, per-status dock, conflict banner, follow-up promo), cancel/refund (segmented presets, restore-credit, terminal frames), E7/E8/E9/E10/E11/E13 extras. Split oversized BookingFilterSheet under the 64KB method limit. Compiles green.
  - ✅ batch 3 — **Find-a-time + Resources/Visits** (commit `78ab45d2`): F4/F6/F7 + F9–F14. Compiles green.
  - ✅ batch 4 — **Event-types + Discovery** (commit `ea33740b`): B2 editor (Collect segmented, Stripe retint), B3 intake (drag-reorder, identity overline), B8 connected-calendars (provider rows + variants), C1 landing, slot-states. Compiles green.
  - ✅ batch 5 — **Business/Payments/Packages/Invoices/Onboarding** (this commit): G5 (skeleton geom + Stripe disc), G6 (SchedulingTopBar + receipt chip + tax toggle), G11 my-packages, G12/G13 invoices (design-primary), A12 onboarding-home (removed off-spec flow toggle). Compiles green.
  - ⏳ remaining: Home calendar **F2/F3** (in `homes/calendar` module — separate care), and the deferred **top-bar 56dp chrome** migration (needs inset handling + per-screen action variation — best with emulator verification).

**Build notes (critical):** use `JAVA_HOME=…/temurin-17`, kill stray Gradle/Kotlin daemons before each build, set `-Pkotlin.daemon.jvmargs="-Xmx10240m …"` (the project's 4g OOMs on full compile), and never let fan-out agents run their own `./gradlew` (single-worker deadlock). See memory `calendarly-android-parity`. ktlint/detekt/token gates are pre-RED on untouched files (out of scope).

## Remaining work & verification notes

- **Resolved — Embed widget (was "notfound both"):** the design itself states it is **web-only** ("Embed / inline booking widget settings (web)" — surfaces /book onto external sites). Correctly absent on iOS/Android; not a gap.
- ✅ **Android top-bar 56dp chrome (DONE — commits `934824ab`, `ceb259ea`):** enhanced `SchedulingTopBar` to be status-bar-inset-aware + a `trailing` slot, migrated all 10 `CenterAlignedTopAppBar` screens (resources/visits/bookings_extra) from 64dp → 56dp. Verified via 3 Paparazzi snapshots (back+overflow / close+Save / status-inset) + `assembleDebug`. Also repaired the scheduling **test target** compile (StatusChip/statusLabel/eventTypeName regressions + pre-existing EventTypeDto.slug). Was: needs-emulator — — ~10 screens use Material3 `CenterAlignedTopAppBar` (64dp). `SchedulingTopBar` (56dp) exists, but migrating requires per-screen status-bar-inset handling (Scaffold-topBar-slot vs in-content) + a trailing slot for the screens with text "Save"/progress actions; compile can't catch under-status-bar rendering, so this should be done with an emulator. Screens: visits/VisitDetail+VisitSetup, resources/Book+Detail+Editor, bookings_extra/Waitlist+ManualBooking+BookingSearchFilter+PostMeetingFollowup+GroupRoster.
- **Polish (low value):** numbered `WizardStepRail` (Android wizards use a segmented bar; needs shared-WizardShell coordination); icon-glyph nits (Android Material has no exact lucide calendar-range/git-commit/etc.; iOS calendarSearch SF remap is 1 line).
- **Data-blocked (no DTO/VM/backend fields — never faked):** group/team/composed-availability frames, itemized fees/tax + saved-card/3DS payment frames, visit offer/reserve/booking-link lifecycle, per-member Free/Busy, live calendar-conflict detection, masked bank/tax-rate literals. Documented per-screen above.
- **Product decision:** the A5 "THIS MONTH" summary card is injected on the hub by BOTH platforms but the A1 design doesn't show it — confirm whether to keep (consistent across platforms) or remove.

## Status table

| Section | Screen | iOS | Android | Better | iOS h/m/l | And h/m/l |
|---|---|---|---|---|---|---|
| Primitives | SHARED PRIMITIVES & CHROME | minor | major | **ios** | 0/0/3 | 3/2/2 |
| Setup | A1 Scheduling Hub | major | minor | **android** | 2/1/2 | 1/2/3 |
| Setup | A12 Scheduling onboarding (Business) | minor | minor | **android** | 0/2/3 | 0/2/4 |
| Setup | A12 Scheduling onboarding (Home) | minor | major | **ios** | 0/0/3 | 2/3/1 |
| Setup | A2 First-run booking-link setup wizard | matches | minor | **ios** | 0/0/3 | 1/2/4 |
| Setup | A3 Booking settings | matches | minor | **ios** | 0/0/4 | 1/1/4 |
| Setup | A4 Scheduling notifications | minor | minor | **ios** | 0/1/3 | 0/2/2 |
| Setup | A5 Scheduling summary card | matches | minor | **ios** | 0/0/2 | 0/2/3 |
| Event types | B1 Event types list | major | minor | **android** | 3/3/2 | 0/2/4 |
| Event types | B2 Event type editor | major | major | **ios** | 0/4/3 | 1/3/3 |
| Event types | B3 Intake questions editor | major | major | **android** | 3/3/2 | 1/3/4 |
| Event types | B8 Connected calendars | minor | major | **ios** | 0/0/5 | 4/3/1 |
| Availability | C Availability schedules | major | minor | **android** | 3/3/2 | 0/0/4 |
| Availability | C Block off time | major | minor | **android** | 2/2/3 | 0/1/3 |
| Availability | C Booking limits | major | minor | **android** | 3/1/2 | 1/1/3 |
| Availability | C Date overrides | minor | major | **ios** | 0/1/3 | 1/2/3 |
| Availability | C Weekly hours editor | major | minor | **android** | 3/3/2 | 0/2/6 |
| Booking page | C Booking page preview | matches | minor | **ios** | 0/0/2 | 0/3/2 |
| Booking page | C Embed widget settings | notfound | notfound | **equal** | 1/0/0 | 1/0/0 |
| Booking page | C One-off booking link | minor | major | **ios** | 0/0/4 | 1/2/5 |
| Booking page | C Share booking link | matches | minor | **ios** | 0/0/2 | 0/1/5 |
| Booking page | C4 Booking link / page management | minor | minor | **ios** | 0/0/4 | 0/1/4 |
| Discovery | C Slot states (no-availability etc) | major | major | **ios** | 2/2/2 | 3/2/2 |
| Discovery | C Timezone selector | minor | major | **ios** | 0/0/3 | 1/2/4 |
| Discovery | C1 Booking landing | major | major | **ios** | 2/1/3 | 3/3/2 |
| Discovery | C2 Slot picker | minor | minor | **ios** | 0/1/3 | 0/2/3 |
| Confirm | D Booking confirmed | minor | major | **ios** | 0/1/4 | 1/4/3 |
| Confirm | D Intake booking form (invitee) | minor | major | **ios** | 0/1/3 | 2/2/2 |
| Confirm | D Manage your booking | minor | major | **ios** | 0/2/3 | 2/2/3 |
| Confirm | D My bookings (customer) | minor | major | **ios** | 0/1/4 | 2/4/2 |
| Confirm | D Review and confirm | minor | major | **ios** | 0/3/2 | 1/4/3 |
| Confirm | D/E Reschedule | major | minor | **android** | 2/1/3 | 0/1/5 |
| Edge | D Add to calendar sheet | matches | major | **ios** | 0/0/1 | 4/2/2 |
| Edge | D Cancellation policy sheet | minor | minor | **ios** | 0/1/3 | 0/0/5 |
| Edge | D Open in app handoff | minor | major | **ios** | 0/0/4 | 2/5/0 |
| Edge | D Payment failed sheet | minor | major | **ios** | 1/2/3 | 2/2/3 |
| Edge | D Policy blocked | minor | major | **ios** | 0/0/4 | 2/4/2 |
| Edge | D Recurring series setup | minor | major | **ios** | 0/2/2 | 4/3/1 |
| Edge | D Slot taken sheet | minor | major | **ios** | 2/0/2 | 2/2/3 |
| Edge | D Unavailable/terminal states | minor | major | **ios** | 0/1/3 | 2/2/4 |
| Bookings | E Cancel / refund sheet | matches | major | **ios** | 0/0/3 | 3/1/3 |
| Bookings | E1 Bookings inbox | minor | minor | **ios** | 0/2/3 | 0/2/5 |
| Bookings | E2 Booking detail | minor | major | **ios** | 0/0/4 | 3/3/2 |
| Bookings | E3 Approve / decline sheet | matches | minor | **ios** | 0/0/3 | 1/2/4 |
| Bookings extras | E10 Double-book warning | matches | major | **ios** | 0/0/1 | 4/3/1 |
| Bookings extras | E11 Send a nudge | minor | major | **ios** | 0/0/3 | 2/3/2 |
| Bookings extras | E12 Manual / on-behalf booking | major | major | **android** | 3/1/2 | 2/2/4 |
| Bookings extras | E13 Waitlist join & management | minor | major | **ios** | 0/2/3 | 3/3/2 |
| Bookings extras | E6 Mark no-show | matches | minor | **ios** | 0/0/3 | 0/3/4 |
| Bookings extras | E7 Post-meeting follow-up | matches | major | **ios** | 0/0/2 | 3/4/1 |
| Bookings extras | E8 Group roster & seats | minor | major | **ios** | 0/0/3 | 4/3/1 |
| Bookings extras | E9 Booking search & filter | minor | major | **ios** | 0/0/3 | 2/3/3 |
| Home | F1 Home calendar | matches | minor | **ios** | 0/0/2 | 0/3/5 |
| Home | F15 Permission-gated scheduler | matches | minor | **ios** | 0/0/2 | 0/0/4 |
| Home | F2 Home event detail | minor | major | **ios** | 0/3/2 | 1/3/3 |
| Home | F3 Home add-edit event | minor | major | **ios** | 0/1/3 | 2/5/1 |
| Home | F8 My availability settings | matches | minor | **ios** | 0/0/2 | 0/3/2 |
| Find-a-time | F4 Find a time setup | minor | major | **ios** | 0/0/4 | 3/3/1 |
| Find-a-time | F5 Find a time slots | matches | minor | **ios** | 0/0/2 | 0/4/4 |
| Find-a-time | F6 Member poll response | major | major | **ios** | 1/2/3 | 2/2/2 |
| Find-a-time | F7 Whos free | matches | major | **ios** | 0/0/3 | 2/4/2 |
| Resources | F10 Resource editor | minor | major | **ios** | 0/2/3 | 2/4/2 |
| Resources | F11 Resource detail | matches | major | **ios** | 0/0/4 | 3/2/3 |
| Resources | F12 Book a resource | minor | major | **ios** | 0/0/3 | 2/1/2 |
| Resources | F9 Resources list | major | major | **android** | 1/3/2 | 2/2/2 |
| Visits | F13 Schedule a visit setup | major | major | **android** | 3/1/3 | 3/0/3 |
| Visits | F14 Visit detail | major | major | **ios** | 2/1/2 | 2/2/1 |
| Business | G1 Round-robin assignment | matches | minor | **ios** | 0/0/2 | 0/2/5 |
| Business | G2 Collective event setup | matches | minor | **ios** | 0/0/2 | 1/1/3 |
| Business | G3 Team booking availability | minor | minor | **ios** | 0/1/3 | 0/1/3 |
| Business | G4 Member working-hours editor | matches | minor | **ios** | 0/0/2 | 1/1/5 |
| Business | G5 Business scheduling settings | minor | major | **ios** | 0/0/4 | 2/2/4 |
| Packages | G10 Buy package | minor | minor | **ios** | 0/0/4 | 0/2/3 |
| Packages | G11 My packages | major | major | **ios** | 2/3/0 | 2/4/1 |
| Packages | G8 Packages list | matches | minor | **ios** | 0/0/3 | 0/1/4 |
| Packages | G9 Create/edit package | minor | minor | **equal** | 1/1/3 | 1/1/3 |
| Payments | G6 Payments setup | minor | major | **ios** | 0/2/2 | 1/4/1 |
| Payments | G7 Payouts and earnings | minor | minor | **android** | 0/0/5 | 0/0/4 |
| Invoices | G12 Invoices list | major | major | **equal** | 2/1/2 | 2/1/2 |
| Invoices | G13 Invoice detail | major | major | **equal** | 5/2/1 | 5/2/1 |
| Automations | H1 Default reminders | matches | matches | **equal** | 0/0/3 | 0/0/4 |
| Automations | H2 Workflows list | matches | minor | **ios** | 0/0/1 | 0/1/3 |

## Detailed findings by section

## Primitives

### SHARED PRIMITIVES & CHROME — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingIdentityTheme.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingStatusPill.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingSlotRow.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/SetupKit.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/WizardStepViews.swift`, `frontend/apps/ios/Pantopus/Core/Design/Radii.swift`, `frontend/apps/ios/Pantopus/Core/Design/Typography.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/SlotPicker.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/PausedExpiredUnavailableState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/HubComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/customer/MyBookingsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/BookResourceScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/HomeSchedulingKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/StatusChip.kt`
- **Verdict:** iOS centralizes the shared chrome into single canonical primitives (one status pill, one slot row, one identity theme, one 56pt top bar, one numbered StepRail) that track the design archetypes closely; Android lacks most of these shared primitives, so the status pill, top bar, section card, and wizard step rail are each re-implemented inconsistently across dozens of screens and drift from the design grammar.
- **Cross note:** Token foundations are in full parity and both correct: type scale (h1 30/h2 24/h3 20/body 16/small 14/cap 12/overline 11 +0.06em), spacing (4/8/12/16/20/24/32), and radii (xs4/sm6/md8/lg12/xl16/xl2 20/xl3 24/pill) are defined identically in iOS (Typography.swift, Radii.swift) and Android (Typography.kt, Radii.kt, Spacing.kt) and match colors_and_type.css exactly; pillar accents #0284c7/#16a34a/#7c3aed are correct on both. The divergence is entirely at the composite-primitive layer: iOS extracted shared scheduling chrome (status pill, slot row, identity theme, top bar, step rail, card modifier) into single sources of truth, while Android only extracted a subset (OwnerPillarChrome, SlotPicker, terminal-state) and left the status pill, top bar, section card, and wizard step rail to be re-implemented per-screen — which is why the same visual element drifts across many Android screens. The systematic fix is to port iOS's shared primitives (SchedulingStatusPill, a SchedulingTopBar, a SchedulingCard modifier, and a numbered WizardStepRail) into Android's scheduling/_shared and migrate the per-file copies onto them.

**iOS deviations:**
- `low`/`chrome` — iOS SetupTopBar is fixed at exactly 56pt for hub/settings screens, but the design archetypes vary the bar height by surface: home-shell/event-editor TopBar=46, settings-archetype=52, onboarding=56. iOS standardizes on the onboarding height everywhere, so the editor/detail surfaces read slightly taller than their frames. **Fix:** This is a reasonable simplification (one canonical 56pt bar) and arguably better than slavishly matching 46/52; keep as-is unless a specific frame review flags the editor bar as too tall.
- `low`/`icon` — The Business pillar glyph is intentionally split: SchedulingIdentityTheme.icon uses .briefcase (editors/onboarding/links) while SetupKit's SchedulingPillarChoice.icon overrides to .store for the A1 hub switcher. This matches the design (event-editor uses briefcase; hub-frames use store) but the two-source-of-truth split is a parity hazard Android must mirror exactly. **Fix:** Document the briefcase-vs-store split as the canonical contract so Android's pillar() and hub chrome reproduce it; no code change needed on iOS.
- `low`/`color` — SchedulingStatusPill maps .pending and .draft to the warning (amber) tone, matching the design's amber pending/draft chip. This is correct, but it is the single source that Android's many pills must match — Android currently disagrees (see Android findings). **Fix:** No change; this is the reference behavior.

**Android deviations:**
- `high`/`chrome` — No canonical scheduling status pill exists. The status chip is re-implemented at least 5+ times with divergent grammar: HubStatusPill (HubComponents.kt) adds a leading icon, has NO border, font 10sp, and colors 'Completed' green; MyBookingsScreen.StatusPill is text-only WITH a hairline border (closest to spec) but colors 'Pending' info-blue; HomeSchedulingKit.SchedulingStatusBadge is text-only, NO border, font 9.5sp; BookResourceScreen.StatusPill is a full-width icon+text row. The design + iOS SchedulingStatusPill spec is: text-only (no icon), font 10/weight 700, 3x8 padding, tinted fill + 1px tone-light hairline border, neutral grey for paused/cancelled/completed/past/expired. None of the Android variants fully match and they disagree with each other across screens. **Fix:** Add a single SchedulingStatusPill composable in scheduling/_shared mirroring iOS SchedulingStatusPill (SchedulingPillStatus enum with backend-alias init, text-only, tone fill + hairline border, neutral grey for terminal/completed states, testTag scheduling.statusPill.{status}). Replace HubStatusPill, MyBookings.StatusPill, SchedulingStatusBadge, BookResource.StatusPill usages with it.
- `high`/`chrome` — No shared scheduling top app bar primitive. OwnerPillarHeader exists but is used by only 4 of ~229 scheduling files; everything else mixes Material3 CenterAlignedTopAppBar (10+ files: ResourceEditor, VisitSetup, ManualBooking, Waitlist, etc.) which defaults to 64dp (taller than every design frame: 46/52/56) and Material navigationIcon spacing, plus bespoke Row top bars at 44/56dp (Hub, Settings, sheets). The design uses a consistent chevron-left + centered 16/600 title + ~56 height bar. iOS funnels all non-wizard surfaces through one SetupTopBar (56pt, chevron, centered semibold title, optional trailing icon). **Fix:** Introduce a shared SchedulingTopBar composable (height 56dp, chevron-left leading 36dp hit target, centered title PantopusTextStyle h3/16-600, optional trailing icon, 1px bottom appBorder) and migrate the CenterAlignedTopAppBar + bespoke-Row screens onto it so height, title weight, and back-affordance match the frames and iOS.
- `high`/`chrome` — No shared section-card primitive — ~60 scheduling files each define their own private Card composable (HubCard, EventTypeKit card, BusinessKit, AvailabilityComponents, InsightsComponents, etc.). The design's card is fixed: white surface, 1px #e5e7eb border, 16px (xl) radius, shadow-sm. Most Android copies hardcode CARD_RADIUS=16.dp locally (HubComponents) and replicate the border/shadow by hand, so radius/border/shadow can drift per screen. iOS centralizes this as the View.setupCard(radius:shadow:) modifier reused everywhere. **Fix:** Add a shared SchedulingCard composable or Modifier.schedulingCard() (appSurface, 1dp appBorder, Radii.xl, pantopusShadow .sm) and replace the per-file Card definitions so the surface contract is single-sourced like iOS setupCard.
- `med`/`chrome` — Wizard step chrome diverges from the design archetype. The onboarding-shell defines a numbered StepRail inside a card body: 22px numbered discs, check-on-done, 2px connectors, active-disc double ring, per-step labels, and a 'You're on step X of N' overline. iOS reproduces this exactly via WizardStepRail (WizardStepViews.swift). Android's shared WizardShell instead renders a continuous SegmentedProgressBar in the chrome plus a plain 'N of M' caption in the top bar — there are no numbered discs, no per-step labels, and no 'You're on step X of N' overline. **Fix:** Either add a WizardStepRail block composable (numbered discs + labels + overline) for scheduling onboarding/setup steps to match the design and iOS, or accept the segmented bar as an intentional Android-wide wizard idiom and confirm with design — but currently the scheduling wizards read materially different from the frames.
- `med`/`color` — Status-pill tone mapping is wrong/inconsistent on several Android variants vs the design+iOS spec. HubStatusPill colors 'Completed' with successBg/success green (design+iOS = neutral grey); MyBookings.StatusPill colors 'Pending' with infoBg/info blue (design+iOS = warning amber). The design grammar is: green=confirmed/active, amber=pending/draft, red=declined/no-show, neutral grey=cancelled/completed/past/paused/expired/etc. **Fix:** When consolidating onto a single SchedulingStatusPill, adopt the iOS tone table verbatim: pending/draft -> warning, confirmed/active -> success, declined/noShow -> error, and all terminal states (cancelled/completed/past/paused/secret/expired/unavailable/waitlisted) -> neutral grey.
- `low`/`color` — SlotPicker selected/today accent defaults to primary600 (sky) and SlotRow's chosen text uses primary700 regardless of pillar, whereas the design tints the selected slot / today disc with the host's pillar accent (e.g. Home green in home-shell). The accent param is plumbed through but several call sites and the chosen-text color fall back to sky rather than the pillar. **Fix:** Ensure every SlotTimeList/MonthCalendar call passes pillar.accent, and make the chosen-row text color derive from the accent (not hardcoded primary700) so Home/Business slots read green/violet as in the frames; iOS SchedulingSlotRow already takes accent for both fill and text.
- `low`/`sizing` — Toggle sizing is inconsistent across Android scheduling chrome: OwnerPillarChrome has no toggle, biz-kit-equivalent screens use Material3 Switch (HubComponents imports Switch/SwitchDefaults ~51x31), while the design + iOS use a compact 32x18 mini-toggle (SetupMiniToggle) for wizard rows and a 36x20 / 51x31 toggle for settings rows depending on surface. Android leans on the Material Switch default, which is visually heavier than the design's 32x18 wizard toggle. **Fix:** Add a shared SchedulingMiniToggle (32x18, pillar/accent fill, 14dp knob) mirroring iOS SetupMiniToggle for wizard/weekly-hours rows; reserve the larger toggle for settings rows to match the two design idioms.

## Setup

### A1 Scheduling Hub — iOS `major` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Hub/SchedulingHubScreen.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Hub/HubComponents.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Hub/HubSummaryCard.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/SetupKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/SchedulingHubScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/HubComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/HubStates.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/SummaryCard.kt`
- **Verdict:** Both platforms render the full A1 hub layout faithfully (pill band, booking-link card with live preview, pause/paused/read-only status rows, agenda, Manage chevron group, pinned footer) and all six frame states, but both inject an extra "THIS MONTH" A5 summary card at the top that the A1 design never shows; iOS additionally renders a back chevron and drops the trailing overflow-dots that the design's tab-root top bar specifies, which Android gets right.
- **Cross note:** Shared deviation: both platforms inject the A5 'THIS MONTH' summary card at the top of the loaded hub, which the A1 spec does not include; both otherwise cover all 6 frames (default/empty/skeleton/paused/home/permission-gated). Key divergence: Android renders the correct trailing more-horizontal/info overflow action in the top bar while iOS omits the trailing action entirely; both wrongly show a back chevron the tab-root design lacks.

**iOS deviations:**
- `high`/`chrome` — SetupTopBar is invoked with leading: .back, rendering a chevronLeft back button; the design's TopBar is a tab-root with an empty 36px leading spacer (no back chevron). **Fix:** Pass leading: .none so the leading control is Color.clear, matching the tab-root frame.
- `high`/`chrome` — trailingIcon is nil when canEdit, so the editable hub has no trailing action; the design always shows a more-horizontal overflow button in the top bar. **Fix:** Set trailingIcon: model.canEdit ? .moreHorizontal : .info and wire an overflow menu action.
- `med`/`extra-element` — loadedBody renders HubSummaryCard (the A5 'THIS MONTH' stats card) above the booking-link card; the A1 FrameDefault goes IdentityPills -> BookingLinkCard -> PauseRow with no summary card. **Fix:** Remove HubSummaryCard from the A1 hub (or gate it behind a separate A5 surface) to match the spec ordering.
- `low`/`icon` — Empty-state 'Connected calendars' placeholder uses .calendarClock; the design's collapsed placeholder and Manage row use calendar-sync. **Fix:** Use the calendarSync glyph for the Connected calendars placeholder row.
- `low`/`color` — HubPausedBanner background uses warningBg.opacity(0.4) to approximate the design's warningSoft (#fffbeb); the design uses a distinct dedicated soft-amber token, not a 40% tint. **Fix:** Use a warningSoft token rather than an opacity tint of warningBg.

**Android deviations:**
- `high`/`chrome` — HubTopBar leading is a ChevronLeft back button (testTag schedulingTopBarBack); the design's tab-root TopBar has an empty 36px leading spacer with no back chevron. **Fix:** Replace the leading ChevronLeft box with an empty 36dp Spacer/Box to match the tab-root frame.
- `med`/`extra-element` — HubLoadedBody renders SummaryCard (the A5 'THIS MONTH' stats block) above BookingLinkCard; the A1 FrameDefault has no summary card before the booking-link card. **Fix:** Remove SummaryCard from the A1 hub body to match the spec section ordering.
- `med`/`sizing` — HubPauseRow uses a Material3 Switch (~52x32dp track, large thumb); the design Toggle is a compact 32x18 track with a 14px thumb. **Fix:** Replace the Material3 Switch with a custom 32x18dp pill toggle matching SetupMiniToggle on iOS.
- `low`/`icon` — Business pill uses PantopusIcon.Briefcase; the design PILLAR.business icon is 'store'. **Fix:** Use a store/storefront glyph for the Business identity pill.
- `low`/`icon` — Empty-state placeholder rows use Grid3x3 and CalendarCog; the design uses layout-grid and calendar-sync for Event types / Connected calendars. **Fix:** Swap to layoutGrid and calendarSync glyphs to match the spec placeholders.
- `low`/`color` — HubPausedBanner uses warningBg as its fill; the design's PausedBanner background is warningSoft (#fffbeb), a lighter amber than warningBg (#fef3c7). **Fix:** Use a warningSoft token for the paused-banner background.

### A12 Scheduling onboarding (Business) — iOS `minor` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Onboarding/SchedulingOnboardingScreen.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Onboarding/SchedulingOnboardingModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/WizardStepViews.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/SetupKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/OnboardingHomeBusinessScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/OnboardingSteps.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/SetupSteps.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/wizard/WizardShell.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/OnboardingHomeBusinessViewModel.kt`
- **Verdict:** Both platforms faithfully reproduce the 4-step business wizard (chrome, step rail, slug field, service tiles, team seats, segmented confirm, success hero) via a shared WizardShell; the main divergences are iOS dropping copy on the team-seat counter/invite row and Android swapping the duration dropdown for a chip stepper and oversizing service-tile icons.
- **Cross note:** Both share the same WizardShell chrome and step blocks, so most geometry is identical and correct; divergence is split — iOS has the better Service step (dropdown duration, 15px icon, pencil adorn, unconditional price) while Android has the better Team step (correct "N of 5 seats used" counter and "2 seats left on your plan" invite copy). Both omit the 4th front-desk teammate and the step rail on the success screen.

**iOS deviations:**
- `med`/`typography` — Team-seats counter in OnboardingTeamList shows "\(seatedTeam.count) seated" but the design (TeamList) reads "3 of 5 seats used" — drops the plan-capacity copy. **Fix:** Change the trailing Text to "\(model.seatedTeam.count) of 5 seats used".
- `med`/`missing-element` — OnboardingTeamList.inviteRow subtitle is "Add by phone or email"; design TeamList invite row reads "2 seats left on your plan". **Fix:** Update the invite-row sub Text to "2 seats left on your plan".
- `low`/`missing-element` — Team list seeds only 3 teammates (You/Priya/Marcus); design TeamList has 4 including "Dana W." Front-desk (Not seated) — the front-desk role the subcopy references is absent. **Fix:** Add a 4th Teammate(id:"t4", name:"Dana W.", role:"Front desk") defaulted off.
- `low`/`missing-element` — Success step (successBody) renders only WizardSuccessHero; design BizSuccess also shows the StepRail (all 4 done) above the hero. **Fix:** Render WizardStepRail with current=4/done=[1..4] above WizardSuccessHero on the success branch.
- `low`/`state` — Price field is gated behind model.paidEnabled, so it can be absent; design Field shows Price ($120) unconditionally beside Duration. **Fix:** Always render priceField in OnboardingServicePicker (or confirm paidEnabled defaults true for business).

**Android deviations:**
- `med`/`layout` — DurationStepperChips renders four 15/30/45/60 chips; design Field shows a single dropdown reading "30 min" with a chevron-down adornment. **Fix:** Replace the chip row with a dropdown/menu field mirroring iOS durationField (value text + ChevronDown).
- `med`/`sizing` — ServiceTile icon size = 18.dp inside the 30-icon-tile pattern; design ServiceTile glyph is width:15/height:15 (iOS uses 15). **Fix:** Set PantopusIconImage size = 15.dp in ServiceTile.
- `low`/`missing-element` — PriceField omits the trailing pencil adornment; design Field for Price has adorn="pencil" (iOS renders Icon(.pencil)). **Fix:** Add a trailing PantopusIcon.Pencil (14.dp, appTextMuted) after the BasicTextField.
- `low`/`missing-element` — Business team seeds only 3 teammates; design TeamList includes a 4th "Dana W." Front-desk (Not seated) row that demonstrates the front-desk role from the subcopy. **Fix:** Add Triple(SeededPerson("t4","Dana W.","Front desk","DW"),"FRONT DESK",false) to BUSINESS_TEAM.
- `low`/`missing-element` — OnboardingSuccess renders SuccessHeroBlock + link card only; design BizSuccess also keeps the StepRail (all done) above the hero. **Fix:** Render WizardStepRail(current=4, all done) above SuccessHeroBlock in OnboardingSuccess.
- `low`/`color` — Service/team divider uses appBorderSubtle (#f3f4f6); design TeamRow/ServiceTile borders use the stronger N.border (#e5e7eb / appBorder). **Fix:** Use PantopusColors.appBorder for the inter-row dividers in SeededPersonRow/InviteRow to match the design hairline.

### A12 Scheduling onboarding (Home) — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Onboarding/SchedulingOnboardingScreen.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/WizardStepViews.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Wizard/WizardShell.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/OnboardingHomeBusinessScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/OnboardingSteps.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/SetupSteps.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/wizard/blocks/SuccessHeroBlock.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/wizard/blocks/HeadlineBlock.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/wizard/blocks/SubcopyBlock.kt`
- **Verdict:** iOS faithfully reproduces the 3-step Home wizard (chrome, step rail, headline 22/700, member list, mode tiles, RR rule, composed-availability card, layered success hero) with only minor nits; Android shares the same step content but adds an out-of-spec Home/Business flow toggle and renders the headline/subcopy and success hero at the wrong sizes/colors.
- **Cross note:** Both platforms render the same loaded step content and share testTags (onboardingMode_*, onboardingRule_*, onboardingMember_*); neither implements loading/error states, which is acceptable since the wizard frames are all seeded/static (no fetchable state in the design). Key divergence: iOS reuses the spec-accurate WizardSuccessHero + WizardHeadline, while Android substitutes generic SuccessHeroBlock/HeadlineBlock/SubcopyBlock that drift on size and color, and Android adds an extra flow toggle the design does not have.

**iOS deviations:**
- `low`/`typography` — Step-2 collective sub in homeStep is hardcoded "Choose how members' availability turns into one set of bookable times." — spec HomeCollective sub is "Three members are scheduled. Choose how their availability turns into one set of bookable times." **Fix:** Make the step-2 sub conditional on combineMode like Android does (collective vs round-robin copy) to match the spec frames.
- `low`/`extra-element` — OnboardingMemberList loops with `if idx < members.count` so a divider Rectangle is also drawn after the last member, just above the invite row, yielding a double rule there vs the spec's single borderTop on the invite button. **Fix:** Change the guard to `idx < members.count - 1` so the trailing divider is the invite row's own top border only.
- `low`/`icon` — Mode tiles use generic SF-style glyphs (.users / .arrowsRepeat) where the spec draws bespoke CollectiveGlyph (overlapping circles w/ vesica) and RoundRobinGlyph (3 dots + dashed arc) at 40x26. **Fix:** Acceptable platform substitution; optionally add custom Shapes to match the spec diagrams if pixel parity is required.

**Android deviations:**
- `high`/`extra-element` — OnboardingHomeBusinessScreen renders OnboardingFlowSwitch (a Home/Business segmented toggle) on stepIndex==1; the design's Home onboarding (onboarding-home-frames.jsx) has no flow picker — it is a dedicated 3-step Home wizard launched with the Home pillar. **Fix:** Drop OnboardingFlowSwitch from the step-1 layout (resolve Home vs Business from the owner/route as iOS does), leaving only the PillarChip.
- `high`/`sizing` — Success uses SuccessHeroBlock: a flat 96dp successBg circle with a 48dp CheckCircle — spec SuccessHero is a layered 96px radial-gradient halo with a solid accent inner disc (~60px), a 32px white check, and a pillar drop-shadow (iOS WizardSuccessHero matches this). **Fix:** Render the success hero with the layered halo+inner-disc+white-check geometry (mirror WizardSuccessHero) instead of the generic flat-circle block.
- `med`/`color` — Success hero on Android paints the disc/icon with semantic green success (#059669) regardless of pillar; spec Home success uses the Home pillar accent #16A34A for the inner disc and shadow. **Fix:** Pass the pillar accent/accentBg into the success hero so the Home flow reads home-green, not generic success-green.
- `med`/`typography` — HeadlineBlock uses PantopusTextStyle.h2 (24sp/600) for the step titles; spec Headline is 22px/700 (iOS WizardHeadline uses 22/700). **Fix:** Use a 22sp Bold headline for these wizard steps (or a dedicated wizard headline style) instead of h2.
- `med`/`typography` — SubcopyBlock uses PantopusTextStyle.body (16sp) for the step sub-copy; spec Headline sub is 13.5px (iOS uses 13.5). **Fix:** Drop the wizard sub-copy to ~13.5sp with ~19px line height to match the spec and iOS.
- `low`/`color` — Member avatars use a flat appSurfaceSunken gray fill with appTextStrong initials; spec MemberRow uses per-member gradient avatars (iOS uses SchedulingHubModel.avatarTone). **Fix:** Apply per-member avatar tones/gradients instead of a single gray fill.

### A2 First-run booking-link setup wizard — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Wizard/FirstRunWizardScreen.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Wizard/FirstRunWizardModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Shared/WizardStepViews.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Wizard/WizardShell.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/FirstRunWizardScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/SetupSteps.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/WizardSteps.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/wizard/WizardShell.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/wizard/blocks/SuccessHeroBlock.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/setup/FirstRunWizardViewModel.kt`
- **Verdict:** Both platforms faithfully reproduce the wizard chrome, StepRail, handle field (all slug states), type picker, hours grid and timezone chip; iOS is closer because Android's success hero uses a green check-circle on a flat successBg disc instead of the spec's sky-accent badge with radial-gradient ring + inner filled disc.
- **Cross note:** Both platforms omit the spec's Resume frame/banner (Frame 6) entirely — a shared cut, not a divergence. The real divergence is the success hero: iOS reproduces the sky radial-gradient badge with inner filled disc + white check, while Android substitutes a generic green check-circle on a flat successBg disc (wrong color, missing inner disc, wrong icon).

**iOS deviations:**
- `low`/`missing-element` — No Resume frame/banner: spec Frame 6 (FrameResume) shows a ResumeBanner (rotate-ccw disc, 'Pick up where you left off', steps 1-2 done) re-entering at step 3; FirstRunWizardModel always starts at .link with no resume path. **Fix:** If resume is in scope, add a ResumeBanner section above the headline when re-entering at step 3 with steps 1-2 done; otherwise accept as a shared cut.
- `low`/`icon` — WizardSuccessLinkCard / Copy chip border uses accentBg as the stroke color (overlay stroke(accentBg)) where the spec's copy chip border is primary100; visually near-identical for personal but technically off-token. **Fix:** Stroke the copy chip with Theme.Color.primary100 to match spec's 1px primary100 border.
- `low`/`sizing` — WizardStepRail active disc uses a double Circle().stroke overlay (2px accent + 26x26 accentBg ring) approximating the spec's 2px+2px focus ring; the outer accentBg ring is drawn as a stroke not a halo so it reads slightly thinner than the spec's box-shadow 0 0 0 4px primary100. **Fix:** Acceptable approximation; optionally widen the outer ring to lineWidth 2 on a 30x30 circle for a fuller halo.

**Android deviations:**
- `high`/`color` — SuccessHeroBlock paints the hero badge with PantopusColors.successBg (green tint) + PantopusColors.success green CheckCircle icon; spec SuccessHero is sky — radial-gradient primary50→primary100 ring with a primary600 filled inner disc and a white check (iOS does this correctly). **Fix:** Replace successBg/success with the pillar sky accent: outer 96dp gradient ring (primary50→primary100), inner filled disc in pillar.accent, white Check icon.
- `med`/`missing-element` — Android success badge has no inner filled disc + shadow: spec nests a 60px primary600 disc (with 0 8px 20px shadow) inside the 96px ring; SuccessHeroBlock draws a single flat 96dp circle with a 48dp icon directly on it. **Fix:** Add the inner ~60dp accent disc with a drop shadow inside the 96dp ring, matching iOS WizardSuccessHero's ZStack.
- `med`/`icon` — SuccessHeroBlock uses PantopusIcon.CheckCircle (a glyph with its own ring) at 48dp; spec/iOS use a bare Check stroke (strokeWidth 3) at 32dp inside the disc, so Android renders a double-ring look. **Fix:** Use PantopusIcon.Check at 32dp, white tint, inside the inner accent disc.
- `low`/`spacing` — SuccessHeroBlock wraps the hero in vertical Spacing.s8 (32dp) and spaces children by s4 (16dp); spec SuccessHero pads 24px top, 22px below the badge and 8px under the title — Android's uniform 16dp gaps compress the badge-to-title rhythm. **Fix:** Match spec rhythm: ~24dp top pad, 22dp under badge, 8dp under headline, 22dp under subcopy before the link card.
- `low`/`sizing` — WizardTypePicker LocationTile icon is 18dp on Android vs 15pt in the spec/iOS (the spec tile glyph is 15px in a 30px square); minor oversize. **Fix:** Set LocationTile PantopusIconImage size to 15.dp to match the spec/iOS.
- `low`/`chrome` — WizardTopBar trailing step readout renders '1 of 4'; spec TopBar shows '1/4' (slash form) and iOS matches with '\(current)/\(total)'. **Fix:** Render the readout as "${current}/${total}" to match the spec and iOS.
- `low`/`missing-element` — No Resume frame/banner (spec Frame 6) on Android either — FirstRunWizardViewModel starts at step 1 with no resume entry; shared gap with iOS. **Fix:** If resume is in scope, mirror a ResumeBanner above the headline at step 3; otherwise accept as a shared cut.

### A3 Booking settings — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Settings/SchedulingSettingsScreen.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Settings/SchedulingSettingsModel.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingIdentityTheme.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/settings/SchedulingSettingsRootScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/settings/SettingsComponents.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/settings/SchedulingSettingsRootViewModel.kt`
- **Verdict:** Both platforms are near-pixel-faithful ports of scheduling-settings-frames.jsx (52pt/dp top bar, pillar-accent overlines, hairline cards at radius 12, danger zone, mono footer, segmented, tz-lock tile); iOS is closer because it implements the saving/saved per-row write frames (inline shimmer + green "Saved" chip) that Android omits entirely.
- **Cross note:** Both omit the masked card last-4 in the payments sub and round the segmented track to 8 instead of 10; the key divergence is state coverage — iOS renders the saving/saved per-row write frames, Android renders neither (only a danger-action toast).

**iOS deviations:**
- `low`/`typography` — timezoneTrailing unlocked uses .keyRound; design's TzRight shows lucide 'lock-open' for the fresh/unlocked state. **Fix:** Add a .lockOpen PantopusIcon and use it instead of .keyRound for the unlocked tz tile.
- `low`/`color` — tzLock locked tile fills Theme.Color.personalBg always, but design pins it to PILLAR.personal.accentBg (#f0f9ff) only as a sky tint while text accent follows the active pillar — fine for Personal, but on Business the violet icon sits on a sky-blue tile per design too, so this matches intent. **Fix:** No change needed; verify personalBg == #f0f9ff token.
- `low`/`state` — Team 'Team & seats' sub is 'Manage members & booking seats'; design FrameBusiness uses '4 members · 2 booking seats'. **Fix:** Change sub string to '4 members · 2 booking seats' to match design copy (or wire real counts).
- `low`/`color` — Payments connected sub renders 'Stripe · connected'; design loaded frame shows 'Stripe · ••4821'. **Fix:** Render masked last-4 ('Stripe · ••\(last4)') when available.

**Android deviations:**
- `high`/`state` — The 'saving' frame (inline Shimmer replacing the cancellation/timezone value) and 'saved' frame (green 'Saved' check chip in the row) from the JSX are not implemented — no savingRow/justSavedRow, no trailing shimmer, no SavedChip; only the danger-action top toast exists. 2 of 5 design frames are missing. **Fix:** Add savingRow/justSavedRow signals to the VM and a savingShimmer + SettingsSavedChip trailing variant mirroring iOS SettingsRowTrailing.savingShimmer/.savedChip.
- `med`/`icon` — SettingsTzRight always renders PantopusIcon.Lock even when locked=false (fresh); design swaps to lucide 'lock-open' for the unlocked state, so the fresh frame shows a wrong closed-lock glyph. **Fix:** Pass icon = if (locked) Lock else LockOpen (add LockOpen to PantopusIcon).
- `low`/`sizing` — SettingsSegmented inner buttons use RoundedCornerShape(Radii.sm)=6dp; design button borderRadius is 8 (iOS uses Radii.md=8). Outer track uses Radii.md=8 vs design 10 on both platforms. **Fix:** Use Radii.md (8dp) for the inner buttons; optionally a 10dp track to match exactly.
- `low`/`color` — SettingsSegmented active button has no shadow; design specifies boxShadow 0 1px 2px rgba(0,0,0,0.08) on the selected segment (iOS applies pantopusShadow(.sm)). **Fix:** Add a small elevation/shadow to the active segment Box.
- `low`/`color` — SettingsConnectPill omits the accent-tinted drop shadow (design boxShadow 0 2px 6px {accent}44; iOS applies it). **Fix:** Add a tinted shadow under the Connect pill using the accent color.
- `low`/`color` — Payments connected sub is 'Stripe · connected'; design loaded frame shows 'Stripe · ••4821'. **Fix:** Render masked last-4 when available.

### A4 Scheduling notifications — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Notifications/SchedulingNotificationPrefsScreen.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Notifications/SchedulingNotificationPrefsModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/settings/NotificationPrefsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/settings/NotifComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/settings/NotificationPrefsViewModel.kt`
- **Verdict:** Both platforms render the channel-triad matrix card (Notify me + reminder chips, Notify attendees) faithfully with matching chrome, order, sizing, tokens and full state coverage; iOS edges ahead only because it implements the design's "SMS coming soon" tooltip while Android omits it, and both share the same minor gaps (no faded sibling "Account & security" card, simplified pause-banner text, yellow warning tokens vs spec's warm-orange amber).

**iOS deviations:**
- `med`/`missing-element` — The design's faded sibling 'Account & security' CategoryCard (SiblingHint, opacity 0.5, between Notify attendees and the Legend with an 18px gap) is absent in loadedBody. **Fix:** Add a non-interactive faded overline + 2-row card (New sign-in / Billing & receipts) at 0.5 opacity above the legend to show the card slots into the matrix.
- `low`/`state` — pauseBanner shows static 'Notifications paused' / 'Emergency alerts still come through' and has NO Resume pill; spec PauseBanner shows 'Paused for 2 hours', 'Resumes 11:42 AM · Emergency alerts still come through' plus a white-bordered 'Resume' button. **Fix:** Add a Resume capsule button and surface the paused-until duration/resume time from the booking page when available.
- `low`/`color` — pauseBanner uses Theme.Color.warningBg/warningLight/warning (yellow #FFFBEB/#FDE68A/#D97706) whereas spec amber band is #fff7ed bg, #fed7aa border+circle, #9a3412 title, #b45309 subtitle. **Fix:** Map to the warmer orange amber values (or a dedicated amber token) for closer hue fidelity; current tokens read more yellow.
- `low`/`spacing` — pushOffNotice applies .padding(.horizontal, Spacing.s3) twice (once inside before background, once after), doubling outer horizontal inset versus the single 12px the spec wraps with. **Fix:** Remove the duplicate horizontal padding so the notice sits at one 12px gutter like PushOffNotice in the spec.

**Android deviations:**
- `med`/`missing-element` — NotifCategoryCard takes no smsHint param and never renders the spec's floating 'SMS coming soon' dark tooltip (with arrow) above the locked S column; the SMS-locked frame variant is therefore not represented. **Fix:** Add a smsHint state + tap target on the S header column that shows a #111827 tooltip with downward arrow, mirroring iOS smsTooltip.
- `med`/`missing-element` — The design's faded sibling 'Account & security' CategoryCard (SiblingHint at 0.5 opacity, 18px above the Legend) is absent in NotifBody. **Fix:** Render a faded NotifOverline + 2-row card (New sign-in / Billing & receipts) at alpha 0.5 above NotifLegend.
- `low`/`state` — NotifPauseBanner text is static 'Notifications paused' and its 'Resume' pill has no clickable/onClick; spec shows duration 'Paused for 2 hours', 'Resumes 11:42 AM · …' and a working Resume action. **Fix:** Wire the Resume pill to an unpause callback and surface the resume time/duration from the booking page.
- `low`/`color` — NotifPauseBanner uses warningBg/warningLight/warning (yellow #FFFBEB/#FDE68A/#D97706) instead of the spec's warm orange amber (#fff7ed/#fed7aa/#9a3412/#b45309). **Fix:** Use the warmer amber values for the banner band, circle, title and subtitle to match the spec hue.

### A5 Scheduling summary card — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Hub/HubSummaryCard.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Setup/Hub/SchedulingHubScreen.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingIdentityTheme.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/SummaryCard.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/hub/SchedulingHubScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`
- **Verdict:** Both platforms reproduce the embedded summary card faithfully (4 stat cells + hairline dividers, sparkline, breakdown chips, period chips, "See insights"), with all four states; iOS mirrors the loading skeleton's full footprint exactly while Android's loading state drops the header/period and "See insights" shimmers and its in-hub Retry is a no-op.
- **Cross note:** Both correctly source pillar accents (sky/green/violet) from identity tokens and reuse the same stat-cell/sparkline/breakdown geometry; the key divergence is the loading skeleton — iOS reproduces the spec's full shimmer footprint (header + cells + sparkline + link) while Android's is partial, and Android's in-hub Retry is unwired.

**iOS deviations:**
- `low`/`color` — Card uses .setupCard(radius: Radii.xl) which matches spec radius 16 and shadow 0 1px 3px; delta vs-last-month threshold uses delta>=0 → success even when delta is 0, but spec shows up/down arrows only with a sign — cosmetic. **Fix:** Treat delta==0 as neutral (no arrow, fg1) to match spec's StatCell delta semantics.
- `low`/`spacing` — statCell uses spacing 3 + label fontSize 10.5 matching spec; divider height is 34 vs spec divider align-self:stretch (cells are ~taller), so the hairline is slightly shorter than the cell — negligible. **Fix:** Optionally make divider stretch to the row height instead of fixed 34pt.

**Android deviations:**
- `med`/`state` — SummaryLoading() renders only four stat shimmers + a 40dp sparkline box; it omits the spec loading frame's header shimmers (88×11 kicker + 120×26 period pill) and the trailing right-aligned 86×12 'See insights' shimmer, so the skeleton footprint doesn't mirror the loaded card. **Fix:** Add a header Row with two shimmer boxes (88×11, 120×26 pill) and a trailing 86×12 shimmer row, mirroring iOS loadingBody.
- `med`/`state` — In SchedulingHubScreen.HubLoadedBody the SummaryCard is given onRetry = { } (no-op), so the Error frame's Retry button does nothing, unlike iOS which calls model.refresh(). **Fix:** Wire onRetry to viewModel.load()/refresh() like the iOS hub does.
- `low`/`chrome` — For non-Data states SummaryHeader still renders a real 'THIS MONTH' kicker during Loading (showPeriod=false), whereas the spec's CardLoading replaces the whole header with shimmer rects. **Fix:** Skip SummaryHeader for the Loading branch and render header shimmers inside SummaryLoading instead.
- `low`/`color` — Card container uses RoundedCornerShape(16.dp) + 1dp border only, with no shadow; spec CardShell adds boxShadow 0 1px 3px rgba(0,0,0,0.06). **Fix:** Add a subtle shadow (e.g. PantopusShadow.sm / Modifier.shadow 1dp) to match the card elevation; use Radii.xl token instead of literal 16.dp.
- `low`/`sizing` — StatDivider height is 36dp vs iOS 34 and spec align-self:stretch; minor but the two platforms differ from each other by 2dp. **Fix:** Align divider height between platforms (or stretch to row height) per spec.

## Event types

### B1 Event types list — iOS `major` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/List/EventTypeListView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/List/EventTypeListViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Shared/ListOfRows/ListOfRowsView.swift`, `frontend/apps/ios/Pantopus/Features/Shared/ListOfRows/RowModel.swift`, `frontend/apps/ios/Pantopus/Core/Design/Components/EmptyState.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/EventTypeListScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/EventTypeKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/EventTypeListViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/identity/IdentitySwitcherPillRow.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`
- **Verdict:** Android is a faithful bespoke build of the design's List-of-Rows event-type screen, while iOS forces the design into the generic ListOfRowsView shell and drops the screen's signature per-row toggle, the 6px dot row, the segmented Active/Hidden filter, the section overline, and the empty-state template chips.
- **Cross note:** Both omit the design's inline 'N hosts' users-badge on business rows (iOS VM notes the list DTO lacks an assignee count; Android EventTypeRowCard renders no hosts pill either) — a shared data-blocked gap, not a pure UI miss.

**iOS deviations:**
- `high`/`missing-element` — Design EventRow has a 36x20 product-blue Toggle as the primary trailing affordance; iOS row uses RowTrailing.kebab only (active/hidden buried in the overflow menu) so the inline toggle is absent. **Fix:** Add a per-row toggle: either a new RowTrailing case carrying the toggle, or render the toggle in the row trailing slot wired to viewModel.toggleHidden.
- `high`/`icon` — Design row leading is a 6px colored dot; iOS uses RowLeading.typeIcon = a 40pt rounded tinted location-icon tile (LeadingView renders 40x40), changing the entire row anatomy. **Fix:** Introduce a small color-dot leading variant (6pt) and drop the 40pt icon tile for this screen.
- `high`/`layout` — Design filter is a segmented Active/Hidden pill control (sunken track, white selected segment, blue700 label); iOS uses the shell's underline TabStrip (horizontal scroll, text+count+2px primary600 underline). **Fix:** Render a segmented control in the customHeader instead of relying on the shell tab strip, or add a segmented tab style to the shell.
- `med`/`missing-element` — Design draws a pillar-accented uppercase section overline ('Your event types' / 'Bookable services'); the VM builds RowSection with header:nil so no overline renders. **Fix:** Set RowSection.header to the pillar-accented overline string (and render it uppercase tinted with the pillar accent).
- `med`/`missing-element` — Design empty state (FrameEmpty) includes a 'START FROM A TEMPLATE' overline plus 15/30/60-min clock template chips below the CTA; iOS uses generic EmptyState which has only icon+headline+subcopy+CTA (VM comment acknowledges the gap). **Fix:** Extend EmptyContent/EmptyState with a template-chip row, or render a bespoke empty view for this screen.
- `med`/`chrome` — Design top bar is a centered 15pt-semibold title with a blue600 '+' (strokeWidth 2.4); iOS uses native navigationTitle (left/inline) and the shell tints the topBarAction icon appText, not primary600. **Fix:** Use a centered inline title and tint the + action primary600 to match the design's product-blue add affordance.
- `low`/`icon` — Design row overflow glyph is ellipsis-vertical; iOS RowTrailing.kebab renders .moreHorizontal (horizontal dots) at 20pt. **Fix:** Render ellipsis-vertical for the kebab in this row context.
- `low`/`sizing` — Design rows are individually-bordered 14px-radius cards spaced 8px apart; iOS .card section groups rows into one Radii.lg(12) card with hairline dividers, so radius and inter-row gap differ. **Fix:** Render free-standing 14pt-radius row cards with 8pt spacing rather than a single grouped card stack.

**Android deviations:**
- `med`/`extra-element` — Design FilterHeader shows a single static identity pill (small uppercase pill, pillar bg+text); Android renders a full-width 3-segment IdentitySwitcherPillRow (Personal/Home/Business) that fills the active segment with solid accent — a different, interactive control plus an extra Home option. **Fix:** Replace with EtIdentityPill (the static pillar pill already defined in EventTypeKit) to match the design, keeping owner switching elsewhere.
- `med`/`color` — SchedulingPillar.accentBg maps Personal to PantopusColors.primary50 (#f0f9ff); the design's personal pill/header bg is personalBg #dbeafe. **Fix:** Map Personal accentBg to the personalBg (#dbeafe) token used by the design.
- `low`/`sizing` — Design empty-state CTA is a content-hugging pill button; Android EtPrimaryButton is fillMaxWidth, so 'Create your first event type' spans full width. **Fix:** Render the empty-state CTA as a wrap-content pill rather than full-width.
- `low`/`color` — Design copied-toast check glyph is appTextInverse (white on dark pill); Android Toast tints the check PantopusColors.success (green). **Fix:** Tint the toast check icon appTextInverse to match the design's monochrome toast.
- `low`/`state` — Design includes Reordering (drag handles, lifted row) and Permission-gated (lock banner, disabled toggles) frames; Android covers populated/business/empty/all-hidden/loading/error but not reorder or the read-only gated banner (canEdit only disables controls). **Fix:** Add a reorder mode and a lock/read-only banner for non-owner catalogs to cover the remaining design frames.
- `low`/`sizing` — Design empty hero is an 84px gradient disc with a radial-gradient fill; Android uses an 84dp linear-gradient (primary50->primary100) disc — close but a linear rather than radial gradient. **Fix:** Use a radial gradient for the empty-state hero disc to match FrameEmpty.

### B2 Event type editor — iOS `major` / Android `major` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/Editor/EventTypeEditorView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/Editor/EventTypeEditorComponents.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/EventTypeEditorScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/EventTypeKit.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/form/FormShell.kt`
- **Verdict:** Both platforms render the full sectioned editor faithfully, but iOS keeps the design's prominent sticky SaveBar (and is overall closer) while Android drops it entirely; both also omit the Pricing "Collect" segmented, and each platform has its own structural gaps (iOS extra slug field + 12px/border-less cards; Android missing collective-mode controls).
- **Cross note:** Shared gap: both platforms omit the Pricing 'Collect' Full amount/Deposit segmented from the design's stripe-connected PricingCard. Key divergence: iOS renders the sticky SaveBar (design-faithful CTA) while Android relies only on a top-right Save text action and drops the bottom bar entirely; conversely Android's card geometry (16dp radius + 1px border + 13dp padding) is more faithful than iOS's 12pt border-less cards.

**iOS deviations:**
- `med`/`sizing` — PillarFieldGroup cards use Radii.lg (12pt corner radius); design Card is 16px radius (radius-xl). **Fix:** Use RoundedRectangle(cornerRadius: Radii.xl/16) for the card shape.
- `med`/`chrome` — PillarFieldGroup paints only .background+.clipShape with no 1px border; design Card has a 1px #e5e7eb border + shadow-sm. **Fix:** Add .overlay(RoundedRectangle(16).stroke(appBorder,1)) and a subtle shadow.
- `med`/`extra-element` — basicsGroup adds a 'Booking link' slug TextField with link-preview caption; design BasicsCard has only Name, Description (multiline), Color. **Fix:** Remove the slug field or move it out of Basics to match the design's three-field card.
- `med`/`missing-element` — pricingControls (stripe connected) shows Price + Currency but omits the design's 'Collect' Full amount/Deposit segmented. **Fix:** Add a Collect segmented (Full amount/Deposit) below the price/currency row.
- `low`/`layout` — singleDuration uses LabeledStepper (full-width title…value ± row) instead of the design's compact bordered stepper box sitting inline beside the 15/45/60 quick chips. **Fix:** Render a compact bordered Stepper with min unit and place quick chips on the same wrap row.
- `low`/`typography` — FormShell top-bar title uses .body (16pt); design TopBar title is 15px/600. **Fix:** Use a 15pt semibold title style in the form top bar.
- `low`/`missing-element` — Assignment collective mode shows required-hosts stepper but no MemberAvatars stack ('2 of 3 hosts required') shown in design FrameCollective. **Fix:** Add a MemberAvatars row under collectiveControls.

**Android deviations:**
- `high`/`missing-element` — EditorContent calls FormShell without bottomActionLabel or stickyBottom, so the design's prominent sticky SaveBar ('Create event type'/'Save event type', full-width 44dp radius-12 button) is never rendered — only the top-right Save text exists. **Fix:** Pass bottomActionLabel = if(isCreate) 'Create event type' else 'Save event type' (or a stickyBottom save bar) to FormShell.
- `med`/`missing-element` — Collective assignment renders only a blurb + 'Manage hosts' link; design FrameCollective adds a 'Required hosts' stepper ('2 of 3') and a MemberAvatars stack. **Fix:** When assignmentMode==collective, render a required-hosts stepper and member-avatar row.
- `med`/`extra-element` — Assignment card adds an 'EtLinkRow Manage hosts' not present in the design's AssignmentCard. **Fix:** Remove the Manage hosts link row (or gate it behind a non-design path).
- `med`/`missing-element` — PricingCard (stripeConnected) shows Price + Currency but omits the design's 'Collect' Full amount/Deposit segmented. **Fix:** Add a Collect segmented below the price/currency row.
- `low`/`color` — StripeConnectCard uses primary50 bg + primary600 icon tile; design StripeCard is violet-tinted (stripeBg #f5f4ff with #635bff Stripe-purple tile). **Fix:** Use a violet/stripe-purple background and icon tile to match the design's payment treatment.
- `low`/`sizing` — EtSegmented non-small height is 30dp; design Segmented (non-small) is 32px. **Fix:** Set the non-small segmented option height to 32dp.
- `low`/`chrome` — FormShell title shows 'New event type' on create; design TopBar title stays 'Event type' in all frames. **Fix:** Pass title='Event type' regardless of create/edit.

### B3 Intake questions editor — iOS `major` / Android `major` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/IntakeQuestions/IntakeQuestionsEditorView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/IntakeQuestions/IntakeQuestionCard.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/IntakeQuestions/IntakeQuestionsEditorViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/IntakeQuestionsEditorScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/IntakeQuestionsModels.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/EventTypeKit.kt`
- **Verdict:** Both platforms re-implement the design's bottom-sheet-over-dimmed-editor as a full-screen FormShell and both drop the drag-to-reorder handle, but Android matches the design's inline-EditGroup + segmented type selector + pillar subtitle far more faithfully, whereas iOS substitutes an accordion card, a Menu type picker, and arrow-button reordering.
- **Cross note:** Both platforms made the same two systemic substitutions: (1) full-screen FormShell instead of the design's bottom-sheet-over-dimmed-editor with grabber/scrim/pillar overline, and (2) no real drag-to-reorder grip handle (iOS uses up/down arrows, Android has none) so FrameReorder's lifted-row + dashed drop-slot is unimplemented on both. Divergence: Android faithfully reproduces the inline blue EditGroup, the 3-column segmented TypeSelector, the bordered required-toggle row, and the Save-question/secondary-button pair; iOS instead uses an accordion card + Menu dropdown + arrow reorder, drifting further from the Form field-group idiom.

**iOS deviations:**
- `med`/`chrome` — FormShell renders a full-screen routed editor with a centered title and no pillar overline/subtitle; design is a bottom sheet (grabber, 24px top radius, scrim, 'Personal · Intro call' accent overline above title, 'Done' trailing button). **Fix:** Present as a sheet (or at minimum pass subtitle: eventName) and tint an overline with the pillar accent; this is a deliberate full-screen port, accept chrome idiom but add the missing subtitle/overline.
- `high`/`interaction` — Custom rows expand via a chevron accordion (IntakeQuestionCard.header toggles isExpanded) and reorder via arrowUp/arrowDown footer buttons; design uses an inline blue EditGroup that replaces the row and a grip-vertical drag handle per row for drag-to-reorder. **Fix:** Replace the accordion+chevron with the design's inline EditGroup-replaces-row pattern and add a grip-vertical drag handle (DragGesture/onMove) instead of up/down arrows.
- `high`/`missing-element` — No grip-vertical drag handle on QuestionRow (design QuestionRow renders a 30×30 grip-vertical at trailing edge); reorder is arrow-button only. **Fix:** Add a trailing grip-vertical handle wired to drag reorder.
- `high`/`interaction` — Answer-type uses a SwiftUI Menu dropdown (typeMenu) rather than the design's 3-column segmented TypeSelector grid (sunken track, 30px cells, selected = white surface + blue700, radius 7). **Fix:** Replace the Menu with a 3-column segmented control matching TypeSelector geometry.
- `med`/`missing-element` — EditGroup is missing the design's 'Save question' primary button and red 'Delete' text button row; iOS edits live-bind with only a footer Delete and no explicit Save-this-question CTA. **Fix:** Add a 'Save question' blue button + red Delete row to the expanded editor.
- `med`/`color` — Expanded editor uses plain appSurface card (IntakeQuestionCard background) instead of the design EditGroup's blue50 fill with 1.5px blue200 border; the active/editing affordance is lost. **Fix:** Give the expanded/editing state a primary50 background and 1.5px primary200 border.
- `low`/`extra-element` — 'Make this required' uses a bare SwiftUI Toggle row, not the design's bordered white surface row (1px border, radius 8, 9/11px padding) containing the label + toggle. **Fix:** Wrap the toggle in a bordered surface row matching the spec.
- `low`/`icon` — Per-row delete affordance is only present in the expanded footer; collapsed QuestionRow shows no inline trash-2 (design shows a trash-2 button on every row beside the grip). **Fix:** Add an always-visible trash-2 delete icon to the collapsed row.

**Android deviations:**
- `med`/`chrome` — IntakeContent uses full-screen FormShell (centered title + subtitle = eventName, 'Done' action) rather than the design bottom sheet (grabber, 24px radius, scrim, pillar accent overline 'Personal · Intro call'). Subtitle is present but uncolored/not an overline. **Fix:** Accept full-screen port but render the subtitle as a pillar-accent overline ('Personal · {name}') above the title, or present as a sheet.
- `high`/`missing-element` — QuestionRow has no grip-vertical drag handle and the screen exposes no reorder affordance at all (only onEdit click + a trailing trash-2); design FrameReorder shows active grip handles, a lifted row, and a dashed drop slot. **Fix:** Add a trailing grip-vertical handle and implement drag reorder (reorderable list); add the lifted-row shadow + dashed placeholder states.
- `med`/`layout` — QuestionRow is drawn as a standalone white card with 1px border + 12px radius; design QuestionRow is a flat row inside the single white ListBlock card separated by 1px bottom borders (no per-row card/border). **Fix:** Render custom questions as flat rows with bottom dividers inside one card, matching the locked-defaults card treatment.
- `med`/`state` — FrameReorder lifted-row (heavy shadow, scale 1.015, rounded) and the dashed blue200 drop-slot placeholder are not implemented anywhere. **Fix:** Add the drag-lifted styling and a 52px dashed blue200/blue50 drop-slot placeholder during reorder.
- `low`/`interaction` — EditGroup secondary action is a 'Cancel' text button; design uses a red 'Delete' button (trash-2 + 'Delete', color E.error) as the secondary action in the edit field group. **Fix:** Change the secondary action to a red trash-2 'Delete' button to match the spec (Cancel can stay as well, but Delete is the speced affordance).
- `low`/`spacing` — Helper caption uses Spacing.s2 (8) item spacing and the 'YOUR QUESTIONS' group label sits with top = Spacing.s2 (8); design GroupLabel margin is 14px top / 4px bottom. **Fix:** Set the group-label top margin to ~14dp to match the design rhythm.
- `low`/`missing-element` — Add button uses EtPrimaryButton; design AddButton carries a blue shadow (0 6px 16px rgba(2,132,199,0.26)) and 44px height with plus icon — confirm shadow/height parity. **Fix:** Apply the design's primary-shadow and 44dp height to the Add-a-question button if EtPrimaryButton differs.
- `low`/`icon` — Options-list rows omit the leading grip-vertical handle shown in the design OptionsList (grip + value + x); Android shows only the EtTextField + x. **Fix:** Add a leading grip-vertical icon to each option row.

### B8 Connected calendars — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/ConnectedCalendars/ConnectedCalendarsView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/EventTypes/ConnectedCalendars/ConnectedCalendarsViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/ConnectedCalendarsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/eventtypes/ConnectedCalendarsViewModel.kt`
- **Verdict:** iOS faithfully reproduces nearly all six design frames (overline, coming-soon hero, connect/connecting/connected/re-auth rows, toggles, status pills) with only minor color/icon nits; Android renders only a coming-soon hero or a bare provider header row, omitting the Connect button, status pills, toggle rows, helper, pillar overline, re-auth banner, and denied state.
- **Cross note:** Both platforms render as a full screen rather than the design's bottom sheet with grab handle + Done (acceptable native idiom). Key divergence: iOS implements ~5 of 6 frames; Android implements only coming-soon + a stripped header row and never surfaces Connect/toggles/pills/re-auth.

**iOS deviations:**
- `low`/`color` — providerTile tints the glyph with appTextSecondary (gray) for all providers, but the design uses per-brand colors (Google #1a73e8, Apple #1d1d1f, Outlook #0f6cbd). **Fix:** Add a brand color to CalendarProvider and tint Icon with it when not muted.
- `low`/`icon` — Outlook uses .calendarRange per spec (good), but the connected-account fallback header uses .calendarDays generically; design's coming-soon hero glyph is calendar-sync which iOS maps correctly (.calendarSync). **Fix:** Minor — keep .calendarSync; ensure fallback header maps to the actual provider glyph.
- `low`/`chrome` — Rendered as a routed full-screen with navigationTitle + appBg background rather than the design's bottom sheet over a dimmed hub with a grab handle and trailing 'Done'; acceptable native presentation but loses the sheet/Done idiom. **Fix:** Optional: present via .sheet with a grab handle and a Done button if matching the sheet chrome is desired.
- `low`/`typography` — pillarOverline tracking is 0.8 vs spec letterSpacing 0.08em (~1.2pt at 10px) and size 10 vs spec 9.5; negligible but slightly off. **Fix:** Set size 9.5 and tracking ~1.0 to match the 0.08em spec.
- `low`/`state` — Permission-denied frame (lock banner + 'Open Settings') from design Frame 6 is not represented in the iOS VM/view (only loading/ready/error + reauth/connected/connecting). **Fix:** Add a denied row variant gated on a backend denied status if that state is reachable.

**Android deviations:**
- `high`/`missing-element` — No ConnectRow: the 'none connected' frame (provider tile + name + 'Not connected' + blue Connect button) is entirely absent; the VM's connect() is never invoked from any UI. **Fix:** Render a ConnectRow per provider (google/apple/outlook) with a primary600 Connect button calling viewModel.connect().
- `high`/`missing-element` — Connected/synced row only shows provider tile + name + account; missing the StatusPill ('Synced'), the two toggle rows (Check for conflicts / Add bookings), the 'Synced 2 min ago' line, and the Disconnect button. **Fix:** Build the connected card with AccountHeader+pill, a divider, two ToggleRows, and a synced-ago + Disconnect footer like iOS connectedCard.
- `high`/`missing-element` — Re-auth/sync-error frame (warning banner 'Reconnect Google…' + warningBg/warningBorder + Reconnect button) and permission-denied frame (lock banner + Open Settings) are not implemented at all. **Fix:** Add reauthCard (warning banner + Reconnect) and DeniedRow (lock banner + Open Settings) variants keyed off calendar status.
- `high`/`missing-element` — Connecting/OAuth-in-flight row (external-link icon + 'Opening Google…' + 76x12 shimmer) is absent; the VM has no connectingId concept. **Fix:** Add a connecting state and a ConnectingRow mirroring iOS connectingCard.
- `med`/`missing-element` — Pillar overline 'PERSONAL · SCHEDULING' (9.5px bold, identity-personal color) is missing entirely. **Fix:** Add a leading overline Text in personal color above the content like iOS pillarOverline.
- `med`/`missing-element` — Helper line 'Connect a calendar to check for conflicts and add bookings automatically.' (spec fontSize 11.5, fg3) is missing. **Fix:** Add the helper Text above the provider rows in the empty/connect state.
- `med`/`icon` — Coming-soon hero and provider tiles use PantopusIcon.CalendarCog (maps to Material EditCalendar) instead of the spec's calendar-sync; Outlook tile uses CalendarClock instead of spec calendar-range. **Fix:** Add a CalendarSync icon and use it for the hero; map Outlook to a calendar-range glyph.
- `low`/`color` — ProviderTile tints all glyphs with primary600 (or appTextMuted when muted) rather than the design's per-brand colors; loading state uses a single 240x120 Shimmer that doesn't mirror the loaded card geometry (3 rows). **Fix:** Tint tiles with per-brand colors and replace the single shimmer with row-shaped skeletons like iOS loadingSkeleton.

## Availability

### C Availability schedules — iOS `major` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Availability/ScheduleList/AvailabilityScheduleListView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Availability/ScheduleList/AvailabilityScheduleListViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Shared/ListOfRows/ListOfRowsView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityListScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityListViewModel.kt`
- **Verdict:** Android renders the spec per-card list, vertical-ellipsis kebab, boxed info note and two-row matching skeleton faithfully, while iOS delegates to the generic ListOfRowsView shell and diverges on card grouping, icon/title sizing, kebab glyph, skeleton, and the single-schedule note.

**iOS deviations:**
- `high`/`layout` — ListOfRowsView section style .card merges all schedules into one shared card with hairline dividers; spec ScheduleRow is a separate white card per schedule with 10px gap. **Fix:** Render each schedule as its own standalone card with 10pt gap.
- `high`/`icon` — Row kebab uses .moreHorizontal (horizontal dots); spec uses a vertical ellipsis (ellipsis-vertical, 18px, fg4) at top-right. **Fix:** Switch the kebab glyph to a vertical ellipsis.
- `high`/`state` — Loading shows six shimmer rows with a 40px circular avatar; spec is two skeleton rows with a 36px rounded-square icon block. **Fix:** Render two skeletons matching the row geometry with a 36pt md-radius block.
- `med`/`missing-element` — Single-schedule note renders as plain section footer text with no info icon and no raised card; spec note is a raised bordered card with a 14px info icon. **Fix:** Render the note as a bordered raised card with a leading info icon.
- `med`/`sizing` — Row icon tile is 40x40 with a 19-20px glyph and title uses body 16pt; spec tile is 36x36 with 18px glyph and name about 13.5pt. **Fix:** Use a 36pt tile, 18pt glyph, and 13.5pt semibold name.
- `med`/`color` — Top-bar add action draws the plus in appText; spec plus is blue600. **Fix:** Tint the add icon primary600.
- `low`/`sizing` — Grouped card uses Radii.lg (12) radius; spec ScheduleRow radius is 16. **Fix:** Use a 16pt corner radius.
- `low`/`extra-element` — helperHeader is always rendered, including on Empty and Error; spec omits the helper line on the Empty frame. **Fix:** Hide the helper line on empty and error states.

**Android deviations:**
- `low`/`typography` — Summary plus timezone renders in one fg3 color; spec bolds the timezone in fg2 with an fg4 separator. **Fix:** Bold the tz segment (appTextStrong) and tint the separator appTextMuted.
- `low`/`sizing` — Top bar is 52dp tall with a 22dp chevron and 16sp title; spec bar is 46px, 20px chevron, 15px title. **Fix:** Use a 46dp bar, 20dp chevron, 15sp title.
- `low`/`state` — Skeleton row omits the small trailing kebab shimmer bar the spec includes. **Fix:** Add a thin trailing shimmer bar.
- `low`/`chrome` — Top bar lacks the spec 1px bottom border under the bar. **Fix:** Add a 1px appBorder bottom divider under the top bar.

### C Block off time — iOS `major` / Android `minor` → better: **android**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Availability/BlockOffTime/BlockOffTimeView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Availability/BlockOffTime/BlockOffTimeViewModel.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/BlockOffTimeScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityComponents.kt`
- **Verdict:** Android is a near-faithful bespoke rebuild of the block-off-time sheet (labeled field-buttons, chip-led conflict card, lock footnote, sky overline, all tokens); iOS reuses the generic FormShell and swaps the design's labeled field-buttons for native DatePicker/Toggle/segmented Picker, loses the sheet chrome (grab handle, left-aligned sky overline) and renames a section, so it diverges materially despite matching the conflict card.
- **Cross note:** Both platforms render the screen as a pushed/flat editor rather than the design's bottom sheet (grab handle + 24px rounded top + scrim over a dimmed availability editor), and neither swaps the bottom bar to the 'Saving…' shimmer in the saving frame. iOS additionally substitutes native pickers for the design's labeled field-buttons, which is the main divergence; Android keeps the field-button idiom faithfully.

**iOS deviations:**
- `high`/`chrome` — Uses FormShell's 44pt top bar with a centered title + leading close X; design is a bottom sheet with a 38x5 grab handle and a LEFT-aligned sky overline 'PERSONAL · AVAILABILITY' (9.5px/700) stacked above the 17px/700 title, with Save top-right. **Fix:** Present as a sheet (presentationDetents) and render a left-aligned overline+title header with grab handle instead of FormShell's centered chrome, or pass the overline as the leading-aligned subtitle.
- `high`/`interaction` — Date/Start/End use native SwiftUI DatePicker (.date / .hourAndMinute) instead of the design's FieldButton — a full-width labeled button with leading accent icon (calendar/clock), tabular value, and trailing chevron-down. **Fix:** Replace DatePickers with tappable FieldButton-style rows (icon + value + chevron-down) that open a date/time picker sheet, mirroring Android's A3FieldButton.
- `med`/`icon` — Repeats is a segmented Picker (.segmented); design is a single FieldButton with a leading 'repeat' icon, value text, and chevron-down (e.g. 'Does not repeat'). **Fix:** Render Repeats as a FieldButton opening a menu/picker; show the selected label with the repeat glyph + chevron-down.
- `med`/`layout` — Reason, Date and time live under a single 'When' FormFieldGroup header; design has NO section overline on the details card (fields sit directly in a plain white card) and groups Reason with the date/time. **Fix:** Drop the 'When' overline; put Reason + Date + All day + time range together in one borderless card matching DetailsCard.
- `low`/`color` — Conflict body text uses Theme.Color.appText (#111827); design uses warm amber ink #78350f for the body and #92400e for the link, not the neutral fg1. **Fix:** Use a warning-dark ink token for the conflict body/link; appText reads colder than spec.
- `low`/`state` — Saving state disables the right action but does not render the design's bottom-bar 'Saving…' shimmer in place of the lock footnote; footnote stays visible. **Fix:** When isSaving, swap the lock footnote for a shimmer 'Saving…' bar matching the sheet's bottom-bar saving frame.
- `low`/`typography` — Footnote uses .caption (12px) where the spec footnote is 10.5px with 15px line-height. **Fix:** Drop the footnote to ~10.5px to match the spec's smaller privacy note.

**Android deviations:**
- `med`/`chrome` — Renders a flat AvailabilityTopBar (centered title + back chevron) over appBg rather than a bottom sheet with a 24px-radius top, grab handle, and -10px shadow; the sky overline is a separate scrolling row ('Personal · Availability') instead of the sheet header overline above the title. **Fix:** Present BlockOffTimeScreen as a ModalBottomSheet with grab handle + rounded top, and place the overline directly above a 17sp title in the sheet header.
- `low`/`typography` — FieldLabel renders at 11sp; the design's FieldLabel in event-editor-shell is ~12px. Top bar title 16sp vs sheet title 17px. **Fix:** Bump FieldLabel to 12sp and the sheet title to 17sp to match spec sizes.
- `low`/`state` — Saving state shows 'Saving' text in the top bar trailing slot and disables fields, but does not swap the bottom lock footnote for the design's 'Saving…' shimmer bar (A3SaveBar exists but isn't used here). **Fix:** On form.saving, replace A3LockFootnote with the A3SaveBar 'Saving…' shimmer to match frame 5.
- `low`/`color` — Conflict body uses appTextStrong (#374151); spec body ink is amber #78350f and link/icon #92400e vs the code's warning token. **Fix:** Use a warning-dark ink for the conflict body and link for an exact match.

### C Booking limits — iOS `major` / Android `minor` → better: **android**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Availability/BookingLimits/BookingLimitsView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Shared/Form/FormFieldGroup.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/BookingLimitsScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityComponents.kt`
- **Verdict:** Android faithfully mirrors the design's single-card stepper rows (inline label + bordered value/unit stepper + caption below) and pillar overline; iOS reuses the generic FormShell/FormFieldGroup so every rule renders as an UPPERCASE overline above a separate native-Stepper card, losing the design's row idiom, and both platforms omit the "Max per week" row.
- **Cross note:** Both platforms omit the "Max per week" row that appears in all three design frames. iOS additionally departs structurally by reusing FormFieldGroup (overline-over-card + native Stepper) instead of the design's inline label+bordered-stepper RowCard, which Android implements correctly via A3Card/A3Stepper.

**iOS deviations:**
- `high`/`layout` — ruleCard wraps each rule in FormFieldGroup, which renders an UPPERCASE overline label ABOVE a separate white card; the design's StepperRow is one card with the label inline on the same row as the stepper and a sub-caption below. **Fix:** Replace FormFieldGroup with a bespoke RowCard: HStack(label .weight + stepper) then caption Text, 16px radius, 1px border, shadow-sm.
- `high`/`icon` — Uses SwiftUI native Stepper (a +/- pill with a separate text label) instead of the design's bordered stepper showing a centered value plus unit suffix (e.g. '4 hours') flanked by − and + with hairline dividers. **Fix:** Build a custom stepper view: rounded 8px border, minus button | value+unit | plus button, red border on error, matching Android A3Stepper.
- `high`/`missing-element` — The 'Max per week' StepperRow (value 20, caption 'Most bookings you'll take in a week.') from the defaults/custom/error frames is absent — only Minimum notice, Book up to, Max per day, Per-person, Start times are rendered. **Fix:** Add a maxPerWeekGroup ruleCard wired to a viewModel.weeklyCap binding, placed between dailyCapGroup and perPersonGroup.
- `med`/`chrome` — FormTopBar shows only a centered 16px 'Booking limits' title; the design sheet header is left-aligned with a sky overline 'PERSONAL · WORKING HOURS' (9.5px/700) above a 17px/700 'Booking limits' title, plus a grab handle. **Fix:** Render a left-aligned header with the personal-accent overline + 17px bold title (sheet presentation already supplies the handle).
- `low`/`typography` — Row label uses .body (16px) via valueLabel and the group overline (11px) rather than the design's 13.5px/600 label and 11px caption; the stepper value is 16px body vs design 13px bold. **Fix:** Set label to 13.5pt semibold, caption 11pt, stepper value 13pt bold to match spec.
- `low`/`missing-element` — No per-type event selector / 'Applies to' affordance, whereas the spec notes this sheet is also reachable as a per-type override; Android exposes an EventTypeSelector dropdown when >1 event type. **Fix:** If iOS supports the per-type override entry, add an equivalent event-type picker card for parity.

**Android deviations:**
- `high`/`missing-element` — The 'Max per week' StepperRow (value 20, caption 'Most bookings you'll take in a week.') present in all three design frames is omitted — LimitsBody renders Minimum notice, Book up to, Max per day, Per-person, Start times only. **Fix:** Add a StepperRow(label='Max per week', value=form.maxPerWeek, caption='Most bookings you'll take in a week.') between Max per day and Per-person limit.
- `med`/`chrome` — AvailabilityTopBar keeps a centered 16sp 'Booking limits' title; the design header is left-aligned with the overline above a 17px/700 title, and the overline is duplicated as SectionOverline inside the scroll body instead. **Fix:** Move the 'Personal · …' overline + 17px bold title into a left-aligned sheet header; drop the redundant body overline or align it with the spec position.
- `low`/`sizing` — Top bar is 52dp tall vs the design sheet header padding ('8px 16px 6px' around a ~20px title); the chrome reads taller than the compact sheet header. **Fix:** Tighten the header vertical padding to ~8/6dp when used as the booking-limits sheet header.
- `low`/`spacing` — Body uses verticalArrangement spacedBy Spacing.s2 (8dp) between cards; the design sheet uses 10px gaps between RowCards. **Fix:** Use a ~10dp gap to match the design's inter-card rhythm (minor).
- `low`/`state` — Adds an Empty state ('No event types yet') not present in the design's three frames (defaults/custom/error); acceptable as a guard but diverges from spec frame coverage. **Fix:** Keep if backend can return zero event types, otherwise drop to match spec.

### C Date overrides — iOS `minor` / Android `major` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Availability/DateOverrides/DateOverridesView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Core/Design/Icons.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/DateOverridesScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityComponents.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/MonthCalendar.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both render every design section faithfully, but iOS preserves the bottom-sheet chrome (grabber + left overline/title + Done) and the correct calendar-range glyph, whereas Android replaces the sheet with a full-screen back-chevron top bar and uses a wrong calendar icon, so iOS is closer to the spec.
- **Cross note:** Both consolidate the design's 4 frame variants into one scrollable surface (calendar → picker → range link → overrides → holiday set → holiday import) and both omit the design's '+ Add a block' inline affordance. Key divergence: iOS keeps the bottom-sheet chrome and correct calendar-range glyph; Android substitutes a full-screen top bar and a CalendarMonth glyph.

**iOS deviations:**
- `med`/`missing-element` — customHoursRow uses two inline hour-only DatePickers + an en-dash, omitting the design's single clock-pill TimeRangeButton (clock icon + '10:00 AM – 2:00 PM' + chevron-down, 1.5px border, radius 9) from PickerBlock. **Fix:** Replace the dual DatePickers with one tappable time-range button matching TimeRangeButton (clock primary600, chevron-down, border 1.5px, radius 9).
- `low`/`missing-element` — Custom-hours mode omits the design's '+ Add a block' inline link (plus icon 13, primary600, 12px semibold) shown under the time range in PickerBlock. **Fix:** Add an inline '+ Add a block' button below customHoursRow when mode == .customHours.
- `low`/`missing-element` — calendarCard uses SwiftUI .graphical DatePicker, which cannot render the design's holiday-dot markers (4px primary600 dot under marked days, e.g. Jul 4 in Frame 2). **Fix:** Either accept the native picker or build a custom compact month grid that supports override/holiday dot markers.
- `low`/`sizing` — primaryCTA corner radius is Radii.lg (12) vs design's 11; height 42 matches. **Fix:** Minor: use radius 11 if an exact token exists, otherwise lg(12) is acceptable.

**Android deviations:**
- `high`/`chrome` — DateOverridesScreen wraps content in AvailabilityTopBar (40dp back chevron + centered 16sp title + Done in trailing 40dp slot), but the design is a bottom sheet: a 38x5 grabber, left-aligned overline 'Personal · Working hours' + 17px 'Date overrides' title, and a 'Done' text button at top-right. **Fix:** Present as a modal bottom sheet with a grabber and a left-aligned overline+title header; move 'Done' to a trailing text button, drop the centered-title back-chevron bar.
- `med`/`icon` — A3LinkRow for 'Block a date range' passes PantopusIcon.CalendarDays (maps to Material CalendarMonth); the design uses lucide 'calendar-range', and no CalendarRange case exists in Icons.kt. **Fix:** Add a CalendarRange entry to PantopusIcon (lucide calendar-range / Material DateRange) and use it for the range link tile.
- `med`/`layout` — The 'Personal · Working hours' overline is rendered as a free-floating SectionOverline above MonthCalendar because the top-bar chrome has no overline slot; in the design it lives in the sheet header next to the title. **Fix:** Once chrome becomes a sheet, fold the overline into the header beside 'Date overrides' rather than floating it over the calendar.
- `low`/`sizing` — MonthCalendar DayCell is 36dp (DAY_CELL) vs the design's 28px circle inside a 30px row, making day cells noticeably larger than spec. **Fix:** Reduce the day circle to ~28dp to match MonthCalendar's 28px selected pill / 30px row height.
- `low`/`sizing` — A3PrimaryButton is 44dp tall with a 17dp icon and 14sp bold label; the design's PickerBlock CTA is 42px tall, 15px icon, 13px bold. **Fix:** Set CTA height 42dp, icon 15dp, label 13sp to match the per-date action button spec.
- `low`/`missing-element` — PickerBlock custom-hours branch shows only the A3TimeRangeButton and omits the design's '+ Add a block' inline link (A3InlineAddButton exists but is unused here). **Fix:** Add A3InlineAddButton('Add a block') under A3TimeRangeButton when choice == CustomHours.

### C Weekly hours editor — iOS `major` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Availability/WeeklyHours/WeeklyHoursEditorView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Availability/WeeklyHours/WeeklyHoursComponents.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Form/FormFieldGroup.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/WeeklyHoursEditorScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/availability/AvailabilityComponents.kt`
- **Verdict:** Android faithfully reproduces the design (correct "Edit schedule" title, Personal header pill, bordered 16px cards, labeled time-range button), while iOS diverges on chrome (wrong title, no personal pill, borderless undersized cards) and replaces the labeled time button with inline date pickers; neither platform ships the dedicated empty "Set your hours" hero frame.
- **Cross note:** Both platforms omit the design's dedicated empty/unset "Set your hours" hero frame (FrameEmpty: CompositionGapCard + EmptyHero + LinksCard with no name/timezone/grid). iOS additionally diverges on chrome and card styling, while Android's main divergences are an extra LinksCard row and copy/text drift.

**iOS deviations:**
- `high`/`chrome` — FormShell title and navigationTitle are both "Weekly hours"; design TopBar reads "Edit schedule" (default/multi/warning/saving) and "Set hours" (empty). **Fix:** Set FormShell title to "Edit schedule" (and "Set hours" for the unset state).
- `high`/`missing-element` — No Personal identity HeaderPill is rendered; design shows the sky `HeaderPill pillar="personal"` below the top bar in every frame. **Fix:** Add a personal pill (User icon + "PERSONAL") between the top bar and the form body.
- `high`/`color` — FormFieldGroup and linkGroup cards apply only a white fill + clip with no border stroke; design cards carry a 1px appBorder on every Card. **Fix:** Add a 1px appBorder RoundedRectangle stroke overlay to the card containers.
- `med`/`sizing` — Cards use Radii.lg (12pt); design Card corner radius is 16px (radius-xl). **Fix:** Use a 16pt corner radius for the form cards.
- `med`/`interaction` — TimeRangeRow renders two inline hourAndMinute DatePickers inside one box; design is a single labeled button "9:00 AM – 5:00 PM" with a clock icon and trailing chevron-down opening a picker. **Fix:** Render a labeled time-range button (clock + range text + chevron-down) that presents the picker, matching A3TimeRangeButton.
- `med`/`state` — No dedicated empty "Set your hours" hero frame; the allOff path always shows name/timezone/grid plus warning instead of CompositionGap + EmptyHero + LinksCard. **Fix:** Add an unset state that renders the composition-gap card, a 54x54-icon EmptyHero with "Set your hours", and the links card.
- `low`/`sizing` — Timezone control is a borderless HStack (globe + text + chevron) with no bordered field button and omits the muted "· auto" suffix; design is a 1.5px-bordered, 8px-radius field button reading "Pacific Time · auto". **Fix:** Wrap the timezone row in a bordered field button and append a muted "· auto" suffix.
- `low`/`typography` — Bottom CTA label is "Save changes"; design SaveBar label is "Save schedule". **Fix:** Change bottomActionLabel to "Save schedule".

**Android deviations:**
- `med`/`extra-element` — LinksCard renders three rows (Date overrides, Booking limits, and an extra "Block off time"); design LinksCard has only two rows (Date overrides & holidays, Booking limits & notice rules). **Fix:** Remove the "Block off time" link row from the weekly-hours LinksCard.
- `med`/`state` — No dedicated empty "Set your hours" hero frame; Content state always renders name/timezone/grid and only conditionally adds the warning card. **Fix:** Add an unset state rendering the composition-gap card + EmptyHero (54dp icon, "Set your hours") + LinksCard.
- `low`/`typography` — LinkRow values read "Set days off & custom hours" / "Notice, caps & start times"; design values are "None set" / "Defaults". **Fix:** Use the design value strings "None set" and "Defaults".
- `low`/`extra-element` — NameCard uses Material3 OutlinedTextField, adding a heavy outlined box; design TextInput is a borderless field under the "Name" label. **Fix:** Use a borderless/underlined text field styling instead of OutlinedTextField.
- `low`/`color` — A3WarningCard title uses warning (#D97706) and body uses appTextStrong; design uses amber-900 #92400e title and #78350f body. **Fix:** Tint warning-card title/body with the darker amber tones from the spec.
- `low`/`typography` — Timezone field button omits the muted "· auto" suffix shown in the design's "Pacific Time · auto". **Fix:** Append a muted "· auto" suffix to the timezone label when auto-detected.
- `low`/`typography` — SectionOverline is 9.5sp; design overline type token is 11px. **Fix:** Bump the card overline to ~11sp.
- `low`/`sizing` — A3TimeRangeButton uses Radii.md (8dp) corner radius with a 1dp border; design time-range button is radius 9 with a 1.5px border. **Fix:** Nudge the time-range button radius to 9dp and border to 1.5dp.

## Booking page

### C Booking page preview — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/BookingPage/BookingPagePreviewView.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/PublicPagePreviewScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/PausedExpiredUnavailableState.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/SchedulingStateScaffolding.kt`
- **Verdict:** Both faithfully reproduce the dark preview-chrome bar + caption pill + inert public render (header, event-type cards, sticky "Pick a time") with exit-x only and no back chevron; iOS matches the spec across all four frames, while Android reuses generic shared components for its loading skeleton and paused notice that diverge from the spec's bespoke geometry/icon.
- **Cross note:** Both correctly drop the back chevron (in-place mode flip, exit-x only) and keep the dark preview bar + caption pill solid across all states. Key divergence: iOS hand-rolls preview-specific loading and paused frames that match the spec geometry/moon icon, whereas Android reuses generic shared scheduling components (SchedulingLoadingSkeleton, PausedExpiredUnavailableState) whose geometry and glyph differ from the bespoke spec frames.

**iOS deviations:**
- `low`/`color` — BookingPreviewBar exit button uses appTextInverse.opacity(0.12) (white@12%) — matches spec rgba(255,255,255,0.12); no deviation here, but the dark chrome behind the status bar relies on appText ignoresSafeArea(edges:.top) rather than a dedicated fg1 panel, so the spec's rounded-notch/home-indicator pills are absent (native device chrome substitutes). **Fix:** Acceptable native idiom; optionally darken to spec fg1 (#0b0f17 vs appText #111827) for the bar background if exact match desired.
- `low`/`sizing` — Preview pill vertical inset uses Spacing.s2 (8) wrapping + Spacing.s1 internal vertical; spec PreviewPhone caption block is padding '9px 0 3px' with pill padding 4px 11px. Close but pill horizontal pad is Spacing.s3 (12) vs spec 11px. **Fix:** Minor; pad horizontal 11 to match exactly if pixel-perfection required.

**Android deviations:**
- `med`/`icon` — Paused frame routes to shared PausedExpiredUnavailableState(state=Paused) whose enum icon is PantopusIcon.PauseCircle; spec FramePaused uses a `moon` glyph. **Fix:** Render the paused notice with a moon icon (add a moon-specific notice or pass an icon override) to match the spec's calm moon glyph.
- `med`/`sizing` — Shared paused state uses HALO_SIZE 84dp / HALO_ICON 36dp / BODY_MAX_WIDTH 260dp and vertically-centered Center arrangement with bottom s16 inset; spec NoticeCard is a 60px circle, 26px icon, body maxWidth 220, padding 30/26/60. **Fix:** Use a preview-local notice card matching 60dp circle + 26dp icon + 220dp body width instead of the larger shared terminal halo.
- `med`/`layout` — Loading uses generic SchedulingLoadingSkeleton(rows=4) which renders 4 white cards each with two text-bar shimmers; spec FrameLoading is a centered 64px avatar circle + 150x16 + 110x12 header lines, then three full-width h=64 r=16 card shimmers. **Fix:** Build a preview-specific loading skeleton mirroring the loaded geometry (centered avatar circle + 2 header bars + 3 card rows) like the iOS PreviewLoadingView does.
- `low`/`color` — PreviewBar exit button background uses PantopusColors.appSurfaceDark (#161B22 opaque dark grey); spec is rgba(255,255,255,0.12) translucent white over the dark bar. **Fix:** Use appTextInverse.copy(alpha=0.12f) for the exit-button circle fill to match the translucent-white spec.
- `low`/`missing-element` — Notice/paused state is missing the explicit Error frame styling parity with iOS (Android Error routes to generic ErrorState headline 'Couldn\'t load preview'); spec only defines 4 frames so this is an honest extra, but verify it sits inside the preview chrome (it does, rendered in the weight(1f) Box). **Fix:** None required; confirm ErrorState renders within preview chrome (it does).

### C Embed widget settings — iOS `notfound` / Android `notfound` → better: **equal**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/BookingPage/BookingPageManagementView.swift (listed, not the screen)`, `frontend/apps/ios/Pantopus/Features/Scheduling/Routing/SchedulingRoute.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/ShareLinkSheet.kt`
- **Verdict:** The embed widget settings screen is a web-only desktop surface (1180px two-column block-editor + live browser preview) and is implemented on neither iOS nor Android; searches across both full apps for every unique token (Inline/Popup/Floating type switcher, data-slug script snippet, "Show inline on /b/yourname", "Hide page header", "Copy snippet", "Embed your booking widget") returned zero hits — the closest native surface is the distinct ShareLinkSheet (C3 "Share your link"), which shares no embed content.
- **Cross note:** Both platforms equally omit this screen because the design spec itself is explicitly web-only ("web-only host settings", 1180px desktop two-column layout with a faux-browser live preview); this is most likely an intentional platform gap, not a regression. No mobile parity expected unless design adds a mobile frame.

**iOS deviations:**
- `high`/`missing-element` — Entire screen absent on iOS: no embed-type segmented control (Inline/Popup/Floating), no Appearance card (brand color/theme/hide-header/button-label/calendar-layout), no <script> embed-snippet block with Copy snippet, no 'Show inline on /b/yourname' toggle, and no live preview pane. **Fix:** This is a web-only host-settings archetype (desktop two-column block editor); confirm with design whether a native mobile equivalent is in scope before building — likely intentionally web-only.

**Android deviations:**
- `high`/`missing-element` — Entire screen absent on Android: same as iOS — no segmented embed-type switcher, Appearance card, code-snippet/Copy snippet block, 'Show inline' toggle, or live preview. ShareLinkSheet.kt is a separate C3 'Share your link' bottom sheet (URL/QR/share targets), not the embed settings screen. **Fix:** Web-only archetype; confirm scope with design. If a mobile port is wanted, model it as a Form/settings screen rather than the desktop two-column+preview layout.

### C One-off booking link — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/BookingPage/OneOffLinkGeneratorView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/BookingPage/BookingPageSupport.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/OneOffLinkGeneratorScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/BookingPageComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/ShareLinkSheet.kt`
- **Verdict:** iOS faithfully reproduces the bottom-sheet form (no chrome, grabber from parent, all four states) with only minor spacing/sizing nits, while Android adds a non-spec top bar with a close button and converts the wrapping chip groups to horizontal-scroll rows.
- **Cross note:** Both platforms color the generate-error note copy with the flat error token (#DC2626) instead of the spec's darker #991b1b/#b91c1c reds, and both default the pillar chip to Personal; the key divergence is Android's extra top-bar/close-button chrome and horizontal-scroll chip rows versus iOS's spec-accurate grabber sheet with wrapping chips.

**iOS deviations:**
- `low`/`sizing` — OneOffShareTile uses frame(maxWidth:.infinity).aspectRatio(1) so each share tile fills its full third of the row; spec ShareTarget caps the square tile at maxWidth 54px and centers it. **Fix:** Constrain the tile square to .frame(maxWidth:54) and center it in the column.
- `low`/`spacing` — generatedCard VStack spacing is Spacing.s4 (16); the spec GeneratedSheet uses gap:13 between hero/url/meta/send-via blocks. **Fix:** Use a 13pt spacing in the generated VStack to match the spec.
- `low`/`color` — OneOffGenerateErrorNote colors the title with Theme.Color.error (#DC2626) for both lines; spec uses #991b1b title / #b91c1c body (darker reds). **Fix:** Introduce darker error-fg tokens for the note title/body, or accept as a non-palette nuance.
- `low`/`missing-element` — Error frame in spec replaces the lower form with just an inline 'Generate link' button after the error note; iOS keeps the full configuring form plus the pinned generate bar (no second inline button), a slightly different error layout. **Fix:** Acceptable since the pinned bar still affords retry; optionally collapse the form when generateError is set to mirror the spec frame.

**Android deviations:**
- `high`/`extra-element` — OneOffTopBar renders a 32dp X close button row at the top; the spec is a bottom sheet with only a grabber and has no top bar or close button (header is title + pillar chip only). **Fix:** Remove OneOffTopBar; present the screen as a ModalBottomSheet with a drag handle so dismissal matches the spec.
- `med`/`layout` — ExpiryChips and ChipScrollRow wrap their pills in Modifier.horizontalScroll(); the spec Chips group uses flexWrap:wrap, so '24 hours / 7 days / 30 days / No expiry' should wrap to a second line on 360-wide, not scroll horizontally. **Fix:** Replace the horizontalScroll Rows with a FlowRow (wrapping) layout at 7dp spacing.
- `med`/`icon` — OptionsCard 'Single use' row uses PantopusIcon.Tag; the spec OptionsCard uses the 'ticket' glyph (the MetaChip already uses Ticket). **Fix:** Switch the Single-use row icon to PantopusIcon.Ticket.
- `low`/`icon` — ErrorNote 'Try again' uses PantopusIcon.RefreshCw; the spec uses 'rotate-ccw'. **Fix:** Use a rotate-ccw / counter-clockwise glyph for the retry affordance.
- `low`/`layout` — ConfigBody places the generate-error ErrorNote last (after Options); the spec error frame positions the inline error note directly after Event type, above Availability. **Fix:** Render cfg.error between the Event type and Availability sections to match the spec order.
- `low`/`spacing` — MetaChip uses vertical Spacing.s1 (4dp); spec MetaChip padding is 6px vertical. **Fix:** Use 6dp vertical padding on the MetaChip row.
- `low`/`state` — BLHeaderPill is hardcoded to SchedulingPillar.Personal; spec accent follows the host pillar (Personal sky / Business violet) and iOS drives it from viewModel.theme. **Fix:** Pass the host's resolved pillar into BLHeaderPill instead of the literal Personal.
- `low`/`typography` — ErrorNote title/body are colored PantopusColors.error (#DC2626) for both lines; spec uses #991b1b title / #b91c1c body. **Fix:** Use darker error text tokens for the note copy.

### C Share booking link — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/ShareLinkSheet.swift`, `frontend/apps/ios/Pantopus/Core/Design/Radii.swift`, `frontend/apps/ios/Pantopus/Core/Design/Shadows.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/ShareLinkSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/BookingPageComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Spacing.kt`
- **Verdict:** Both platforms faithfully reproduce all five spec frames (default, draft, copied toast, QR fullscreen, regenerate confirm) with correct sections, order, tokens and icons; iOS matches the spec almost exactly while Android has a handful of minor radius/icon/color nits and an unscannable QR.
- **Cross note:** Both platforms diverge identically on the draft-banner headline color (#92400e in spec); iOS renders a real scannable CoreImage QR while Android's QR is a decorative synthetic grid.

**iOS deviations:**
- `low`/`color` — draftBanner headline uses Theme.Color.appText (#111827) but spec sets banner ink to dark amber #92400e and border to WARNING_BORDER #FDE68A (iOS uses warning.opacity(0.4) for the border). **Fix:** Use a #92400e-equivalent token for the banner headline and the warningLight (#FDE68A) token for the border stroke.
- `low`/`state` — Regenerate confirm uses a native SwiftUI .alert rather than the spec's centered card with a 44px red-circle rotate-ccw medallion and a red Regenerate button; acceptable native idiom but loses the medallion/destructive styling. **Fix:** Optional: render a custom centered confirm card to match the icon medallion + destructive button treatment, as Android does.

**Android deviations:**
- `med`/`icon` — RegenerateButton and RegenerateConfirmDialog use PantopusIcon.RefreshCw (clockwise) but spec specifies rotate-ccw (counter-clockwise) in both places. **Fix:** Swap to a RotateCcw / rotate-ccw glyph to match the spec intent.
- `low`/`sizing` — UrlCard Copy button uses RoundedCornerShape(Radii.md)=8dp vs spec borderRadius 10; QR thumb plate uses Radii.md=8 vs spec 10; 'Show QR' pill uses Radii.md=8 vs spec 9. **Fix:** Use a 10dp radius for the Copy button and QR plate and 9-10dp for the Show QR pill instead of Radii.md.
- `low`/`color` — DraftBanner headline text is PantopusColors.warning (#D97706) but spec uses dark amber #92400e for the banner copy. **Fix:** Introduce/use a #92400e token for the draft-banner headline text.
- `low`/`color` — SettingsCard toggle-row divider (BLToggleRow) uses PantopusColors.appBorderSubtle (#f3f4f6) but spec uses E.border (#e5e7eb). **Fix:** Use the appBorder (#e5e7eb) token for the inter-row divider.
- `low`/`interaction` — ShareLinkSheet toggles (showOnProfile/addToSignature) are local mutableState only and not surfaced via callbacks, so changes don't persist (iOS exposes onToggleShowOnProfile/onToggleSignature callbacks). **Fix:** Hoist the two toggle states to callbacks/parameters so the presenting screen can persist them, matching iOS.
- `low`/`other` — QR on both the thumb and fullscreen is a synthetic decorative grid (qrCells, not scannable), whereas iOS generates a real CoreImage QR; spec QR is decorative so acceptable but functionally weaker. **Fix:** Optional: generate a real QR (e.g. ZXing) so 'Point a camera here' actually works.

### C4 Booking link / page management — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/BookingPage/BookingPageManagementView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/BookingPage/BookingPageSupport.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/BookingPageManageScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookingpage/BookingPageComponents.kt`
- **Verdict:** Both platforms faithfully reproduce all 8 sections in spec order with token-clean card geometry, pillar overlines, sky toggles, slug availability line, footer actions, sticky save bar, and dark Saved toast; iOS edges ahead by matching the design's exact 2-row links card and the lock-icon disabled save state, whereas Android injects an extra "Preview your page" link row not in the spec.
- **Cross note:** Shared strength: both are tokens-only, reproduce the full section order (Status, Your link, Page header, Services, Intro & confirmation, Visibility, links, footer actions over sticky save bar), the live/paused/draft status chip tones, sky-tinted toggles/CTAs regardless of pillar, the slug error border + suggestion chips, and the dark 'Saved' toast with a green bare check. Key divergence: Android's LinksCard carries an extra 'Preview your page' row absent from the spec, while iOS keeps the exact 2-row card; iOS also includes the disabled-save lock glyph that Android drops.

**iOS deviations:**
- `low`/`sizing` — BookingMgmtFooterButton uses Radii.lg (12) corner radius for the Copy/Share/QR buttons; design FooterButtons specify borderRadius:10. **Fix:** Use a 10pt corner radius (between md=8 and lg=12) for the footer action buttons to match spec.
- `low`/`spacing` — BookingMgmtSlugCard slug box uses .padding(.vertical, Spacing.s2)=8; design SlugCard input uses 10px vertical padding ('10px 11px'). **Fix:** Bump slug-box vertical padding to ~10pt.
- `low`/`typography` — Service-row duration uses size 10.5; slightly below the 11px caption token though the design does not pin this value tightly. **Fix:** Optional: raise duration label to 11pt to align with the caption token.
- `low`/`state` — Slug 'checking' state shows an in-box ProgressView; design variants only describe a transient debounce and never render a checking glyph, so intent matches and impact is minimal. **Fix:** Acceptable; ensure the in-box spinner stays within the 12.5pt mono row height.

**Android deviations:**
- `med`/`extra-element` — LinksCard adds a 'Preview your page' LinkRowItem (Eye icon) as the first row; the design LinksCard has only two rows: 'Intake questions' (list-checks) and 'Connect Stripe...' (credit-card). **Fix:** Remove the 'Preview your page' row from LinksCard (or surface preview elsewhere) so the card matches the spec's 2-row layout.
- `low`/`sizing` — RowScopeFooterAction uses Radii.md (8) corner radius for Copy/Share/QR buttons; design FooterButtons specify borderRadius:10. **Fix:** Use a 10dp corner radius for the footer action buttons.
- `low`/`icon` — Disabled save state ('Fix your link to save') renders text only; design BLSaveBar disabled variant prepends a lucide 'lock' icon (iOS includes it via bottomActionIcon:.lock). **Fix:** Prepend a Lock icon before 'Fix your link to save' in BLSaveBar's disabled branch.
- `low`/`icon` — Slug 'checking' hint uses PantopusIcon.Clock with text 'Checking…'; design shows only an in-box spinner with no clock-glyph hint line. **Fix:** Replace the Clock+'Checking…' hint with an inline spinner inside the slug box to match the design's debounce treatment.
- `low`/`interaction` — BLSaveBar saving state uses Material CircularProgressIndicator; design uses a custom 16px thin ring. Visually close but a heavier Material glyph. **Fix:** Optional: swap for a 2dp custom ring sized 16dp to match the spec's spinner weight.

## Discovery

### C Slot states (no-availability etc) — iOS `major` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/DiscoverySlotPickerView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SlotPicker.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingSlotRow.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/NoAvailabilityView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Routing/Stubs/InviteeDiscovery/InviteeNoAvailabilityStubView.swift`, `frontend/apps/ios/Pantopus/Core/Design/Icons.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/SlotPickerScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/DiscoveryComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/NoAvailabilityState.kt`
- **Verdict:** Both platforms reproduce the picker chrome + loading skeleton + a single no-availability card faithfully, but each collapses the design's five distinct slot-states (notably the team-"composing" caption and the framed Home composed-empty card with Free/Busy member dots + explainer pill) into one generic empty state, so both are major; iOS edges ahead because its EmptyCard supports the spec's secondary CTA + count chip while Android's primary no-availability card drops the secondary action entirely.
- **Cross note:** Shared gap: BOTH platforms collapse the design's five slot-states into ~3 (skeleton, day-full, one generic no-availability) and omit the team 'composing' caption+pill (frame 2) and the Home composed-empty framed card with Free/Busy member dots (frame 5); neither renders the 'Join waitlist'/'3 waiting' chip. iOS is slightly closer because its SlotPickerEmptyCard already supports a secondary CTA + count chip (just never invoked with the waitlist variant), whereas the Android no-range card never passes any secondary action.

**iOS deviations:**
- `high`/`missing-element` — Frame 5 composed-empty (Home intersect) is absent: SlotPicker.swift has no framed soft-tinted (p.soft + accent33 border) card, no Free/Busy member-dot row, and no calendar-x variant — only a generic dashed no-availability card. **Fix:** Add a framed variant to SlotPickerEmptyCard (soft bg, accent-tinted border, white halo) plus the three-member Free/Busy avatar row for the Home collective-intersect empty state.
- `high`/`missing-element` — Frame 2 composing is absent: no avatar cluster + 'Finding times that work for everyone' caption and no ComposedPill ('Times come from each member's availability', calendar-range icon, accent-soft pill) above the skeleton list for team/Home hosts. **Fix:** Add a composing caption (AvatarCluster + text) and a ComposedPill below the skeleton when the host is collective.
- `med`/`state` — The two distinct empty frames (no-times-in-range vs no-times-anywhere) are collapsed: noAvailabilityCard only renders the 'no times in month' copy with a See-next-month CTA and 'Get notified' secondary; the 'No times are open right now' / 'Notify me' + 'Join waitlist' + '3 waiting' chip variant is never produced. **Fix:** Add a second LoadState (or flag) for the truly-empty case with bell-primary, waitlist secondary, and count chip.
- `med`/`icon` — Spec uses calendar-search for no-times-in-range, but noAvailabilityCard passes .calendarClock (a code comment concedes calendar-search was unavailable); separately .calendarSearch in Icons.swift maps to a bare magnifyingglass SF symbol, losing the calendar glyph intent. **Fix:** Use .calendarSearch and remap it to calendar.badge SF symbol so the empty-range card shows a calendar-search glyph.
- `low`/`layout` — dayFullCard (SlotPicker.swift) is a borderless centered icon+text+link with s8 vertical padding, not the spec's dashed-border card with 44pt halo, bold title and body line used by the composed-empty/day-full framing. **Fix:** Wrap dayFullCard in the same dashed RoundedRectangle(Radii.xl) card geometry as the empty card for visual consistency.
- `low`/`typography` — Empty-card title uses .system(size:15,weight:.bold) but design title line-height is 20px and max-width 230; without explicit lineSpacing the two-line Home title ('Everyone's calendars don't overlap…') may not match the 20px leading. **Fix:** Set lineSpacing to hit 20pt and clamp width to ~230 to match spec wrapping.

**Android deviations:**
- `high`/`missing-element` — Frame 5 composed-empty is absent: NoAvailabilityState.kt only draws a dashed surface card; no framed soft-tinted (pillar.soft + accent33 border) variant, no white halo, and no three-member Free/Busy avatar-dot row for the Home intersect case. **Fix:** Add a framed parameter to NoAvailabilityState (soft bg + accent border + white circle halo) and a MemberFreeBusyRow composable.
- `high`/`missing-element` — Frame 2 composing is absent in SlotPickerScreen.kt: the SlotRegion loading branch shows only DiscoverySkeletonSlots — no AvatarCluster + 'Finding times that work for everyone' caption and no ComposedPill (calendar-range icon, accent-soft tinted pill). **Fix:** Add a composing caption + ComposedPill above the skeleton when the host is collective/Home.
- `high`/`missing-element` — The month-empty NoAvailabilityState call site (SlotPickerScreen.kt line 438) passes no secondaryLabel/onSecondary, so the spec's 'Get notified when times open' bell secondary button (frame 3) and the 'Join waitlist' + '3 waiting' chip (frame 4) never render — only the primary See-next-month button shows. **Fix:** Pass secondaryLabel/onSecondary (and a count-chip slot) so the no-range and no-anywhere variants carry their secondary CTA + chip.
- `med`/`icon` — For no-times-in-range Android passes PantopusIcon.CalendarX, but the spec uses calendar-search for that frame (calendar-x is reserved for the composed-empty frame 5); the intent glyph is wrong. **Fix:** Use PantopusIcon.CalendarSearch for the month-empty card and reserve CalendarX for the composed-empty state.
- `med`/`extra-element` — SlotPickerScreen.kt adds a 'Pick a day with open times' empty state (selectedDay == null) that has no design frame, and a 'No times left this day' DayFullyBookedNotice whose body copy ('Every slot on this day is booked.') diverges from spec which keeps day-full inside the picker rather than a separate dashed card. **Fix:** Confirm the selectedDay==null branch matches a design state; align day-full copy/treatment with the spec's calm inline framing.
- `low`/`sizing` — Empty-card title uses PantopusTextStyle.body (16sp) while the design title is 15px/700 with 20px line-height; body style is one notch larger than spec. **Fix:** Use a 15sp/Bold style with 20sp line-height for the empty-card title to match the spec.
- `low`/`typography` — DayHeading + month label use PantopusTextStyle.small (14sp) where spec day heading is 13px/700 and month label 13.5px/700 — slightly oversized vs the 13px spec. **Fix:** Drop to ~13sp Bold for the day heading and month label to match spec.

### C Timezone selector — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/TimezoneSelectorSheet.swift`, `frontend/apps/ios/Pantopus/Core/Design/Typography.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/TimezoneSelectorSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/TimezonePicker.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** iOS faithfully reproduces all four design frames (default/search/no-match/overridden) with correct chrome, tokens, and the override banner + search highlight; Android renders the same shell via the shared TimezonePickerSheet but omits the entire overridden-state banner and search-term highlight and oversizes the header title (20sp vs 15.5px).
- **Cross note:** Both rely on the native sheet's system grabber rather than drawing the 38x5 design grabber (acceptable platform idiom). Key divergence: iOS ships all four frames; Android is missing the overridden banner and search highlight, and oversizes the title.

**iOS deviations:**
- `low`/`typography` — Section header `sectionHeader` uses `.overline` (11pt) vs spec SectionLabel 9.5px; Detected pill also uses `.overline` (11pt) vs spec 8.5px, so both run slightly large. **Fix:** Inline 9.5pt for the section label and 8.5pt for the Detected pill instead of the 11pt overline token.
- `low`/`color` — `listCard` has no shadow; spec ListCard carries shadow-sm (0 1px 3px rgba(0,0,0,0.04)). **Fix:** Add a .shadow(radius:1,y:1) at low opacity to match shadow-sm.
- `low`/`spacing` — `searchField` uses .vertical(Spacing.s2=8) vs spec 9px and the row icon is 16pt vs spec 15px search glyph — both negligible. **Fix:** Optional: nudge search padding to 9pt and search icon to 15pt to match exactly.

**Android deviations:**
- `high`/`missing-element` — The overridden state is entirely absent: `TimezonePickerSheet` has no info caption ('You changed this from your detected zone.') and no 'Reset to detected' link (rotate-ccw) — design Frame 4 is unbuilt; grep finds no such strings. **Fix:** Add an info-banner composable (infoBg, primary100 border, radius lg, info icon + Reset-to-detected button) rendered when selectedId != detectedId, mirroring iOS `overriddenBanner`.
- `med`/`missing-element` — No search-term highlight: `ZoneRow` renders zone.name plainly; design highlights the matched substring with an amber <mark> (Frame 2), which iOS implements via `highlightedName`. **Fix:** Build an AnnotatedString that backgrounds the query match in warningBg/#fde68a.
- `med`/`typography` — `Header` title uses `PantopusTextStyle.h3` = 20sp/SemiBold vs spec 15.5px/700 — markedly oversized for a sheet title. **Fix:** Use a ~15.5sp Bold inline style (or `small`/`body` bumped to bold) for the title.
- `low`/`typography` — `Header` 'Done' uses `body` (16sp) SemiBold vs spec 14.5px/700; section label and Detected pill use `overline` (11sp) vs spec 9.5px/8.5px — all slightly large. **Fix:** Drop Done to ~14.5sp Bold and use smaller inline sizes for section label (9.5sp) and pill (8.5sp).
- `low`/`sizing` — Check column Box is 22dp wide vs spec 18px; header left spacer is 56dp vs spec 48px, slightly skewing title centering. **Fix:** Set the check Box width to 18dp and the leading spacer to 48dp.
- `low`/`color` — `ZoneList` card has no shadow; spec ListCard uses shadow-sm. **Fix:** Wrap the card in a Surface/Modifier.shadow at low elevation to match shadow-sm.
- `low`/`spacing` — `SearchBox` inner padding uses vertical Spacing.s3 (12dp) vs spec 9px, making the field taller than the design. **Fix:** Reduce search field vertical padding to ~9dp.

### C1 Booking landing — iOS `major` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/BookingLandingView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/DiscoveryNotice.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/DiscoverySupport.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/BookerLandingContent.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/BookerLandingScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/DiscoveryComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/DiscoveryModels.kt`
- **Verdict:** Both platforms faithfully render the default/single/paused/empty/error frames with correct banner+overlapping-header+event-row+footer geometry, but both omit the entire group-event card (Save your spot / Fully booked / waitlist), the team-cluster avatar, and the composed/round-robin pill; iOS is closer because it includes the "View {name}'s profile" footer link and correctly suppresses the open-in-app banner in single/empty/paused frames, both of which Android gets wrong.
- **Cross note:** Shared gap: BOTH platforms omit the group-event card (Save your spot / Fully booked / waitlist), the team-cluster avatar with 'Meet with the team', and the composed/round-robin pill — 3 of the 9 design frames are unimplemented on each. Key divergence: iOS gates the open-in-app banner to multi-type pages and includes the 'View profile' footer link and an in-scaffold paused/empty card; Android always shows the open-in-app banner, drops the 'View profile' link, and diverts paused to a separate full-screen terminal state.

**iOS deviations:**
- `high`/`missing-element` — No group-event card: BookingLandingView only renders EventTypeCardRow lists; the spec's GroupEventCard (Sat Jun 21 fixed-time tile, '4 of 8 spots left' / 'Fully booked' pill, 'Save your spot' filled CTA and 'Join waitlist' secondary) is entirely absent. **Fix:** Add a GroupEventCard view for group/seated event types with the fixed-time row, seats pill, and Save-your-spot / Join-waitlist buttons.
- `high`/`missing-element` — No team/composed variant: HostAvatar only draws a single initials disc; the spec's FrameTeam uses a 3-avatar overlapping TeamCluster (52dp discs, -18 overlap) with 'Meet with the team' headline. **Fix:** Render an overlapping multi-avatar cluster when the page owner is a team/collective and use the 'Meet with the team' headline.
- `med`/`missing-element` — No composed-availability pill: the spec's ComposedPill (violet businessBg banner, calendar-range icon, 'Times come from each member's availability.' + 'Round-robin' tag) shown above the list for team pages is not implemented. **Fix:** Add a ComposedPill above the list for round-robin/collective pages.
- `low`/`sizing` — Header name uses .pantopusTextStyle(.h3) which is 20pt/600 per the token scale, but the spec name is fontSize:18 fontWeight:700. **Fix:** Use an 18pt/bold style for the host name instead of .h3.
- `low`/`icon` — DiscoveryLocation.icon falls back to .calendar for unknown/empty location modes, whereas spec EventTypeRow leading tiles use mode-specific glyphs (video/users/phone) — the default video event uses the spec's 'video' icon correctly but the fallback differs from the spec's per-row icon set. **Fix:** Default unknown modes to .video (matching the spec/Android) rather than .calendar.
- `low`/`color` — Empty-state title uses .small (14pt) but spec FrameEmpty/FramePaused title is fontSize:14.5 fontWeight:600 — acceptable, however the paused icon is .moon (matches) while empty is .calendarOff in a rounded tile (matches); only minor type-size drift. **Fix:** Nudge notice title to ~14.5pt if exact parity is desired; otherwise leave.

**Android deviations:**
- `high`/`missing-element` — No group-event card: BookerLandingContent only defines EventTypeRow; the spec's GroupEventCard (fixed-time tile, '4 of 8 spots left' / 'Fully booked' pill, 'Save your spot' CTA, 'Join waitlist' secondary) is absent. **Fix:** Add a GroupEventCard composable mirroring the spec for group/seated event types.
- `high`/`missing-element` — No team cluster or composed pill: HostAvatar is single-disc only and there is no ComposedPill; spec FrameTeam needs the 3-avatar overlapping cluster, 'Meet with the team' headline, and the violet 'Round-robin' explainer pill. **Fix:** Render an overlapping multi-avatar cluster + ComposedPill for team/round-robin pages.
- `high`/`missing-element` — LandingFooter omits the 'View {name}'s profile' link with arrow-up-right icon; it renders only the 'Powered by Pantopus' row. Spec Footer shows both. **Fix:** Add a primary600 'View {hostName}'s profile' button with PantopusIcon.ArrowUpRight above the Powered-by row.
- `med`/`extra-element` — In LandingLoaded the OpenInAppBanner is rendered unconditionally for every loaded page (including single-type, paused, and empty), but the spec shows the open-in-app banner only on the multi-type default frame (FrameMulti) and never on single/paused/empty. **Fix:** Show OpenInAppBanner only when eventTypes.size > 1 and not paused/empty (mirror iOS's count>1 gate).
- `med`/`extra-element` — SectionOverline('Book a time') is rendered above the paused and empty in-card states (it sits outside the when-branch), but spec FramePaused/FrameEmpty place the card directly in ListArea with no section label. **Fix:** Move the 'Book a time' overline inside the else (event-list) branch so it doesn't show in paused/empty.
- `med`/`state` — Paused/unavailable maps to BookerLandingUiState.Terminal → full-screen PausedExpiredUnavailableState with a 'Go back' button, diverging from spec FramePaused which keeps the banner+header and shows a calm in-list paused card (moon icon, 'This page isn't taking bookings right now'). **Fix:** Route paused into the in-list LandingPausedCard within the normal landing scaffold instead of a separate full-screen terminal state.
- `low`/`icon` — LandingPausedCard uses PantopusIcon.PauseCircle and LandingEmptyCard uses PantopusIcon.CalendarX, whereas spec uses 'moon' (paused) and 'calendar-off' (empty). **Fix:** Switch paused icon to a moon glyph and empty icon to calendar-off to match the spec intent.
- `low`/`color` — PillarBanner uses a 2-stop linear gradient (e.g. primary400→primary800) while the spec banner is a 3-stop gradient (light 0% → accent 58% → dark 100%), making the Android banner read flatter/darker. **Fix:** Use a 3-stop brush (primary300/400 → primary600 → primary800) to match the spec banner ramp.

### C2 Slot picker — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/DiscoverySlotPickerView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SlotPicker.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingSlotRow.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Discovery/DiscoverySupport.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/SlotPickerScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/SlotPicker.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/NoAvailabilityState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/DiscoveryComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/discovery/SlotPickerViewModel.kt`
- **Verdict:** Both platforms faithfully reproduce the stacked summary-header + month-calendar + Morning/Afternoon slot-row layout with correct accent/today/selected styling; iOS edges ahead because it renders the DST/timezone-change hint (absent on Android) while Android adds a non-spec docked Continue bar, though iOS in turn under-builds the day-full card.
- **Cross note:** Both share the same MonthCalendar/slot-row geometry and both correctly omit in-screen reschedule-banner and slot-taken-toast frames (those live in separate Reschedule/SlotTaken screens). The key divergence: iOS renders the DST hint but Android drops it entirely; Android adds a Continue bar absent from the spec default while iOS keeps the tap-to-advance model but under-builds the day-full card.

**iOS deviations:**
- `low`/`color` — infoCaption (SlotPicker.swift:150) renders the DST hint text in appTextSecondary gray with no border and Radii.sm (6) corners, vs spec's INFO #0369A1 text, INFO_BORDER #BAE6FD 1px border, and 11px radius on an INFO_BG card. **Fix:** Use Theme.Color.info for text, add a 1px info-border, and bump the corner radius to ~11 to match the spec's tz-hint banner.
- `med`/`missing-element` — dayFullCard (SlotPicker.swift:207-221) is a bare centered icon + one line + link, omitting the spec QuietNotice's dashed-border card, circular icon halo, and body copy 'Every slot on this day is booked.' **Fix:** Render day-full with the same dashed-border + circle-halo card shape used for noAvailabilityCard, including the body line.
- `low`/`icon` — noAvailabilityCard (SlotPicker.swift:228) uses .calendarClock as a stand-in; spec frame 4 uses calendar-off and the day-full spec uses calendar-x. **Fix:** Add calendar-off / calendar-x glyphs to PantopusIcon and use them per frame.
- `low`/`spacing` — Summary tile uses Spacing.s3 (12) for both axes; spec is padding 11px 12px and adds a shadow-sm, which iOS omits (border only). **Fix:** Use 11pt vertical padding and add the shadow-sm drop shadow on the summary card and calendar card.

**Android deviations:**
- `med`/`missing-element` — SlotPickerUiState.Content (SlotPickerViewModel.kt:285) has no dstHint field and SummaryHeader (SlotPickerScreen.kt:216) never renders the timezone/DST info banner from spec frame 5 ('Clocks change this weekend — times are adjusted.'). **Fix:** Add a dstHint string to Content and render an INFO_BG/INFO-border caption under the timezone chip when present, mirroring iOS.
- `med`/`extra-element` — A docked ContinueBar (SlotPickerScreen.kt:202-210, 486) appears after a slot is chosen; the spec's default/day-with-slots frame has no footer and advances on the row's chosen check-circle state — only the reschedule-cutoff frame has a footer ('Message host'). **Fix:** Confirm the booking flow truly needs a Continue CTA; the spec selects-then-advances, so consider removing the bar or aligning it to a spec'd footer.
- `low`/`missing-element` — DiscoverySkeletonSlots (DiscoveryComponents.kt:149) draws only a leading icon shimmer + time bar, omitting the spec SkelSlotRow's trailing chevron square that iOS includes. **Fix:** Add a trailing Spacer + 16x16 shimmer square to match the loaded row geometry.
- `low`/`spacing` — Summary tile (SlotPickerScreen.kt:228) uses Spacing.s3 (12) on both axes and no shadow; spec is padding 11px 12px with shadow-sm on the white cards. **Fix:** Use 11dp vertical padding and add an elevation/shadow to the summary and calendar cards.
- `low`/`typography` — MonthCalendarCard month title and DayHeading use PantopusTextStyle.small (14sp) at FontWeight.Bold; spec month title is 13.5px/700 and day heading 13px/700, so they render slightly larger than the design. **Fix:** Use a 13-13.5sp bold inline size for the month title and day heading to match the spec scale.

## Confirm

### D Booking confirmed — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeConfirmedView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/ConfirmSupport.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeConfirmedViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ConfirmedScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ConfirmComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/InviteeConfirmFlow.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/InviteeConfirmModels.kt`
- **Verdict:** Both render the success/pending hero, summary card, receipt, calendar cluster, manage note and dock faithfully; iOS is closer (correct 104/80 halo, confetti, sending-receipt shimmer) while Android shrinks the halo, drops confetti and the receipt sending state, uses the wrong manage icon, and shows the account nudge on every variant.

**iOS deviations:**
- `med`/`missing-element` — receiptCapsule in InviteeConfirmedView omits the spec's monospace TXN/timestamp line (ReceiptCapsule renders 'TXN_4F9C20A1 · Jun 13, 2026 · 9:41 AM' above the divider). **Fix:** Add a Theme.Color.appTextSecondary monospaced 10pt line with the transaction id + date below the amount row.
- `low`/`sizing` — ConfirmPrimaryButton uses frame(height: 48) but the spec Dock primary button is height:46. **Fix:** Set the dock CTA height to 46 to match the spec.
- `low`/`missing-element` — No account NudgeCard is rendered on the free/confirmed variant (spec FrameConfirmedFree ends with NudgeCard 'Create an account to manage your bookings'). **Fix:** Append a NudgeCard below manageNote for the unauthenticated free path.
- `low`/`typography` — Dock primary label uses 14.5pt bold while spec Dock button is fontSize:14/700. **Fix:** Drop the primary CTA label to 14pt to match spec.
- `low`/`color` — Hero body uses Theme.Color.appTextStrong (fg2 #374151) which matches spec E.fg2; acceptable — no change needed but verify against fg3 usages elsewhere. **Fix:** No change; documented for completeness.

**Android deviations:**
- `med`/`sizing` — ConfirmHalo uses size 96dp outer / 74dp inner, but the spec Halo is 104 outer / 80 inner with a third pulse ring. **Fix:** Resize the halo to 104dp/80dp and add the soft pulsing outer ring.
- `high`/`state` — ReceiptCapsule only handles paid/deposit; the spec's 'sending' mode (shimmer bar + mail icon, frame FrameEmailSending) and PriceMode enum (Free/Full/Deposit) have no Sending case, so the email-sending variant is unreachable. **Fix:** Add a sending/processing mode that swaps mail-check for mail and renders a shimmer line instead of 'Receipt emailed'.
- `med`/`missing-element` — ReceiptCapsule omits the spec's monospace TXN/timestamp line ('TXN_4F9C20A1 · Jun 13, 2026 · 9:41 AM') shown above the divider. **Fix:** Add a monospace appTextSecondary caption line with the transaction id and date below the amount row.
- `med`/`icon` — ManageNote uses PantopusIcon.CalendarClock, but the spec ManageNote icon is settings-2 (a gear). **Fix:** Switch the manage-note leading icon to a settings/gear glyph to match settings-2.
- `med`/`extra-element` — AccountNudge is rendered unconditionally in ConfirmedBody, but the spec only shows NudgeCard on the free confirmed frame (paid/deposit/credit/pending frames have no nudge). **Fix:** Gate AccountNudge to the free, signed-out, non-pending variant only.
- `low`/`interaction` — No confetti is played on mount; the spec fires ConfettiSpray on confirmed (non-pending) frames (suppressed under reduced motion). **Fix:** Add a confetti overlay on the confirmed body, disabled when the system reduce-motion setting is on.
- `low`/`missing-element` — No CreditUsedRow (ticket-check 'session credit used') for the package-credit-redeemed frame, and no AppBanner/RedirectPill variants exist. **Fix:** Add a credit-used capsule and the app-user/redirect variants if those frames are in scope.
- `low`/`color` — ApprovalEtaPill tints icon+text with PantopusColors.info; spec EtaPill info tone uses INFO_DK (#0369A1, primary700) for the foreground text, not the brighter info blue. **Fix:** Use primary700 for the ETA pill text/icon to match the darker info-tone foreground.

### D Intake booking form (invitee) — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeIntakeFormView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeIntakeFormViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/ConfirmSupport.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/IntakeFormScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/InviteeConfirmFlow.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ConfirmComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/InviteeConfirmModels.kt`
- **Verdict:** iOS faithfully reproduces all key intake frames (default/prefilled-chip/errors/expired) with only small CTA-sizing and a missing existing-account banner; Android omits the entire prefilled "Booking as" chip + "Not you?" path and the existing-account InfoBanner, and hardcodes the phone field into "Your info" instead of the host-questions section.
- **Cross note:** Both platforms use the correct token palette (sky/primary accent, error/warn/info tones, 8dp input radius, 44dp inputs, alert-circle/check-circle validation icons) and both reproduce the summary card, hold row, validation errors, and add-guests collapsed/expanded states well. Shared divergence: neither nails the CTA at 46 (both 48). Key divergence: iOS implements the prefilled 'Booking as' chip + 'Not you?' and a question-driven phone field, while Android lacks the prefilled chip entirely and hardcodes phone into 'Your info', so iOS covers more of the six spec frames.

**iOS deviations:**
- `med`/`missing-element` — No existing-account InfoBanner frame: the spec's FrameExistingAccount renders an info banner ("You have an account. Open in app…", INFO_BG #F0F9FF / INFO_BORDER #BAE6FD) inside the Your-info section; InviteeIntakeFormView never shows it. **Fix:** Add an info-tone ConfirmBanner under yourInfoSection when the entered email matches a known account, with an 'Open in app' link.
- `low`/`sizing` — ConfirmPrimaryButton uses height 48 while the spec PrimaryCTA is height:46; CTA label is 14.5 vs spec fontSize:14. **Fix:** Set the button frame height to 46 and the label font size to 14 to match the spec.
- `low`/`state` — No submitting/shimmer CTA on this screen (ConfirmShimmerButton 'Submitting your booking' exists but is unused here); acceptable since iOS commits on D2, but the spec FrameSubmitting belongs to D1. **Fix:** If a brief D1→D2 transition is shown, swap the footer to ConfirmShimmerButton('Submitting your booking') and dim the body opacity ~0.85.
- `low`/`color` — Info-banner foreground in ConfirmBanner.info uses primary700; spec body text is #0c4a6e (primary-900) with primary600 link — slightly lighter than the spec ink (only relevant once the banner is added). **Fix:** Use appText/primary900 for the info banner body copy and primary600 for the link to match the spec.

**Android deviations:**
- `high`/`missing-element` — No prefilled 'Booking as <name>' chip: IntakeFormBody always renders the editable first/last/email fields; ConfirmFlowState/IntakeValues have no isPrefilled path, so the spec's FramePrefilled (avatar chip + email + 'Not you?') is entirely absent. **Fix:** Add an isPrefilled flag + BookingAsChip composable (avatar, 'Booking as …', email, 'Not you?' clearing prefill) shown in place of the Your-info fields when a signed-in invitee identity is present.
- `high`/`missing-element` — No existing-account InfoBanner: spec FrameExistingAccount shows an info banner ('You have an account. Open in app…') below the email field; the Android body has no such banner (only an error banner at the top of the flow). **Fix:** Render a ConfirmBanner(tone=Info, icon=Info) with an 'Open in app' action under the email field when the email resolves to an existing account.
- `med`/`layout` — Phone is a hardcoded field inside the 'Your info' section (LabeledField 'Phone number' + helper 'For a text reminder before your booking.'), but the spec places phone under the 'A few questions' overline as a host question; this re-orders/misplaces the section and the helper copy differs from spec ('before the call.'). **Fix:** Drive phone from the host-questions schema (under the 'A few questions' overline) rather than a fixed Your-info field, and align the helper text with the spec.
- `med`/`state` — Submitting shimmer label is wrong for the slot-expired interaction and color-only: when holdExpired the body is dimmed via alpha but the spec FrameExpired dims to opacity 0.5 with a warn ExpiredBanner — Android dims the summary card to 0.55 and shows the banner, but the rest of the form (Your info/questions/guests) is not wrapped in the 0.5 dim group as the spec does. **Fix:** Wrap the whole non-banner body in an alpha(0.5)+disabled group when holdExpired (matching FrameExpired) instead of only dimming the summary card.
- `low`/`typography` — Field label uses fontSize 11.5sp SemiBold (matches), but the host-questions textarea/select all collapse to a plain text input (placeholder 'Type your answer'); spec textarea placeholder is host-interpolated ('A sentence or two helps Maria prepare.') and select shows 'Select one' with a chevron. **Fix:** Render typed widgets for textarea (host-interpolated placeholder) and select (chevron + 'Select one') when the question schema is present, mirroring iOS questionField.
- `low`/`sizing` — FilledCta uses heightIn(min=48.dp); spec PrimaryCTA height is 46. **Fix:** Set the CTA height to 46.dp to match the spec.

### D Manage your booking — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeManageBookingView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/ConfirmSupport.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingStatusPill.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ManageBookingScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ConfirmComponents.kt`
- **Verdict:** Both ship all states with the right section order and components, but Android's token-expired state and PolicyCard drop the spec's required "Contact host"/"Request a new link" actions while iOS only has minor sizing/icon nits.
- **Cross note:** Both omit the design's distinct 30px avatar nothing — iOS oversizes it (38), Android is correct. Key divergence: Android strips the two token-expired CTAs and the PolicyCard 'Contact host' button that iOS faithfully renders, so Android is materially less complete despite both covering all lifecycle states.

**iOS deviations:**
- `med`/`icon` — SchedulingStatusPill renders label-only with no leading icon; spec StatusBadge has a 13px check-circle-2/history/x-circle glyph before the label. **Fix:** Add the status icon (Icon 13px, strokeWidth 2.3) inside the pill before the text.
- `low`/`sizing` — Status pill text is 10/bold; spec is 11.5/700. **Fix:** Bump the pill font to size 11.5, weight .bold.
- `med`/`sizing` — BookingSummaryCard identityRow uses a 38pt HostAvatarBadge; spec avatar is 30px. **Fix:** Pass size: 30 to HostAvatarBadge for the manage-booking summary.
- `low`/`typography` — Summary event-name Text uses size 14/bold; spec is 13/700. **Fix:** Set the event-name font to size 13, weight .bold.
- `low`/`color` — cancelledBanner title uses Theme.Color.error; spec uses the darker ERR_DK (#991B1B) for the headline. **Fix:** Use a darker error token (error-dark / #991B1B) for the cancelled-on headline only.

**Android deviations:**
- `high`/`missing-element` — ManageExpired only renders a single 'Go back' button; spec token-expired footer has a primary 'Request a new link' (mail icon) plus a 'Contact host' ghost button. **Fix:** Replace 'Go back' with a PrimaryButton 'Request a new link' (mail icon) + a ghost 'Contact host' button matching iOS expiredHalo.
- `high`/`missing-element` — PolicyCard renders only the info icon + body text; spec PolicyCard includes a 'Contact Maria' mail-icon button (PILLAR) beneath the body. **Fix:** Add a primary-colored 'Contact {hostName}' button with a 12dp mail icon below the policy text.
- `med`/`typography` — ActionRow sub uses PantopusTextStyle.caption (12sp); spec action sub-label is 10.5px. **Fix:** Use a 10.5sp style for the action-row sub text.
- `med`/`state` — Loading state calls generic SchedulingLoadingSkeleton(rows=3) instead of mirroring the loaded geometry (badge pill + summary card with avatar/lines/chip + overline + two 56dp action tiles). **Fix:** Hand-roll a skeleton matching the spec FrameLoading layout like iOS loadingSkeleton.
- `low`/`color` — Cancelled-banner 'Book again' action label is drawn in error red via ConfirmBanner (fg) with no rotate-ccw icon; spec renders it in PILLAR with a rotate-ccw glyph. **Fix:** Render 'Book again' in the pillar accent with a 13dp rotate-ccw icon, not the banner's error fg.
- `low`/`color` — Token-expired title uses h3 (20sp SemiBold); spec headline is 20/700 bold. **Fix:** Set the expired title fontWeight to Bold.
- `low`/`other` — Copy drift: Past note reads 'This booking has already happened.' (spec 'This call has already happened.') and expired body says 'Check your latest confirmation email' (spec 'Request a fresh one and we'll email it to you.'). **Fix:** Align both strings with the spec copy.

### D My bookings (customer) — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/MyBookingsView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/MyBookingsViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/EdgeComponents.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/EdgeSupport.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingStatusPill.swift`, `frontend/apps/ios/Pantopus/Core/Design/Components/EmptyState.swift`, `frontend/apps/ios/Pantopus/Core/Design/Radii.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/customer/MyBookingsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/customer/MyBookingsModels.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/EmptyState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Spacing.kt`
- **Verdict:** Both faithfully ship the chrome + segmented + grouped white-card rows with status pills and the loading/empty/error/loaded states, but neither builds the "Book again" or "Pay/Balance due" affordances; iOS is near-pixel (gradient+initials avatar, correct type sizes) while Android diverges on title size, avatar treatment, overline/pill type tokens, segmented radius/gap, and drops the empty-state CTA.
- **Cross note:** Both platforms omit the same two spec affordances — the 'Book again' past-row footer and the 'Pay $40 / Balance due' action-needed footer (plus the Balance/Approve pill variants) — because the lean /my-bookings payload lacks event/host/manage data; this is a shared, documented backend gap rather than a pure UI miss. Key divergence: iOS keeps spec type sizes and the gradient+initials avatar and ships the empty CTA, whereas Android oversizes the title, uses token type styles that don't match the spec's smaller sizes, substitutes a flat User-icon avatar, and drops the empty-state CTA.

**iOS deviations:**
- `med`/`missing-element` — FrameActionNeeded's pay affordance (footer 'Pay $40' pill + '$40 due at confirm', spec lines 191-195) and FramePast's 'Book again' footer (rotate-ccw + 'Book again', spec lines 184-189) are not rendered — bookingRow only ever draws the status pill + chevron. **Fix:** Add an optional footer row to bookingRow with a top-border divider for the bookAgain link and the payAffordance balance/Pay button.
- `low`/`missing-element` — Row secondary line shows only the {when}; spec line 177 is 'with {host} · {when}', and the primary line should be the event-type name not a synthesized '{Pillar} booking' title (titleLabel, line 169-171). **Fix:** Render event-type name + host once the lean /my-bookings payload joins them; acceptable as a documented backend gap today.
- `low`/`color` — StatusPill maps pending → warning (amber) tone (SchedulingStatusPill tone, lines 81-83), but spec's pending pill is INFO blue (bg #F0F9FF / fg #0369A1, my-bookings StatusPill 'pending', line 157). **Fix:** Give the my-bookings pending status the info tone to match the booker-side spec, or pass an explicit info kind.
- `low`/`color` — Segmented container uses Radii.lg (12) and buttons Radii.sm (6); spec uses 10 (container) and 7 (buttons) (Segmented, lines 70-78). **Fix:** Use a 10pt container radius and 7pt button radius (or accept as <20% nit).
- `low`/`missing-element` — Empty-state hero icon is calendar but spec empty CTA is 'Find something to book' with a leading search glyph (FrameEmpty, lines 275-277); iOS ships the CTA title text but with no leading icon and a no-op action. **Fix:** Add a search leading icon to the EmptyState CTA and wire the discovery destination when available.

**Android deviations:**
- `high`/`typography` — TopBar title uses PantopusTextStyle.h3 = 20sp/SemiBold (line 163); spec top-bar title is 15px/600 (TopBar, line 62) — the title is ~33% oversized. **Fix:** Render the title at 15sp SemiBold (a caption/small-derived style) instead of h3.
- `high`/`missing-element` — EmptyBody() calls EmptyState with no ctaTitle/onCta (lines 361-368), so the primary 'Find something to book' button (spec lines 275-277) is absent on Android, unlike iOS. **Fix:** Pass ctaTitle = "Find something to book" with a search-icon CTA and a discovery handler.
- `med`/`icon` — HostAvatar draws a flat pillar-bg circle with a generic User glyph (lines 300-310); spec HostAvatar is a 135° pillar gradient disc with white initials + a bordered pillar identity dot (HostAvatar, lines 145-151). iOS matches the gradient+initials spec. **Fix:** Use a pillar LinearGradient fill with the host initials text, mirroring iOS EdgePillarAvatar.
- `med`/`typography` — GroupOverline uses PantopusTextStyle.overline = 11sp/SemiBold (lines 250-253); spec overline is 10px/700 with 0.08em tracking (Overline, line 139). **Fix:** Render the overline at 10sp Bold with 0.08em letterSpacing.
- `med`/`typography` — StatusPill label uses PantopusTextStyle.overline (11sp/SemiBold, line 347); spec pill text is 9.5px/700 (StatusPill, line 163), so Android pills read noticeably larger/lighter. **Fix:** Set the pill label to ~9.5sp Bold.
- `med`/`missing-element` — BookingPillKind only has Confirmed/Pending/Past/Cancelled (MyBookingsModels lines 14-18) and BookingRow never renders a footer; spec's 'Balance due'/'Approve pending' pills and the Pay/Book-again footers (spec lines 159-160, 184-195) are unbuildable. **Fix:** Add Balance/Approve pill kinds and an optional row footer for pay + book-again affordances.
- `low`/`spacing` — SegmentedTabs has no inter-button gap (uses Spacing.s0 and md=8 container radius, lines 183-196); spec is gap:3 with container radius 10 and button radius 7 (Segmented, lines 70-78). **Fix:** Add a 3dp gap between the two segment boxes and use a 10dp container / 7dp button radius.
- `low`/`missing-element` — LoadingSkeleton renders 4 ungrouped skeleton rows (lines 371-396); spec loading frame groups 2+2 rows under two shimmer overline bars (FrameLoading, lines 298-313). iOS mirrors the grouped skeleton. **Fix:** Emit a shimmer overline bar before each pair of skeleton rows.

### D Review and confirm — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeReviewConfirmView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/InviteeReviewConfirmViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Confirm/ConfirmSupport.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ReviewConfirmScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/InviteeConfirmFlow.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/confirm/ConfirmComponents.kt`
- **Verdict:** Both render the who/what/when/where summary card + price block + Stripe-button payment section faithfully, but both intentionally defer the fee/tax line items, package-credit ($0/CreditChip), saved-card rows, ApplyCreditRow, and the card-error/3DS & Stripe-unavailable payment frames; iOS is closer (blurred shadowed footer, correct surface tokens, tappable Refund-policy link, matching confirming dim+shimmer and slot-taken banner) while Android drops the Refund-policy accent link, uses a plain footer, and a flat sunken shimmer box.
- **Cross note:** Both platforms share the same deferral scope: single priced line instead of the fee/tax itemization, no package-credit/$0 mode, no saved cards, no ApplyCreditRow, and no dedicated card-error/3DS or Stripe-unavailable frames (all gated behind the real Stripe/A14 integration). The key divergence is fidelity of the present pieces: iOS matches the blurred sticky footer, shadowed CTA, tappable Refund-policy link, and the confirming dim+shimmer; Android uses a plain footer, a flat sunken shimmer, and omits the Refund-policy link.

**iOS deviations:**
- `med`/`missing-element` — TotalsBox renders only one priced line (priceRows) — the spec's 3-row itemization (Intro call · 30 min $40, Service fee $3.00, Tax $5.00) is collapsed; the credit row (−$48.00 in success green) and package-credit $0 strike-through mode are absent entirely. **Fix:** Add fee/service/tax/credit PriceRow support and a credit ($0 + strike-through + CreditChip) total mode once the model exposes fee/tax/credit fields.
- `med`/`missing-element` — No ApplyCreditRow ('Apply package credit or promo code' dashed tag button) which the spec shows in the full/deposit/credit frames below the refund line. **Fix:** Add the dashed-border tag-icon button beneath refundLink in priceSection.
- `med`/`missing-element` — Payment section only ever shows the single PaymentMethodButton; the spec's SavedCardRow rows (Visa Default / Mastercard brand badges + 'Use another method' link, frames 5/8) are not implemented. **Fix:** Render saved-card rows with BrandBadge + Default pill when the booker is logged in (behind A14 payments).
- `low`/`state` — Dedicated card-error/3DS (warn banner + 'Try again' rotate-ccw CTA) and Stripe-unavailable (info cloud-off banner + disabled CTA) frames are not modeled; the generic confirm() error path shows alertTriangle banners instead. **Fix:** Add inline-banner tones for declined/3DS and Stripe-down with the matching warn/disabled CTA states.
- `low`/`color` — totalsBox uses appSurfaceMuted (#f8fafc) vs the spec's #fafbfc fill — visually negligible but a token approximation. **Fix:** Acceptable; no change needed unless an exact token is added.

**Android deviations:**
- `high`/`missing-element` — RefundLine drops the tappable 'Refund policy' accent link and the spec copy ('Free cancellation up to 24h before. Refund policy'); it shows a single non-interactive sentence and no chevron/sheet trigger, unlike the iOS refundLink and the spec RefundLink. **Fix:** Append a primary-600 'Refund policy' span that opens the cancellation-policy sheet, mirroring iOS.
- `med`/`missing-element` — PriceBreakdown renders only one priced line — the spec's Service fee / Tax rows, the success-green credit row (−$48.00), and the credit $0 strike-through total mode (PriceMode.Credit) are not rendered. **Fix:** Add fee/tax/credit LineRows and a credit total mode with strike-through + green $0.
- `med`/`missing-element` — No ApplyCreditRow (dashed 'Apply package credit or promo code' button), no SavedCardRow brand-badge rows, and no 'Use another method' link — payment region is only the single PaymentMethodButton. **Fix:** Add the dashed apply-credit button and saved-card rows (BrandBadge + Default pill) behind the paid flag.
- `med`/`chrome` — ConfirmFooter is a plain appSurface Column with a top HorizontalDivider; the spec footer is a translucent blurred bar (rgba(255,255,255,0.97)+blur) with a top hairline, and the CTA lacks the spec's colored drop shadow (0 6px 16px accent). **Fix:** Use a translucent/elevated surface for the footer and add the accent-tinted shadow to FilledCta to match the spec sticky dock.
- `med`/`state` — The confirming state is a flat appSurfaceSunken box with text ('Confirming your booking'); the spec frame 6 uses a shimmer skeleton CTA AND dims+disables the whole form (pointerEvents:none, opacity 0.85), which Android does not apply to the body. **Fix:** Add a shimmer to the in-flight CTA and apply opacity/disable to the ReviewConfirmBody while submitting, mirroring iOS .opacity(0.85).
- `low`/`state` — Dedicated slot-taken error banner (calendar-x, 'This time was just taken … Nothing was charged' + dimmed form) and the card-error/Stripe-unavailable frames are not modeled; conflicts surface only via a bottom sheet, not the inline frame-7 banner. **Fix:** Render the inline error banner above a dimmed summary on conflict, matching frame 7.
- `low`/`typography` — Summary identity 'with {host}' uses PantopusTextStyle.caption (12sp) where the spec is 11.5px, and the price 'We'll confirm…' footer note uses caption vs spec 10px — minor scale drift. **Fix:** Tighten the host sub-line and footer note sizes toward spec (11.5/10).
- `low`/`color` — PriceBreakdown background uses appSurfaceRaised (#F9FAFB) vs spec totals fill #fafbfc — negligible token approximation (iOS uses the closer #f8fafc). **Fix:** Acceptable; align to the same surface token iOS uses if exactness matters.

### D/E Reschedule — iOS `major` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/RescheduleReassignSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/RescheduleReassignViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SlotPicker.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingsSupport.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/RescheduleReassignSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/RescheduleReassignViewModel.kt`
- **Verdict:** Both render the reschedule sheet (current→new header, tz chip, slots, authority toggle, notify, propose-sent, error) faithfully, but iOS swaps the design's compact 5-day weekday strip for a full month-calendar SlotPicker and reduces the member-avatar rail to a single toggle, while Android mirrors the design's strip/rail with only minor nits; neither renders the inline amber conflict banner (both route 409 to a separate sheet).
- **Cross note:** Shared gap: neither platform renders the design's inline amber conflict banner (Frame 6) — both replace it with a separate SlotTaken/ConflictAlternatives sheet on 409. Key divergence: Android faithfully reproduces the 5-day weekday strip + avatar rail from the design, whereas iOS reuses the month-calendar SlotPicker and collapses the avatar rail into a single reassign toggle, making Android the closer match.

**iOS deviations:**
- `high`/`layout` — iOS embeds the shared SlotPicker (full month-grid calendar + 'Times shown in' chip + day heading) instead of the design's compact horizontal 5-day weekday strip (48x58 chips, radius 13) over a flat SlotList. **Fix:** Render a horizontal day strip + SlotList in the sheet (as Android does) rather than reusing the month-calendar SlotPicker.
- `high`/`missing-element` — The MemberPicker frame's round avatar rail (PR/DV/AK/+All, 46px circles with business gradient, accent ring on selected) is replaced by a single 'Reassign to an available teammate' toggle row; named-member selection is absent. **Fix:** Add the avatar rail under 'Assign to' for home/business reassign, matching FrameMemberPicker.
- `med`/`missing-element` — No inline amber conflict banner (FrameConflict: triangle-alert + 'This time overlaps "Brand strategy"' + 'View' on WARN_BG/WARN_LIGHT); 409 conflict is shown only via the SlotTakenSheet. **Fix:** Render a warning banner inline when a soft conflict is detected, with a View action.
- `low`/`typography` — SheetTitle uses the shared title style; design SectionTitle is fontSize 16.5 / weight 700; verify the iOS SheetTitle matches rather than a larger h-scale. **Fix:** Pin the title to 16.5pt bold to match SectionTitle.
- `low`/`icon` — tz chip shows a trailing chevron-down and copy 'Times shown in <tz>' vs design 'Times in Pacific · tap to change' with no chevron. **Fix:** Match the design copy and drop the trailing chevron (minor; native-acceptable).
- `low`/`interaction` — Proposed-success uses a PrimaryButton 'Done' and a 'Waiting on <name> to accept' chip; layout padding (Spacing.s6) vs design 40px top padding and 72px circle is close but the success copy is VM-generated rather than the design's fixed sentence. **Fix:** Verify success copy/spacing matches FrameProposed (72 circle, 'Waiting on Rosa to accept' sunken chip).

**Android deviations:**
- `med`/`missing-element` — No inline amber conflict banner (FrameConflict): triangle-alert + overlap text + 'View' on WARN_BG/WARN_LIGHT is absent; 409 routed to ConflictAlternativesSheet only. **Fix:** Add an inline warning banner with a View action for soft conflicts.
- `low`/`missing-element` — MemberRail omits the design's '+ / All' trailing avatar (sunken bg, fg3 '+') used to assign to anyone; only roster members render. **Fix:** Append an 'All'/'+' option to the avatar rail.
- `low`/`color` — MemberRail avatars fill with the flat pillar accentBg + accent-colored initials, whereas design uses a business gradient (linear-gradient #a78bfa→#6d28d9) with white initials. **Fix:** Use a gradient avatar fill with white initials to match AV.business (or accept flat as a native simplification).
- `low`/`typography` — Section overlines are hardcoded ALL-CAPS strings ('ASSIGN TO','HOW TO APPLY','REASSIGN TO') at 10sp/Bold; design uses sentence-case text ('Assign to','How to apply') with textTransform:uppercase + 0.08em letterSpacing and no explicit letter-spacing here. **Fix:** Use sentence-case source text with uppercase styling and add 0.08em letterSpacing for the overline.
- `low`/`interaction` — Proposed-success copy ('We sent the new time...we'll let you know when they respond.') and a 'Done' outline button differ from design's 'Rosa will get a push...accept Tue, Oct 22 · 2:00 PM' plus the 'Waiting on Rosa to accept' sunken chip (which Android omits). **Fix:** Match success body copy and add the 'Waiting on <name> to accept' chip.
- `low`/`spacing` — tz chip copy is 'Times in <tz>' (drops the design's '· tap to change' affordance suffix) and the chip is non-interactive (no timezone selector wired). **Fix:** Append '· tap to change' and wire a timezone change action.

## Edge

### D Add to calendar sheet — iOS `matches` / Android `major` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/AddToCalendarSheet.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/AddToCalendarSheet.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/AddToCalendarViewModel.kt`
- **Verdict:** iOS faithfully renders all 5 design frames (web-default, native, generating, added-morph, multi-calendar picker) with token-correct sizing; Android implements only the native frame and omits the Apple-Calendar web-default row, the Done bar, the multi-calendar picker level, and the added-success status line, plus uses an oversized title and pillar accent instead of the neutral spec.
- **Cross note:** Both keep the recap chip, provider rows (icon disc + label/sub + chevron), .ics shimmer ('Preparing your file'), and bell caption. Divergence: iOS covers all 5 frames and the Done bar; Android is a single-frame native-only sheet missing the web-default Apple row, the multi-calendar picker, the Done bar, and the added-success status, and over-sizes the title.

**iOS deviations:**
- `low`/`color` — recapChip uses primary700 text/icon and primary50/primary100 bg/border (matches spec blue700/blue50/blue100), and the accent is a hardcoded sky default rather than inheriting the opening surface's pillar accent as the spec note suggests. **Fix:** Optionally thread the opening surface's pillar accent into the recap/primary CTA; the current sky neutral is acceptable.

**Android deviations:**
- `high`/`missing-element` — AddToCalendarSheet renders only the native (EventKit primary) frame; the web-default 'Apple Calendar' / 'Save to your iPhone' first row (FrameWebDefault) and its added-success morph (row turning green with check) are absent. **Fix:** Add a device-calendar row as the first list row with a done state, and a nativePrimary toggle like iOS to switch presentations.
- `high`/`missing-element` — The level-2 multi-calendar picker (FrameMultiCalendar: 'Choose a calendar', chevron-left back, color-dot rows Personal/Work/Family, selected check, 'Add to Personal' primary) is not implemented at all. **Fix:** Add a second sheet level with sheetHeader+back, dot rows, and a pillar-accent 'Add to <name>' footer button as iOS does.
- `high`/`missing-element` — No DoneBar footer: spec web/generating/added frames end with a top-bordered full-width 44px radius-12 ghost 'Done' button; Android relies only on swipe-dismiss. **Fix:** Add a bordered ghost Done button pinned to the bottom with a 1px top divider.
- `high`/`typography` — Title 'Add to your calendar' uses PantopusTextStyle.h3 (20sp/semibold) but spec SheetHeader is 15px/700 with -0.2 tracking. **Fix:** Use a 15sp/Bold inline style (or a 15px header token) instead of h3.
- `med`/`missing-element` — The added-success status line ('Added — closing in a moment' with check-circle-2, fg3 600) from FrameAdded is absent; Android only flips the primary button label to 'Added to calendar'. **Fix:** Add the centered check + 'Added — closing in a moment' caption and auto-dismiss after the write.
- `med`/`color` — RecapChip and NativeAddButton use pillar.accent / pillar.accentBg (could render home green or business purple), but the spec recap is fixed blue700-on-blue50 and the primary CTA is fixed ACCENT=blue600; spec chrome is context-neutral. **Fix:** Use primary600/primary50/primary700 for the recap and CTA (or constrain the pillar to the sky default).
- `low`/`other` — Provider-row icon disc uses RoundedCornerShape(Radii.md)=8dp vs spec disc radius 10; RowCard wrapper uses Radii.xl=16dp vs spec card radius 14. **Fix:** Use a 10dp disc radius and a 14dp card radius (inline or add tokens) to match spec.
- `low`/`sizing` — ProviderRow has no fixed 56dp height (relies on vertical s3 padding), so rows may render shorter/taller than the spec's fixed 56px PickerRow. **Fix:** Pin each row to height(56.dp).

### D Cancellation policy sheet — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Payments/CancellationPolicyEditorView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Payments/CancellationPolicyEditorViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Settings/BusinessSchedulingSettingsView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Shared/BusinessKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/payments/CancellationRefundPolicyScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`
- **Verdict:** Both platforms faithfully reproduce the preset-picker + custom-rows + live-preview policy editor with correct tokens and section order; the only shared divergence is rendering the design's bottom-sheet as a full-screen back-chevron view, plus small per-platform spacing/sizing/tint nits.
- **Cross note:** Both platforms present the design's bottom-sheet as a full-screen routed editor (no grabber, no trailing X) and both add an error toast not in the design frames; this is the main shared divergence. iOS matches typography/no-show-row geometry slightly better, while Android uses the solid businessBg token (closer than iOS's opacity tint) and a stepper button size closer to the design's small variant — net fidelity is very close.

**iOS deviations:**
- `low`/`chrome` — BizTopBar renders a full-screen back-chevron bar; spec is a bottom SheetFrame with grabber handle (36x5) + left title (17/700) + subhead inline + trailing 28pt circular X close. **Fix:** Acceptable native substitution, but consider presenting as a .sheet with grabber + trailing close to match the modal idiom.
- `low`/`color` — PresetCard selected bg uses accent.opacity(0.10) and preview uses model.accent.opacity(0.08); spec fills both with the solid token businessBg (#f3e8ff). **Fix:** Use Theme.Color.businessBg directly instead of an opacity-derived tint.
- `med`/`sizing` — UnitStepper buttons are 26x26 with 12pt glyphs and value at 11.5pt; the design's CustomRows uses Stepper small (h=22, icon=11, value 12). **Fix:** Set stepper button frame to 22x22, glyph size 11, value font 12 to match the small variant.
- `low`/`typography` — Intro line 'Pick how refunds work…' is 11.5pt in body; in the spec this is the SheetFrame subhead at 12px/fg3. **Fix:** Bump to 12pt to match the subhead spec.

**Android deviations:**
- `low`/`chrome` — OwnerPillarHeader renders a full-screen pillar top bar; spec is a bottom SheetFrame with grabber handle + left title (17/700) + subhead + trailing circular X close. **Fix:** Acceptable native substitution; optionally present as a ModalBottomSheet with handle + close to mirror the design.
- `low`/`spacing` — CustomRow rows use horizontal padding 12dp; spec rows pad 11px/2px (2px horizontal) inside the card, so Android indents row content more than the spec. **Fix:** Reduce CustomRow horizontal padding toward 2dp (or rely on the card's own inner pad) to match.
- `low`/`typography` — Footnote text is 11sp; spec footnote is 10.5px (iOS uses 10.5). PreviewBox value 12sp/medium is correct. **Fix:** Set footnote fontSize to 10.5.sp.
- `low`/`color` — StepperButton disabled tint uses appBorderStrong (#d1d5db); spec disabled stepper glyph uses muted fg4 (#9ca3af). **Fix:** Use PantopusColors.appTextMuted for the disabled stepper tint.
- `low`/`sizing` — PolicyToast Text is hardcoded to width 240.dp, which can clip longer error messages; iOS lets the capsule size to content. **Fix:** Drop the fixed width and let the row wrap/size to content with a max-width constraint.

### D Open in app handoff — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/DeepLinkHandoffView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/EdgeComponents.swift`, `frontend/apps/ios/Pantopus/Core/Design/Components/Buttons.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/OpenInAppInterstitialScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/Buttons.kt`
- **Verdict:** iOS faithfully renders all three native states (resolving/resolved/failed) with the gradient host avatar, event preview, and identity pill; Android matches the structure but drops the identity-line pill entirely, swaps the gradient host avatar for a flat user-icon circle, and uses heavier type tokens (h3 20sp / small 14sp) than the spec's inlined 19/13.5/12.5 sizes.
- **Cross note:** Both platforms correctly implement only the three native interstitial frames (resolving / resolved / failed) and omit the two web-only frames (smart-banner over dimmed booking page; stay-on-web collapsed strip + toast), which is appropriate for native. Shared gap: the common PrimaryButton on both platforms is title-only, so neither shows the spec's leading CTA glyphs (smartphone / arrow-right). Android additionally uses Radii.xl(16) for the cards vs spec radius 14.

**iOS deviations:**
- `low`/`icon` — PrimaryButton (shared) renders title only, so the resolved CTA's leading smartphone glyph and the failed CTA's arrow-right glyph (spec PrimaryCTA icon prop) are absent. **Fix:** Add an optional leading PantopusIcon param to PrimaryButton and pass .smartphone / .arrowRight.
- `low`/`sizing` — Skeleton card and event preview card use Radii.lg (12) where the spec EventPreviewCard/resolving card is borderRadius 14. **Fix:** Introduce a 14pt radius (or accept the 2pt delta as within tolerance).
- `low`/`interaction` — Resolving pulse dot is a static Circle with no animation; spec applies animation:'dlPulse 1.4s' to the 7px dot. **Fix:** Add a repeating opacity/scale animation to the resolving dot.
- `low`/`missing-element` — Identity pill omits the 1px appBorder stroke present in spec IdentityLine; iOS sets only appSurfaceSunken fill + Capsule. **Fix:** Add a 1pt appBorder stroke around the identity Capsule.

**Android deviations:**
- `high`/`missing-element` — ResolvedBody has no IdentityLine: the spec's 'Continuing as Maya Chen · times in PT' sunken/bordered pill with 18px avatar is entirely absent (iOS renders it via identityLine()). **Fix:** Add a capsule pill below the event card with a small pillar avatar + continuing-as/timezone text using appSurfaceSunken + appBorder.
- `high`/`icon` — Resolved host avatar is a flat CircleShape(accentBg) with a plain User icon; spec HostAvatar is a 64px sky-gradient circle showing initials 'DL' plus a small primary user badge bottom-right (iOS uses EdgePillarAvatar). **Fix:** Replace with a gradient initials avatar + bottom-trailing user badge mirroring EdgePillarAvatar size=64.
- `med`/`typography` — Headlines use PantopusTextStyle.h3 (20sp SemiBold) but spec h2 is 19px/700; resolved/failed body use style.small (14sp) vs spec 12.5px, and the event-row title uses small(14sp) vs spec 13.5px. **Fix:** Use inlined 19sp/Bold for the title, 12.5sp for body lines, and 13.5sp/Bold for the event title to match the frame.
- `med`/`icon` — BrandMark renders PantopusIcon.Calendarly on a flat Radii.lg(12) accent tile; spec PantopusMark is a sky-gradient rounded square (radius ~size*0.28) with the concentric-ring glyph (iOS reproduces the ring glyph + gradient + shadow). **Fix:** Use a primary600→primary700 diagonal gradient with the ring glyph and ~0.28*size corner radius.
- `med`/`color` — Resolved avatar background uses primary50 flat fill instead of the spec gradient HOST_AV (#38bdf8→#0369a1). **Fix:** Apply the host sky gradient to the resolved avatar background.
- `med`/`icon` — PrimaryButton is title-only so the resolved CTA's smartphone glyph and failed CTA's arrow-right glyph (spec) are missing. **Fix:** Add a leading-icon param to PrimaryButton and pass Smartphone / ArrowRight.
- `med`/`state` — Failed-state ghost CTA reads 'Go back' (onBack); spec frame 4 secondary action is 'Try the app again' (retry the resolve), which iOS implements via viewModel.retry(). **Fix:** Relabel to 'Try the app again' and wire it to re-run the resolve, not navigate back.

### D Payment failed sheet — iOS `minor` / Android `major` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/PaymentFailedView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/EdgeComponents.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Routing/Stubs/InviteeEdge/InviteePaymentFailedStubView.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/PaymentRetryScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/EdgeComponents.kt`
- **Verdict:** Both render all four states (declined / hold-expired / network-timeout / succeeded) with the right halo + reassuring copy, but iOS pushes it as a full-screen nav destination instead of the spec's bottom sheet while Android correctly uses ModalBottomSheet yet drops the saved-card row, the live hold countdown, the timeout info callout, and the success progress bar.
- **Cross note:** Shared gap: neither platform renders the spec's live mm:ss hold countdown with the slot time (iOS has the label scaffolding via slotTimeLabel/holdLabel but ties it to VM data; Android shows a static string). Key divergence: iOS got the presentation wrong (full-screen push vs sheet) but kept more content (saved-card row attempt, timeout callout card, declined row); Android got the sheet presentation right but stripped the saved-card row, countdown, info callout, and success progress bar down to plain chips.

**iOS deviations:**
- `high`/`chrome` — PaymentFailedView is a pushed nav destination with .navigationTitle("Payment") + back chevron; spec is a bottom sheet (grab handle, top radius 20, dimmed backdrop over Review & confirm) so the held slot context stays visible. **Fix:** Present via .sheet/.presentationDetents over the review screen with a grab handle instead of a NavigationStack push.
- `med`/`missing-element` — declined cardDeclinedRow shows a generic credit-card glyph tile + "Your card"; spec's SavedCardRow shows a Visa brand badge (38x26, #1A1F71) with label "Visa •• 4421" and a Declined pill. **Fix:** Render the actual A14.6 saved-card brand badge + masked PAN instead of a placeholder credit-card icon.
- `med`/`missing-element` — succeeded state omits the spec's auto-advance progress bar (70% wide, height 5, success fill) above the pulsing "Confirming your booking" dot; iOS shows only a system ProgressView spinner. **Fix:** Add the determinate/animated progress bar matching the success-tone auto-advance indicator.
- `low`/`color` — info-tone halo/callout ring maps to Theme.Color.primary100 (EdgeTone.info.ring) rather than the info-light token #BAE6FD that the spec's INFO_RING uses. **Fix:** Point EdgeTone.info.ring at Theme.Color.infoLight to match the token palette.
- `low`/`sizing` — EdgeIconHalo size:84 (inner ~66, glyph ~30) vs spec halo outer 66 / inner 52 / icon 25 — iOS halo is ~27% larger than the design. **Fix:** Pass size:66 for the payment-failed halo or lower the shared default to match spec.
- `low`/`icon` — holdChip always uses .timer even in the released branch; spec swaps to timer-off when the hold is released. **Fix:** Use a timer-off glyph when released == true.

**Android deviations:**
- `high`/`missing-element` — DeclinedBody omits the SavedCardRow entirely (no Visa badge, masked PAN, or Declined pill); spec frame 1 shows the declined saved card prominently between the halo and the CTAs. **Fix:** Add an EdgeComponents saved-card row with brand badge + "Visa •• 4421" + Declined pill in the declined state.
- `high`/`missing-element` — Hold countdown is a static CenteredChip "Your time is still held" with no live timer value; spec HoldChip reads "Holding your 2:00 PM time for 4:48" with a tabular-nums countdown. **Fix:** Bind the chip label to the live hold-remaining mm:ss from the ViewModel and include the slot time.
- `med`/`missing-element` — TimeoutBody replaces the spec's info callout card (icon disc + "If the first try did go through, checking again won't charge you a second time.") with a single small pill; the framed info-bg/info-ring callout block is absent. **Fix:** Render a PolicyNoteCard/info callout block with the full reassurance sentence instead of the compact chip.
- `med`/`missing-element` — SucceededBody omits the auto-advance progress bar + pulsing "Confirming your booking" indicator and uses a single "Done" button; spec frame 4 has no button and auto-advances with a progress bar. **Fix:** Add the success progress bar + confirming indicator and drop/secondary-ize the Done CTA to match the auto-advance morph.
- `low`/`icon` — HoldExpired chip uses PantopusIcon.Timer; spec uses timer-off for the released state. **Fix:** Use a TimerOff glyph for the released hold chip.
- `low`/`icon` — Succeeded halo uses PantopusIcon.CheckCircle; spec uses check-circle-2 (the double/filled check) for the success morph. **Fix:** Map the success halo glyph to check-circle-2 to match spec intent.
- `low`/`typography` — EdgeHalo title uses PantopusTextStyle.h3 and body PantopusTextStyle.small rather than the spec's explicit 17/700 title and 12/fg2 body; iOS inlines 19/bold + 12.5 — both diverge slightly but Android is further from the 17px title. **Fix:** Confirm h3 resolves near 17sp bold; otherwise inline the spec sizes.

### D Policy blocked — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/PolicyBlockedView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/EdgeComponents.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingStatusPill.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/RescheduleCancelPolicyScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/EdgeComponents.kt`
- **Verdict:** iOS faithfully reproduces all 5 spec frames (status pill, summary card, amber/green note card, per-state sticky dock, manage rows) with only color/icon nits, while Android covers the same modes but uses a flat inline button layout instead of the fixed translucent dock, swaps several icons, drops policy copy detail, and adds an off-spec "Add to calendar" manage row.
- **Cross note:** Both platforms color the note card title with the neutral appText instead of the spec's dark tone (t.dk, e.g. #92400E); shared nit. Key divergence: iOS reproduces the per-state translucent bottom Dock, the "Cancel instead" + "See full policy" links, and the cancelled-success screen, whereas Android folds CTAs inline, omits those affordances, and inserts an off-spec Add-to-calendar manage row — making Android the weaker match despite Android being the only one with the correct check-circle StatusBadge icon.

**iOS deviations:**
- `low`/`icon` — SchedulingStatusPill (line 7 comment) deliberately renders NO leading glyph; spec StatusBadge has a check-circle-2 icon before "Confirmed". **Fix:** Add a 13pt check-circle leading icon to the confirmed pill (or a status-pill variant) to match the design badge.
- `low`/`color` — EdgeNoteCard title uses Theme.Color.appText (PolicyBlockedView line 148 area) and the still-note text uses appText; spec PolicyCard title/still use the dark tone color t.dk (e.g. WARN_DK #92400E). **Fix:** Color the note title and still text with the tone's dark variant (warningDark) instead of appText.
- `low`/`color` — Status pill padding is Spacing.s2 (8) horizontal + 3 vertical vs spec 5px 11px 5px 9px asymmetric padding. **Fix:** Use slightly larger horizontal padding (~10/11) to match the badge proportions; minor.
- `low`/`typography` — Note body font is 11.5 and still 11 — matches spec; no action. (recorded as confirmation, iOS note card geometry/radius/30pt tile all match spec). **Fix:** None.

**Android deviations:**
- `high`/`layout` — ManageContent (RescheduleCancelPolicyScreen.kt lines 187-227) places all CTAs inline at the bottom of the scrolling Column; spec pins them in a fixed translucent bottom Dock (rgba(255,255,255,0.97)+blur, top border) separate from scroll content. **Fix:** Move the per-mode action buttons into a sticky bottom dock (Box/Column over the scroll) mirroring iOS EdgeDock and the design Dock.
- `high`/`extra-element` — Manage list adds an "Add to calendar" ManageActionRow (lines 217-222) plus a generic "Keep my booking" ghost in the closed states; spec's within-policy Manage section has only Reschedule + Cancel booking, and closed states show no manage rows at all (only dock buttons). **Fix:** Remove the Add-to-calendar row from this screen's Manage section and render only Reschedule + Cancel; route dock CTAs per mode instead of inline ghost.
- `med`/`missing-element` — No per-mode dock CTAs matching spec: cancel-closed/reschedule-closed need "Keep my booking" + "Message host" (hostGhost), partial-refund needs destructive "Cancel and refund $24" + Keep, not-online needs primary "Message host" + Keep; Android only conditionally shows a single GhostButton. **Fix:** Implement the four dock button combinations (ghost/hostGhost/destructive/primary) keyed off ManagePolicyMode.
- `med`/`icon` — policyNote (lines 324-331) uses PantopusIcon.Clock for the reschedule-window-closed warn card; spec uses clock-alert (FrameRescheduleClosed icon="clock-alert"). **Fix:** Use a clock-alert glyph for RescheduleClosed/FreeCancel to match the design intent.
- `med`/`missing-element` — Spec FrameRescheduleClosed has an inline "Cancel instead" link (x-circle, primary) below the note; Android has no such inline link (iOS has cancelInsteadLink). **Fix:** Add a centered primary "Cancel instead" inline link in the RescheduleClosed mode.
- `med`/`typography` — PartialRefund body (line 337) is "Cancelling now refunds {refund}." dropping spec's "within 24 hours of your visit, refunds half — $24 of the $48 you paid"; refundEstimateLabel is also injected into the title as the percent slot. **Fix:** Render the fuller refund body with paid/refund amounts and the percent in the title like iOS PolicyCopy.partialRefund.
- `low`/`missing-element` — PolicyNoteCard still-row (lines 275-292) has no top-border separator before the info line; spec adds borderTop:1px tone.bd above the still note (iOS draws the divider). **Fix:** Add a 1dp tone.ring top border / divider above the still row.
- `low`/`icon` — BookingSummaryCard avatar (line 328) is a generic PantopusIcon.User on accentBg; spec uses a 135deg gradient disc with host initials ("MK") and a pillar identity dot, which iOS reproduces via EdgePillarAvatar. **Fix:** Replace the User-icon disc with a gradient initials avatar plus pillar dot to match the design.

### D Recurring series setup — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/RecurringSetupView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/RecurringSetupViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/customer/RecurringSetupScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/customer/RecurringSetupViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** iOS faithfully reproduces the default, count-picker, and recap frames (conflict/partial are data-gated but structurally present), while Android omits the recap/summary screen entirely, renders Repeats as a Daily/Weekly toggle and Time as a +/- stepper instead of a select and a sky pill, and ships an impoverished series strip.
- **Cross note:** Both platforms gate the conflict/partial frames on per-week availability data that isn't fetched yet, so neither renders the amber conflict/full occurrence treatments; iOS at least models all three OccurrenceStatus cases and the recap/count-picker frames, whereas Android collapses the control card and omits the recap step.

**iOS deviations:**
- `med`/`state` — Per-occurrence conflict (Frame 2) inline mini slot-picker (InlineSlotPicker: warn-bg panel with 3 selectable time rows under the conflicting occurrence) is replaced by the foundation SlotTakenSheet modal; the inline-resolve layout never renders. **Fix:** Render an inline warn-tinted slot list beneath a .conflict OccurrenceRow when availability flags a single week.
- `med`/`state` — Partial-series frame (Frame 3: 'We can book 4 of 6' warn banner + struck-through Full rows + dual CTA 'Book the 4 that work' / 'Adjust the series') is unreachable because status(forIndex:) always returns .open, so .unavailable rows and the partial banner never appear. **Fix:** Feed per-week availability into status() so unavailable rows, the partial banner, and the 'Book the N that work' CTA label render.
- `low`/`color` — Recap row remove-button uses appTextSecondary tint vs design's fg3 inside an appSurfaceSunken circle (matches), but the conflict banner copy differs from spec ('We can book 4 of 6') — iOS shows a generic 'Some of those times are taken' string. **Fix:** Match banner title/body to the spec's count-aware copy once partial state is wired.
- `low`/`missing-element` — Recap header omits the '· with <host>' suffix the design shows ('Intro call · with Maria Kessler'); only the event name renders. **Fix:** Append the host display name to recapEventName when available.

**Android deviations:**
- `high`/`missing-element` — The entire series-summary/recap step (Frame 4: repeat-halo header, per-occurrence recap card with remove-x controls, '$40 each' + '$240' total row) is absent; the primary CTA confirms directly instead of routing to a review screen. **Fix:** Add a reviewing state and a RecapBody composable mirroring iOS recap (header, RecapRow list with x-button, total row), reached via a 'Review N bookings' CTA.
- `high`/`extra-element` — Repeats is rendered as a two-Segment Daily/Weekly toggle (ConfigCard line 220-231); the spec is a single select field showing 'Weekly' with a repeat icon and chevron-down, no Daily option. **Fix:** Replace the toggle with a single RepeatsSelect row (repeat icon + 'Weekly' + chevron-down, h42, Radii.md border).
- `high`/`extra-element` — Time is a +/- Stepper (line 240-241) but the spec is a tappable sky pill chip (clock + '2:00 PM' + chevron-down, accentBg fill, primary100 border, pill radius, primary700 text). **Fix:** Replace the Time Stepper with an accent-tinted pill chip that opens a time picker.
- `high`/`state` — No count-picker expanded panel (Frame 5): the 4/6/8/12 quick chips are always shown inline and there is no 'Number of sessions / Until a date' segmented toggle or appSurfaceSunken panel; tapping the count does nothing. **Fix:** Gate the quick-pick chips + an end-condition toggle behind an expandable sunken panel toggled by tapping the session count.
- `med`/`missing-element` — SeriesStrip drops the header row ('calendar-range' icon + 'Your 6 Tuesdays' label) and each pill's month overline + status dot/alert-circle below the day circle; it renders only bare 34dp day circles. **Fix:** Add the header row and per-pill month label + accent status dot (alert-circle on conflict) matching the design pill.
- `med`/`missing-element` — OccurrenceRow 'Open' badge is an overline-only pill with no leading check icon and no successLight border, and conflict/full/unavailable variants (calendar-x tile, strikethrough, 'Full' pill, alert-circle tile) are not modeled — only Open vs Failed. **Fix:** Add the check icon + border to the Open pill and implement unavailable/conflict row treatments (tile icon, strikethrough, Full pill).
- `med`/`missing-element` — SummaryChip has no primary100 border (design has border:E.blue100) and its subtitle omits the '· $240 total' price segment shown in the spec. **Fix:** Add the primary100 border and append the total-price segment to the range label.
- `low`/`icon` — Series-strip header would need calendar-range, which is absent from the Android PantopusIcon set (no CalendarRange enum), so even after adding the header the glyph is unavailable. **Fix:** Add a CalendarRange lucide mapping to Icons.kt (or substitute Calendar) for the strip header.

### D Slot taken sheet — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SlotTakenSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingSlotRow.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/SlotTakenScreenView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/ConflictAlternativesSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/Buttons.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/BookResourceScreen.kt`
- **Verdict:** Both render the amber-halo conflict sheet with saved-details footer; iOS covers all three frames (alternatives/fully-booked/refreshing) but its slot rows drop the design's two-column weekday/time-range layout and the "Soonest" badge, while Android nails the row layout + badge but omits the entire re-fetching skeleton frame, the dashed fully-booked empty card, and the leading button icons.
- **Cross note:** Each platform implements the half the other misses: iOS has full state coverage but the wrong row anatomy (no Soonest badge, single-column); Android has the correct row + Soonest badge but no refreshing skeleton frame, no dashed fully-booked card, and icon-less buttons. Both leave a faithful version reachable by porting the other's strengths.

**iOS deviations:**
- `high`/`layout` — iOS reuses SchedulingSlotRow (single-column: clock + time bold, detail caption below, chevron) instead of the design SlotRow's two-column layout — weekday+date on the left, time-range+duration right-aligned on the right. **Fix:** Render alternatives with a dedicated two-column row matching the spec (left: weekday/date, right: time-range/duration).
- `high`/`missing-element` — The design's first/nearest row carries a uppercase 'Soonest' pill (8px/700, blue700 on blue50, blue100 border, pill radius); iOS SchedulingSlotRow has no Soonest badge at all. **Fix:** Add a Soonest overline pill to the first alternative row using primary tokens.
- `low`/`typography` — iOS row time uses .small (14pt) bold; spec time/weekday are 12px bold and date/duration are 10px secondary — iOS is materially larger than the spec's compact dual-column type. **Fix:** Drop row primary text to 12pt and secondary detail to 10pt to match the spec densities.
- `low`/`icon` — calendar-search ghost-button glyph maps to SF 'magnifyingglass' (Icons.swift line 807) rather than a calendar-with-search intent; minor intent drift from the lucide calendar-search. **Fix:** Map .calendarSearch to a calendar-badge SF symbol (e.g. calendar.badge.clock / magnifyingglass overlay) to preserve the calendar intent.

**Android deviations:**
- `high`/`state` — ConflictAlternativesSheet has no refreshing/stale-grid frame — no Mode enum, no shimmer SkeletonRows, no 'Checking live availability' pulse dot+caption; the design's third frame is entirely absent (iOS covers it via .refreshing). **Fix:** Add a refreshing mode that renders three shimmer slot-row skeletons plus the accent pulse + 'Checking live availability' caption.
- `high`/`missing-element` — Fully-booked branch (alternatives empty) only swaps the halo body text and shows the CTAs; the design's dashed-border card (1px dashed borderStrong, 14px radius, sunken circle + calendar-x, 'This day is fully booked' title, body) is missing. **Fix:** Render a dashed-bordered empty card with the calendar-x disc, title, and body before the Join-the-waitlist / See-another-day buttons.
- `med`/`icon` — Android GhostButton/PrimaryButton (Buttons.kt) take only a title — no leading glyph — so 'Pick another time'/'See another day' lack the calendar-search icon and 'Join the waitlist' lacks the bell-plus icon the spec shows. **Fix:** Add an optional leading-icon param to the shared buttons and pass calendar-search / bell-plus.
- `med`/`typography` — ErrorHalo body uses PantopusTextStyle.small (14sp); spec body is 12px (and fully-booked card body 11.5px), so the subtitle is oversized. **Fix:** Use the caption (12px) style for the halo body text.
- `low`/`spacing` — SavedNote row has no top hairline separator and uses vertical s2 padding; the spec footer has a 1px top border and asymmetric 10px top / 18px bottom padding. **Fix:** Add a top 1px border and set padding to ~10dp top / 18dp bottom on the saved-details footer.
- `low`/`sizing` — SavedNote shield-check icon is 14dp; spec is 13px. **Fix:** Reduce the shield-check size to 13dp.
- `low`/`sizing` — AlternativeRow uses vertical padding Spacing.s3 (12dp); spec row padding is 9px vertical / 11px horizontal, making the Android rows slightly taller than spec. **Fix:** Set row padding to ~9dp vertical / 11dp horizontal.

### D Unavailable/terminal states — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/TerminalStateView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/TerminalStateViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Invitee/Edge/EdgeComponents.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invitee/edge/UnavailableExpiredScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/PausedExpiredUnavailableState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** iOS faithfully reproduces the one status-driven terminal screen (flat halo aside) including the fixed "Get the app" pillar CTA and the status-specific outlined secondary button; Android drops the "Get the app" button entirely and mis-styles the status CTA as the filled pillar button, plus oversized body type.
- **Cross note:** Both platforms model the same 6 design states as one status-driven screen (Android adds an extra 'Unavailable' state — an acceptable superset). The core divergence: iOS keeps the design's two-tier dock (status outlined-secondary + fixed 'Get the app' pillar + 'Back to Pantopus' ghost), while Android collapses it to one status CTA as the filled pillar and omits 'Get the app' entirely. The shared paused-card host-note + reopen chip exist on both, but Android substitutes a User icon for the design's initials avatar.

**iOS deviations:**
- `med`/`icon` — EdgeIconHalo renders a double-circle with a 2px ring stroke and a 0.6-opacity outer glow; the design IconHalo is a single flat 84pt sunken disc with no ring and no glow. **Fix:** Use a plain Circle().fill(appSurfaceSunken) 84pt with a 36pt fg3 glyph (strokeWidth 1.8), drop the ring/outer-glow layers for this screen.
- `low`/`color` — EdgeDock background is .regularMaterial (translucent blur); the design Dock uses a solid background:E.surface with a 1px top border. **Fix:** Fill the dock with Theme.Color.appSurface instead of .regularMaterial; keep the appBorder top hairline.
- `low`/`spacing` — EdgeDock pads .top s3(12)/.bottom s2(8); the design Dock is padding 12px 16px 20px (20 bottom for home-indicator clearance). **Fix:** Increase bottom padding toward ~20 (or rely on safe-area inset) to match the design's 20px dock bottom.
- `low`/`typography` — Body Text uses lineSpacing(2) on a 12.5pt font (~17pt line); design body is 12.5px with explicit 18px line-height. **Fix:** Bump lineSpacing slightly so the rendered line-height reaches ~18pt to match the spec.

**Android deviations:**
- `high`/`missing-element` — The design's fixed filled-pillar primary CTA is always 'Get the app' (smartphone icon, 46h, r12, primary shadow); UnavailableExpiredScreen never renders it — it has no Get-the-app button anywhere. **Fix:** Add a permanent 'Get the app' PillarPrimaryButton (smartphone icon) as the dock's filled CTA on every state.
- `high`/`extra-element` — The status CTA (Notify me / Request a new link / Book again) is passed as primaryAction and rendered as the filled pillar button; in the design it is an OUTLINED secondary SecondaryButton (surface bg, borderStrong, 42h, r10) sitting above Get-the-app. **Fix:** Render the status CTA as an outlined secondary button (the design's SecondaryButton) above the fixed Get-the-app pillar button, not as the filled primary.
- `med`/`typography` — Body uses PantopusTextStyle.small (14sp/20lh); design body is 12.5px/18lh fg3, so the body copy is oversized. **Fix:** Use a ~12.5sp/18lh style (caption-scale) for the body text instead of small.
- `med`/`icon` — PausedNoteCard avatar shows PantopusIcon.User on the gradient disc; the design PausedCard shows host initials text ('MK') on the gradient circle. **Fix:** Replace the User glyph with the host's initials (Text, 9sp bold, inverse) inside the 24dp gradient disc.
- `low`/`typography` — Title uses PantopusTextStyle.h3 (20sp/28lh SemiBold); design title is 19px/24lh weight 700. **Fix:** Use a 19sp/24lh Bold style for the headline to match the spec.
- `low`/`color` — Paused-card avatar disc uses SchedulingPillar.Personal.accentBg/accent (sky tint); design uses a blue gradient (linear-gradient #38bdf8→#0369a1) like the iOS primary400→primary700. **Fix:** Fill the 24dp disc with a primary400→primary700 gradient to match the design avatar.
- `low`/`chrome` — The bottom dock Column has solid appSurface bg but no 1px top border; the design Dock has borderTop 1px E.border. Dock padding is s4/s4 (16/16) vs design 12px top / 20px bottom. **Fix:** Add a 1dp appBorder top divider on the dock and set padding to ~12 top / 20 bottom.
- `low`/`layout` — Cancelled-state 'Book again' inline link uses pillar.accent tint and ArrowsRepeat glyph at PantopusTextStyle.small (14sp); design CancelledLink is 13px/700 rotate-ccw, PILLAR color. **Fix:** Use a 13sp Bold label with rotate-ccw glyph for the inline 'Book again' link to match weight/size.

## Bookings

### E Cancel / refund sheet — iOS `matches` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/CancelRefundSheet.swift`, `frontend/apps/ios/Pantopus/Core/Design/Radii.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/CancelRefundSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/CancelRefundViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingsKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`
- **Verdict:** iOS faithfully renders all 8 design frames (form, refund card with segmented preset + money rows, credit switch, submitting, refund-failed banner, already-cancelled/cancelled read-only terminals), differing only in deferred money figures; Android implements only the form frame, collapsing the refund card to one copy line and omitting the credit switch and both terminal read-only frames.
- **Cross note:** Both platforms defer concrete refund money amounts because the owner-side booking payload carries no price; iOS still renders the full money-row structure with placeholders while Android omits the rows entirely. Key divergence: iOS covers all 8 frames, Android covers only the live form frame.

**iOS deviations:**
- `low`/`sizing` — refundCard() and restoreCreditCard() use RoundedRectangle(cornerRadius: Radii.lg)=12 but the spec's RefundSection/CreditSwitch use borderRadius:14. **Fix:** Use a 14pt corner radius for the refund and restore-credit cards instead of Radii.lg.
- `low`/`missing-element` — Money rows render deferredAmount '—' and the terminal 'Refunded to card' value as 'Issued' instead of the spec's concrete $40.00/$20.00/$0.00 figures, because the owner BookingDTO carries no price (documented deferral). **Fix:** Wire the pricing payload so Paid / Refund to card show real currency amounts once available.
- `low`/`color` — Refund 'Refund to card' value uses Theme.Color.success (#059669) whereas the spec uses SUCCESS_DK (#047857); minor tonal difference. **Fix:** Add a successDark token (#047857) and apply it to the refund-to-card value to match the spec.

**Android deviations:**
- `high`/`missing-element` — RefundCard (CancelRefundSheet.kt line 193) only renders the receipt overline + one copy line; the spec's RefundSection has a Full/Partial/Per-policy segmented control plus 'Paid' and 'Refund to card' money rows with a divider and success-colored amount. **Fix:** Add the 3-segment preset control and the Paid/divider/Refund-to-card money rows inside RefundCard, mirroring iOS refundCard().
- `high`/`missing-element` — No CreditSwitch / restore-credit card (spec frame 5): the blue50 ticket tile + 'Restore session credit' row with toggle is entirely absent; CancelSheetUiState has no creditRedeemed/restoreCredit fields. **Fix:** Add a creditRedeemed branch and a RestoreCreditCard composable mirroring iOS restoreCreditCard().
- `high`/`state` — No already-cancelled (frame 8) or post-cancel confirmation (frame 5/8) read-only terminal frame — circle-slash 64px circle, title, body, 'Refunded to card' row, ghost 'Done' button are all missing; the VM only models the live form and dismisses on success. **Fix:** Add terminal/confirmation composables and VM state for alreadyCancelled/succeeded, mirroring iOS terminalScaffold.
- `med`/`missing-element` — Refund-failed (frame 7) shows no error banner under the header — the spec places an alert-circle ERR_BG banner immediately below HeaderBlock, but CancelRefundSheet renders errorMessage only at the foot before the CTA. **Fix:** When refundFailed, render CancelInlineError directly under the title block (above the reason chips) as the spec does.
- `low`/`icon` — Retry CTA uses PantopusIcon.RefreshCw (line 117); the spec's frame 7 confirmIcon is lucide 'rotate-cw' (iOS uses .rotateCw). **Fix:** Switch the retry leading icon to PantopusIcon.RotateCw to match the spec glyph.
- `low`/`sizing` — RefundCard uses RoundedCornerShape(Radii.xl)=16 while the spec's RefundSection is borderRadius:14; NotifySwitch uses Radii.lg=12 vs spec 12 (ok). **Fix:** Use a 14dp corner radius for the refund card.
- `low`/`interaction` — Submitting (frame 6) shows only the button loading spinner; the spec CTA reads 'Cancelling' with a spinner (acceptable but label text is dropped). **Fix:** Optionally pass a loading label so the button reads 'Cancelling' during submit.

### E1 Bookings inbox — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingsInboxView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingRowView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingStatusPill.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingsInboxScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingsKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingsInboxModels.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/StatusChip.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/ErrorState.kt`
- **Verdict:** Both platforms faithfully reproduce the tabbed list-of-rows shell (top bar, scope pills, segmented control, day-sectioned rows, FAB, empty/loading/error), with iOS edging ahead on state copy/icons while Android wins on scope-pill interactivity; both omit the assigned-member-name chip and the member-gated banner.
- **Cross note:** Shared gaps on both platforms: the assigned-member chip never shows the actual member name and the member-gated info banner (Frame 9) is missing. Key divergence: iOS disables the scope-pill switcher entirely (allowsHitTesting false) while Android keeps it interactive (onSelect wired); conversely Android's error state regresses to a generic AlertCircle/'Something went wrong' surface whereas iOS uses the correct cloud-off + 'Couldn't load bookings'.

**iOS deviations:**
- `med`/`interaction` — Scope pills are rendered with .allowsHitTesting(false) (scopePill, BookingsInboxView.swift:137) so only the active pill works and the All/Personal/Home/Business switcher is non-functional; the spec's horizontally-scrolling switcher is meant to re-scope. **Fix:** Wire pill taps to re-scope via an owner directory, or at minimum allow selecting among resolved owners.
- `med`/`missing-element` — BookingRowView has no AssignedChip — Frames 7/9 show a violet 'Assigned · Priya/Devon/You' member chip beside the owner glyph (AssignedChip in spec); the row only renders BookingOwnerGlyph. **Fix:** Add an assigned-member chip (user-round icon + member name, business-bg) rendered when the booking carries an assignee.
- `low`/`missing-element` — No member-gated banner; Frame 9 shows a blue 'You're seeing bookings assigned to you' info banner (MemberBanner) above the segmented control for member-scoped views. **Fix:** Render an info banner (user-check icon, blue50/blue200) when the viewer is a non-admin member.
- `low`/`other` — Segmented track uses Radii.lg (12) in segmentedTabs (BookingsInboxView.swift:162) vs the spec's 10px container radius (inner buttons 7). **Fix:** Use a 10pt corner radius for the segmented track.
- `low`/`typography` — Top-bar title is .system(size:16) (topBar, BookingsInboxView.swift:61) vs spec 15.5. **Fix:** Set title size to 15.5.

**Android deviations:**
- `med`/`icon` — Error state calls ErrorState(message = s.message, onRetry=…) (BookingsInboxScreen.kt:146) which uses PantopusIcon.AlertCircle and the default headline 'Something went wrong'; spec Frame 6 uses a cloud-off glyph and 'Couldn't load bookings' on an error-red hero circle. **Fix:** Pass headline='Couldn't load bookings' and use a cloud-off icon variant (or a dedicated error scaffold) instead of the generic AlertCircle ErrorState.
- `med`/`missing-element` — AssignedChip() renders a hardcoded 'Assigned' label (BookingsInboxScreen.kt:639) instead of the member name; spec shows the assignee name (Priya/Devon/Unassigned/You). **Fix:** Pass and render row.assignedName in AssignedChip.
- `low`/`missing-element` — No member-gated info banner; Frame 9's 'You're seeing bookings assigned to you' MemberBanner above the segmented control is absent. **Fix:** Add an info banner composable for member-scoped sessions.
- `low`/`color` — Active segment label is always PantopusColors.primary700 (InboxSegmented, BookingsInboxScreen.kt:410); spec ties the active-tab accent to the scope (home green / business violet), e.g. Frame 7 accent #7c3aed, Frame 8 #16a34a. **Fix:** Pass the active scope accent into InboxSegmented and use it for the selected label.
- `low`/`state` — Pending segment is always present (BookingSegment.entries in InboxSegmented); spec Frame 8 (auto-confirm) and Frame 9 (member-gated) hide the Pending segment (hidePending). **Fix:** Filter out the Pending segment when the owner auto-confirms or the viewer is a gated member.
- `low`/`other` — Segmented track uses RoundedCornerShape(Radii.md=8) (InboxSegmented, BookingsInboxScreen.kt:387) vs spec 10 corner radius. **Fix:** Use a 10dp track radius.
- `low`/`sizing` — Top bar height 48dp and icon hit targets 40dp (InboxTopBar/TopBarIcon, BookingsInboxScreen.kt:198,296) vs spec 46 bar height and 34 buttons. **Fix:** Set bar height to 46dp and icon button size to 34dp.

### E2 Booking detail — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingDetailView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingDetailSections.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingDetailViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingDetailScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingDetailModels.kt`
- **Verdict:** iOS faithfully reproduces all 10 design frames (chrome, pillar header, all banners, every section card incl. status timeline + intake accordion + message button, conflict banner, all 5 status-contextual dock variants, shimmer, error), with only small nits; Android has the same chrome/header/cards but drops the Status timeline card, the requester Message button, the follow-up promo + refund line, the conflict banner, and collapses the dock to just Pending vs a single Reschedule button — so it omits major design content.
- **Cross note:** Both platforms simplify the assigned-member card to a generic label instead of the design's named member with a Reassign action button inline; iOS keeps an inline Reassign text button (Android drops it from the card, relegating reassign to the kebab menu). Neither implements the dedicated frame-7 'Reassign to' member-picker card inline (handled via separate sheets, which is acceptable native idiom).

**iOS deviations:**
- `low`/`missing-element` — Avatar in requesterCard uses BookingAvatar but the design's Avatar carries a white badge-check verified ring (badge-check, pillar-colored) at bottom-right; cannot confirm BookingAvatar renders that badge. **Fix:** Ensure BookingAvatar overlays the 16x16 white circle + badge-check glyph in the pillar accent like design Avatar.
- `low`/`color` — refundLine shows literal text 'Issued' in success color rather than the design's tabular-nums amount (e.g. '$120.00') in SUCCESS_DK with a 'Full refund issued…' subline. **Fix:** Render the refunded amount value + the free-cancellation-window subline when currency/amount is available.
- `low`/`missing-element` — Assigned-member card renders a generic 'Team member' label with a userRound glyph instead of the design's member initials avatar (AV gradient) + member name ('Priya R.'). **Fix:** Show member initials in the gradient avatar and the real member name when available.
- `low`/`layout` — Design orders cards Requester → Location → Intake → Status(timeline); the loaded() VStack orders requester → location → assignedMember → intake → timeline, putting assigned-member between location and intake (acceptable, Business-only) but worth confirming against frame order. **Fix:** Confirm card ordering matches the per-status design frames.

**Android deviations:**
- `high`/`missing-element` — DetailContent has no Status timeline card; design frames 1/3/5 all show a 'Status' card with a vertical check/dashed-node Timeline (Requested→Confirmed→Reminder→Meeting etc). **Fix:** Add a timeline section card mirroring iOS timelineCard with done/not-done nodes and connectors.
- `high`/`missing-element` — DetailDock only renders Pending (Decline+Approve) vs an else branch with a single Reschedule button; design specifies distinct docks for confirmed (Reschedule+Message), past/completed (Rebook+Follow up), no-show (Message+Send rebook link), and cancelled (single Rebook this time). **Fix:** Branch the dock on every BookingStatus and add the missing two-button and single-button variants with correct icons/labels.
- `high`/`missing-element` — RequesterCard omits the trailing 36x36 outlined message-circle icon button (brand blue, Radii.lg border) present in the design AttendeeRow. **Fix:** Add the trailing Message icon-button to RequesterCard like iOS.
- `med`/`missing-element` — No conflict-warning banner (frame 6): warningBg pill with triangle-alert, 'This overlaps another booking' + 'View' action above the dock; isActive/hasConflict path is absent on Android. **Fix:** Add a ConflictBanner composable rendered above the dock when a conflict is present.
- `med`/`missing-element` — Past/completed state shows a generic 'rescheduled' banner only; design frame 3 has a blue 'Send a follow-up' sparkles promo and frame 4 has a Refund card ($120.00 + free-cancellation subline) — both absent. **Fix:** Add the sparkles follow-up promo for completed/past and a Refund card for cancelled-with-payment.
- `med`/`state` — isActive gates the dock to only Pending/Confirmed, so completed, past, no-show, cancelled and declined bookings render NO dock at all (design shows a dock in every one of those frames). **Fix:** Show the dock for terminal/past statuses too and route to the correct actions.
- `low`/`icon` — No-show banner uses PantopusIcon.UserMinus; design uses user-x (user-x glyph) and personalizes copy ('<name> didn't attend') whereas Android hardcodes 'the invitee didn't attend'. **Fix:** Switch to a user-x icon and interpolate the requester's first name.
- `low`/`typography` — CardOverline uses fontSize 10.sp + FontWeight.Bold but omits the design's 0.08em letterSpacing on the uppercase overline; minor but visible. **Fix:** Add letterSpacing ≈ 0.8.sp to the overline Text to match the tracked-caps spec.

### E3 Approve / decline sheet — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/ApproveDeclineSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingSheetComponents.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Core/BookingRowView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/ApproveDeclineSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingsKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings/BookingDetailModels.kt`
- **Verdict:** Both render the sheet faithfully (title, requester+badge card, slot card, intake preview, note, reason chips, inline error, spinner-on-submit, decline-expanded mode); iOS is the closer match because Android tints the Approve CTA with the pillar accent (purple) instead of the spec's sky-blue PRIMARY and omits the frame-3 conflict banner that iOS includes.
- **Cross note:** Approve-button color is the key divergence: iOS correct sky-blue PRIMARY, Android wrong pillar-purple. iOS also implements the conflict banner and the propose-another-time link (both gated on optional callbacks) that Android lacks entirely. Both default-hide the propose link unless the host passes the callback.

**iOS deviations:**
- `low`/`other` — BookingSlotCard corner radius uses Radii.lg (12); spec SlotLine borderRadius is 14. **Fix:** Introduce a 14pt radius for the slot/requester cards or accept the 2pt delta.
- `low`/`typography` — IntakeAnswersDisclosure title is size 13 semibold; spec IntakePreview label is fontSize 12.5 weight 600. **Fix:** Drop the intake label to 12.5pt to match spec.
- `low`/`layout` — iOS intake row is a richer disclosure (34pt icon tile + chevron up/down expand); spec IntakePreview is a flat 16px clipboard + 'Intake answers' + 'N answers' + chevron-down with no icon tile. **Fix:** Acceptable enhancement; optionally drop the icon background tile to match the flatter spec row.

**Android deviations:**
- `high`/`color` — PillarFilledButton tints the Approve CTA with state.pillar.accent (purple #7C3AED for business); spec Actions approve button background is PRIMARY = E.blue600 (#0284c7 sky) with a sky shadow rgba(2,132,199,0.28). **Fix:** Use PantopusColors.primary600 for the Approve button background instead of the pillar accent.
- `med`/`missing-element` — No conflict banner is rendered; spec frame 3 (FrameConflict) shows an amber ConflictBanner ('This slot overlaps a confirmed booking' + 'View conflict') above the actions in review mode. iOS has conflictBanner; Android has no equivalent path. **Fix:** Add a warning-tone banner driven by a showConflictWarning flag mirroring iOS's conflictBanner.
- `med`/`missing-element` — Decline mode omits the 'Propose another time' calendar-plus link that spec ReasonChips renders (marginTop:13, primary600, weight 700) below the chips. **Fix:** Add the calendar-plus 'Propose another time' link in decline mode (iOS exposes it via onProposeTime).
- `low`/`sizing` — CTA_HEIGHT is 46.dp for both buttons; spec approve / decline-request primary button height is 48 (only the secondary decline is 46). **Fix:** Make the primary Approve / Decline-request button 48dp tall.
- `low`/`sizing` — NoteField heightIn min = 56.dp; spec NoteInput minHeight is 58. **Fix:** Bump note min height to 58dp.
- `low`/`other` — RequesterCard and SlotLine use Radii.xl (16); spec Requester/SlotLine borderRadius is 14. **Fix:** Use a 14dp (or Radii.lg=12) radius for these cards to match spec.
- `low`/`color` — SlotLine border uses accent.copy(alpha=0.25f); spec strokes 1.5px with the solid identity-bg tint (ID.business.bg #f3e8ff), not an alpha of the accent. **Fix:** Stroke with the pillar's identity-bg token to match the lighter lavender tone.

## Bookings extras

### E10 Double-book warning — iOS `matches` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/DoubleBook/DoubleBookWarningDialog.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasUI.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/DoubleBookWarning.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/Buttons.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`
- **Verdict:** iOS faithfully reproduces both soft-overlap and hard-conflict frames with correct token sizes/colors; Android renders the same structure but with multiple wrong type-scale substitutions, an oversized disc, a non-design green for the member pill, and a missing users icon.
- **Cross note:** Both platforms cover the two design frames (soft-overlap, hard-conflict) and share the testTag/accessibilityIdentifier "scheduling.doubleBook". The divergence is purely visual fidelity: iOS inlines the exact spec px values while Android reuses coarse semantic type-scale tokens (h3/body/small/caption) that don't match the spec's 16.5/13/11.5/11 sizes, plus a wrong green and a dropped users glyph.

**iOS deviations:**
- `low`/`sizing` — ExtrasGhostButton/ExtrasSolidButton frame(height:46) but spec buttons (Cancel/Book anyway/lock) are height:44. **Fix:** Change the shared Extras button height from 46 to 44 to match the spec CTA row.

**Android deviations:**
- `high`/`typography` — Title uses PantopusTextStyle.h3 (20sp/28 semibold) with a Bold override; spec title is fontSize 16.5 weight 700. **Fix:** Use a ~16.5sp Bold style instead of h3 for the title Text.
- `high`/`typography` — Body message uses style=small (14sp regular) and color=appTextSecondary(fg3); spec is fontSize 13, color fg2 (appTextStrong), lineHeight 19. **Fix:** Drop to ~13sp with appTextStrong color and ~19sp line height.
- `high`/`color` — MemberConflictNote uses successBg/success (#F0FDF4 bg, #059669 dot+text); spec uses HOME palette: bg #dcfce7, dot #16a34a, text #15803d. **Fix:** Bind to the home/identity-home tokens (homeBg/home/homeDark) instead of success.
- `high`/`icon` — "Pick another member" Text has no leading glyph; spec line 106 prefixes a lucide users icon (15px) before the label. **Fix:** Add a PantopusIconImage(PantopusIcon.Users, size=15) before the label, matching iOS.
- `med`/`sizing` — ExtrasIconDisc DISC_SIZE=44dp / DISC_ICON=22dp; spec disc is 40x40 with a 20px glyph. **Fix:** Set DISC_SIZE=40.dp and DISC_ICON=20.dp.
- `med`/`typography` — ConflictCard title uses style=body (16sp) SemiBold and detail uses caption (12sp); spec title is 13/bold, detail 11/fg3. **Fix:** Use ~13sp Bold for title and ~11sp for detail.
- `med`/`sizing` — ConflictCard icon box is size(34.dp) with a 17.dp wrench; spec card disc is 36x36 radius 9 with an 18px glyph. **Fix:** Set the box to 36.dp (Radii ~9) and icon to 18.dp.
- `low`/`sizing` — Dialog Column uses widthIn(max=320.dp) and uniform padding(Spacing.s5=20) on all sides; spec card maxWidth is 300 with padding 20/18/16 (top/horizontal/bottom). **Fix:** Cap width at 300.dp and use 18dp horizontal / 16dp bottom padding.

### E11 Send a nudge — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Nudge/SendNudgeSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Nudge/SendNudgeViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasControls.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasUI.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/NudgeSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/GroupRosterScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/Buttons.kt`
- **Verdict:** iOS faithfully reproduces all four frames (composing/over-limit/sent toast/no-recipients) as a large detent sheet with sticky send-icon footer and a full success overlay; Android has the same composer/audience/channel body but degrades the success frame to a plain Snackbar, drops the send-icon CTA, has no sticky footer, and uses the wrong over-limit border color.
- **Cross note:** Both platforms share the same composer/audience/channel structure and the personal sky accent (#0284C7) from tokens, and both render the no-recipients empty strip ('No one to message in this group' with users-round icon). Key divergence: iOS ships all four design frames including the rich success scene and sticky send-icon footer, while Android collapses the success frame to a Snackbar and uses an inline, icon-less CTA.

**iOS deviations:**
- `low`/`sizing` — ExtrasSolidButton uses height 46 + icon size 16 + Radii.lg(12); spec footer button is height 48, send icon 17, cornerRadius 13. **Fix:** Bump button height to 48, icon to 17, corner radius to ~13 to match the spec CTA.
- `low`/`color` — ExtrasMessageBox over-limit border uses Theme.Color.errorLight (token #FECACA) vs spec ERR_LIGHT #FCA5A5 (one step darker); counter/text use error #DC2626 correctly. **Fix:** Acceptable token substitution; optionally add an error-300 token (#FCA5A5) for the over-limit border.
- `low`/`typography` — ExtrasOverline tracking 0.8pt at 10px ~= spec 0.08em and chip weight .bold matches spec 700; only button metrics differ materially. **Fix:** None needed.

**Android deviations:**
- `high`/`state` — Frame 3 (Sent) spec is a full-screen success scene: 72dp success disc with check, centered 'Update sent' title, and a dark bottom toast 'Update sent to 12 attendees'. Android GroupRosterScreen only calls snackbar.showSnackbar(it) — no success disc, no centered title, no styled dark toast. **Fix:** Render a success overlay inside NudgeSheet (72dp check disc + 'Update sent' + dark count toast) mirroring SendNudgeSheet.successOverlay instead of a plain Snackbar.
- `high`/`missing-element` — Spec CTA is a sticky footer pinned to the sheet bottom with a top hairline border (borderTop 1px E.border) over surface; Android places PrimaryButton as the last item in the scrolling Column with bottom=Spacing.s4 padding and no divider/sticky footer. **Fix:** Wrap the CTA in a non-scrolling footer with a top Divider(appBorder) and surface background, matching iOS ExtrasStickyFooter.
- `med`/`icon` — Spec CTA has a leading send glyph ('<send/> Send to 12'); Android PrimaryButton/PantopusButton renders title only with no icon. **Fix:** Use a button variant accepting a leading PantopusIcon.Send, or render an icon+label row inside the CTA.
- `med`/`color` — ExtrasMessageBox over-limit border uses PantopusColors.error (solid #DC2626) at full weight; spec uses lighter ERR_LIGHT #FCA5A5 for the box border (only counter/text turn full red). **Fix:** Use a lighter error token (#FCA5A5) for the over-limit border, keep counter text at error #DC2626.
- `med`/`typography` — Char counter uses style caption (~12sp) vs spec 10px/700/tabular-nums; channel-row label uses body Medium vs spec 12.5px semibold; header uses h3 (20sp) Bold vs spec 16.5px/700. **Fix:** Inline spec point sizes/weights (counter 10sp bold monospaced, channel label 12.5sp semibold, header 16.5sp bold).
- `low`/`sizing` — ExtrasChannelRow uses Material3 Switch default (~52x32dp) vs spec toggle pill 42x25 with 20dp thumb; ExtrasPillChip selected uses 1.5dp accent border + accent.copy(0.12) fill vs spec borderless fill #e0f2fe. **Fix:** Switch is an acceptable native idiom; for selected chips drop the border and use the lighter personal-bg fill.
- `low`/`layout` — Generic send error (state.error) renders after the channel rows just above the CTA; over-limit 'Shorten your message' is correctly under the box. **Fix:** Keep over-limit error under the box (already correct); send-failure error above CTA is acceptable.

### E12 Manual / on-behalf booking — iOS `major` / Android `major` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/ManualBooking/ManualBookingView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/ManualBooking/ManualBookingViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Wizard/WizardShell.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/ManualBookingScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** Both render the 4-step wizard faithfully on the happy path, but each drops the design's Step-3 invitee directory surfaces (verified-result card + not-on-Pantopus invite banner); Android additionally over-sizes step titles/tiles while iOS swaps the named StepRail for a generic N/M segmented bar.
- **Cross note:** Shared gap: BOTH platforms drop the Step-3 invitee directory surfaces (verified-result card + not-on-Pantopus invite banner) — the design's two distinct Step-3 frames — because the ManualBooking model lacks an invitee directory. Key divergence: Android keeps the design's named StepRail but over-sizes titles/tiles; iOS sizes type correctly but replaces the StepRail with a generic N/M segmented progress bar and omits the success-screen action buttons from the body.

**iOS deviations:**
- `high`/`chrome` — WizardShell renders a generic "N/4" trailing readout + SegmentedProgressBar instead of the design's named StepRail (numbered/checked circles with Event/Time/Details/Review labels + connector lines). **Fix:** Add a StepRail subview mirroring the design (22px circles, accent fill on current, check on done, active label) below the top bar.
- `high`/`missing-element` — Step 3 omits both the verified-invitee result card (avatar + badge-check + "Verified neighbor") and the not-on-Pantopus invite banner (blue50/blue200 + user-plus) — explicitly deferred in a code comment; design shows these as two key frame variants. **Fix:** Render an invitee-match result card and a not-on-Pantopus invite banner once a directory lookup is wired.
- `high`/`missing-element` — Created step body (createdStep) shows only disc + title + subtitle; the design's two action buttons (View booking / Book another) are not in the success body — they live only in WizardShell's sticky CTA, diverging from the centered two-button layout. **Fix:** Render View booking (filled) + Book another (ghost) as a centered button stack inside createdStep, or hide chrome and match the design's no-chrome success frame.
- `low`/`chrome` — Top bar title uses .body (16pt); design TopBar title is 14.5/600. **Fix:** Use a 14.5pt semibold style for the wizard title.
- `med`/`layout` — Review summary lists "Contact" as the 4th row; design's 4th row is "Member" (Priya R.) and invitee shows "· verified". **Fix:** Add a Member row sourced from the acting member and append a verified suffix to the invitee value.
- `low`/`extra-element` — Details renders bordered card-style "Invite by" rows with checkCircle; design step-3 (verified branch) has no channel selector at all and the invite branch uses Phone/Email rows with a "Recommended" sub — close but the verified-path layout differs. **Fix:** Gate the channel selector behind the not-on-Pantopus branch as in the design.

**Android deviations:**
- `high`/`typography` — StepScaffold renders every step title with PantopusTextStyle.h2 (24sp); design StepTitle is 17px/700 — titles are ~40% too large. **Fix:** Introduce a 17sp/Bold style for the step title instead of h2.
- `high`/`missing-element` — Step 3 omits the verified-invitee result card and the not-on-Pantopus invite banner (blue50/blue200 + user-plus) that are two of the design's eight frames. **Fix:** Render a match result card (InitialsAvatar already exists with a verified badge) and an invite banner driven by a directory lookup.
- `med`/`typography` — EventTypeTile name uses body (16sp) and duration uses caption (12sp); design is 13.5/700 name and 11px sub. **Fix:** Drop the name to ~13.5sp bold and the sub to ~11sp.
- `med`/`extra-element` — Details "Invite by" uses pill chips ("Phone · recommended" / "Email") instead of the design's full-width channel-selector rows (icon + title + Recommended sub + trailing check). **Fix:** Replace ExtrasChipFlow/ExtrasPillChip with stacked full-width selector rows matching the invite-branch design.
- `low`/`sizing` — DayChip width is 54dp and EventTypeTile icon disc is 40dp; design DayStrip cell is 48px wide and the tile icon disc is 38px. **Fix:** Set DayChip width to 48dp and the tile icon box to 38dp.
- `low`/`color` — Selected EventTypeTile/chip backgrounds use accent.copy(alpha=0.08/0.12) instead of the design's solid identity-business-bg (#F3E8FF) token. **Fix:** Use PantopusColors.businessBg (identity-bg token) for the selected fill to match the spec swatch.
- `low`/`layout` — Review "Skip approval" subtitle reads "Follows the event type's approval setting"; design says "Confirm the booking now". **Fix:** Update the subtitle copy to match the spec.
- `low`/`chrome` — Step title via h2 also makes the top bar title use h3/SemiBold; design top-bar title is 14.5/600 (minor). **Fix:** Use ~14.5sp semibold for the CenterAlignedTopAppBar title.

### E13 Waitlist join & management — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Waitlist/WaitlistJoinSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Waitlist/WaitlistManagementView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/CapacityRoster.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasUI.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/WaitlistScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/WaitlistJoinSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/CapacityRoster.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/WaitlistViewModel.kt`
- **Verdict:** iOS faithfully renders both surfaces (join sheet + host capacity/promote list) with correct tokens and states, missing only the "#3 in line" queue-position badge; Android's host screen replaces the design's capacity strip with an ad-hoc "N people waiting" row, never disables Promote when full, and adds an out-of-spec event-type chip selector.
- **Cross note:** Both platforms drop the design's "#3 in line" queue-position badge in the joined-confirmation and neither implements the frame-3 "already on waitlist" read-only variant. Key divergence: iOS host view correctly uses CapacityHeaderCard + RosterRow with promote-disabled-when-full, while Android's WaitlistContent abandons the shared CapacityHeaderCard, hardcodes enabled=true on promote, and bolts on an extra event-type chip picker not in the spec.

**iOS deviations:**
- `med`/`missing-element` — joinedConfirmation omits the design's "#3 in line" queue-position badge (pill with bold accent #3 + "in line") and shows generic copy instead of "You're #3 — we'll text you if a spot frees up." **Fix:** Add a pill (ID.business.bg background, accent bold position + fg2 "in line") below the headline and surface the queue position in the subcopy.
- `med`/`missing-element` — No "already on waitlist" frame (clock disc, "You're already waiting", joined-date copy, position badge, Leave waitlist) — WaitlistJoinSheet only handles fresh-join vs joined, not the already-joined read-only variant. **Fix:** Add an alreadyJoined branch in the join sheet rendering the clock disc + joined-date copy + position badge.
- `low`/`state` — Joined CTA is a ghost "Done" button; design frames 2/3 use a ghost "Leave waitlist" action. **Fix:** Rename/repurpose the ghost CTA to "Leave waitlist" wired to a leave action, keeping ghost tone.
- `low`/`missing-element` — Join form omits the design's "Preferred time" multi-line textarea (minHeight 42, padded box); iOS uses a single-line 44pt TextField field matching the other inputs. **Fix:** Render preferred-time as a taller multi-line input box to match the design's textarea geometry.
- `low`/`color` — fullyBookedPill warning tone uses Theme.Color.warning (token #D97706) vs design WARN #B45309; close but the design's warn is darker. **Fix:** Acceptable token mapping; optionally add a darker warning-strong token to match #B45309.

**Android deviations:**
- `high`/`missing-element` — Host WaitlistContent never renders CapacityHeaderCard; it builds an ad-hoc Row showing "N people waiting / N seats per session" instead of the design's "11 of 12 seats filled · 3 waiting" strip with a progress fill bar. **Fix:** Replace the ad-hoc Row with CapacityHeaderCard(filled, total, waiting, accent, showStats=false) and compute filled from seatTotal − openSeats.
- `high`/`state` — PromoteSeatButton is always called with enabled=true; the design's frame-5 capacity-full state (grayscale bar + disabled Promote + "Open a seat to promote" caption) is unreachable. **Fix:** Track isFull in WaitlistData and pass enabled=!isFull plus the "Open a seat to promote" caption below the button when full.
- `high`/`extra-element` — WaitlistContent adds an out-of-spec event-type chip selector (ExtrasChipFlow + "Event type" overline) and a per-type loader; the design is a single event's roster with no type picker. **Fix:** Remove the chip selector; the screen should target one event/slot's waitlist like the spec and iOS.
- `med`/`layout` — PromoteSeatButton is a compact inline pill in the row's trailing slot; the design puts a full-width 34pt promote button on its own line below the avatar/name row (column layout with a divider). **Fix:** Render the promote button full-width on a second line inside the row (mirror iOS RosterRow promoteSection) rather than as a trailing pill.
- `med`/`missing-element` — RosterRow lacks the more-vertical kebab (16dp, fg4) the design shows on each waitlist row's trailing edge. **Fix:** Add a kebab IconButton in the row trailing slot before/with the promote action.
- `low`/`sizing` — CapacityHeaderCard fill bar BAR_HEIGHT=8.dp vs design 9px; joined-confirmation disc is 64dp vs design 74; check disc icon 30dp vs 34. **Fix:** Bump bar to 9dp and the joined disc to 74dp / icon 34dp to match the spec.
- `med`/`missing-element` — Join sheet's JoinedConfirmation omits the "#3 in line" position badge and the already-on-waitlist variant, same as iOS but compounded by generic copy. **Fix:** Add the accent position badge + queue copy, and an already-joined branch.
- `low`/`icon` — FullyBookedPill uses PantopusIcon.Users (users) vs design users-round; minor glyph intent match. **Fix:** Use a users-round equivalent if available, otherwise acceptable.

### E6 Mark no-show — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/MarkNoShow/MarkNoShowDialog.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasUI.swift`, `frontend/apps/ios/Pantopus/Core/Design/Icons.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/NoShowSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/Buttons.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both platforms faithfully implement the centered no-show confirmation dialog with all four frame variants; iOS mirrors the spec's exact point sizes and uses the correct user-x glyph, while Android leans on its 20sp h3 type token (vs 16.5), a 44dp disc (vs 40), a person-remove icon (vs user-x), and adds an unspec'd note-field leading icon.
- **Cross note:** Both expose all four states (default, group multi-select, submitting spinner, inline error) and share the scheduling.markNoShow test-tag/accessibilityIdentifier. Shared divergence: neither perfectly hits the spec's 13.5px button label; the main divergence is Android relying on coarse type tokens (h3=20, small=14) and a larger 44dp disc where iOS inlines the exact 16.5/13/12.5px sizes and 40px disc from the frame.

**iOS deviations:**
- `low`/`typography` — Group note placeholder is 12px (matches spec 12 fg4) and 1:1 body lineSpacing is 2pt giving ~17pt line height vs spec lineHeight 19px on 13px body. **Fix:** Set .lineSpacing(6) on the 1:1 Body text so total line height reaches ~19pt.
- `low`/`sizing` — Avatar in attendeeRow uses owner.theme.accent fill (e.g. business purple/home green) whereas spec hardcodes the personal sky gradient AV.personal for all roster avatars. **Fix:** Acceptable owner-context choice, but if matching spec exactly use the personal-blue gradient swatch.
- `low`/`color` — Inline error strip border uses Radii.md (8) matching spec error box radius 10 only approximately; spec error radius is 10, attendee radius 11 vs code Radii.lg (12). **Fix:** Optionally add a Radii value of 10/11 for these compact dialog elements; current 8/12 is within tolerance.

**Android deviations:**
- `med`/`typography` — Title uses PantopusTextStyle.h3 (20sp/28) overridden only to Bold; spec title is 16.5px bold with letterSpacing -0.2, so the Android title renders ~20% larger than design. **Fix:** Use a 16.5sp bold custom TextStyle (or PantopusTextStyle.body bolded) instead of h3 for the dialog title.
- `med`/`typography` — Body/subtitle uses PantopusTextStyle.small (14sp); spec 1:1 body is 13px and group subtitle is 12.5px, so copy is slightly oversized and the variants don't differ as in spec. **Fix:** Use ~13sp for the 1:1 body and ~12.5sp for the group subtitle rather than the shared small style.
- `med`/`icon` — ExtrasIconDisc is fed PantopusIcon.UserMinus which maps to Material Icons.Filled.PersonRemove (a person with a minus); spec glyph is lucide user-x (person with an X). iOS correctly uses .userX. **Fix:** Map a UserX icon (lucide user-x equivalent) and pass PantopusIcon.UserX to the disc to match intent.
- `low`/`extra-element` — Group note field (ExtrasInputField) renders a leading PantopusIcon.MessageSquare; the spec note field has no leading icon, just left-aligned placeholder text. **Fix:** Use a plain bordered text field without a leadingIcon for the optional note.
- `low`/`sizing` — ExtrasIconDisc DISC_SIZE=44dp / DISC_ICON=22dp vs spec 40px disc with 20px glyph (~10% large); attendee avatar diameter=34dp and checkbox 22dp vs spec 30/21. **Fix:** Set disc to 40dp/20dp, roster avatar to 30dp, and checkbox to 21dp to match the frame.
- `low`/`spacing` — Card padding is a uniform Spacing.s5 (20dp) on all sides and section gap is a uniform Spacing.s3 (12); spec uses asymmetric padding 20/18/16 and tighter per-element margins (title 8, body 16, etc.). **Fix:** Apply 20 top / 18 horizontal / 16 bottom padding and tune inter-element spacing rather than one spacedBy value.
- `low`/`typography` — Confirm/Keep-open button labels render via PantopusButton body style (16sp); spec button text is 13.5px bold. **Fix:** Lower the shared dialog button label to ~13.5sp bold (or accept the 44dp button with 16sp as a platform idiom).

### E7 Post-meeting follow-up — iOS `matches` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/FollowUp/BookingFollowUpSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/FollowUp/BookingFollowUpViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasUI.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasControls.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/PostMeetingFollowupScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/PostMeetingFollowupViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** iOS reproduces the design's bottom-sheet near pixel-for-pixel (grabber, header+subtitle, accent chips, PRIMARY rebook chip/toggle, ghost↔solid CTA, full success scrim+disc+toast), while Android rebuilds it as a full-screen Scaffold with a top app bar, drops the header subtitle and the rich success overlay, and inflates several font sizes.
- **Cross note:** Core divergence: iOS treats E7 as the spec's bottom-sheet archetype (sheet chrome + success scrim/toast), Android treats it as a routed full screen (Scaffold + TopAppBar + plain success), so Android loses the header subtitle, the success overlay richness, and uses owner-accent where the spec keeps brand PRIMARY. Android does add real Loading/Error full-screen states (parity-rule compliant) that the design doesn't depict.

**iOS deviations:**
- `low`/`interaction` — Spec frame 4 fires a success scrim then auto-dismisses after the toast; iOS sleeps 1.3s before onClose() which is fine, but the toast offset uses Spacing.s8 (32) bottom vs spec bottom:34 — ~6% off, negligible. **Fix:** Optionally nudge the toast bottom inset from Spacing.s8 to ~34 to match exactly.
- `low`/`sizing` — ExtrasSolidButton/ExtrasGhostButton footer buttons are height 46 vs spec button height 48. **Fix:** Bump the footer button frame height from 46 to 48 to match the spec CTA.

**Android deviations:**
- `high`/`chrome` — PostMeetingFollowupScreen renders a full-screen Scaffold + CenterAlignedTopAppBar (back chevron + centered 'Follow up' title); spec is a bottom sheet with a grabber pill, dim scrim over the parent booking, and rounded top corners — no top app bar. **Fix:** Present as a ModalBottomSheet with a drag handle and the in-body SheetHeader instead of a Scaffold/TopAppBar.
- `high`/`missing-element` — The SheetHeader subtitle ('Garden walkthrough · Mara Reyes · Jun 9', 11.5/fg3) is absent — FollowUpContent jumps straight to the Outcome section; the 16.5/700 'Follow up' title is also moved into the app bar instead of the body. **Fix:** Add an in-body header Column with the 'Follow up' title (16.5sp bold) and the booking·invitee·date subtitle (11.5sp appTextSecondary) above the Outcome section.
- `high`/`state` — FollowUpSuccess shows only a centered disc + single 'Follow-up sent to {name}' line; spec frame 4 has a dimmed scrim, a 72px successBg disc with successLight ring + heavy check, a white 17/700 'Follow-up sent' headline, AND a dark pinned bottom toast with check-circle-2 + 'Follow-up sent to Mara'. **Fix:** Rebuild success as a scrim overlay: 72dp ringed disc, white bold headline 'Follow-up sent', plus a dark pinned bottom toast row mirroring iOS successOverlay.
- `med`/`typography` — ExtrasMessageBox text/placeholder and ExtrasChannelRow label use PantopusTextStyle.body (16sp) vs spec composer/placeholder 12.5 and toggle label 12.5/600; ExtrasPillChip uses small (14sp) vs spec chip 12/700. **Fix:** Introduce 12.5sp styles for the composer/placeholder/toggle label and 12sp/bold for chips instead of body/small.
- `med`/`sizing` — ExtrasMessageBox default minHeight is 96dp and the private-note box is passed 72dp; spec composer is minHeight 84 and private note minHeight 46. **Fix:** Set composer minHeight to 84dp and private-note minHeight to 46dp.
- `med`/`missing-element` — Private-note footer renders a plain 'Only you can see this' caption with no leading eye-off icon (spec has eye-off 11px + 10px muted text); the lock overline icon is 12dp tinted appTextSecondary vs spec 13px appTextMuted. **Fix:** Add the eye-off icon before the caption and use appTextMuted tints to match the spec.
- `med`/`color` — Outcome chips, rebook chip, message-box cursor, and push toggle all use the owner pillar accent (state.pillar.accent); spec keeps the rebook chip, push toggle, and primary CTA on brand PRIMARY (blue600) — only the selected outcome chip uses the home/owner accent. **Fix:** Mirror iOS: pass Theme primary600 to ExtrasChipButton, ExtrasChannelRow, and the CTA; keep owner accent only on the selected outcome chip.
- `low`/`extra-element` — PrimaryButton title is 'Send follow-up'/'Try again' but the default no-outcome ghost CTA uses 'Save note only'; the error CTA icon/rotate-cw glyph from spec isn't explicitly wired on PrimaryButton, and footer button height/radius come from PrimaryButton defaults rather than the spec 48h/13 radius. **Fix:** Ensure the error CTA shows a rotate-cw icon and verify footer button height≈48dp and radius≈13dp to match the spec.

### E8 Group roster & seats — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Roster/GroupRosterView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/CapacityRoster.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/GroupRosterScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/CapacityRoster.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/EmptyState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/ErrorState.kt`
- **Verdict:** iOS is a near-faithful reproduction of all six frames with only small nits, while Android reproduces the same content but reorders/restyles the host controls, drops the pinned capacity header in the empty state, and uses the wrong error icon/copy plus generic shared empty/error scaffolds.
- **Cross note:** Both platforms correctly share a CapacityHeaderCard/RosterRow archetype and cover all six frames (under-capacity/full/waitlist/loading/empty/error); the divergence is that Android leans on generic shared EmptyState/ErrorState scaffolds (losing the pinned capacity header and the cloud-off error identity) and restyles the host controls, while iOS hand-builds each frame to match the JSX.

**iOS deviations:**
- `low`/`color` — RosterRow avatar fills with the flat owner accent (theme.accent) whereas spec uses a 135deg business gradient (#a78bfa→#6d28d9). **Fix:** Render the avatar background as a LinearGradient matching AV.business instead of a solid Circle fill.
- `low`/`chrome` — Top-bar overflow menu offers 'Message all attendees' + 'Mark no-show'; the design overflow is unspecified but the Add-attendee affordance lives only in host controls (matches), so this is acceptable but adds an extra Message-all entry duplicating the FAB. **Fix:** Optional: drop the duplicate 'Message all attendees' menu item since the FAB already covers it.
- `low`/`spacing` — Promote section divider uses spacing Spacing.s2-2 (6pt) gaps vs spec's 10px top padding / borderTop separation; visually slightly tighter than the 10px gap in JSX. **Fix:** Use 10pt top padding above the divider to match promote { gap:10 }.

**Android deviations:**
- `high`/`layout` — CapacityControls renders the Capacity stepper FIRST and 'Add or invite attendee' SECOND; spec HostControls order is Add/invite attendee FIRST then Capacity. **Fix:** Swap the two children in CapacityControls so the add-attendee row precedes the capacity stepper row.
- `high`/`missing-element` — 'Add or invite attendee' is a generic full-width PrimaryButton; spec is a white card row with a 32px primary50 icon tile (user-plus, blue600) on the left, label, and a trailing chevron-right. **Fix:** Replace PrimaryButton with a bordered surface Row carrying a userPlus icon tile + label + chevronRight, mirroring iOS hostControls.
- `high`/`missing-element` — Empty state calls the generic full-screen EmptyState, dropping the capacity header (0 of N seats) that the spec pins above the 'No signups yet' block. **Fix:** Render CapacityHeaderCard pinned at top in the Empty branch, then the users disc + headline + Share CTA below it.
- `high`/`icon` — Error state uses PantopusIcon.AlertCircle with generic copy ('Something went wrong'/connection hint); spec uses a cloud-off glyph in an error disc with 'Couldn’t load the roster' / 'Check your connection and try again.' **Fix:** Pass an explicit cloud-off icon and the spec headline/message to ErrorState (or build a dedicated error block).
- `med`/`chrome` — Top-bar overflow menu contains 'Add or invite attendee' (+ Mark no-show); spec keeps add-attendee in host controls and the overflow is for message-all / no-show, so the add-attendee entry is misplaced into chrome. **Fix:** Remove the 'Add or invite attendee' DropdownMenuItem from the top bar; surface it via the host-controls row instead.
- `med`/`icon` — InitialsAvatar picks from a hashed 5-color palette and uses a green CheckCircle verified badge; spec avatar is the business purple gradient with a badge-check glyph tinted in the owner accent (#7c3aed). **Fix:** Fill the avatar with the business gradient/accent and use PantopusIcon.BadgeCheck tinted with the pillar accent for the verified badge.
- `med`/`extra-element` — 'Open a seat to promote' caption is rendered once below the whole waitlist section when seatsOpen==0; spec attaches the disabled caption beneath each individual promote button. **Fix:** Move the disabled caption into PromoteSeatButton (or per-row) so it appears under each disabled button.
- `low`/`typography` — Capacity headline uses PantopusTextStyle.body (16sp) and stat values use h3 (20sp); spec headline is 14/700 and stat value is 17/800. **Fix:** Use 14sp bold for the headline Text and ~17sp heavy for StatCell values rather than body/h3 styles.

### E9 Booking search & filter — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Filter/BookingFilterSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasControls.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Bookings/Extras/Shared/BookingsExtrasUI.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingFilterSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingSearchFilterScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/bookings_extra/BookingsExtrasUi.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/EmptyState.kt`
- **Verdict:** iOS faithfully reproduces the FilterSheetShell (correct title/overline/chip/footer geometry and a no-results frame that keeps search + active-summary visible), while Android renders chips/title/Clear-all with oversized body/small/h3 type, drops the sticky-footer hairline, and replaces the no-results frame with a full-screen EmptyState that hides the search field and active summary.
- **Cross note:** Both platforms reuse a shared Extras component set; iOS's set encodes the exact spec point sizes (34/30dp chips, 10/700 overline, sticky footer, 60pt no-results disc) while Android's BookingsExtrasUi token-maps to coarser h3/body/small styles and the Android sheet additionally lives under an extra results-list Scaffold screen (CenterAlignedTopAppBar 'Search bookings' + chevron + sliders) that the spec — a pure bottom sheet — does not depict.

**iOS deviations:**
- `low`/`sizing` — Footer CTA uses ExtrasSolidButton with frame(height: 46) and Radii.lg (12); spec CTA is height 48 with cornerRadius 13. **Fix:** Bump the sheet CTA to height 48 and corner radius 13 (or add a 48/13 variant) to match the spec button.
- `low`/`typography` — CTA label font is .system(size: 14, weight: .bold) in ExtrasSolidButton; spec CTA text is 14.5px/700. **Fix:** Set the footer button font to size 14.5 to match spec.
- `low`/`state` — Status chips always pass through chipColors and show a dot for the tinted statuses, but the eventType/dateRange chips never show a dot — matches spec; however Pending uses warning tint correctly while spec Pending uses #B45309/#FFFBEB (warning is #D97706/#FFFBEB). **Fix:** Optional: map Pending status tint to the exact spec amber (#B45309 text) if exact match is required; otherwise acceptable as a token approximation.

**Android deviations:**
- `high`/`state` — No-results renders components/EmptyState (fillMaxSize, 72dp circle, 32dp icon, h3 headline, plus a 'Try removing a filter…' subcopy) and omits the SearchField and Active-filters summary that the spec FrameNoResults keeps visible above a 60dp disc/26px icon/14px headline with no subcopy. **Fix:** Replace EmptyState with an inline centered block (60dp sunken disc, 26dp search-x, 14sp/700 headline, no subcopy, Clear-all link) rendered below the still-visible SearchField + ActiveSummary, mirroring iOS noResults.
- `high`/`chrome` — CTA (PrimaryButton) sits inside the scrolling Column with vertical s4 padding and no top hairline; spec has a sticky footer pinned below the scroll area with a 1px top border on surface. **Fix:** Move PrimaryButton into a non-scrolling footer Box with Divider/top border (mirror iOS ExtrasStickyFooter), outside the verticalScroll Column.
- `med`/`typography` — Title 'Filter bookings' uses PantopusTextStyle.h3 (20sp) and 'Clear all' uses small (14sp); spec is 16.5px title and 13px Clear-all. **Fix:** Use a 16.5sp/Bold title and 13sp/Bold Clear-all instead of the h3/small token styles.
- `med`/`typography` — Pill chips and removable chips render label with PantopusTextStyle.small (14sp); spec pill text is 12px/700 and removable-chip text is 11.5px/700. **Fix:** Set pill chip text to 12sp/Bold and removable-chip text to 11.5sp/Bold.
- `med`/`sizing` — ExtrasPillChip/ExtrasRemovableChip have no fixed height (rely on vertical=Spacing.s2) so heights drift from spec's fixed 34dp (pill) and 30dp (removable); selected chips also add a 1.5dp border the spec selected chips do not have (spec selected = border:none). **Fix:** Pin pill chips to height 34dp and removable chips to 30dp; drop the border on selected chips (border only on unselected).
- `low`/`sizing` — CTA PrimaryButton floors at heightIn(min=44dp) with Radii.lg (12); spec CTA is 48dp tall, radius 13. **Fix:** Force the footer CTA to 48dp height and ~13dp radius.
- `low`/`typography` — ExtrasInputField search placeholder uses PantopusTextStyle.body (16sp); spec search placeholder is 12.5px. **Fix:** Use a 12.5sp text style for the search field value/placeholder.
- `low`/`state` — All facet chips pass showDot=true so Status chips get a selected dot; spec only shows dots on Owner-context chips (and tinted statuses), not a universal dot, and Date-range/Event-type chips never show a dot. **Fix:** Pass showDot=true only for the Owner-context section (and keep it off for plain date/event chips) to match spec dot usage.

## Home

### F1 Home calendar — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Homes/Calendar/HomeCalendarView.swift`, `frontend/apps/ios/Pantopus/Features/Homes/Calendar/MonthStripHeader.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Calendar/HomeAgendaComponents.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Calendar/HomeCalendarCreateMenu.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Routing/Stubs/Home/HomeCalendarStubView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/HomeCalendarScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/MonthStripHeader.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/HomeAgendaComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/HomeCalendarCreateMenu.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** Both platforms faithfully reproduce the day-grouped agenda, filter chips, FAB create-menu, and all 7 frame variants; iOS is a near-exact match while Android drifts on the month strip (extra event dots, 3-letter weekday, chevron-down, button-chrome chevrons) and an oversized 20sp top-bar title with an extra "Who's free" text label.
- **Cross note:** Both correctly source home pillar colors from tokens (homeBg/home/homeDark) and ship all 7 frame variants (default, create-menu, empty, loading, error, offline, filtered-empty). Key divergence: iOS month strip is a faithful single-letter, dot-less, chrome-less match while Android's strip adds dots, 3-letter weekdays, a chevron-down, and chevron button-chrome — and Android's top bar is oversized with an extra label.

**iOS deviations:**
- `low`/`chrome` — Top bar uses .navigationTitle("Calendar") with .inline mode, centering the title, whereas the design TopBar has center=false (left-aligned 15.5px title). **Fix:** Acceptable native idiom; if exact parity is wanted, render a leading-aligned custom title via a principal/leading toolbar item.
- `low`/`icon` — First-run empty state uses Icon(.calendar) which matches design's EmptyState icon="calendar"; no issue (noted for cross-platform contrast with Android's CalendarDays). **Fix:** No change needed; keep .calendar.

**Android deviations:**
- `med`/`typography` — CalendarTopBar title text uses PantopusTextStyle.h3 (20sp) but the design TopBar title is 15.5px/SemiBold — roughly 29% too large. **Fix:** Use a 15.5.sp SemiBold style for the top-bar title instead of h3.
- `med`/`extra-element` — CalendarTopBar renders a "Who's free" Text label beside the Users icon; the design's right action is icon-only (label is only a hover tooltip). **Fix:** Drop the trailing Text and keep just the Users icon button (keep the contentDescription for a11y).
- `med`/`extra-element` — MonthStripHeader DotsRow renders 1-3 event-count dots under each day cell, but the home-shell MonthStrip explicitly drops per-day dots. **Fix:** Remove the DotsRow from DayCell to match the spec (eventCount stays in state for tests only).
- `low`/`extra-element` — MonthRow adds a ChevronDown glyph next to the month label; the design month label has no dropdown affordance. **Fix:** Remove the PantopusIcon.ChevronDown from the month label row.
- `low`/`typography` — DayCell weekday uses dowFmt 'EEE' uppercased (e.g. "SUN"/"MON") while design renders single-letter initials (S/M/T/W/T/F/S), matching iOS's single-letter dayOfWeek. **Fix:** Project single-letter weekday initials (take first char) like iOS, or set dowFmt to a single-letter pattern.
- `low`/`chrome` — Prev/next month chevrons are wrapped in 26dp sunken-background ChevronButton boxes; design renders them as plain 17px glyphs (muted/secondary tint) with no button chrome. **Fix:** Render bare PantopusIconImage glyphs (~17dp, appTextMuted/appTextSecondary) without the sunken background box.
- `low`/`typography` — Error and filtered-empty headlines use PantopusTextStyle.h3 (20sp) where the design EmptyState/error title is 15.5px bold. **Fix:** Use a 15.5.sp Bold style for these headlines to match the spec and iOS.
- `low`/`spacing` — MonthStripHeader horizontal padding is Spacing.s4 (16dp) vs the design strip's ~10px horizontal padding. **Fix:** Reduce the strip's horizontal padding to ~10dp to match the design container.

### F15 Permission-gated scheduler — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/GatedScheduler/GatedSchedulerView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/GatedScheduler/GatedSchedulerViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/home/PermissionGatedSchedulerScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/home/PermissionGatedSchedulerViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both platforms faithfully render all three design frames (read-only, my-assignments, request-pending) with correct chrome, hint bar, sky-outline assignment rows, "Your slot" pill, and Accept/Decline buttons from the token palette; iOS is pixel-faithful while Android substitutes a shield-check (GppGood) glyph for the design's shield-plus on the "Ask to manage" pill.
- **Cross note:** Both platforms correctly gate the MonthStrip to the read-only/pending frames (shown only when assignments is empty) and omit it in the my-assignments frame, and both add loading/no-access/error states beyond the 3 design frames per the four-state convention (not a deviation). Key divergence: Android uses shield-check/GppGood where iOS uses the design's shield-plus.

**iOS deviations:**
- `low`/`other` — AssignmentRow Accept/Decline use clipShape(RoundedRectangle(cornerRadius: Radii.md)) = 8pt; design buttons are borderRadius:9 (corner-radius nit). **Fix:** Use a literal 9pt cornerRadius for the two action buttons to match spec exactly.
- `low`/`typography` — My-assignments overline uses .tracking(0.6) where design letterSpacing:0.06 is em-relative (~0.66pt at 11pt size); negligible difference. **Fix:** Optionally set tracking to 0.66 for exactness; no functional change needed.

**Android deviations:**
- `low`/`icon` — AskToManagePill uses PantopusIcon.ShieldCheck (maps to Material Icons.Filled.GppGood); design GatedTopBar specifies lucide shield-plus (iOS correctly uses .shieldPlus). **Fix:** Add a ShieldPlus case to Icons.kt (lucide shield-plus) and use it on the Ask-to-manage pill.
- `low`/`other` — ActionButton uses RoundedCornerShape(Radii.md) = 8.dp; design Accept/Decline buttons are borderRadius:9 (corner-radius nit). **Fix:** Use RoundedCornerShape(9.dp) for the action buttons.
- `low`/`spacing` — RequestSentBanner is a hand-rolled Row (clip lg, infoBg, s3 padding) rather than the shared Banner component used in design Frame 3; visually equivalent but diverges from the canonical Banner and iOS HomeInfoBanner. **Fix:** Reuse the shared info Banner component for the pending state to match design and iOS.
- `low`/`sizing` — AssignmentRow time column is fixed Modifier.size(width=42.dp, height=36.dp) and the divider Box is fixed height 36.dp; design divider is alignSelf:'stretch' to row height with no fixed 36, so it will not stretch if the title wraps. **Fix:** Use IntrinsicSize.Min on the Row and Modifier.fillMaxHeight() on the divider instead of a hardcoded 36.dp.

### F2 Home event detail — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Homes/Calendar/EventDetailView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Routing/Stubs/Home/HomeEventDetailStubView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/EventDetailScreen.kt`
- **Verdict:** Both render every section (header, detail grid, attendees w/ RSVP pills, Your RSVP, notes, footer Edit/Delete) plus loaded/loading/error/deleting/offline/pending/recorded states, and both replace the spec's text "Edit" top-bar action with a pencil icon; iOS is faithful save a few chip/divider nits, while Android ships a non-mirroring loading skeleton, undersized card radii, and a neutral category chip that diverge more.
- **Cross note:** Both platforms substitute a pencil icon for the spec's text "Edit" top-bar action — a shared chrome divergence worth fixing on both. iOS otherwise mirrors the spec (4-state skeleton, 16dp cards, dividers, BYDAY recurrence) more faithfully; Android's loading skeleton and 12dp card radii are the key regressions.

**iOS deviations:**
- `med`/`chrome` — ContentDetailTopBarAction uses a .pencil icon button, but spec TopBar right is a text "Edit" label (14/700, home accent700). **Fix:** Render a text "Edit" trailing action in home-green instead of the pencil glyph.
- `med`/`icon` — CategoryPill renders an Icon(category.icon) glyph, but spec CatChip uses a 7×7 colored dot, not a glyph. **Fix:** Replace the leading Icon with a 7pt Circle filled with the category color.
- `med`/`color` — CategoryPill background is category.background (category accent bg, e.g. green), but spec CatChip background is neutral N.sunken (appSurfaceSunken) with only the dot colored. **Fix:** Set the chip background to Theme.Color.appSurfaceSunken and text to appTextStrong.
- `low`/`missing-element` — DetailGrid stacks DetailRows with Spacing.s1 and explicitly draws no inner dividers, but the spec DetailRow carries a 1px N.border bottom divider between rows. **Fix:** Add a 1px appBorder divider between detail rows (omit on last), matching the attendees card.
- `low`/`spacing` — DetailGrid card uses padding .horizontal s3 / .vertical s1, but spec Card pad is 4px/13px (≈vertical 4, horizontal 13). **Fix:** Use ~13pt horizontal and ~4pt vertical inner padding on the detail card.

**Android deviations:**
- `high`/`state` — LoadingShell body renders two plain 320×120 Shimmer rectangles instead of mirroring the loaded geometry; spec FrameLoading shows a detail-grid card of 4 icon-tile rows + an attendees card of 3 avatar rows. **Fix:** Build skeleton cards of icon-tile rows and avatar rows mirroring DetailGrid/Attendees, as iOS LoadingShell does.
- `med`/`chrome` — topBarAction uses PantopusIcon.Pencil, but spec TopBar right is a text "Edit" label (14/700, home accent700). **Fix:** Render a text "Edit" trailing action in home-green instead of the pencil icon.
- `med`/`sizing` — DetailGrid, SectionCard and YourRsvpCard use RoundedCornerShape(Radii.lg)=12dp, but spec Card borderRadius is 16 (Radii.xl). **Fix:** Use Radii.xl (16dp) for all detail cards.
- `med`/`color` — Detail/Notes cards lack the card drop shadow and the pending RSVP card omits the spec's green glow (boxShadow 0 0 0 4px H.bg50) that iOS approximates; Android pending card only switches border color/width. **Fix:** Add the shadow-sm card elevation and a 4dp home-bg glow ring on the pending RSVP card.
- `low`/`spacing` — NotesSection text is padded horizontal s3 / vertical s2 inside SectionCard which itself adds vertical s2, doubling vertical padding vs spec NotesCard (Card pad 13 with body directly under overline). **Fix:** Drop the extra vertical padding on the notes Text so it matches the 13px card padding.
- `low`/`typography` — NotesSection Text sets fontSize 12.5 but no lineHeight; spec body is 12.5 with lineHeight 18. **Fix:** Set lineHeight = 18.sp on the notes text.
- `low`/`missing-element` — recurrenceLabel returns generic "Weekly" and never resolves BYDAY to "Every Monday"; spec DetailGrid shows "Every Monday" and iOS implements the BYDAY→day mapping. **Fix:** Parse BYDAY and return "Every <Day>" like the iOS weeklyDay mapping.

### F3 Home add-edit event — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Homes/Calendar/AddEventFormView.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Routing/Stubs/Home/HomeEventEditorStubView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/homes/calendar/AddEventFormScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/form/FormShell.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/form/FormFieldGroup.kt`
- **Verdict:** Both render every design section in order via a shared FormShell, but iOS's bespoke EventSection faithfully carries the Home-green overlines, pill chips, round check and 16px bordered cards, whereas Android reuses the generic FormFieldGroup (grey overlines, no border, 12px radius) and ships square check/category chips plus a restructured schedule layout — making Android the weaker match.
- **Cross note:** Both platforms share FormShell, so both inherit two identical deviations: the Save action is sky primary600 instead of Home-green H.accent700, and the leading control is an X icon instead of the design's 'Cancel' text button. Key divergence: iOS built a bespoke EventSection (green overline, pill chips, round check, 16px bordered card) that tracks the design, while Android reused the generic FormFieldGroup and kept square chips/checks and a label-above-pills schedule layout.

**iOS deviations:**
- `med`/`color` — Shared FormShell renders the 'Save' action in Theme.Color.primary600 (sky), but design SheetBar (home-shell.jsx:378) draws Save in Home-green H.accent700; this is the Home pillar so the action should be green. **Fix:** Pass a pillar-accent color into FormTopBar action (or override foregroundStyle to Theme.Color.homeDark) for the F3 form.
- `low`/`chrome` — FormShell leading control is an X icon (.x), but design SheetBar uses a left-aligned 'Cancel' text button (fg2, 13.5/600); native dismiss idiom but text label is the spec. **Fix:** Optionally render a 'Cancel' text leading control for sheet-presented forms to match SheetBar.
- `low`/`interaction` — ScheduleGroup uses a .compact DatePicker pill (AddEventFormView.swift:369) instead of the design's static sunken value pill 'Mon Jun 16 · 6:30 PM'; functionally fine but visually a system picker rather than the styled sunken pill. **Fix:** Acceptable as a native picker; optionally style to match the sunken value pill geometry.
- `low`/`typography` — Centered title uses .body (16pt) via FormShell, while design SheetBar title is 14.5px/700; minor size/weight drift. **Fix:** Use ~14.5pt bold for the form title to match SheetBar.

**Android deviations:**
- `high`/`color` — Section overlines use FormFieldGroup's PantopusColors.appTextSecondary (grey), but design Section overlines (add-event-frames.jsx:13) are Home-green H.accent700; iOS correctly uses homeDark. **Fix:** Render F3 sections with a green overline (mirror iOS EventSection) instead of the generic grey FormFieldGroup header.
- `high`/`icon` — AttendeesGroup CheckMark is a square RoundedCornerShape(Radii.xs) box (AddEventFormScreen.kt:508), but design Check (home-shell.jsx:402) is a round (borderRadius 50%) 20px circle; iOS RoundCheck is correctly circular. **Fix:** Make CheckMark a CircleShape 20dp with green fill + white tick.
- `med`/`sizing` — CategoryChip uses RoundedCornerShape(Radii.md=8) (AddEventFormScreen.kt:274), but design CatPick is a full pill (borderRadius 9999); iOS uses Capsule. **Fix:** Clip CategoryChip with RoundedCornerShape(Radii.pill) to make it a pill.
- `med`/`layout` — ScheduleGroup DateTimeRow stacks label-above two separate full-width date/time PickerChips (AddEventFormScreen.kt:336), whereas design ValueRow puts the label left and a single combined value pill right inline; restructures the row. **Fix:** Use an inline label-left / value-pill-right ValueRow with a single combined date·time value to match the design.
- `med`/`sizing` — FormFieldGroup card has no border and uses Radii.lg(12) radius/no shadow (FormFieldGroup.kt:50), but design Card is 1px appBorder, 16px radius, subtle shadow; iOS EventSection uses Radii.xl + 1px border. **Fix:** Give the F3 section cards a 1px appBorder and Radii.xl(16) radius.
- `med`/`extra-element` — RequestRsvpGroup is wrapped in FormFieldGroup(title='RSVP') (AddEventFormScreen.kt:574), adding an 'RSVP' overline the design omits — its RSVP Section has no overline (add-event-frames.jsx:88); iOS uses an overline-less EventSection. **Fix:** Render the RSVP toggle in a borderless, overline-less section.
- `med`/`color` — Shared FormShell 'Save' action is primary600 (sky) not Home-green H.accent700; same pillar miss as iOS, plus generic discard-dialog instead of the design's styled Discard/Keep-editing dialog. **Fix:** Color the F3 commit action Home-green to match the SheetBar Save.
- `low`/`chrome` — FormShell leading is an X icon, but design SheetBar uses a left-aligned 'Cancel' text button; native idiom but differs from spec. **Fix:** Optionally render a 'Cancel' text leading control for the sheet form.

### F8 My availability settings — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/HouseholdAvailability/MyHouseholdAvailabilityView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/HouseholdAvailability/MyHouseholdAvailabilityViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/home/HouseholdAvailabilityScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both platforms faithfully implement the F8 exposure-only boundary screen (context header, Personal-sky deep-link, three exposure toggles + quiet-hours disclosure, footnote, not-set-up/saving/opt-out states); iOS is closer because it uses the spec's bespoke 36x20 home-green toggle, the correct moon icon, and exact font sizes, whereas Android substitutes a larger Material Switch, a sunset glyph, and slightly off font sizes plus an extra pillar accent bar.
- **Cross note:** Both platforms correctly cover all four spec frames (default, not-set-up with info banner + CTA, saving inline spinner, opt-out confirm) and gate the footnote + quiet-hours row on personalIsSetUp exactly as the JSX does; the shared divergence is that Android leans on Material primitives (Switch, AlertDialog, sunset icon) while iOS hand-rolls the spec's exact toggle and uses the moon icon.

**iOS deviations:**
- `low`/`sizing` — Row icon tiles (DeepLinkRow/HouseholdToggleRow/DisclosureRow) use Radii.md (8) while the spec sets borderRadius:9 on the 32x32 tiles (and 12 on the 40x40 header tile, where iOS uses Radii.lg=12 correctly). **Fix:** Use an explicit 9pt corner radius for the 32x32 row icon tiles to match spec borderRadius:9.
- `low`/`interaction` — Opt-out confirmation uses a native .confirmationDialog (action sheet) rather than the spec's centered modal card with an eye-off warning circle, 16pt headline, 12.5pt body, and stacked Hide/Keep-sharing buttons; copy matches but the visual treatment differs. **Fix:** Acceptable native idiom; optionally present a custom centered alert card to mirror the spec's warning-circle modal if visual parity is required.

**Android deviations:**
- `med`/`icon` — Quiet-hours DisclosureRow uses PantopusIcon.Sunset (Material WbTwilight) but the spec specifies icon="moon" for Household quiet hours. **Fix:** Switch the DisclosureRow icon to PantopusIcon.Moon to match the spec glyph and iOS.
- `med`/`extra-element` — OwnerPillarHeader renders a Home-green pillar accent bar (ACCENT_BAR_HEIGHT) under the title; the spec's TopBar is a plain neutral bar titled 'My availability' with no accent stripe. **Fix:** Hide the accent bar for this screen (or pass a variant without ACCENT_BAR_HEIGHT) so the chrome is a plain TopBar.
- `med`/`sizing` — ToggleRow uses Material3 Switch (~52x32dp) instead of the spec's bespoke 36x20 ToggleGreen with a 16dp thumb; the control is visibly larger than design (iOS matches with a custom GreenSwitch). **Fix:** Replace Material Switch with a custom 36x20 home-green toggle matching the spec primitive.
- `low`/`typography` — ToggleRow title is 13.sp (spec 12.5), subtitle 11.sp (spec 10.5), and DisclosureRow value 12.sp (spec 11.5) — small but consistent oversizing vs spec. **Fix:** Set toggle/disclosure title to 12.5.sp, subtitle to 10.5.sp, and disclosure value to 11.5.sp.
- `low`/`color` — Material Switch checkedThumbColor is set to appTextInverse (white) which is correct, but the unchecked/disabled track relies on SwitchDefaults rather than the spec's borderStrong (off) / sunken (disabled) track colors. **Fix:** Pass uncheckedTrackColor=appBorderStrong and disabledTrackColor=appSurfaceSunken (moot if the switch is replaced by the bespoke 36x20 toggle).

## Find-a-time

### F4 Find a time setup — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeSetupView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeComponents.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeSetupScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeModels.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeSetupViewModel.kt`
- **Verdict:** iOS faithfully renders all six design frames (default, round-robin, invalid, computing, no-overlap, explainer-expanded) with only small color/radius nits; Android omits the computing spinner, the no-overlap warning + quick-action buttons, and the round-robin rule section entirely, so it covers only 3 of 6 states.
- **Cross note:** Both platforms place sections in the correct spec order and use token colors; the divergence is state coverage — iOS implements all six frames, Android only the static form (default + invalid inline errors) and skips computing, no-overlap, and round-robin-rule. Both use a native top-bar (Android adds a back chevron, iOS uses FormShell .close) which is acceptable as a native idiom versus the spec's pure sheet bar. Android's DateWindow also uses a preset dropdown rather than a real from/to range picker, so the invalid 'end before start' state is effectively unreachable.

**iOS deviations:**
- `low`/`color` — Explainer body text uses Theme.Color.appTextStrong (#374151) in FindATimeSetupView.explainer; spec sets the primary line to #075985 (primary-800) at fontWeight 500. **Fix:** Color the explainer headline line with Theme.Color.info/primary-800 to match the spec blue.
- `low`/`color` — Category pill text uses Theme.Color.homeDark (green) in titleCard; spec renders the 'Family' label in H.accent700 over H.bg100 with a purple #7c3aed dot, so the screen mixes home-green text with the family accent. **Fix:** Verify the label color matches the family-pillar accent the spec intends (accent700) rather than home green.
- `low`/`sizing` — FindATimeCard uses Radii.xl (16) corner radius; the spec Card/Section radius is 12 (Radii.lg). **Fix:** Use Radii.lg (12) for the section cards to match the spec.
- `low`/`spacing` — requirementSegment vertical padding is hardcoded to 5pt and horizontal Spacing.s2 (8); spec ReqOpt buttons use padding 4px 9px. **Fix:** Match button padding to ~4x9 to align pill height with the spec.

**Android deviations:**
- `high`/`state` — FindATimeSetupUiState only has Loading/Loaded/Error; the spec 'computing' frame (64dp spinner with users glyph, 'Checking everyone's availability' + three shimmer rows) is entirely absent — submit() just navigates without a composing state. **Fix:** Add a Computing UI state rendering the spinner + headline + shimmer rows that iOS' computingBody mirrors.
- `high`/`missing-element` — The spec no-overlap frame's warning banner ('No time works for all 3') and its two quick-action buttons ('Make Dad optional' user-minus, 'Widen to two weeks' calendar-plus) never render; the VM exposes makeSomeoneOptional()/widenWindow() but SetupFormBody never calls them. **Fix:** Render an FtBanner(Warning) plus the user-minus and calendar-plus action buttons when a no-overlap result is present.
- `high`/`missing-element` — The 'Round-robin rule' section (Segmented 'Fair rotation' / 'By role') from the round-robin frame is missing; SetupFormBody never shows a rule picker even though FindMode.RoundRobin is selectable. **Fix:** Add a Round-robin rule FtCard with an FtSegmented shown when form.mode == FindMode.RoundRobin, matching iOS' ruleSection.
- `med`/`icon` — Explainer's second bullet uses PantopusIcon.Users; the spec uses the 'layers' glyph ('Pantopus overlays everyone…') — iOS correctly uses .layers. **Fix:** Change the second explainer bullet icon to PantopusIcon.Layers.
- `med`/`icon` — Stepper minus button uses PantopusIcon.X (close glyph) in StepperButton; spec/iOS use a minus glyph. **Fix:** Use PantopusIcon.Minus for the decrease button.
- `med`/`layout` — Stepper renders two full-width circular buttons spread across the row with a weighted center label; spec/iOS render a compact inline minus / value / plus control bordered as one unit. **Fix:** Rebuild Stepper as a single bordered inline -/value/+ control matching the spec geometry.
- `low`/`icon` — DateWindow uses PantopusIcon.Calendar; the spec uses 'calendar-range' (iOS uses .calendarRange). **Fix:** Switch the date-window leading icon to a calendar-range glyph.

### F5 Find a time slots — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeSuggestedView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeComponents.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeModels.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeSuggestedViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/SuggestedSlotsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeFormat.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/SuggestedSlotsViewModel.kt`
- **Verdict:** Both platforms faithfully implement all five spec frames (composing/results/no-overlap/single-best/sent) plus a booked state; iOS matches the spec's exact font sizes and geometry while Android drifts on a few sizes (h3 headlines, 64dp empty-state circle), uses a wrong sub-head icon, and trims the loading-state geometry.
- **Cross note:** Both correctly tint everything Home green from tokens, render member free/busy dot stacks, the BEST/BEST MATCH pills, and expand-to-book; both add a booked success state beyond the 5 spec frames. Shared divergence: neither reproduces the spec's radial-gradient ring behind the sent-success check (both use a solid disc), which is an acceptable native simplification.

**iOS deviations:**
- `low`/`chrome` — Spec composing frame keeps the trailing 'Edit' action present-but-muted; FindATimeSuggestedView's canEdit hides Edit entirely during .loading/.sent/.booked instead of showing it muted. **Fix:** Show a disabled/muted Edit on the composing phase to match the spec's right={{text:'Edit', muted:true}}.
- `low`/`color` — singleBestCard uses Radii.xl2 (~20pt) while spec single-best card is borderRadius:18 with a 0 4px 16px shadow; iOS draws only a stroke border, no soft shadow. **Fix:** Drop card radius to ~18pt and add the spec's soft accent shadow under the single-best card.

**Android deviations:**
- `med`/`icon` — SubHead second line uses PantopusIcon.Users for 'From everyone's personal availability.'; spec (and iOS) use the lucide 'layers' glyph tinted Home accent. **Fix:** Swap PantopusIcon.Users to PantopusIcon.Layers in SubHead.
- `med`/`typography` — NoOverlapBody headline and SuccessBody title use PantopusTextStyle.h3 (20sp); spec headlines are 15.5px (no-overlap) and 18px (sent) — both oversized. **Fix:** Set the no-overlap headline to ~15.5sp and the success title to ~18sp inline instead of h3.
- `med`/`missing-element` — SubHeadPlaceholder (loading) renders only one 180x12 shimmer bar, dropping the tz-pill and the second availability line; spec/iOS keep the full SubHead chrome during composing. **Fix:** Render the full SubHead skeleton (note bar + tz pill + second line) so loading mirrors loaded geometry.
- `med`/`sizing` — NoOverlapBody circle is 64dp with a 28dp calendar-x icon; spec is a 56px circle with a 26px icon. **Fix:** Change the no-overlap circle to 56dp and icon to 26dp.
- `low`/`missing-element` — ComposingBody shimmer cards stack two text bars (160x12 + 90x10); spec/iOS show a 12-high title bar then a 56x20 pill + 50x9 bar mirroring the avatar-stack row. **Fix:** Replace the second bar with a 56x20 rounded pill + 50x9 bar to mirror the slot's member row.
- `low`/`typography` — SingleBestCard uses h3 (20sp) for the day and body (16sp) for the time; spec is 19px day and 15px time, and card radius Radii.xl (16) vs spec 18. **Fix:** Set day ~19sp / time ~15sp and bump the single-best card radius toward 18dp.
- `low`/`state` — NoOverlap headline is the static 'No time works for everyone' and body says 'this window'; spec uses the member-count 'No time works for all 3' and 'this week'. **Fix:** Interpolate the required-member count into the headline and use 'this week' to match the window phrase.
- `low`/`icon` — Booked SuccessBody reuses PantopusIcon.Check; iOS booked state uses calendar-check, and the success body line differs ('… is set' vs iOS '<title> · <label>'). **Fix:** Use a calendar-check glyph for the booked state to distinguish it from the proposal-sent check.

### F6 Member poll response — iOS `major` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/PollResponseView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeComponents.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/MemberPollResponseScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/MemberPollResponseViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeModels.kt`
- **Verdict:** Both faithfully render the organizer header, overline, 3-way vote control and closed/expired frame, but neither implements the design's conflicts-detected frame (per-slot conflict pill + "From your personal calendar" line + info pre-fill banner) and both swap the person-photo avatar for a generic icon disc; iOS is marginally closer because it doesn't inject the off-spec name/email capture form Android adds.
- **Cross note:** Shared gap: neither platform implements the conflicts-detected frame (conflict pill, personal-calendar caption, info pre-fill banner) and both replace the organizer person-photo avatar with a generic icon disc. Divergence: iOS adds a dedicated full-screen submitted state; Android instead adds an off-spec name/email capture form and an inline submitted banner, and ties submit-enablement to email presence rather than to having voted.

**iOS deviations:**
- `high`/`missing-element` — No conflicts-detected frame: PollResponseView has only loading/error/ready/closed/submitted, omitting the design's red 'Conflicts: Dentist' pill (errorBg/#b91c1c) on a slot, the 'From your personal calendar' line (10pt fg3 + calendar icon), and the info pre-fill banner. **Fix:** Add a conflict flag to PollOptionRow and render the conflict pill + calendar caption inside pollSlot(), plus an info-tone banner above the slots when any slot conflicts.
- `med`/`icon` — organizerHeader draws Icon(.users) inside a homeBg circle, but the design uses Avatar m={M.mom} — a person photo for the organizer, not a generic users glyph. **Fix:** Render the organizer's MemberAvatarBadge / photo at 38pt instead of the users icon disc.
- `med`/`spacing` — pollSlot is a FindATimeCard padded Spacing.s4 (16pt) all around; the design PollSlot uses pad 11px/13px and a 9px gap between label and control. **Fix:** Use ~12pt vertical / 13pt horizontal padding for the slot card to match the tighter design rhythm.
- `low`/`state` — Adds a full-screen .submitted success state (84pt circle + 'Response submitted' + Done) that has no corresponding design frame (design just keeps the answered sheet). **Fix:** Acceptable as a confirmation, but verify against intended UX; design shows no separate success screen.
- `low`/`sizing` — voteControl track uses Radii.lg (12) and segments Radii.sm (6); design uses borderRadius 9 for the track and 7 for segments. **Fix:** Use a 9pt track radius and 7pt segment radius (or nearest token) for exact parity.
- `low`/`chrome` — Uses navigationTitle('Respond') on a pushed NavigationStack screen; the design is a Sheet with an explicit 'Close' cancel control in the SheetBar. **Fix:** Present as a sheet with a leading 'Close' button, or confirm push navigation is the intended container.

**Android deviations:**
- `high`/`missing-element` — No conflicts-detected frame: MemberPollResponseScreen lacks the per-slot red 'Conflicts: Dentist' pill, the 'From your personal calendar' line, and the info pre-fill banner that the design's FrameConflicts shows. **Fix:** Add conflict data to PollOptionUi and render the conflict pill + calendar caption in PollOptionCard, plus an Info-tone FtBanner above the slots when conflicts exist.
- `high`/`extra-element` — LoadedPoll injects an off-spec 'Your details' card with name + email FtInputFields (state.needsEmail) and gates submit on a non-blank email (canSubmit requires voterEmail); the design has no email/name capture and disables submit only until a vote is cast. **Fix:** Remove the email/name capture for the member flow (member is signed-in) and gate Submit on at least one vote, matching the design's disabled-until-answered footer.
- `med`/`icon` — OrganizerHeader puts a Vote glyph in a homeBg circle; the design uses the organizer's person photo (Avatar mom) at 38dp. **Fix:** Render the organizer avatar (FtAvatar/photo) at 38dp instead of the Vote icon disc.
- `med`/`extra-element` — After submit, an in-place FtBanner 'Response submitted' appears inside the Loaded list and the CTA flips to 'Update response'; the design's answered frame shows neither an inline success banner nor an Update label. **Fix:** Drop the inline success banner / 'Update response' relabel, or align with the intended confirmation pattern.
- `low`/`sizing` — VoteControl track uses Radii.md (8) and segments Radii.sm (6); design uses 9 (track) and 7 (segments). **Fix:** Adjust to a 9dp track radius and 7dp segment radius for exact parity.
- `low`/`chrome` — FtTopBar uses backIcon = X with a centered 'Respond' title and a 1dp divider; matches the sheet 'Close' intent well, but the design SheetBar 'Close' is a text label rather than an X glyph. **Fix:** Optionally use a 'Close' text control to mirror the SheetBar exactly; the X is an acceptable native idiom.

### F7 Whos free — iOS `matches` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/WhosFreeView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/FindATime/FindATimeComponents.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/WhosFreeScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/WhosFreeViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/findatime/FindATimeComponents.kt`
- **Verdict:** iOS faithfully reproduces all five frames and all five cell states with the hatch texture and correct icons; Android drops the Tentative and Off-hours cell states entirely, omits the offline-cached frame, and uses wrong icons, so iOS is materially closer to the design.
- **Cross note:** Both platforms render the tap-to-plan affordance as an inline action card rather than a floating popover; iOS additionally uses a true .popover. Android's biggest gap is reducing the five-tier heat grid to three tiers (no Tentative/Off-hours) and omitting the offline frame, neither of which iOS drops.

**iOS deviations:**
- `low`/`sizing` — popoverCard uses .frame(width: 200) but the design popover is width:134. **Fix:** Reduce the popover frame width toward ~134 to match spec proportions.
- `low`/`missing-element` — offlineBanner body is "Reconnect to refresh availability." — the design's "Last updated 2h ago." lead sentence is dropped. **Fix:** Prepend the last-synced timing sentence to the offline banner body.
- `low`/`color` — Off-hours and unknown cells both fill with appSurfaceMuted, whereas design uses distinct fills (off=#f9fafb vs unknown=#f1f3f5) differentiated mainly by hatch pattern; visually close since both are hatched. **Fix:** Optionally map off-hours to appSurfaceMuted and unknown to a slightly cooler surface to mirror the two base tones.

**Android deviations:**
- `high`/`missing-element` — CellState enum is only Free/Busy/Unknown (WhosFreeViewModel.kt:27); the design's Tentative (#fef3c7) and Off-hours (#f9fafb hatched) cell states are entirely absent, so the heat grid loses two of five availability tiers. **Fix:** Add Tentative and OffHours to CellState, fill amber-bg and muted-surface respectively, and project them from slot data.
- `high`/`missing-element` — No offline-cached frame: WhosFreeScreen has no wifiOff amber banner and the card is never dimmed to 0.6, unlike design Frame 5. **Fix:** Add an offline state that renders the FtBanner(Warning, WifiOff) and wraps the grid card at 0.6 alpha.
- `med`/`missing-element` — Legend() lists only Free/Busy/Unknown (WhosFreeScreen.kt:362); design legend is Free/Busy/Tentative/Off-hours (and swaps Off-hours→Unknown only in the opted-out frame). **Fix:** Render four legend items matching the active cell tiers, swapping Off-hours→Unknown when a member is opted out.
- `med`/`icon` — Head explainer line uses PantopusIcon.Users (WhosFreeScreen.kt:146); design ExplainerLine uses the layers glyph. **Fix:** Swap to PantopusIcon.Layers tinted home green.
- `med`/`icon` — "Tap a free block to plan something" hint uses PantopusIcon.Plus (WhosFreeScreen.kt:209); design uses hand-pointer. **Fix:** Use PantopusIcon.HandPointer (or nearest pointer glyph) to match intent.
- `med`/`color` — Off-hours/unknown cells lack the 45° diagonal hatch texture the design draws (Cell off/unknown backgroundImage); Unknown just fills appSurfaceRaised with a flat "?". **Fix:** Add a diagonal-hatch overlay (mirroring iOS DiagonalHatch) clipped to the cell for off-hours and unknown states.
- `low`/`typography` — Explainer/hint text uses PantopusTextStyle.caption (12sp) where design specifies fontSize 10.5; column headers/labels also map to overline/caption rather than the 9/10.5 spec sizes. **Fix:** Use explicit ~10.5sp for explainer/hint and 9sp bold for column headers to match the dense grid spec.
- `low`/`sizing` — NAME_COL_WIDTH is 64.dp; design name column is 58px. **Fix:** Set NAME_COL_WIDTH = 58.dp.

## Resources

### F10 Resource editor — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceEditorView.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Form/FormShell.swift`, `frontend/apps/ios/Pantopus/Features/Shared/Form/FormFieldGroup.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceEditorScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceComponents.kt`
- **Verdict:** iOS faithfully renders all five sections plus the collapsible booking-rules disclosure, photo row, and the dedicated saving overlay; Android drops the Photo section and the booking-rules collapse/helper, swaps the segmented Who-can-book for chips, and degrades both the saving and delete-confirm frames, making iOS clearly closer to the spec.
- **Cross note:** Both platforms correctly use the home-green pillar, SelectChip type pills, WeekdayPicker, and CounterRow stepper; the key divergence is that iOS implements all five spec frames (create/edit/invalid/saving/delete) faithfully while Android omits the Photo section and the collapsible booking-rules disclosure and falls back to generic Material dialogs/spinner for the saving and delete states.

**iOS deviations:**
- `med`/`extra-element` — detailsGroup wraps the name+type card in FormFieldGroup("Details"), adding a grey 'DETAILS' overline above the first card; the design's first Section has no overline at all. **Fix:** Render the name+type card as a bare Card (no overline label) to match FrameCreate's first Section.
- `med`/`color` — FormFieldGroup renders every section overline in Theme.Color.appTextSecondary (grey, placed above the card), but the spec's Overline uses H.accent700 home-green placed inside the card. **Fix:** Tint section overlines home-green (Theme.Color.homeDark) and keep them inside the card surface as the rules section already does.
- `low`/`interaction` — whoCanBookGroup uses a native SwiftUI .segmented Picker; the spec uses a custom small/full Segmented control with home accent — acceptable native idiom but loses the green selected tint. **Fix:** Optionally use a home-tinted segmented control to match the accent; low priority.
- `low`/`icon` — hoursGroup uses native DatePicker wheels for start/end, whereas the design renders a static sunken pill '7 AM – 10 PM' value row; functional parity but visually heavier than spec. **Fix:** Render a tappable value pill matching N.sunken background per HoursWindow ValueRow, opening a picker on tap.
- `low`/`chrome` — FormShell top bar uses pantopusTextStyle(.body) 16pt title and primary600 (sky) for the enabled Save action; spec SheetBar title is fine but the pillar accent for Save would read home-green. **Fix:** Pass a home accent to the FormShell Save action so the enabled label is home-green, matching the Home-pillar sheet.

**Android deviations:**
- `high`/`missing-element` — EditorBody has no Photo section; the spec's FrameCreate includes a Section overline="Photo" with the dashed 'Add a photo · Optional' PhotoRow (image-plus icon, 34dp tile). **Fix:** Add a SectionCard(overline="Photo") containing a dashed PhotoAddRow mirroring the iOS PhotoAddRow.
- `high`/`missing-element` — Booking rules is a plain always-open SectionCard with no chevron, no collapse, and no smart-defaults helper line; the spec's RulesDisclosure is a collapsible header (chevron-down/up) that shows a defaultHelper (e.g. 'Charger defaults: 4 hr max · No approval') when collapsed. **Fix:** Wrap the rules rows in a collapsible disclosure with a chevron and the ruleHelper subtitle, defaulting collapsed on create like iOS rulesGroup.
- `med`/`interaction` — Who-can-book is rendered as three weight(1f) SelectChip pills, but the spec uses a Segmented control with options All / Specific / Guest link. **Fix:** Replace the chip row with a segmented control matching the design's Segmented small/full component.
- `med`/`state` — Saving state only swaps the top-bar action for a CircularProgressIndicator; the spec FrameSaving dims the form (opacity 0.45) and floats a centered white card with a spinning green loader-circle + 'Saving resource' label. **Fix:** Add a dimmed overlay with a centered rounded white card (radius 16, shadow) showing a home-green spinner and 'Saving resource', mirroring iOS savingOverlay.
- `med`/`state` — Delete confirm uses a default Material AlertDialog; the spec FrameDelete is a custom Dialog with a trash-2 error icon, full-width 44dp red Delete + bordered Keep buttons stacked vertically. **Fix:** Use a custom dialog with the trash-2 error glyph and two stacked full-width 44dp buttons (red Delete, bordered Keep).
- `med`/`missing-element` — The name+type card has no Photo and the invalid frame's name-field error helper text matches, but there is no Type label color/divider parity — the spec separates Name and Type within the first card with marginTop:11 and a 'Type' label; Android stacks them via SectionCard's 12dp gap with no divider (iOS adds a Divider). Minor structural drift inside the first card. **Fix:** Match spec spacing: 11dp gap between name field and the 'Type' label; the design has no divider so this is acceptable but keep the 11dp rhythm.
- `low`/`typography` — SectionCard overline is 10sp; the spec Overline (--type-over) is 11px. Slightly undersized. **Fix:** Bump SectionCard overline fontSize to 11sp to match the overline token.
- `low`/`sizing` — hoursGroup renders two stacked PickerValueRow rows ('Available from'/'Available until') with chevrons; the spec HoursWindow shows a single 'Available hours' value row reading '7 AM – 10 PM'. **Fix:** Collapse to a single 'Available hours' row showing the start–end range, opening a combined picker.

### F11 Resource detail — iOS `matches` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceDetailView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceDetailViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceComponents.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceDetailScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceDetailViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceComponents.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/SchedulingStateScaffolding.kt`
- **Verdict:** iOS faithfully renders all five frames (loaded, loading, fully-booked banner + next-opening CTA, pending-approval badge + inline queue, error), while Android omits the fully-booked and pending-approval states entirely and hardcodes the bookings label, so it covers only loaded/loading/error/approval-queue.
- **Cross note:** Both platforms share two minor token nits: primary CTA uses radius 8 instead of spec 12, and home-pillar chip/badge text uses the base accent (#16a34a) rather than the darker accent700/warning700 the spec specifies. iOS implements every frame; Android's ViewModel Loaded state simply lacks the fully-booked and pending-badge data, so those frames cannot render.

**iOS deviations:**
- `low`/`sizing` — HomePrimaryButton uses Radii.md (8) for the sticky CTA corner radius; spec PrimaryBtn is borderRadius:12. **Fix:** Use Radii.lg (12) on HomePrimaryButton background/clip.
- `low`/`color` — RuleChip text uses Theme.Color.home (#16a34a); spec rule-summary chips use H.accent700 (#15803d). **Fix:** Use the home-700 token for rule chip foreground text.
- `low`/`color` — pendingApprovalBadge label and fullyBookedBanner title use Theme.Color.warning (#D97706); spec uses warning700 for the text (warning only for icons). **Fix:** Apply a warning-700 token to the badge/banner text while keeping warning for the icon.
- `low`/`icon` — approvalQueueCard divider via Divider().background(appBorder) only tints overlay, not the rule line; spec ApprovalRequest uses a 1px N.border bottom rule. **Fix:** Use a explicit overlayColor/Rectangle(height:1, fill: appBorder) divider so the line color matches spec.

**Android deviations:**
- `high`/`missing-element` — No fully-booked frame: Loaded state lacks isFullyBooked/fullyBookedThroughLabel/nextOpeningLabel, so the amber calendar-x 'Fully booked through…' Banner is never rendered (spec Frame 3). **Fix:** Add those fields to Loaded and render an amber Banner(icon=calendar-x) above the bookings list when fully booked.
- `high`/`missing-element` — Sticky CTA is always 'Book this' (plus icon); spec Frame 3 swaps it to 'Book next opening · Sat 9 AM' with a calendar-clock icon when fully booked. **Fix:** Branch StickyFooter on isFullyBooked to show the next-opening CTA + calendar-clock icon.
- `high`/`missing-element` — No pending-approval badge in HeaderCard; spec Frame 4 shows an amber clock 'Pending approval (N)' button with chevron-right inside the header card. **Fix:** Add pendingApprovalCount to Loaded and render the warning-bordered badge button in HeaderCard, mirroring iOS pendingApprovalBadge.
- `med`/`state` — Bookings overline is hardcoded 'Upcoming bookings'; spec Frame 4 relabels it 'Confirmed' when the approval queue is shown above. **Fix:** Compute label = if (approvals.isEmpty) "Upcoming bookings" else "Confirmed", as iOS does.
- `med`/`sizing` — BookingRow status dot is Modifier.size(6.dp); spec BookingRow dot is width:5,height:5. **Fix:** Set the dot to 5.dp.
- `low`/`sizing` — HomePrimaryButton/InlineHomeButton corner radius is Radii.md/Radii.sm; spec PrimaryBtn is 12 and Approve/Decline buttons are borderRadius:9. **Fix:** Use Radii.lg (12) for the primary CTA and ~9 for the inline approve/decline buttons.
- `low`/`color` — RuleChipView and approval overline use PantopusColors.home (#16a34a) and PantopusColors.warning; spec uses accent700 for chip text and warning700 for the 'Approval queue · N' overline. **Fix:** Use home-700 for rule chip text and warning-700 for the approval-queue overline.
- `low`/`layout` — Loading state uses generic SchedulingLoadingSkeleton(rows=4) row list; spec Frame 2 is a header-card shimmer (icon tile + name + 3 chips) over 3 booking-row skeletons. **Fix:** Replace with a skeleton mirroring the loaded header-card + booking-row geometry.

### F12 Book a resource — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/BookResourceView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceComponents.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/BookResourceScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/BookResourceViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceComponents.kt`
- **Verdict:** iOS faithfully reproduces all six design frames (rules chips, hour grid, conflict line, for-whom, notes, dimmed submitting overlay, and both success variants with note pill); Android nails the form/grid/conflict/success core but drops three design elements — the Notes section, the dimmed "Booking the charger" submitting overlay, and the success note pill.
- **Cross note:** Both platforms render this design sheet as a full-screen back-chevron + centered-title top bar with a sticky footer Submit instead of the design's SheetBar-with-trailing-action — a shared, consistent native idiom, not a divergence. Real divergence is content completeness: iOS implements all six frames; Android is missing the Notes section, the dimmed submitting overlay, and the success note pill.

**iOS deviations:**
- `low`/`chrome` — Design Frame 1 SheetBar shows title + a trailing 'Submit' action (with actionDisabled/actionSaving states); BookResourceView uses a navigationTitle + bottom sticky footer button only, no top-bar action. **Fix:** Acceptable native idiom (sticky-footer submit); optionally surface a disabled/saving trailing toolbar item to mirror SheetBar states.
- `low`/`color` — Success halo in successBody fades homeBg→homeBg (outer == inner) because the design's lighter #f0fdf4 inner-to-bg100 gradient lacks a token, so the 84pt radial gradient reads flatter than the spec's circle-at-30%-30% bg50→bg100. **Fix:** Add a homeBg-lighter token for the inner halo stop so the radial gradient shows depth like the design.
- `low`/`sizing` — Submitting overlay card uses Radii.xl (16) which matches spec borderRadius:16, but the dim wrapper applies 0.45 opacity to the whole VStack rather than only the scrollable body region as in the design (which keeps SheetBar crisp). **Fix:** Apply the 0.45 opacity/hit-test block to the ScrollView content only, leaving the nav bar fully opaque.

**Android deviations:**
- `high`/`missing-element` — Design Frame 1 has a third Section overline='Notes' with a multiline optional note Field; BookForm renders only When + For whom sections — no Notes section or note TextField exists. **Fix:** Add a SectionCard(overline="Notes") with an OutlinedTextField bound to a note field, mirroring iOS notesSection.
- `high`/`state` — Design Frame 4 (submitting) dims the sheet to opacity 0.45 and overlays a centered rounded card with a spinning loader-circle + 'Booking the charger'; BookForm only passes isLoading to the footer button — no dimmed overlay or spinner card. **Fix:** Wrap BookForm in a Box; when form.isSubmitting, dim content to 0.45/disable hits and overlay a centered Surface(radius 16) with a rotating PantopusIcon.LoaderCircle + 'Booking the charger' text.
- `med`/`missing-element` — Design Frames 5/6 show a success note pill (calendar-check icon + 'Added to the home calendar' / 'EV charger · Sat 9–11 AM') in home-green; BookSuccess has no pill and the VM folds that text into the body string instead. **Fix:** Add a note field to Success state and render a pill (PantopusIcon.CalendarCheck + text, homeBg bg, homeDark text, Radii.pill) between body and the Back-to-calendar button, like iOS successNote.
- `low`/`sizing` — Success disc icon in BookSuccess is size = 26.dp; design SuccessSheet icon is width/height 28 (iOS uses 28). **Fix:** Change the PantopusIconImage size to 28.dp.
- `low`/`chrome` — Design SheetBar carries a trailing 'Submit' action with disabled/saving variants; CenterAlignedTopAppBar exposes only a back chevron + title, submit lives in the sticky footer. **Fix:** Acceptable native idiom; optionally mirror the SheetBar trailing action as a TopAppBar action for state parity.

### F9 Resources list — iOS `major` / Android `major` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceListView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceListViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ResourceKit.swift`, `frontend/apps/ios/Pantopus/Features/Shared/ListOfRows/ListOfRowsView.swift`, `frontend/apps/ios/Pantopus/Features/Shared/ListOfRows/RowModel.swift`, `frontend/apps/ios/Pantopus/Core/Design/Components/StatusChip.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceListScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceListViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/list_of_rows/ListOfRowsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/shared/list_of_rows/ListOfRowsUiState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/StatusChip.kt`
- **Verdict:** Both platforms route F9 through the shared ListOfRows shell and faithfully reproduce the chrome (centered "Resources" title, back chevron, "Add" trailing) but share the same row-typography/trailing-treatment misses and both drop the design's signature empty-state template quick-start; Android edges ahead by rendering rows as individual cards (matching the design) where iOS groups them into one divider-separated card, though Android alone omits the offline frame entirely.
- **Cross note:** Both screens are driven by the same shared ListOfRows shell, so they share three high-impact misses identically: the empty-state templates quick-start is replaced by a generic EmptyState+CTA, row titles are oversized (16pt/sp semibold vs design 13.5px/700), and the trailing free/booked indicator is a filled pill instead of the design's dot+colored-text. Key divergence: iOS groups rows into a single divider card (style .card) while Android uses flat individual cards (matches design); conversely Android omits the offline banner that iOS provides.

**iOS deviations:**
- `high`/`missing-element` — Empty frame uses the shared EmptyState (icon+headline+subcopy+'Add a resource' CTA button); the design's empty frame is an explainer Card + 'TEMPLATES' overline + 5 tappable template rows (Guest room/Driveway/EV charger/Tools/Other) with NO primary button. VM comment acknowledges the shell can't render it. **Fix:** Add a bespoke templates empty-state slot to ListOfRowsView (or a feature-local empty view) rendering the explainer card + TEMPLATES overline + 5 template rows; drop the CTA button.
- `med`/`layout` — rebuild() emits RowSection with style: .card, so the 5 rows render inside ONE rounded card with hairline dividers; the design renders each resource as a separate Card (gap:9 between them, individual borders/shadows). **Fix:** Pass style: .flat in the RowSection so each row is a standalone card, matching the design and Android.
- `med`/`typography` — Row title renders via RowView titleLine at body 16pt semibold; design name is fontSize 13.5 weight 700. **Fix:** Shell-level: the statusChip template title should render ~13.5pt bold for these rows (or add a compact title variant).
- `med`/`color` — Trailing status renders as a filled StatusChip pill (successBg/homeSunken background); design trailing is a bare 7px colored dot + 11pt colored text ('Free now' in success, 'Booked until…' in fg3) with no pill background. **Fix:** Add a dot+label trailing variant (RowTrailing) that renders a 7pt dot + plain colored text instead of reusing the pill StatusChip.
- `low`/`sizing` — Leading tile uses Radii.md (8pt) corner; design tile radius is 11px. Type badge text is caption (~12pt); design badge is 10px. **Fix:** Use an 11pt corner for the type tile and a 10pt label for the type badge to match the spec.
- `low`/`state` — Offline frame: .offlineBanner shows the strip, but the design also dims each cached row to 0.55 opacity ('Showing cached resources'); rows are not dimmed when offline. **Fix:** Thread an isOffline flag into row building to apply 0.55 opacity to rows while offline.

**Android deviations:**
- `high`/`missing-element` — Empty frame maps to the shared EmptyState (Package icon + headline + subcopy + 'Add a resource' CTA); the design's empty frame is an explainer Card + 'TEMPLATES' overline + 5 tappable template rows with no primary button. **Fix:** Add a templates empty-state slot to ListOfRowsScreen (or render a feature-local empty composable) with the explainer card + TEMPLATES overline + 5 template rows; remove the CTA button.
- `high`/`state` — ResourceListScreen has NO OfflineBanner in its chrome stack, so the design's Frame 5 (amber 'You're offline' banner + dimmed cached rows) is entirely absent; iOS at least shows the offline strip. **Fix:** Wrap the screen body with OfflineBanner (ui/components/OfflineBanner.kt) and dim rows to 0.55 alpha when offline, mirroring iOS.
- `med`/`typography` — Row title renders via ContentColumn at PantopusTextStyle.body (16sp) SemiBold; design name is 13.5px weight 700. **Fix:** Render the statusChip-template title at ~13.5sp Bold for resource rows (compact title variant in the shell).
- `med`/`color` — Trailing status uses the filled StatusChip pill (Success/Neutral background); design trailing is a 7px colored dot + 11px colored text with no pill. **Fix:** Add a dot+label RowTrailing variant rendering a 7dp dot + plain colored text instead of the pill StatusChip.
- `low`/`sizing` — TypeIcon leading tile uses RoundedCornerShape(Radii.md)=8dp; design tile radius is 11px. The type RowChip label is caption (~12sp) vs design 10px. **Fix:** Use an 11dp corner on the type tile and a 10sp label for the type chip.
- `low`/`spacing` — Loaded list uses Flat rows spaced by Spacing.s2 (8dp); design gap between resource cards is 9px (close, acceptable) but row internal padding is Spacing.s3 (12dp all sides) vs design pad 11px×12px. **Fix:** Minor — tighten vertical row padding to ~11dp to match the design's 11×12 card padding.

## Visits

### F13 Schedule a visit setup — iOS `major` / Android `major` → better: **android**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ScheduleVisitView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/ScheduleVisitViewModel.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Shared/Form/FormFieldGroup.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/visits/VisitSetupScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/visits/VisitSetupViewModel.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/resources/ResourceComponents.kt`
- **Verdict:** Both platforms intentionally reduced the design's offer-slots/composed-availability engine to a plain concrete-visit form (date+start-time+length) and dropped the weekday grid, time-range presets, shareable-link toggle, and 3 of 5 frame states; Android is marginally closer because it uses the Home-green section overline and the spec's explainer copy, while iOS uses a gray overline and off-spec explainer text.
- **Cross note:** Shared root cause: both ViewModels explicitly declare the offer-slots / shareable-link engine "intentionally out of scope / no v1 backend," so both shipped the same reduced concrete-visit form and both omit the weekday grid, time-range presets, link toggle, and the no-host/generating/link-created states. Android edges ahead only on the Home-green section overline (iOS overline is gray) and on using the spec's exact explainer copy.

**iOS deviations:**
- `high`/`missing-element` — "Offer these times" WeekdayGrid (7 day cells, height 30, radius 8, Home-green selected fill) + time-range preset chips ("Mornings only" / "9 AM – 12 PM") from spec FrameDefault are entirely absent; whenGroup only has Date/Start time/Visit length pickers. **Fix:** Add a WeekdayGrid composable and preset chip row under a "Offer these times" FormFieldGroup.
- `high`/`missing-element` — "Generate a shareable booking link" Toggle section (spec FrameDefault last Section with ValueRow + Toggle) is missing — no link generation UI at all. **Fix:** Add a final FormFieldGroup with a ValueRow + Toggle bound to a generateLink flag.
- `high`/`state` — Three of five spec frames are unimplemented: no-host-available (warning Banner + "Add another host"/"Widen the window" buttons), generating (door-open spinner + "Building the slots you'll offer" + shimmers), and link-created success (check medallion + copy-link row + Share button). VM comment says the engine is "intentionally out of scope." **Fix:** Implement noHostAvailable, generating, and linkCreated states mirroring spec FrameNoHost/FrameGenerating/FrameLinkCreated.
- `med`/`color` — FormFieldGroup section overline uses Theme.Color.appTextSecondary (gray #6b7280); spec Section overline color is H.accent700 (Home green #16A34A). **Fix:** Pass a Home-green overline color into FormFieldGroup for this screen (or add an accent param) to match the pillar accent.
- `low`/`typography` — Explainer copy is "Pick a time and who needs to be home..." instead of the spec string "Slots come from when your chosen hosts are personally free." **Fix:** Use the spec explainer text.
- `low`/`layout` — "Who must be home" subtitle is static "Required at home"; spec invalid/no-host frames show contextual subtitles ("Free: none in this window") and a "Pick at least one host" inline error tied to the missing host state. **Fix:** Drive the member subtitle from availability and surface the host-missing inline error in the invalid state.
- `low`/`icon` — Chip rest-state label color uses Theme.Color.appTextStrong (fg2 #374151); spec unselected chip text color is N.fg2 too but icon intent and the Vendor/Guest/Delivery/Service set match — minor: ensure all four VisitKind cases map to wrench/user-round/package/hard-hat glyphs. **Fix:** Verify VisitKind.icon maps to the four spec lucide glyphs.

**Android deviations:**
- `high`/`missing-element` — "Offer these times" WeekdayGrid (7 cells height 30dp radius 8, Home-green selected) + preset chips ("Mornings only"/"9 AM – 12 PM") from spec FrameDefault are absent; the "When" SectionCard only has Date/Start time/Visit length rows. **Fix:** Add a WeekdayGrid composable + preset chip FlowRow under a "Offer these times" SectionCard.
- `high`/`missing-element` — "Generate a shareable booking link" toggle section (spec FrameDefault final Section: ValueRow + Toggle) is missing entirely. **Fix:** Add a trailing SectionCard with a ValueRow + Switch bound to a generateLink form field.
- `high`/`state` — VisitSetupLoadState only has Loading/Ready/Error; the spec's no-host-available (warning banner + add-host/widen-window buttons), generating (door-open spinner + "Building the slots you'll offer" + shimmers), and link-created success (check medallion + copy-link row + Share button) frames are unimplemented (VM marks the engine out of scope). **Fix:** Add NoHostAvailable/Generating/LinkCreated states and screens mirroring FrameNoHost/FrameGenerating/FrameLinkCreated.
- `low`/`layout` — "Who must be home" subtitle is static "Required at home" and lacks the spec's contextual "Free: none in this window" plus the "Add another host" CTA shown in the no-host frame. **Fix:** Drive member subtitle from availability and add the host CTA when implementing the no-host state.
- `low`/`typography` — Explainer hardcodes fontSize 11.5.sp with no lineHeight; spec sets line-height 16px (and color primary800 #075985 which matches). Minor: set lineHeight to 16.sp for parity. **Fix:** Add lineHeight = 16.sp to the explainer Text.
- `low`/`icon` — Access section omits the spec's "Link an access code" secondary row (key-round icon tile + chevron-right) shown in FrameDefault AccessNote; only the entry-note field is present. **Fix:** Add the "Link an access code" tappable row beneath the entry-note field.

### F14 Visit detail — iOS `major` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/VisitDetailView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Home/Resources/VisitDetailViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/visits/VisitDetailScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/visits/VisitDetailViewModel.kt`
- **Verdict:** Both platforms deliberately collapse the design's 9-state offer/reserve/link lifecycle into just Confirmed/Done (no backend), so both drop the ShareManage card and all link/banner states; iOS is closer because it renders the 4-step Status timeline and the Completed terminal chip, both of which Android omits.
- **Cross note:** Both platforms share the same documented backend limitation: F14's offer/reserve/link lifecycle (ShareManage card + 7 of 9 states) has no v1 backend, so neither renders it — this is the largest shared gap. Key divergence: iOS implements the Status timeline and Completed terminal chip and correctly suppresses the Done banner, while Android drops the timeline, omits the terminal chip, and adds a non-spec 'Visit complete' Done banner.

**iOS deviations:**
- `high`/`missing-element` — ShareManage card (link status pill, monospace link row, Copy/QR buttons, Slots counter, Revoke/Extend/Preview text-btns) is entirely absent; design shows it in 6 of 9 states (offered/reserved/active-link/expired/revoked). **Fix:** Add a ShareManage SectionCard when an offer/booking link exists, with the link state pill, link code row, Copy/QR actions, slots line, and Revoke/Extend/Preview footer.
- `high`/`state` — Only confirmed/done lifecycles modeled; the design's offered, reserved, cancelled, no-show, active-link, expired-link, revoked states (each with distinct banner, time tone, terminal chip and footer CTAs) are not rendered. **Fix:** Extend Lifecycle to cover offered/reserved/cancelled/no-show/link states once backend exists; map each to its banner, timeColor, terminalChip and footer per spec.
- `med`/`missing-element` — Reserved-state amber 'Visitor picked a time' Banner and Confirm/Decline footer, plus cancelled/no-show error/warning banners, are not implemented; only the confirmed 'On the home calendar' home banner exists. **Fix:** Add reserved/cancelled/no-show banner variants and the Confirm+Decline footer pairing keyed off lifecycle.
- `low`/`typography` — Status overline uses tracking(0.6) at size 10; design Overline is fontSize 9–11 with letterSpacing 0.06em (~0.6–0.66px) — close, acceptable, but Done-state body banner is suppressed correctly. **Fix:** No change needed; tracking is within tolerance.
- `low`/`icon` — Header avatar uses categoryUnboxing gradient as a proxy for the design's fixed teal #2dd4bf→#0d9488 'PV' badge; intent matches but token is a category alias not the visit-teal. **Fix:** Confirm categoryUnboxing maps to teal 2dd4bf/0d9488; if not, introduce a dedicated visit-teal token.

**Android deviations:**
- `high`/`missing-element` — The Status timeline card (4-step Offered→Reserved→Confirmed→Done with numbered/check circles and connectors) is completely absent from VisitLoaded; iOS renders it and the design makes it the second card in every state. **Fix:** Add a StatusTimeline composable (Status overline + row of 22dp step circles, check icon when done, 2dp connectors, home accent fill) and place it after the banner.
- `med`/`extra-element` — LifecycleBanner renders a 'Visit complete' banner for the Done state, but the design's Completed state has no banner — only the header terminal chip plus timeline at Done. **Fix:** Return no banner (skip the Row) when lifecycle == Done, matching the design and iOS.
- `med`/`missing-element` — Done-state header omits the green 'Completed' terminal chip; HeaderCard never renders a terminalChip, whereas the design (and iOS) show a successBg/check pill for completed/cancelled/no-show. **Fix:** Add a terminalChip slot in HeaderCard's top row rendered for Done (Completed, check, successBg/#047857).
- `high`/`missing-element` — ShareManage card and the offered/reserved/cancelled/no-show/active-link/expired/revoked states are not implemented (same backend gap as iOS). **Fix:** Once the offer/link backend exists, add a ShareManage card and the remaining lifecycle banners/footers per spec.
- `low`/`color` — Done-state timeText uses appTextSecondary (fg3) as the muted tone; design's muted time tone is fg3 (#6b7280) so this matches, but the absent timeline means the Done progress affordance is lost. **Fix:** Resolved by adding the timeline; time color is fine.

## Business

### G1 Round-robin assignment — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/RoundRobin/RoundRobinAssignmentSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Shared/BusinessKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/RoundRobinSheet.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessAssignment.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both faithfully reproduce the bottom-sheet (header "Assign bookings" + subhead, three selectable rule cards, "Bookable members" overline, seat-card with checkbox/avatar/name/"Uses personal availability"/weight-stepper-or-grip, none-selected amber warning, single-member info blurb, sky Done CTA); iOS edges ahead because Android substitutes a speedometer Gauge icon for the Balanced "scale" glyph and ships a thinner loading shimmer.
- **Cross note:** Both add an error state not present in the 4 design frames (acceptable per the four-state rule) and both render the seat sub-line as the hardcoded "Uses personal availability" exactly as the spec's SeatRow does. Shared divergence: both round the rule card to 12/16 rather than the spec's 14, and both correctly keep the Done CTA product-sky while using business-violet for active rule/checkbox/stepper accents.

**iOS deviations:**
- `low`/`other` — RuleCard uses Radii.xl (16) for the card corner radius; design RuleCard borderRadius is 14. **Fix:** Use a 14pt cornerRadius (between lg/xl) for the rule card clip/overlay.
- `low`/`typography` — Sheet title via BizSheetHeader is font size 17 bold; design 'Assign bookings' header is fontSize:18 weight 700. **Fix:** Bump the BizSheetHeader title to 18pt to match the spec.

**Android deviations:**
- `med`/`icon` — Balanced RuleCard uses PantopusIcon.Gauge which maps to Icons.Filled.Speed (a speedometer); design icon is lucide 'scale' (balance scales). iOS uses the correct scale glyph. **Fix:** Add/route a PantopusIcon.Scale (lucide scale) and use it for the Balanced rule card.
- `low`/`icon` — Priority-order RuleCard uses PantopusIcon.List ('list', plain bullets); design uses 'list-ordered' (numbered list). **Fix:** Use a ListOrdered/list-ordered icon for the Priority rule card to convey ranked order.
- `med`/`sizing` — RoundRobinLoading shimmer row drops the trailing weight pill (design 54x24) and the second name-line bar, showing only avatar + one full-width bar; iOS BizShimmerRow includes checkbox, two bars and the trailing pill. **Fix:** Mirror loaded geometry: add a 22x22 checkbox box, a 34 avatar, two stacked text bars, and a ~54x24 pill so the skeleton matches the SeatRow.
- `low`/`other` — RuleCard clip/border uses RoundedCornerShape(Radii.lg)=12 corner radius; design RuleCard borderRadius is 14 (iOS is 16, also off). **Fix:** Use a 14dp corner for the rule card.
- `low`/`other` — RuleCard icon box uses RoundedCornerShape(Radii.md)=8 corner radius; design icon box borderRadius is 9 (iOS uses literal 9). **Fix:** Use a 9dp corner on the 32dp rule-card icon box.
- `low`/`other` — BizCheckbox uses RoundedCornerShape(Radii.sm)=6 corner radius; design Checkbox borderRadius is 7 (iOS uses 7). **Fix:** Use a 7dp corner on the 22dp checkbox.
- `low`/`typography` — Sheet title uses PantopusTextStyle.h3 (20sp per token scale); design 'Assign bookings' header is fontSize:18 weight 700. **Fix:** Use an 18sp/700 style for the sheet title instead of h3.

### G2 Collective event setup — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Collective/CollectiveEventSetupSheet.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Shared/BusinessKit.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Collective/CollectiveEventSetupViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/CollectiveEventSetupScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/CollectiveEventSetupViewModel.kt`
- **Verdict:** iOS faithfully reproduces the bottom-sheet collective config (grabber, title+subhead header, all 5 sections in order, sky CTA); Android renders the same content correctly but in a full-screen top-bar shell instead of the design's bottom-sheet archetype, plus a couple icon/type nits.
- **Cross note:** Both platforms intentionally defer the Frame 3 no-overlap amber warning (a backend shared-availability dependency), so neither shows it — a shared low-severity omission, not a regression. Key divergence: iOS correctly uses a bottom sheet while Android uses a full-screen top-bar shell.

**iOS deviations:**
- `low`/`typography` — BizStepper value Text uses font size 11.5 bold (BusinessKit.swift L269) vs the spec Stepper value of 13.5px (collective-frames Stepper non-small). **Fix:** Bump the stepper value font to .system(size: 13.5, weight: .bold).
- `low`/`state` — Frame 3 (no-overlap amber alert-triangle Note) is not rendered; the VM defers live overlap detection so the warning never appears even though the design lists it as a state. **Fix:** When a shared-availability check returns empty, render BizNote(tone: .warning, icon: .alertTriangle, ...) above the Members card as in FrameNoOverlap.

**Android deviations:**
- `high`/`chrome` — Screen uses BizTopBar (48dp top bar with a ChevronLeft back button and centered title, BusinessKit.kt L60) instead of the design's bottom-sheet archetype: a SheetFrame with a 36x5 grabber, no chevron, and a left-aligned 17px/700 title + subhead inside the sheet header. **Fix:** Present this as a ModalBottomSheet with a drag handle and a left-aligned title/subhead header (mirror iOS BizSheetHeader); drop the centered top bar and back chevron.
- `med`/`layout` — The subhead ('Every required member must be free...') is rendered as the first body Text inside the scroll content (CollectiveEventSetupScreen.kt CollectiveBody) rather than paired with the title in a sheet header as in the spec. **Fix:** Move the subhead into the sheet header beside/under the title, matching the SheetFrame title+subhead block.
- `low`/`icon` — The intersect explainer Note uses PantopusIcon.ArrowRightLeft (rendered as Material SwapHoriz, Icons.kt L961) whereas the spec uses the git-merge glyph (converging streams); no GitMerge exists in the icon enum. **Fix:** Add a GitMerge lucide icon to PantopusIcon and use it here to match the converging-availability intent.
- `low`/`typography` — BizNote body text uses PantopusTextStyle.caption (12px) (BusinessKit.kt L201) vs the spec Note text of 11.5px weight 500. **Fix:** Use an 11.5sp / FontWeight.Medium style for the note body to match the spec.
- `low`/`state` — Frame 3 no-overlap amber warning Note is not rendered; the VM defers live overlap detection (CollectiveEventSetupViewModel.kt), matching iOS but still missing vs the design's 4 frames. **Fix:** On empty shared-availability, render BizNote(tone = Warning, icon = AlertTriangle) above the Members card.

### G3 Team booking availability — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Business/TeamAvailability/TeamBookingAvailabilityView.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Business/Settings/BusinessSchedulingSettingsView.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/TeamBookingAvailabilityScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessKit.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`
- **Verdict:** Both platforms are faithful to the spec — same TopBar title, Explainer note, Team overline+card roster (avatar/name/summary/Personal-vs-Business chip/toggle/chevron), coverage card and gated lock note — but both add an undesigned trailing assignment action whose icon and color differ between the two platforms; iOS edges ahead on exact coverage-box radius and chip spacing.
- **Cross note:** Shared issue: both platforms add a trailing TopBar assignment action that does not exist in any of the 5 design frames, and they implement it inconsistently (iOS slidersHorizontal/appText vs Android UsersRound/bizAccent). Everything else — Explainer note, Team overline, roster card (36 avatar, 13/600 name, 11/fg3 summary, Personal/Business chip, 46x28 violet toggle, chevron-right), coverage card, warning note, and gated lock line — matches the spec closely on both.

**iOS deviations:**
- `med`/`extra-element` — assignmentAction adds a trailing TopBar button (slidersHorizontal, 19pt, appText) but the spec TopBar in all 5 frames has title only with no trailing action. **Fix:** Remove the trailing assignment action to match the design, or get the design updated to include it (and align the icon/color with Android).
- `low`/`icon` — Trailing assignment icon is .slidersHorizontal tinted appText, whereas Android uses UsersRound tinted business accent — the two platforms diverge from each other for the same affordance. **Fix:** Pick one canonical glyph+tint for the assignment action across both platforms.
- `low`/`spacing` — rosterRow uses .padding(.vertical, Spacing.s3)=12 but omits the spec's horizontal:2 row inset (padding '12px 2px'). **Fix:** Add .padding(.horizontal, 2) to the row content to match the 2px horizontal inset.
- `low`/`state` — Spec frame 3 'member not bookable' shows the dimmed row with toggle OFF and 'Not taking bookings' summary; iOS renders this only when the VM supplies bookable=false, so coverage depends on data rather than an explicit modeled variant (acceptable but unverified here). **Fix:** Confirm the VM emits a non-bookable row (bookable=false, summary 'Not taking bookings') so the frame-3 appearance is reachable.

**Android deviations:**
- `med`/`extra-element` — BizTopBar trailing slot adds an assignment action (UsersRound, 20dp, bizAccent) but the spec TopBar has no trailing action in any of the 5 frames. **Fix:** Remove the trailing assignment action to match the design, or get it added to the spec and unify with iOS.
- `low`/`sizing` — CoverageCard inner 32dp icon box uses RoundedCornerShape(Radii.md)=8dp; spec borderRadius is 9. **Fix:** Use a 9dp corner (or add a Radii token =9) for the coverage icon box to match the spec radius.
- `low`/`spacing` — MemberRow text Column uses Arrangement.spacedBy(3.dp) for name→summary→chip, collapsing the spec's distinct gaps (summary marginTop:1, chip marginTop:5). **Fix:** Set name→summary gap ~1dp and chip top padding 5dp instead of a uniform 3dp spacing.
- `low`/`icon` — AssignmentPickerSheet header reads 'Assign bookings' / 'Pick a service…' with ArrowsRepeat/UsersRound leading icons; this off-frame companion sheet diverges from iOS copy 'Assignment' / 'Choose a service…'. **Fix:** Align the sheet title/subhead copy and leading-icon choice with iOS so the off-frame surfaces match.

### G4 Member working-hours editor — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Business/MemberHours/MemberWorkingHoursSheet.swift`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/ios/Pantopus/Features/Scheduling/Business/Shared/BusinessKit.swift`
- Android files: `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/MemberWorkingHoursScreen.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessKit.kt`, `/Users/yingpengwang/pantopus/native/pantopus/frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Typography.kt`
- **Verdict:** iOS is a near-pixel-faithful bottom-sheet reproduction (correct drag-handle + left-title + circular close chrome, exact pill/card/dated-card specs, 6 states); Android is faithful in body content but renders as a full-screen with a back-chevron centered-title top bar instead of the sheet header, plus a generic loading skeleton.
- **Cross note:** Both platforms reproduce all body sections in the correct order (TzChip, optional DatedCard, WeekGrid, Copy-Monday link, 'Date overrides' overline, OverrideRows) and the read-only inherits variant; the divergence is chrome (iOS = true bottom sheet with sheet header; Android = full-screen with back-chevron top bar) and loading fidelity (iOS mirrors the day-row shimmer; Android uses a generic skeleton).

**iOS deviations:**
- `low`/`sizing` — dayRow weekday label uses .frame(width: 32) but spec DayRow label width is 30. **Fix:** Change the day-label frame width from 32 to 30.
- `low`/`spacing` — editingBody/readOnlyBody use VStack spacing Spacing.s3 (12) which matches spec gap:12, but the Header wraps content in a column whose top gap (TzChip→WeekGrid) is also 12 — consistent; dayRow drops the spec's 2px horizontal inset (padding '11px 2px'). **Fix:** Add 2pt horizontal padding to dayRow content to mirror the spec inset (cosmetic).

**Android deviations:**
- `high`/`chrome` — Screen uses BizTopBar (48dp app bar with a left back-chevron and a centered SemiBold 16sp title) instead of the spec's bottom-sheet header: a 36x5 drag handle then a LEFT-aligned 17px/700 title with a trailing 28x28 circular sunken close (x) button. iOS matches the sheet; Android does not. **Fix:** Render this as a modal bottom sheet (or at minimum swap BizTopBar for a BizSheetHeader: drag handle + left 17sp/Bold title + trailing circular X), removing the back chevron and centered title.
- `med`/`state` — Loading state calls generic SchedulingLoadingSkeleton(rows=5); spec frame 6 shows a TzChip-width shimmer plus a 7-row day-grid shimmer (30-wide label + 60% pill + 26 circle per row) inside a Card. iOS reproduces this exactly. **Fix:** Replace SchedulingLoadingSkeleton with a 7-row shimmer inside BizCard mirroring the day-row geometry, preceded by a ~150x30 pill shimmer for the TzChip.
- `low`/`typography` — BizOverline ('Date overrides') uses PantopusTextStyle.overline = 11sp, but the design Overline component renders fontSize:10 (700, 0.08em). iOS BizOverline uses 10pt. **Fix:** Set the overline font size to 10sp to match the rendered spec value.
- `low`/`spacing` — RangeChip uses horizontal padding Spacing.s2 (8) and gap Spacing.s1 (4); spec RangeChip is padding '5px 9px' with gap 5. **Fix:** Use 9dp horizontal padding and 5dp gap for the range chip.
- `low`/`state` — TzChip hides the chevron-down icon when disabled (read-only), but spec frame 4 (Inherits read-only) still shows the TzChip with its chevron. **Fix:** Always render the chevron-down in TzChip regardless of enabled state.
- `low`/`extra-element` — An inline BizNote (error toast) is rendered inside the scroll body on save errors; not present in the design frames. **Fix:** Acceptable as a functional addition, but prefer a snackbar/transient toast so it doesn't shift the static layout.
- `low`/`spacing` — DayRow vertical padding uses Spacing.s3 (12) vs spec '11px'; TzChip horizontal padding uses Spacing.s3 (12) vs spec 11; OverrideRow disc uses Radii.md (8) vs spec radius 9. **Fix:** Use literal 11dp for day-row/TzChip vertical/horizontal padding and 9dp for the override disc to hit spec exactly (cosmetic).

### G5 Business scheduling settings — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Settings/BusinessSchedulingSettingsView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Shared/BusinessKit.swift`, `frontend/apps/ios/Pantopus/Core/Design/Spacing.swift`, `frontend/apps/ios/Pantopus/Core/Design/Colors.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessSchedulingSettingsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/business/BusinessKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/SchedulingStateScaffolding.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Color.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Spacing.kt`
- **Verdict:** Both platforms reproduce all five frames with the correct section order (Intro · Confirmation · Scheduling · Policy · Notifications · Payments) and gated/auto/payments variants; iOS is a faithful hand-built mirror with only icon-glyph and overline nits, while Android has a wrong loading-skeleton geometry and an off-palette Stripe disc that push it to major.
- **Cross note:** Both share the same two icon-glyph misses (Buffers should be git-commit-horizontal; iOS gets Booking-horizon right with calendarRange but Android wrongly uses CalendarDays). Key divergence: iOS faithfully mirrors the 1/4/2 shimmer geometry and uses a real Stripe-brand token, whereas Android falls back to a generic 5-card skeleton and a violet magicBg disc — the two reasons it lands at major while iOS is minor.

**iOS deviations:**
- `low`/`icon` — Buffers row uses Icon(.arrowRightLeft); spec SRow icon is git-commit-horizontal (the buffers/commit glyph). **Fix:** Add a gitCommitHorizontal PantopusIcon case and use it for the Buffers row.
- `low`/`typography` — BizOverline renders font size 10 / tracking 0.8; spec Overline token is 11px, weight 600, letter-spacing 0.06em (≈0.66 at 11px). **Fix:** Set BizOverline to .system(size: 11, weight: .semibold) with tracking ~0.66.
- `low`/`color` — Stripe disc background is stripeBrand.opacity(0.10) derived; spec uses a dedicated C.stripeBg / C.stripe pair — acceptable since stripeBrand is a real token, but the 0.10 wash is lighter than the spec's solid stripeBg. **Fix:** Define an explicit stripeBg token and pass it as iconBg rather than a 0.10 opacity of the glyph color.
- `low`/`state` — iOS adds an error frame (BizSettingsError) that the spec does not show; this is required by the four-state rule but is an extra vs the 5 design frames. **Fix:** Keep it — additive per state-rule; no change needed, noted for completeness.

**Android deviations:**
- `high`/`state` — Loading uses generic SchedulingLoadingSkeleton(rows=5) — five identical plain cards with two text shimmers each; spec FrameLoading is three ShimGroups of 1/4/2 rows, each row a 32x32 disc + two stacked text lines, with a 90x9 overline shimmer above each group. **Fix:** Build a feature-local 1/4/2 ShimGroup skeleton with overline shimmer + 32dp disc + two text lines per row, mirroring iOS BizSettingsSkeleton.
- `high`/`color` — Stripe disc uses PantopusColors.magicBg (#EDE9FE lavender) with categoryTask (#4F46E5 indigo) glyph; spec uses Stripe-brand C.stripeBg/C.stripe, and iOS uses a stripeBrand token — Android reads as a violet magic chip, not the Stripe swatch. **Fix:** Add a stripeBrand/stripeBg color token and use it for the Stripe payments disc bg + glyph.
- `med`/`icon` — Booking horizon row uses PantopusIcon.CalendarDays; spec SRow icon is calendar-range. **Fix:** Use a CalendarRange icon for the Booking horizon row.
- `low`/`icon` — Buffers row uses PantopusIcon.ArrowRightLeft; spec icon is git-commit-horizontal. **Fix:** Add a GitCommitHorizontal icon and use it for the Buffers row.
- `med`/`sizing` — Connect button is a Box with vertical=6.dp padding and no fixed height (caption text style); spec button is height 28, horizontal padding 13, font 11.5/700. **Fix:** Constrain the Connect button to height 28.dp with 13.dp horizontal padding and 11.5sp bold label.
- `low`/`typography` — Confirmation description and SettingsRow sub use PantopusTextStyle.caption (12sp); spec description is 10.5px and row sub is fontSize 11. **Fix:** Use ~10.5sp for the confirmation description and 11sp for row sub-labels.
- `low`/`chrome` — BizTopBar height is 48.dp (TOP_BAR_HEIGHT); spec/iOS top bar is 46pt. **Fix:** Set TOP_BAR_HEIGHT to 46.dp to match the spec frame height.
- `low`/`spacing` — loadedBody/SettingsBody uses horizontal padding Spacing.s4 (16) while iOS uses Spacing.s3 (12) and the spec Scroll inset is the kit's standard gutter; the wider 16dp gutter narrows cards vs spec. **Fix:** Align outer horizontal padding to 12.dp to match the spec gutter and iOS.

## Packages

### G10 Buy package — iOS `minor` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/BuyPackageView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackagesKit.swift`, `frontend/apps/ios/Pantopus/Core/Design/Icons.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/BuyPackageScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackagesKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both platforms are faithful, near-identical mirrors of the 4-frame checkout sheet (logged-in/guest/declined/upsell) plus added loading/paid/error states; both adapt the spec's mock VISA card chip to a neutral "Add a payment method" placeholder, and Android alone uses two wrong icon glyphs in the eligibility and upsell rows.
- **Cross note:** Both platforms intentionally replace the spec's mock data (Marlow & Co. owner, VISA ••4242, fixed prices, named service list) with backend-driven content and a neutral payment-method placeholder, and both add a PkgTopBar over the spec's SheetFrame — these are shared, deliberate adaptations, not regressions. The only genuine per-platform divergence is Android's two wrong icon glyphs (BadgeCheck for ticket-check, Tag for ticket) which iOS gets right; iOS's only edge is the missing footnote line-height that Android sets.

**iOS deviations:**
- `low`/`chrome` — Design renders this as a SheetFrame (label header only, no nav bar); BuyPackageView wraps it in PkgTopBar with back chevron + centered 'Buy package' title — an acceptable native presentation but adds chrome not in the spec. **Fix:** Acceptable as native sheet/nav adaptation; optionally present as a sheet to match the modal framing.
- `low`/`extra-element` — payMethodRow renders a neutral credit-card glyph + 'Add a payment method' instead of the spec's VISA gradient chip (34x24, blue gradient, 'VISA') + 'Visa ••4242' label; saved-card backend is deferred. **Fix:** When card-on-file is wired, render the brand chip + masked PAN per spec.
- `low`/`typography` — footnote sets font size 10.5 but omits an explicit line-height; spec footnote is lineHeight 15px (Android sets lineHeight=15.sp). **Fix:** Add .lineSpacing or a 15pt line height to the footnote Text to match.
- `low`/`missing-element` — eligibleRow body shows 'All of this provider's services'/'The selected service' rather than the spec's concrete service list ('Haircut · Beard trim · Kids cut'); acceptable as data-driven content. **Fix:** None needed; backend-driven service list is a reasonable substitution for mock copy.

**Android deviations:**
- `med`/`icon` — EligibleRow uses PantopusIcon.BadgeCheck (a verified-seal/VerifiedUser glyph) but the spec's eligibility row icon is ticket-check; iOS correctly uses .ticketCheck. **Fix:** Use PantopusIcon.Ticket (closest available; no TicketCheck enum) for the EligibleRow leading icon.
- `med`/`icon` — UpsellBanner uses PantopusIcon.Tag, but the spec's already-owns-credits banner icon is ticket; iOS correctly uses .ticket and PantopusIcon.Ticket exists on Android. **Fix:** Change the UpsellBanner icon from PantopusIcon.Tag to PantopusIcon.Ticket.
- `low`/`chrome` — Design is a SheetFrame (no nav bar); BuyPackageScreen adds PkgTopBar with back chevron + centered 'Buy package' title — acceptable native adaptation but extra chrome vs spec. **Fix:** Acceptable; optionally present as a bottom sheet to match modal framing.
- `low`/`extra-element` — PayMethodRow renders a neutral credit-card glyph (tinted info blue) + 'Add a payment method' instead of the spec's VISA gradient chip + 'Visa ••4242'; saved-card backend deferred. **Fix:** When card-on-file is wired, render the brand chip + masked PAN per spec.
- `low`/`color` — PayMethodRow card-chip icon is tinted PantopusColors.info while iOS tints it Theme.Color.stripeBrand; minor cross-platform inconsistency on a placeholder element. **Fix:** Align the placeholder tint between platforms (use a shared brand/stripe token).

### G11 My packages — iOS `major` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/MyPackagesView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackagesKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/MyPackagesScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackagesKit.kt`
- **Verdict:** Both platforms render a faithful active/spent/empty card but each omits the design's two signature elements — the collapsible redemption-history sub-section and the card-level amber expiring-soon banner — and both replace the spec's "Expires/Ended {date}" caption and real owner names with a "Purchased {date}" caption and a "Business provider" placeholder; iOS edges ahead only because Android uses the wrong empty-state icon.
- **Cross note:** Both screens were clearly built from the same simplified projection: identical card layout, identical 'Purchased {date}' caption, identical owner placeholder, and both drop the redemption-history sub-section and the expiring-soon amber banner. Fixes should land in parallel on both platforms; the only divergence is Android's wrong empty-state icon (Tag vs the spec ticket, which iOS gets right).

**iOS deviations:**
- `high`/`missing-element` — CreditCard has no redemption-history section; spec Frame 1/4 PkgCard renders a collapsible 'REDEMPTION HISTORY' overline + chevron-up + HistoryRow rows (check icon, service, date, '1 credit'). **Fix:** Add a history block under the CTA gated on credit history: overline (10/700 uppercase fg3), chevron-up, and rows with Icon(.check, 13, success), service 11.5 fg2, date 10.5 fg4, '1 credit' 10/700 fg3.
- `high`/`missing-element` — No expiring-soon variant; spec Frame 4 adds a card-top amber banner (clock-alert 14, warnBg fill, warnBorder hairline, 'N credits expire in N days — book soon.' 11/600 warn). **Fix:** Render a leading banner row inside the card when expiringSoon: HStack(clockAlert + warn text) on Theme.Color.warningBg with bottom warningLight border.
- `med`/`missing-element` — Spent state never distinguishes Expired from All used; spec shows an 'Expired' pill when credits remain but the package lapsed vs 'All used' when left==0, plus greyed meter via borderStrong. **Fix:** Branch on expired-vs-used in the model and show PkgChip(text:'Expired') for the expired case alongside the existing 'All used'.
- `med`/`typography` — Caption reads 'Purchased {date}' (size 10.5); spec caption is 'Expires {date}' for active and 'Ended {date}' for spent (10.5 fg3). **Fix:** Replace the purchased caption with expiry copy keyed off spent: 'Expires …' / 'Ended …'.
- `med`/`other` — Owner label is the placeholder '\(ownerTheme.title) provider' (e.g. 'Business provider'); spec OwnerRow shows the real owner name ('Marlow & Co.', 'Pawfect'). **Fix:** Surface the real owner display name from the credit/package DTO instead of the pillar placeholder.

**Android deviations:**
- `high`/`missing-element` — CreditCard omits the redemption-history section that spec Frame 1/4 PkgCard renders (collapsible 'REDEMPTION HISTORY' overline + chevron-up + HistoryRow rows with check icon, service, date, '1 credit'). **Fix:** Add a history block below the CTA: overline (10.sp/Bold uppercase appTextSecondary), chevron-up, and rows with PantopusIcon.Check (13.dp, success), service 11.5.sp appTextStrong, date 10.5.sp, '1 credit' 10.sp/Bold.
- `high`/`missing-element` — No expiring-soon variant; spec Frame 4 prepends a card-level amber banner (clock-alert 14.dp on warningBg, warningLight border, 'N credits expire in N days — book soon.' 11.sp/600 warning). **Fix:** Render a banner Row at the top of the card when expiring soon, using PantopusIcon.ClockAlert + warning text on PantopusColors.warningBg.
- `med`/`icon` — Empty state uses PantopusIcon.Tag; spec EmptyHero icon is 'ticket' and PantopusIcon.Ticket already exists in Icons.kt. **Fix:** Change EmptyState icon to PantopusIcon.Ticket to match the spec ticket disc.
- `med`/`missing-element` — Spent cards never show an 'Expired' pill — code only emits 'All used' when remaining==0; spec distinguishes Expired (credits remain but lapsed) from All used. **Fix:** Compute expired vs used in the VM and render PkgChip(text='Expired', tone=Neutral, uppercased=true) for the expired case.
- `med`/`typography` — Caption renders 'Purchased {date}' (10.5.sp); spec caption is 'Expires {date}' (active) / 'Ended {date}' (spent). **Fix:** Replace purchased caption with expiry copy keyed off spent state.
- `med`/`other` — Owner label is placeholder '${pillar.providerLabel()} provider' (e.g. 'Personal provider'); spec OwnerRow shows real owner names. **Fix:** Render the real owner display name from credit.bookingPackage instead of the pillar placeholder.
- `low`/`typography` — Count uses FontWeight.Black (~900); spec is fontWeight:800 and iOS uses .heavy (800). **Fix:** Use a SemiBold/ExtraBold weight closer to 800 to match the spec and iOS.

### G8 Packages list — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackagesListView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackagesKit.swift`, `frontend/apps/ios/Pantopus/Core/Design/Components/EmptyState.swift`, `frontend/apps/ios/Pantopus/Core/Design/Radii.swift`, `frontend/apps/ios/Pantopus/Core/Design/Icons.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackagesListScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackagesKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/EmptyState.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Radii.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/theme/Icons.kt`
- **Verdict:** Both platforms render all five spec states (active / empty / payouts-gate / archived / loading) with token-accurate chrome, segmented filter, paper-card rows, chip + sold count, and amber Stripe gate; iOS is pixel-faithful while Android substitutes a Package glyph for the spec's layers icon and renders a full-width empty CTA the spec constrains to 200pt.
- **Cross note:** Shared (non-regression): both platforms shimmer the filter segment in the loading state where the spec keeps the live FilterSeg, and both render a full-width empty CTA where the spec constrains it to width 200. Key divergence: iOS uses the correct lucide 'layers' glyph (its Icon enum has .layers) while Android lacks a Layers glyph and falls back to PantopusIcon.Package across the row disc, empty hero, and payouts gate.

**iOS deviations:**
- `low`/`missing-element` — loadingBody replaces the filter segment with Shimmer(height:36); the spec FrameLoading keeps the real FilterSeg (value='Active') above the skeleton card and only shimmers the rows. **Fix:** Render the live PkgSegmented (disabled) in loadingBody instead of a 36pt shimmer bar.
- `low`/`sizing` — emptyActive uses the shared EmptyState whose PrimaryButton is full-width; the spec wraps the empty CTA in a width:200 container. **Fix:** Constrain the empty-state CTA to ~200pt to match the centered fixed-width button in the spec.
- `low`/`interaction` — Active rows open a SwiftUI Menu (Edit / Archive) on the ellipsis; the spec's actions menu lists Edit / Duplicate / Archive (HTML caption names all three). **Fix:** Add a Duplicate action to the row's Menu to match the documented Edit / Duplicate / Archive set.

**Android deviations:**
- `med`/`icon` — PackageRow, EmptyActive and PayoutsGate use PantopusIcon.Package (a box glyph); the spec uses the lucide 'layers' icon for the row disc and empty hero, and iOS correctly uses .layers. **Fix:** Add a Layers glyph to PantopusIcon (Icons.kt) and swap Package→Layers in the row disc, EmptyActive, and PayoutsGate.
- `low`/`sizing` — EmptyActive's shared EmptyState renders a full-width PrimaryButton; the spec constrains the empty CTA to width:200. **Fix:** Pass a width-200 modifier (or a CTA-width param) so the empty CTA matches the fixed-width spec button.
- `low`/`missing-element` — PackagesLoadingBody replaces the filter segment with Shimmer(height=36); the spec FrameLoading keeps the real Active/Archived FilterSeg and only shimmers the rows. **Fix:** Render the live PkgSegmented in the loading body instead of a 36dp shimmer bar.
- `low`/`layout` — EmptyState uses fillMaxSize()+Arrangement.Center inside the loaded scroll Column which also applies padding(top=s10); the centered fill can fight the column's top padding/scroll, unlike the spec's top-anchored EmptyHero. **Fix:** Have EmptyActive/EmptyArchived use a content-height (wrapContentHeight) layout with top padding rather than fillMaxSize centering inside the scrollable column.
- `low`/`interaction` — Active row DropdownMenu offers only Edit / Archive; the spec actions menu is Edit / Duplicate / Archive. **Fix:** Add a Duplicate DropdownMenuItem to PackageRowMenu.

### G9 Create/edit package — iOS `minor` / Android `minor` → better: **equal**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackageEditorView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackagesKit.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Packages/PackageEditorViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackageEditorScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackagesKit.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/packages/PackageEditorViewModel.kt`
- **Verdict:** Both platforms are near-identical, faithful renders of the create/edit form — correct section order, cards, per-session math, and infoBlue edit note — but both omit Frame 4 (has-active-buyers: warning lock note + locked sessions/eligibility) and both render the section overlines gray instead of the spec's pillar-violet accent.
- **Cross note:** iOS and Android are deliberate 1:1 mirrors (same PkgCard/PkgStepper/PkgSegmented/PkgToggleRow kit, same section order, same VM shape), so they share every deviation — most importantly the entirely missing has-active-buyers (locked) variant and the gray-instead-of-violet section overlines. Neither is closer to the spec.

**iOS deviations:**
- `high`/`state` — Frame 4 (has-active-buyers) is unimplemented: PackageEditorView never renders the warning PkgNote ('12 people own credits…' with lock icon) and never passes a locked flag to redeemCard/sessionsCard, so sessions+eligibility can't be locked. **Fix:** Add a lockedByBuyers flag from the VM (sold/active-credit count); render PkgNote(tone:.warning, icon:.lock, …) above the Redeems card and disable PkgStepper + EventTile when locked.
- `med`/`color` — PkgCard overline uses Theme.Color.appTextSecondary (gray #6b7280); the event-editor Card spec paints the overline in the pillar accent (business violet #7c3aed) — the kit's stated non-negotiable. **Fix:** Pass the owner pillar accent (Theme.Color.business) to PkgCard's overline foreground instead of appTextSecondary.
- `low`/`sizing` — PkgStepper renders a borderless sunken-filled pill (radius 8, no dividers); design Stepper is a 1.5px-bordered box with divider lines, minus tinted fg2 and plus tinted blue600. **Fix:** Give PkgStepper a 1.5px appBorder, internal divider hairlines, and tint the plus glyph primary600 to match the event-editor Stepper.
- `low`/`icon` — EventTile uses .calendar (and .layers for an injected 'All services' tile); design RedeemCard tiles use a scissors glyph and contain no 'All services' tile. **Fix:** Use a service/scissors-style glyph for real event tiles; keep the synthetic 'All services' tile but acknowledge it's an addition beyond the four spec tiles.
- `low`/`sizing` — PkgToggleRow icon disc is 32×32 radius 9 with a constant gray sunken bg; design ToggleRow disc is 30×30 radius 8 and tints blue50 bg + blue600 icon when on. Label 13 vs spec 12.5, sub 11 vs 10.5. **Fix:** Shrink disc to 30/radius8, tint it blue50/blue600 in the on state, drop label to 12.5 and sub to 10.5.

**Android deviations:**
- `high`/`state` — Frame 4 (has-active-buyers) is unimplemented: EditorCards never renders the warning PkgNote ('12 people own credits…' lock icon) and RedeemTiles/PkgStepper receive no locked flag, so sessions+eligibility can't be locked. **Fix:** Expose a lockedByBuyers flag on PackageEditorUiState.Content; render PkgNote(tone=Warning, icon=Lock) before the Redeems card and pass enabled=false to PkgStepper + disable EventTile clicks when locked.
- `med`/`color` — PkgCard overline uses PantopusColors.appTextSecondary (gray); spec event-editor Card overline is the pillar accent (business violet #7c3aed). **Fix:** Color the overline with state.pillar.accent (PantopusColors.business) instead of appTextSecondary.
- `low`/`sizing` — PkgStepper is a borderless sunken pill; design Stepper is a 1.5px-bordered, divider-segmented box with plus tinted blue600. **Fix:** Add a 1.5dp appBorder + internal divider lines to PkgStepper and tint the plus glyph primary600.
- `low`/`sizing` — PkgSegmented uses outer radius Radii.md(8)/inner Radii.sm(6) and 30dp segment height; design Segmented is outer 9 / inner 7 with 32px height. **Fix:** Set the segmented outer shape to ~9dp, inner to ~7dp, and segment height to 32dp.
- `low`/`icon` — RedeemTiles use PantopusIcon.Calendar (and Package for the injected 'All services' tile); design tiles use scissors and have no 'All services' entry. ToggleRow disc 32/radius8 stays gray when on vs spec 30/radius8 tinted blue50+blue600. **Fix:** Use a service/scissors glyph for event tiles; tint the toggle icon disc blue50/blue600 in the on state and shrink to 30dp.

## Payments

### G6 Payments setup — iOS `minor` / Android `major` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Payments/PaymentsSetupView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Payments/PaymentsKit.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Settings/BusinessSchedulingSettingsView.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/wallet/scheduling/PaymentsSetupScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/_shared/OwnerPillarChrome.kt`
- **Verdict:** Both faithfully reproduce the status-hero + readiness-pills + Account/Tax groups across all five frames, but iOS nails the chrome and Stripe-brand badge while Android diverges on top-bar (left title + extra accent bar), badge color, and row-value placement.
- **Cross note:** Both platforms make the same two intentional content simplifications (descriptor \"Managed on Stripe\" instead of the on-receipt monospace value; Collect-tax chevron instead of the spec toggle). Key divergence: iOS top bar and Stripe-brand badge match the spec, while Android's shared OwnerPillarHeader (left title + accent bar) and violet badge do not.

**iOS deviations:**
- `med`/`missing-element` — Connected statement-descriptor row shows sub "Managed on Stripe"; spec Receipt renders the on-receipt value "MARLOW CO" in a monospace sunken pill with a receipt glyph, and the Payouts row sub is "Bank ••4291 · daily". **Fix:** Render the descriptor value as a monospace chip and surface the masked bank/payout cadence in the Payouts sub.
- `med`/`interaction` — Tax group "Collect tax" connected trailing is a chevron (rowTrailing); spec SRow trailing is a Business-violet IToggle (on). **Fix:** Render an on Toggle tinted Theme.Color.business in the connected Collect-tax row instead of a chevron.
- `low`/`other` — ReadinessPill uses Radii.md (8) for its background corner radius; spec ReadyPill borderRadius is 10. **Fix:** Use a 10pt corner radius on the pill (or a Radii token equal to 10).
- `low`/`color` — On-pill bg uses Theme.Color.successBg; spec ReadyPill on bg is C.okBg (the success-bg tone) — verify the token maps to #F0FDF4. **Fix:** Confirm successBg maps to the spec okBg value.

**Android deviations:**
- `high`/`chrome` — OwnerPillarHeader left-aligns the title at PantopusTextStyle.h3 (~20sp) and appends a pillar accent bar; spec TopBar centers the title at 15sp/semibold (tracking -0.2), 46dp tall with only a 1dp bottom border and a trailing slot, no accent bar. **Fix:** Use a centered 15sp/semibold title, drop the accent bar, add a trailing slot, and match the 46dp bar with a bottom border.
- `med`/`color` — Stripe badge Box background is PILLAR.accent (business violet #7C3AED); spec StripeBadge background is C.stripe (#635bff Stripe indigo). **Fix:** Add a stripeBrand color token (#635bff) and use it for the badge background, matching iOS.
- `med`/`layout` — PaymentRow renders the connected value (USD, "Managed on Stripe", "automatic") as right-aligned trailing text; spec SRow shows the value as a sub line beneath the label (left-aligned) with a chevron on the right. **Fix:** Move value into a 11sp sub line under the label and keep the chevron as the sole trailing element.
- `med`/`missing-element` — Connected descriptor/payout rows omit the spec Receipt monospace "MARLOW CO" pill and the "Bank ••4291 · daily" payout detail (uses "Managed on Stripe"/null). **Fix:** Render the descriptor as a monospace chip and the masked bank/cadence on the Payouts row.
- `med`/`interaction` — Tax "Collect tax" connected trailing is a chevron→dashboard; spec trailing is a Business-violet IToggle (on). **Fix:** Render an on Switch tinted PILLAR.accent in the connected Collect-tax row.
- `low`/`color` — On-pill and "Connected" chip backgrounds use PantopusColors.successLight (#D1FAE5); spec ReadyPill/Chip success bg is C.okBg (the softer success-bg tone). **Fix:** Use the success-bg token for the on-pill and Connected-chip backgrounds.

### G7 Payouts and earnings — iOS `minor` / Android `minor` → better: **android**
- iOS files: `frontend/apps/ios/Pantopus/Features/Wallet/Scheduling/PayoutsEarningsView.swift`, `frontend/apps/ios/Pantopus/Features/Wallet/Scheduling/PayoutsEarningsViewModel.swift`, `frontend/apps/ios/Pantopus/Core/Design/Components/BalanceHero.swift`, `frontend/apps/ios/Pantopus/Features/Wallet/Components/PayoutMethodCard.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Payments/PaymentsKit.swift`, `frontend/apps/ios/Pantopus/Core/Design/Radii.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/wallet/scheduling/PayoutsEarningsScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/wallet/scheduling/PayoutsEarningsViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/components/BalanceHero.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/wallet/components/PayoutMethodCard.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/payments/CancellationRefundPolicyScreen.kt`
- **Verdict:** Both faithfully reproduce the Wallet hero + source-filter + grouped earnings rows + payout tile + tax row + sticky withdraw across all four frames; Android nails the spec's literal radii/heights (13/14/48) while iOS rounds them up via shared Radii tokens, so Android is marginally closer.
- **Cross note:** Shared issues on both: the reused BalanceHero oversizes the balance (44 vs spec 38) and the PayoutMethodCard shows a generic 'Bank account ••••' rather than the spec's 'Chase checking ••7421'. All four design frames (populated / on-hold-reverify / payouts-not-enabled / empty) are covered on both via the same payoutState branches inside Loaded; the separate 'Payments coming soon' scaffold (iOS PaidSurfaceGate, Android PaymentsComingSoon) is a feature-flag gate, not the spec's not-enabled frame, so it does not displace Frame 3.

**iOS deviations:**
- `low`/`sizing` — PayoutsWithdrawButton uses height 52 and Radii.xl (16) corner; spec Withdraw is height 48 / radius 13. **Fix:** Set frame(height: 48) and use a 13pt corner radius for the withdraw button (and locked variant).
- `low`/`typography` — Withdraw label/amount font is 15pt and icon size 17; spec is 14px text with a 16px arrow-down-to-line icon. **Fix:** Drop withdraw text to 14pt and the icon to 16pt.
- `low`/`sizing` — earningsList / connect-tile / empty card use Radii.xl (16) corner; spec card radius is 14. **Fix:** Use a 14pt radius for the tx list, connect-Stripe tile, and empty card.
- `low`/`typography` — Shared BalanceHero renders the balance at 44pt/heavy; spec hero amount is 38px/weight 800 (~16% oversized), and currency chip/overline are 10pt vs spec 9.5pt. **Fix:** Reduce hero amount to ~38pt and chip/overline to 9.5pt in BalanceHero.
- `low`/`color` — Payout tile body shows generic 'Bank account ••••' rather than the spec's 'Chase checking ••7421'; data-driven so acceptable, but last4 renders as '••••' placeholder. **Fix:** Plumb the real bank name + last4 from the connect account into WalletPayoutMethod.

**Android deviations:**
- `low`/`layout` — EmptyEarnings body Text omits center alignment and the spec's 200px maxWidth, so the copy left-aligns full-width inside the dashed card instead of centered/clamped. **Fix:** Add textAlign = TextAlign.Center and widthIn(max = 200.dp) to the empty-state body text.
- `low`/`sizing` — Filter chips use vertical = 7.dp padding instead of a fixed 30dp height; chip height drifts with text metrics vs spec's exact height 30. **Fix:** Replace vertical padding with Modifier.height(30.dp) and center the label.
- `low`/`typography` — Shared BalanceHero renders the balance at 44sp/ExtraBold; spec hero amount is 38px/800, and the currency chip/overline are 10–10.5sp vs spec 9.5px. **Fix:** Reduce hero amount to ~38sp and chip/overline to 9.5sp in BalanceHero.kt.
- `low`/`color` — Payout tile body shows generic 'Bank account ••••' instead of spec 'Chase checking ••7421'; last4 is a '••••' placeholder. **Fix:** Plumb real bank name + last4 from the connect account into WalletPayoutMethod.

## Invoices

### G12 Invoices list — iOS `major` / Android `major` → better: **equal**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Invoices/InvoicesListView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Invoices/InvoicesListViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Invoices/InvoicesKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invoices/InvoicesListScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invoices/InvoicesListViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invoices/InvoicesKit.kt`
- **Verdict:** Both platforms are near-identical mirror implementations that nail the chrome, layout order, spacing, and loading/empty/gate states, but both omit the design's defining row elements — the trailing status pill and the payer display name — and relabel the summary KPIs, due to a shared lean-InvoiceDTO gap.
- **Cross note:** iOS and Android are deliberate mirror implementations: same states (loading/comingSoon/gate/empty/error/loaded), same shared PkgTopBar/PkgCard/PkgRowCard/PkgStripeGate/EmptyState components, identical row geometry (34dp avatar, 11dp gaps, sizes 13/10.5/13.5), and the exact same documented InvoiceDTO gap driving the missing status pills, missing payer names, and relabeled summary KPIs. Neither is closer.

**iOS deviations:**
- `high`/`missing-element` — InvoiceRow's trailing VStack renders only the amount; the design's required status pill (Paid/Sent/Overdue/Void/Refunded, fontSize 9 bold uppercase, bg/fg per STATUS map) is omitted — explicitly deferred in a comment pending a DTO status field. **Fix:** Add a PkgChip status pill below the amount in the trailing column once status is available; surface it from totals/status data.
- `high`/`missing-element` — InvoiceRow primary line shows the mono INV- reference (font .monospaced size 13 semibold); the design's bold payer name (size 13 weight 600) is the primary line with the reference relegated to the mono sub-line `INV-… · service`. **Fix:** Render payer display name as the primary line and move the mono reference into the caption next to the service.
- `med`/`typography` — summary stat labels read 'Invoices' and 'Total invoiced'; the design's Summary shows 'Outstanding' ($642, amber when overdue) and 'Collected · month' ($3,180). **Fix:** Wire Outstanding/Collected KPIs from status+paid_at and keep the amber overdue treatment that hasOverdue already supports.
- `low`/`state` — selectFilter is view-only (selectedFilter never filters the list since the DTO lacks status), so Paid/Sent/Overdue/Refunded chips render but do nothing — the design implies functional filtering (frame 3 Overdue). **Fix:** Apply the selected status filter to the invoice list once status lands.
- `low`/`color` — Day section header uses appTextMuted (fg4 #9ca3af) which matches the design's E.fg4 grouping label — correct; no fix needed (noted for completeness). **Fix:** None.

**Android deviations:**
- `high`/`missing-element` — InvoiceRow renders avatar + reference/service + amount only; the design's trailing status pill (9sp bold uppercase, per-status bg/fg) is absent — deferred pending a status field on InvoiceDto. **Fix:** Add a status pill chip beneath the amount in the trailing column once status is decoded.
- `high`/`missing-element` — InvoiceRow primary line is the FontFamily.Monospace INV- reference (13sp SemiBold); design's primary is the bold payer name with the mono reference in the `INV-… · service` caption. **Fix:** Use the payer display name as the primary text and move the mono reference into the secondary caption.
- `med`/`typography` — SummaryCard Stat labels are 'Invoices' and 'Total invoiced'; design Summary is 'Outstanding' (amber-when-overdue) and 'Collected · month'. **Fix:** Switch to Outstanding/Collected KPIs and add the amber overdue recolor matching the design's Summary overdue variant.
- `low`/`state` — selectFilter only updates _filter; the list is never filtered (DTO has no status), so the Paid/Sent/Overdue/Refunded chips are non-functional vs the design's filtered Overdue frame. **Fix:** Filter sections by the selected status once the field is available.
- `low`/`layout` — Section day headers sit inside a single PkgRowCard with HorizontalDivider between sections; design draws each day group as its own Card with a top hairline between groups — visually close but groups share one card here. **Fix:** Acceptable as a minor simplification; optionally split each day into its own card to match Grouped's per-section Card.

### G13 Invoice detail — iOS `major` / Android `major` → better: **equal**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Invoices/InvoiceDetailView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Business/Invoices/InvoiceDetailViewModel.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/invoices/InvoiceDetailScreen.kt`
- **Verdict:** Both platforms ship the same intentionally-simplified invoice detail (mono header, total hero, payer/payee cards, line-items table, payment terms, Share+Send dock) and both omit the same large set of design sections — status pill, linked booking row, subtotal/service-fee/tax fee block, timeline, receipt capsule, note-from-sender, and all six non-default status frames — so neither is meaningfully closer to the spec.
- **Cross note:** Identical scope: both implement the same simplified subset and both explicitly document (iOS VM comment) that status/timeline/due/memo/payer-name design sections are omitted because the InvoiceDTO lacks those fields. The only platform divergence is Android's hardcoded-padding baseline hack on the hero vs iOS's correct firstTextBaseline.

**iOS deviations:**
- `high`/`missing-element` — TopBar (PkgTopBar) has no trailing StatusPill; spec puts a status pill (Draft/Sent/Paid/Deposit paid/Overdue/Void/Refunded) in the top bar trailing slot. **Fix:** Add a StatusPill trailing view to PkgTopBar driven by invoice.status with the 7 PILL bg/fg color pairs.
- `high`/`missing-element` — No BookingRow: spec shows a linked-booking card (calendar icon tile, service title, date, chevron) between payer/payee and Line items; iOS jumps straight to lineItemsSection. **Fix:** Insert a tappable BookingRow card after payerPayee that pushes the linked booking route.
- `high`/`missing-element` — ItemsTable omits the fee block — spec's raised footer lists Subtotal, Service fee (3%), Tax (5.7%) above a divider before Total; iOS table jumps from line rows straight to the Total row. **Fix:** Render subtotal/service-fee/tax rows in the appSurfaceRaised footer above the Total divider.
- `high`/`missing-element` — No Timeline section (icon=activity): spec shows a payment timeline (Created/Sent/Paid/Deposit/Refunded/Voided dots + mono timestamps) that scales with status. **Fix:** Add a Timeline section component fed by invoice lifecycle events.
- `med`/`missing-element` — No ReceiptCapsule: spec shows a success-bg 'Paid with Pantopus Pay' capsule with shield-check icon + mono receipt id when status=paid. **Fix:** Render a ReceiptCapsule below Timeline when status is paid.
- `med`/`missing-element` — No 'Note from sender' section (icon=message-square-quote) with italic raised-bg quote; present in 6 of 7 design frames. **Fix:** Add a conditional sender-note Section when the invoice has a memo.
- `high`/`state` — Only a single default render: hero never shows the paid check-prefix / warn-orange overdue / split balance-due (partial) / business-violet refunded variants, and the dock is fixed to Share+Send instead of the per-status docks (Mark paid+Resend+Overflow, Share+Download PDF, Send balance, etc.). **Fix:** Drive hero color/prefix/split, totalLabel, and dock button set from invoice.status across the 7 spec states.
- `low`/`typography` — Mono header omits the '· due Jun 18' segment — iOS renders only reference + 'issued' (issuedLabel), dropping the due date the spec shows. **Fix:** Append ' · due <dueLabel>' to the monospace header string.

**Android deviations:**
- `high`/`missing-element` — PkgTopBar has no trailing StatusPill; spec puts a status pill (Draft/Sent/Paid/etc.) in the top bar trailing slot. **Fix:** Add a trailing StatusPill composable to PkgTopBar driven by state.status with the 7 PILL color pairs.
- `high`/`missing-element` — No BookingRow linked-booking card between the payer/payee Row and the Line items SectionHeader. **Fix:** Insert a clickable BookingRow (calendar tile + title + date + chevron) after the identity Row.
- `high`/`missing-element` — ItemsTable lacks the fee block — spec footer shows Subtotal / Service fee (3%) / Tax (5.7%) rows above the Total divider; Android goes from line rows straight to the Total row. **Fix:** Add subtotal/service-fee/tax rows in the appSurfaceRaised footer above Total.
- `high`/`missing-element` — No Timeline section (activity icon) rendering the payment-lifecycle dot list with mono timestamps. **Fix:** Add a Timeline composable fed by lifecycle events between line items and payment terms.
- `med`/`missing-element` — No ReceiptCapsule ('Paid with Pantopus Pay', shield-check, mono receipt id) shown when status=paid. **Fix:** Render a success-bg ReceiptCapsule when state is paid.
- `med`/`missing-element` — No 'Note from sender' italic-quote section (message-square-quote icon) present in 6 of 7 design frames. **Fix:** Add a conditional sender-note SectionHeader + italic raised-bg text when a memo exists.
- `high`/`state` — Single default render only: hero never shows paid check-prefix / overdue warn color / partial split-balance / refunded violet, and dock is fixed to Share+Send instead of per-status docks (Mark paid+Resend+Overflow, Share+Download PDF, Send balance). **Fix:** Branch hero color/prefix/split, totalLabel, and dock buttons on state.status across the 7 spec states.
- `low`/`layout` — Hero baseline is faked with Alignment.Bottom + a hardcoded bottom-padding 4.dp on the 'total · USD' label instead of true text-baseline alignment (spec uses alignItems:baseline); iOS uses firstTextBaseline correctly. **Fix:** Use Alignment alignByBaseline (Row with alignBy(LastBaseline)) and drop the manual 4.dp padding.

## Automations

### H1 Default reminders — iOS `matches` / Android `matches` → better: **equal**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Automations/Reminders/DefaultRemindersView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Automations/Shared/AutomationsKit.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/automations/RemindersQuickSetupScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/automations/AutomationsKit.kt`
- **Verdict:** Both platforms reproduce the H1 Default-reminders sheet near-pixel-perfectly (header, helper, 6-row check-circle card with Push/Email chips, dashed add-custom chip, sticky Save, and all four frames); the only shared divergence is rendering the saved-success confirmation as a floating bottom toast instead of the spec's slim dark banner stacked inside the footer above Save.
- **Cross note:** Both platforms share the identical (minor) divergence: the Frame-3 saved confirmation is a floating bottom toast rather than the spec's inline dark banner stacked above Save inside the footer. All four spec frames (default, first-open copy, saved, push-off banner) are covered on both, plus extra loading/error states. Shared kits (AutoCard radius 16/Radii.xl, header 17/bold + 12 subhead, channel chips h24 fs10.5, dashed chip h34 1.5px dash, Save 46h radius-lg fs14.5 bold) match spec values 1:1 on both platforms.

**iOS deviations:**
- `low`/`state` — Frame 3 success confirmation is rendered as a floating bottom toast (AutoToast overlay, padding(.bottom,92)) rather than the spec's slim dark #0f172a banner stacked above the Save button inside the footer. **Fix:** Optionally render the saved message as an inline footer banner above AutoPrimaryButton to match Frame 3 layout, or accept native toast idiom.
- `low`/`color` — Toast check-circle uses Theme.Color.success (#059669) vs spec emerald #34d399, and toast bg uses appText (#111827) vs spec #0f172a. **Fix:** Acceptable token substitution; no change needed unless exact emerald is required.
- `low`/`extra-element` — customStepper expansion (Custom reminder card with stepper + Add time) is built but is not depicted in any of the four spec frames, which only show the static dashed 'Add custom time' chip. **Fix:** Out-of-spec enhancement; keep, it is a reasonable expansion of the chip's intent.

**Android deviations:**
- `low`/`state` — Frame 3 success confirmation is a floating bottom AutoToast (align BottomCenter, padding bottom 92.dp) instead of the spec's inline dark banner above Save in the footer. **Fix:** Optionally move the saved message inline into AutoSheetFooter above AutoPrimaryButton, or accept native toast idiom.
- `low`/`color` — Push-off banner copy says 'Push is off in system settings.' vs iOS spec text 'Push is off in iOS Settings.' (intentional platform wording; iOS code keeps 'iOS Settings'). **Fix:** Acceptable platform-correct wording; no change needed.
- `low`/`color` — Toast tint uses PantopusColors.success (#059669) vs spec #34d399 and bg appText (#111827) vs #0f172a. **Fix:** Token substitution; acceptable.
- `low`/`extra-element` — CustomStepper card (stepper + segmented unit + Add time) appears on chip tap but is not in any spec frame, which only show the dashed chip. **Fix:** Out-of-spec enhancement mirrored from iOS; keep.

### H2 Workflows list — iOS `matches` / Android `minor` → better: **ios**
- iOS files: `frontend/apps/ios/Pantopus/Features/Scheduling/Automations/Workflows/WorkflowsListView.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Automations/Workflows/WorkflowsListViewModel.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/Automations/Shared/AutomationsKit.swift`, `frontend/apps/ios/Pantopus/Features/Scheduling/SharedUI/SchedulingStatusPill.swift`
- Android files: `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/automations/WorkflowsListScreen.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/automations/WorkflowsListViewModel.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/automations/AutomationsSupport.kt`, `frontend/apps/android/app/src/main/java/app/pantopus/android/ui/screens/scheduling/automations/AutomationsKit.kt`
- **Verdict:** Both platforms faithfully reproduce the spec's chrome (top bar + plus action), A08 underline tab strip, pinned Default-reminders card, Your-workflows group, FAB, and all five frames; Android differs only in using a Sparkles empty-state icon (spec/iOS use workflow) and a few small geometry nits, so iOS is marginally closer.

**iOS deviations:**
- `low`/`state` — statusKey() returns only "active"/"paused"; spec WfRow also defines a third "draft" (warning-tone) chip that the list can never render. **Fix:** Surface a draft state (e.g. when workflow.isActive is null/unpublished) so SchedulingStatusPill can show the Draft variant the spec draws.

**Android deviations:**
- `med`/`icon` — WorkflowsEmpty passes PantopusIcon.Sparkles to AutoInlineEmpty, but the spec FrameEmpty (and iOS) uses the lucide "workflow" glyph in the 56px circle. **Fix:** Change both AutoInlineEmpty calls in WorkflowsEmpty to PantopusIcon.Workflow to match the spec glyph.
- `low`/`sizing` — AutoErrorView asCard "Try again" button uses padding(vertical = 9.dp) rather than the spec's fixed 38px button height (iOS frames the Text at height 38). **Fix:** Give the retry button a fixed 38.dp height (heightIn(min=38.dp)) to match the spec/iOS button geometry.
- `low`/`state` — AutoStatusChip(active:) renders only Active/Paused; the spec WfRow defines a third Draft (warning) chip that is unreachable. **Fix:** Extend AutoStatusChip to a tri-state (Active/Paused/Draft) and feed it a status derived from the workflow, mirroring the spec's three tones.
- `low`/`color` — AutoErrorView non-card branch and other shared paths are fine, but the error retry CTA text/copy ordering is identical to spec; no extra issue — included only to note the asCard error card matches spec chrome (radius 16, 1px border, 48px sunken circle, filled-sky button). **Fix:** No change needed; verified parity for the error frame.